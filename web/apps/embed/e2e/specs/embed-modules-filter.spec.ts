import { test, expect } from '@playwright/test';

const TEST_ORG_ID = '01952a6b-1c00-7000-8000-000000000001';

test.describe.skip('modules filter (EMBED-06)', () => {
  test('todo: Wave 2 implements - sidebar shows only listed modules', () => {});
  test('todo: Wave 2 implements - unlisted entity route redirects to first allowed', () => {});
});
