import { test, expect } from '@playwright/test';

const TEST_ORG_ID = '01952a6b-1c00-7000-8000-000000000001';

test.describe.skip('auth-expired event (EMBED-07, D7-14)', () => {
  test('todo: Wave 2 implements - fires composed:true on 401', () => {});
  test('todo: Wave 2 implements - host receives event with request_id detail', () => {});
});
