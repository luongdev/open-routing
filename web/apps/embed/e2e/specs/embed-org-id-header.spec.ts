import { test, expect } from '@playwright/test';

const TEST_ORG_ID = '01952a6b-1c00-7000-8000-000000000001';

test.describe.skip('X-Org-Id propagation (EMBED-02)', () => {
  test('todo: Wave 2 implements - every API call carries X-Org-Id from attribute', () => {});
});
