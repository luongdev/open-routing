import { test, expect } from '@playwright/test';

const TEST_ORG_ID = '01952a6b-1c00-7000-8000-000000000001';

test.describe.skip('embed Shadow DOM isolation (EMBED-04, D7-04)', () => {
  test('todo: Wave 2 implements - host CSS does not leak in', () => {});
  test('todo: Wave 2 implements - embed CSS does not escape', () => {});
  test('todo: Wave 2 implements - no portal escape from <sl-dialog> replacement', () => {});
});
