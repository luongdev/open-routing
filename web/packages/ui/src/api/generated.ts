/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe
         * @description Returns HTTP 200 when the server process is alive. Does not check downstream dependencies. Use `/readyz` for deep health checks. Bypasses `OrgContext` middleware (D-17, D-21).
         */
        get: operations["GetHealthz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readyz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Readiness probe
         * @description Deep readiness check. Pings the Postgres pool, pings Redis, and reads the current `schema_migrations` version. Returns HTTP 503 when any check fails. Bypasses `OrgContext` middleware (D-17, D-21).
         */
        get: operations["GetReadyz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/openapi.yaml": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * OpenAPI spec
         * @description Returns the embedded OpenAPI 3.0 spec bytes from the running binary (D-45). Always exactly the spec the binary was compiled against — no stale file risk. Bypasses `OrgContext` middleware (D-21 extended by D-45).
         */
        get: operations["GetOpenAPISpec"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/docs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Interactive API documentation
         * @description Returns a static HTML page that loads the Scalar API Reference viewer via CDN (D-45). The viewer fetches `/openapi.yaml` from the same origin. Public in v0.1 (stub auth); gated behind admin role in v1. Bypasses `OrgContext` middleware (D-21 extended by D-45).
         */
        get: operations["GetDocs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/meta/expr-functions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Condition-expression function catalog
         * @description Static, org-independent catalog of the functions available in the flow condition DSL (str.*, num.*, arr.*, logic.*, date.*). The flow builder's Advanced editor fetches this for autocomplete. Cacheable.
         */
        get: operations["GetExprFunctions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/agents": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /**
         * List agents
         * @description Returns a paginated list of agents. Skills are NOT embedded on list items — use `GET /agents/{id}` for the detail with skills (OQ-1A). Disabled agents (`enabled=false`) are excluded unless `?include_disabled=true` (CAT-09).
         */
        get: operations["ListAgents"];
        put?: never;
        /**
         * Create an agent
         * @description Creates a new agent in the org. The server mints a UUIDv7 `id`. `code` is required and must be unique within the org; `external_id` is optional and unique within the org when set. Skills can be provided at creation time via the `skills` array.
         */
        post: operations["CreateAgent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/agents/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /**
         * Get agent by ID
         * @description Returns the full agent record including the embedded `skills` array (CAT-03, OQ-1A). Cross-org probes return HTTP 404 — not HTTP 403 (FOUND-08 leakage guard).
         */
        get: operations["GetAgent"];
        put?: never;
        post?: never;
        /**
         * Soft-delete an agent
         * @description Soft-deletes the agent by setting `enabled=false`. The record persists in the database. Use `?include_disabled=true` on list endpoints to surface soft-deleted agents (CAT-09).
         */
        delete: operations["DeleteAgent"];
        options?: never;
        head?: never;
        /**
         * Update an agent
         * @description Partial update of an agent. Include `version` from the last GET response for optimistic concurrency (CAT-08). A mismatch returns HTTP 409 with the current server-side record in the body.
         *     Providing `skills` replaces the entire join-table set atomically (PUT-semantics on the skills array — OQ-1A resolution). Omit `skills` to leave the existing assignments unchanged.
         */
        patch: operations["UpdateAgent"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/agents/{id}/status": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /**
         * Get agent state
         * @description Returns the current agent state record (STATE-01). The UI polls this endpoint every ~5 seconds (REST polling only in v0.1 — no SSE, OQ-3D). Use `state_version` to detect stale polling responses (STATE-08).
         */
        get: operations["GetAgentStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Transition agent state
         * @description Initiates an agent-state transition (STATE-02). Returns the full
         *     updated `AgentState` on success to avoid a follow-up GET (OQ-3C).
         *
         *     **Allowed agent-initiated transitions** (STATE-02):
         *     - `NotReady → Ready`
         *     - `Ready → NotReady`
         *     - `Ready → Break` (requires `break_reason_id` — STATE-04)
         *     - `Break → Ready`
         *     - `Break → NotReady`
         *     - `WrapUp → Ready`
         *     - `WrapUp → NotReady`
         *
         *     A transition not in the matrix returns HTTP 409 with the requested
         *     `from`/`to` pair in the body (STATE-03, D-37).
         *
         *     `engaged_channel` is required when `to == Engaged`, but `Engaged` is
         *     system-initiated only — agents cannot self-transition to `Engaged`
         *     (STATE-05). Attempting it returns HTTP 409 `invalid_transition`.
         *
         *     Setting `post_interaction_state` while the agent is `Engaged` stores
         *     the preference for the next WrapUp expiry transition (STATE-06).
         */
        patch: operations["PatchAgentStatus"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/skills": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /**
         * List skills
         * @description Returns a paginated list of skills. Used by the "Add skill" dropdown on Agent detail (OQ-1C — the org's skill list uses cursor pagination). Disabled skills excluded by default (CAT-09).
         */
        get: operations["ListSkills"];
        put?: never;
        /** Create a skill */
        post: operations["CreateSkill"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/skills/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get skill by ID */
        get: operations["GetSkill"];
        put?: never;
        post?: never;
        /** Soft-delete a skill */
        delete: operations["DeleteSkill"];
        options?: never;
        head?: never;
        /**
         * Update a skill
         * @description Partial update of a skill. Include `version` for optimistic concurrency (CAT-08).
         */
        patch: operations["UpdateSkill"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List flow drafts */
        get: operations["ListFlows"];
        put?: never;
        /** Create a flow draft */
        post: operations["CreateFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get a flow draft by ID */
        get: operations["GetFlow"];
        put?: never;
        post?: never;
        /** Soft-delete a flow draft */
        delete: operations["DeleteFlow"];
        options?: never;
        head?: never;
        /** Update a flow draft */
        patch: operations["UpdateFlow"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/validate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Validate a flow draft graph */
        post: operations["ValidateFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/simulate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Deterministically simulate a flow draft */
        post: operations["SimulateFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/traces": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** List simulation/runtime traces for a flow, newest first */
        get: operations["ListFlowTraces"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/publish": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Publish a flow draft as an immutable version and activate its binding */
        post: operations["PublishFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/rollback": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Re-activate a prior published version for a route entry point */
        post: operations["RollbackFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flows/{id}/versions": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** List published versions of a flow */
        get: operations["ListFlowVersions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/flow-versions/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get a published flow version by ID */
        get: operations["GetFlowVersion"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/route-requests": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List route requests (the interaction spine), newest first */
        get: operations["ListRouteRequests"];
        put?: never;
        /** Drive a route request through the published flow (admin/test entry point) */
        post: operations["CreateRouteRequest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/route-requests/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get a route request by ID */
        get: operations["GetRouteRequest"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/route-requests/{id}/trace": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get the runtime trace for a route request */
        get: operations["GetRouteRequestTrace"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/reservations/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get a reservation by ID */
        get: operations["GetReservation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/reservations/{id}/accept": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Accept an offered reservation (test-double / simulator signal) */
        post: operations["AcceptReservation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/reservations/{id}/reject": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Reject an offered reservation (test-double / simulator signal) */
        post: operations["RejectReservation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/route-requests/{id}/input": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit a captured value to a route waiting at an interactive-input node
         * @description Resumes a route parked at an interactive-input node (get_dtmf, prompt_text, manual_approval, …) by supplying the captured value. The node stores it into its variable and takes the captured branch instead of timing out. The route must be in the `waiting` state.
         */
        post: operations["SubmitRouteInput"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/traces/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get a trace by ID */
        get: operations["GetTrace"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/route-requests/{id}/reservations": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** List the reservations a route request generated (sequential offers) */
        get: operations["ListRouteRequestReservations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/reservations/{id}/complete": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Complete an accepted reservation (test-double / simulator signal) */
        post: operations["CompleteReservation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/bindings": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List active route entry bindings (the routing table) */
        get: operations["ListFlowEntryBindings"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/queues": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List queues */
        get: operations["ListQueues"];
        put?: never;
        /** Create a queue */
        post: operations["CreateQueue"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/queues/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get queue by ID */
        get: operations["GetQueue"];
        put?: never;
        post?: never;
        /** Soft-delete a queue */
        delete: operations["DeleteQueue"];
        options?: never;
        head?: never;
        /** Update a queue */
        patch: operations["UpdateQueue"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/channels": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List channels */
        get: operations["ListChannels"];
        put?: never;
        /** Create a channel */
        post: operations["CreateChannel"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/channels/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get channel by ID */
        get: operations["GetChannel"];
        put?: never;
        post?: never;
        /** Soft-delete a channel */
        delete: operations["DeleteChannel"];
        options?: never;
        head?: never;
        /**
         * Update a channel
         * @description Partial update of a channel. Include `version` from the last GET response for optimistic concurrency (CAT-08, OQ-1/A4). A mismatch returns HTTP 409 with the current server-side record in the body.
         */
        patch: operations["UpdateChannel"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/adapters": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /** List adapters */
        get: operations["ListAdapters"];
        put?: never;
        /**
         * Create an adapter
         * @description Creates an adapter registry row (CAT-06). In v0.1 this is a catalog record only — no SDK contract, no execution. The `config` field is a free-form JSONB blob.
         */
        post: operations["CreateAdapter"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/adapters/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get adapter by ID */
        get: operations["GetAdapter"];
        put?: never;
        post?: never;
        /** Soft-delete an adapter */
        delete: operations["DeleteAdapter"];
        options?: never;
        head?: never;
        /**
         * Update an adapter
         * @description Partial update of an adapter. Include `version` from the last GET response for optimistic concurrency (CAT-08, OQ-1/A4). A mismatch returns HTTP 409 with the current server-side record in the body.
         */
        patch: operations["UpdateAdapter"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/break-reasons": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        /**
         * List break reasons
         * @description Returns a paginated list of break reasons for the org. The agent desktop uses this to populate the break-reason picker. Ordered by `display_order` ascending.
         */
        get: operations["ListBreakReasons"];
        put?: never;
        /** Create a break reason */
        post: operations["CreateBreakReason"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/break-reasons/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        /** Get break reason by ID */
        get: operations["GetBreakReason"];
        put?: never;
        post?: never;
        /**
         * Soft-delete a break reason
         * @description Soft-deletes the break reason by setting `enabled=false`. Agents currently on this break reason are not affected mid-session; the reason will no longer be available for new Break transitions.
         */
        delete: operations["DeleteBreakReason"];
        options?: never;
        head?: never;
        /**
         * Update a break reason
         * @description Partial update of a break reason. Include `version` for optimistic concurrency (CAT-08). Changing `routable` takes effect immediately for agents currently on this break reason — no running transitions are interrupted; routability is checked on the next routing decision.
         */
        patch: operations["UpdateBreakReason"];
        trace?: never;
    };
    "/v1/orgs/{org_id}/catalog/import": {
        parameters: {
            query: {
                /** @description The catalog entity type being imported. */
                entity: components["parameters"]["EntityTypeQuery"];
                /** @description CSV schema version for import validation (IMP-08). Required when `Content-Type: text/csv`. Mismatched version returns HTTP 400 with supported versions listed. Example: `v0.1`. */
                schema_version?: components["parameters"]["SchemaVersionQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Bulk import catalog entities
         * @description Bulk import for any of the 6 catalog entities (IMP-01). Accepts both
         *     JSON and CSV payloads on the same endpoint — the handler dispatches on
         *     `Content-Type` (OQ-2D resolution).
         *
         *     **JSON format:** Array of entity objects matching the `Create*Request`
         *     shape for the target entity.
         *
         *     **CSV format:** RFC 4180 with header row. Field names match the JSON
         *     field names. BOM handling: UTF-8 BOM is stripped automatically (IMP-02).
         *     Line endings: CRLF and LF both accepted (IMP-02).
         *
         *     **Upsert semantics:** Rows are upserted keyed by `(org_id, code)` —
         *     the universal user-facing canonical identifier introduced by Phase 04.1
         *     (D04_1-01). Existing rows update fields, new rows insert. Running the
         *     same input twice produces no duplicates (IMP-03). `external_id` is
         *     supported as an optional integration-mapping field on every
         *     `Import*Request` (mutable per D04_1-07) but is NOT the upsert key.
         *
         *     **Idempotency:** Clients MAY pass an `Idempotency-Key` UUIDv7 header to
         *     make a POST retry-safe. A repeated request with the same key in the
         *     same org returns the persisted prior result with `idempotent_replay:
         *     true` (D5-13 / D5-27). Note: in v0.1 the replay response carries an
         *     empty `succeeded` array because the server stores only counters +
         *     errors JSONB — original succeeded IDs are not reconstructable. Callers
         *     that need full replay should use the non-idempotent path or wait for
         *     v0.2.
         *
         *     **Size limits:** Maximum 50 MB body and 10,000 rows. Oversized
         *     requests return HTTP 413 (request_too_large_use_async_pathway) before
         *     import processing begins. The v0.2 async pathway will handle larger
         *     imports.
         *
         *     **Partial success:** Valid rows succeed; failed rows return structured
         *     errors (IMP-04). HTTP 207 when any rows fail, HTTP 200 when all succeed
         *     (IMP-05).
         *
         *     **Import job:** A record is persisted in `import_jobs` for audit
         *     purposes (IMP-06). The job ID is returned in the response for `GET
         *     /imports/{id}` retrieval.
         *
         *     **CSV schema_version:** Required for CSV payloads (IMP-08). Use
         *     `?schema_version=v0.1`. Mismatched versions return HTTP 400 with
         *     supported versions. JSON payloads do not require this parameter.
         */
        post: operations["BulkImportCatalog"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{org_id}/imports/{import_id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Import job UUIDv7 returned by the POST /catalog/import endpoint. */
                import_id: components["parameters"]["ImportJobIdPath"];
            };
            cookie?: never;
        };
        /**
         * Get import job result
         * @description Returns the persisted result of a past import job (IMP-06). The job ID is the UUIDv7 returned in the `BulkImportResult` from the original POST. In v0.1 this is a synchronous record — no polling needed. In v0.2 async imports will require polling this endpoint for completion.
         */
        get: operations["GetImportJob"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description One condition-DSL function (str.*, num.*, arr.*, logic.*, date.*). */
        ExprFunction: {
            /** @example str */
            ns: string;
            /** @example upper */
            name: string;
            /**
             * @description Fixed argument count, or -1 for variadic.
             * @example 1
             */
            arity: number;
            /** @example str.upper(s) -> string */
            signature: string;
            /** @example Uppercase */
            summary: string;
        };
        ExprFunctionCatalog: {
            functions: components["schemas"]["ExprFunction"][];
        };
        /**
         * Format: uuid
         * @description A UUIDv7 (RFC 9562 §5.7) time-ordered unique identifier.
         *     Version must be 7 or higher; UUIDv4 and lower are rejected.
         *     Example: `01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e`
         * @example 01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e
         */
        UUIDv7: string;
        /**
         * @description Closed enum of machine-readable error codes (D-36, D-75, ROADMAP Phase 3
         *     criterion 4). Clients branch on this value — never on `reason` or HTTP
         *     status alone.
         *
         *     Phase 04.1 added `duplicate_code`, `duplicate_external_id`, and
         *     `immutable_field` to support the universal `code` identity model
         *     (D04_1-16, D04_1-20).
         * @enum {string}
         */
        ErrorCode: "invalid_body" | "invalid_id" | "not_found" | "internal" | "version_conflict" | "cross_org" | "duplicate_code" | "duplicate_external_id" | "invalid_org_id" | "invalid_transition" | "import_failed" | "immutable_field" | "rate_limited" | "invalid_reference" | "invalid_value";
        /**
         * @description Canonical error envelope (D-35). Present on every 4xx/5xx response.
         *     `request_id` is omitted on bypass routes that run before the RequestID
         *     middleware (e.g. responses from the OrgContext middleware itself for
         *     missing/malformed X-Org-Id).
         */
        ErrorResponse: {
            error: components["schemas"]["ErrorCode"];
            /**
             * @description Human-readable contextual detail for debugging. Clients MUST NOT branch on this value — it is subject to change across minor versions.
             * @example missing_header
             */
            reason: string;
            /** @description Per-request UUIDv7 minted by the RequestID middleware. Embedded in error bodies for support DX — users include this in bug reports. Absent when the response is emitted before the RequestID middleware runs (bypass routes, middleware-reject paths). */
            request_id?: components["schemas"]["UUIDv7"];
        };
        /**
         * @description HTTP 409 response for optimistic concurrency mismatches (CAT-08, D-37).
         *     Extends the canonical error envelope with `current` — the server-side
         *     record as of the moment of the conflict. The UI uses `current` to show
         *     a diff/merge affordance so the user can see what changed.
         */
        VersionConflictErrorResponse: {
            /** @enum {string} */
            error: "version_conflict";
            /** @example version_mismatch */
            reason: string;
            request_id?: components["schemas"]["UUIDv7"];
            /** @description The complete server-side entity record at the time of the conflict. Schema depends on which entity was being updated — see per-entity response schemas. */
            current: unknown;
        };
        /**
         * @description HTTP 409 response for invalid agent-state transitions (STATE-03, D-37).
         *     Extends the canonical error envelope with `from` and `to` — the
         *     requested state pair that is not in the allowed transition matrix.
         */
        InvalidTransitionErrorResponse: {
            /** @enum {string} */
            error: "invalid_transition";
            /** @example not_in_allowed_matrix */
            reason: string;
            request_id?: components["schemas"]["UUIDv7"];
            from: components["schemas"]["AgentStatus"];
            to: components["schemas"]["AgentStatus"];
        };
        /**
         * @description Common cursor-based pagination envelope (CAT-10). Reused across all
         *     six catalog entity list endpoints via `allOf` with an entity-specific
         *     `items` property override.
         *
         *     The cursor is an opaque base64-encoded composite of `{created_at, id}`.
         *     Cursor internals are not part of the public contract — clients MUST
         *     treat it as an opaque string. Do not parse or construct cursor values.
         */
        PaginatedList: {
            /** @description The current page of results. */
            items: unknown[];
            /**
             * @description Opaque base64 cursor for the next page. `null` when this is the last page. Pass as `?cursor=<value>` on the next request.
             * @example eyJjcmVhdGVkX2F0IjoiMjAyNi0wNS0xNVQwMDowMDowMFoiLCJpZCI6IjAxOTAxYjJjLTdmM2EtN2FiYy04ZDRlLTVmNmE3YjhjOWQwZSJ9
             */
            next_cursor?: string | null;
            /** @description `true` when there are more results beyond this page. Prefer `has_more` over checking `next_cursor != null` for clarity. */
            has_more: boolean;
        };
        /** @description A skill assigned to an agent with a proficiency rating. */
        AgentSkillAssignment: {
            /** @description The skill being assigned. */
            skill_id: components["schemas"]["UUIDv7"];
            /**
             * @description Skill name — included on GET detail responses, omitted on write bodies.
             * @example Billing Support
             */
            readonly name?: string;
            /**
             * @description Agent proficiency rating on this skill. Inclusive range 1 (beginner) to 10 (expert). The OpenAPI schema intentionally omits `minimum`/`maximum` so the oapi-codegen Layer 1 validator does NOT short-circuit out-of-range values with HTTP 400. The handler (catalog package, Plan 03-09) enforces the 1-10 range and returns HTTP 422 with `ErrorCode=invalid_value` when the value is out of range — see ROADMAP Phase 3 success criterion 4. Clients should still display min=1/max=10 in UI to prevent obvious mistakes; the wire shape distinguishes "wrong value" (422 invalid_value) from "wrong type" (400 invalid_body).
             * @example 8
             */
            proficiency: number;
        };
        /** @description An agent in the catalog. Agents are the humans (or bots) who handle routed interactions. Each agent belongs to exactly one org. */
        Agent: {
            /** @description Server-minted UUIDv7 primary key. */
            id: components["schemas"]["UUIDv7"];
            /** @description The org this agent belongs to. */
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example emp_0042
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example HR-EMP-0042
             */
            external_id?: string | null;
            /**
             * @description Display name of the agent.
             * @example Alice Nguyen
             */
            name: string;
            /**
             * Format: email
             * @description Agent email address.
             * @example alice@example.com
             */
            email: string;
            /**
             * @description Soft-delete flag. `false` means the agent is disabled and will not appear in default list responses (CAT-09). Use `?include_disabled=true` to surface disabled agents.
             * @example true
             */
            enabled: boolean;
            /**
             * @description Monotonically increasing optimistic-lock version counter (CAT-08). Must be included in `PATCH` requests; mismatch returns HTTP 409.
             * @example 1
             */
            version: number;
            /** @description Skills assigned to this agent with proficiency ratings. Embedded on detail GET responses only; omitted from list responses (CAT-10 list items are flat, OQ-1A). Not present on create/list responses. */
            readonly skills?: components["schemas"]["AgentSkillAssignment"][];
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        /** @description Flat agent record returned in list responses. Skills are intentionally omitted (OQ-1A resolution — embed skills only on detail responses). */
        AgentListItem: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example emp_0042
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example HR-EMP-0042
             */
            external_id?: string | null;
            /** @example Alice Nguyen */
            name: string;
            /**
             * Format: email
             * @example alice@example.com
             */
            email: string;
            /** @example true */
            enabled: boolean;
            /** @example 1 */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        /** @description Request body for creating an agent. */
        CreateAgentRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example emp_0042
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example HR-EMP-0042
             */
            external_id?: string | null;
            /**
             * @description Display name.
             * @example Alice Nguyen
             */
            name: string;
            /**
             * Format: email
             * @description Agent email address.
             * @example alice@example.com
             */
            email: string;
            /**
             * @description Initial enabled state. Defaults to `true`.
             * @default true
             */
            enabled: boolean;
            /** @description Initial skill assignments. Omit to create with no skills. */
            skills?: components["schemas"]["AgentSkillAssignment"][];
        };
        /** @description Request body for updating an agent. Include `version` from the last GET response for optimistic concurrency (CAT-08). */
        UpdateAgentRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example emp_0042
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example HR-EMP-0042
             */
            external_id?: string | null;
            /** @example Alice Nguyen */
            name?: string;
            /**
             * Format: email
             * @example alice@example.com
             */
            email?: string;
            enabled?: boolean;
            /**
             * @description Current optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
            /** @description Full-replacement skill assignment array (PUT semantics on the join table — OQ-1A resolution). Replaces the entire set atomically. Omit to leave skills unchanged. */
            skills?: components["schemas"]["AgentSkillAssignment"][];
        };
        /** @description A skill in the catalog. Skills are assigned to agents with a proficiency rating. */
        Skill: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example skill_voice_tier1
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example HR-SKILL-VOICE
             */
            external_id?: string | null;
            /** @example Billing Support */
            name: string;
            /**
             * @description Optional long-form description.
             * @example Handle billing inquiries, refunds, and payment disputes.
             */
            description?: string | null;
            /**
             * @description Category or domain of the skill (e.g. "technical", "language", "product"). Free text — the platform does not restrict values.
             * @example support
             */
            skill_type: string;
            /** @example true */
            enabled: boolean;
            /** @example 1 */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateSkillRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example skill_voice_tier1
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example HR-SKILL-VOICE
             */
            external_id?: string | null;
            /** @example Billing Support */
            name: string;
            /** @example Handle billing inquiries and payment disputes. */
            description?: string | null;
            /** @example support */
            skill_type: string;
            /** @default true */
            enabled: boolean;
        };
        UpdateSkillRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example skill_voice_tier1
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example HR-SKILL-VOICE
             */
            external_id?: string | null;
            /** @example Billing Support */
            name?: string;
            description?: string | null;
            /** @example support */
            skill_type?: string;
            enabled?: boolean;
            /**
             * @description Optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
        };
        /** @description A routing flow draft. The graph is the canonical authoring artifact (UI graph is the source of truth). `version` is the optimistic-lock revision of the draft, distinct from the immutable published versions. */
        Flow: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier. Required, immutable after create. Composite UNIQUE (org_id, code).
             * @example flow_inbound_voice
             */
            code: string;
            /** @example Inbound Voice */
            name: string;
            /** @description Canonical flow graph (nodes + edges). Opaque pass-through in Stage 1; structurally validated before simulation and publish. */
            graph: {
                [key: string]: unknown;
            };
            /** @example true */
            enabled: boolean;
            /** @example 1 */
            version: number;
            /**
             * Format: date-time
             * @example 2026-06-02T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-06-02T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateFlowRequest: {
            /** @example flow_inbound_voice */
            code: string;
            /** @example Inbound Voice */
            name: string;
            /** @description Initial flow graph. Defaults to an empty graph when omitted. */
            graph?: {
                [key: string]: unknown;
            };
            /** @default true */
            enabled: boolean;
        };
        UpdateFlowRequest: {
            /** @description Must equal the stored value (immutable post-create). A different value returns HTTP 422 with ErrorCode=immutable_field. */
            code?: string;
            name?: string;
            graph?: {
                [key: string]: unknown;
            };
            enabled?: boolean;
            /** @description Expected current version for optimistic concurrency. */
            version: number;
        };
        /**
         * @description Typed taxonomy of routing failures recorded on a route request and its trace.
         * @enum {string}
         */
        RoutingFailureCode: "missing_published_flow" | "missing_catalog_reference" | "no_eligible_candidate" | "multiple_active_bindings" | "invalid_graph" | "reservation_transition_conflict" | "loop_limit" | "invalid_loop_input";
        /** @description Maps a route entry point (channel + entry code) to its active published flow version. */
        FlowEntryBinding: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            channel: string;
            entry_code: string;
            flow_version_id: components["schemas"]["UUIDv7"];
            flow_code: string;
            active: boolean;
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at?: string;
        };
        /** @description The immutable version created/re-activated plus the binding it activated. */
        FlowPublishResult: {
            version: components["schemas"]["FlowVersion"];
            binding: components["schemas"]["FlowEntryBinding"];
        };
        /** @description A scripted reservation outcome for deterministic simulation. With `node_id` set it pins THAT reservation node's result port directly (per-node, order-independent). Without `node_id`, entries form an ordered per-offer queue (legacy). */
        SimulateScriptedReservationOutcome: {
            /** @enum {string} */
            outcome: "accepted" | "rejected" | "timeout" | "no_candidate";
            /** @description When set, this outcome is the result port of that reservation node. */
            node_id?: string;
            agent_id?: components["schemas"]["UUIDv7"];
        };
        /** @description An immutable published version of a flow, created by publish. Route requests pin a flow_version_id at start; traces reference it. */
        FlowVersion: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            flow_id: components["schemas"]["UUIDv7"];
            /** @example flow_inbound_voice */
            flow_code: string;
            /** @example 1 */
            version_number: number;
            /** @description Immutable snapshot of the authored graph at publish time. */
            graph: {
                [key: string]: unknown;
            };
            /** @example 1 */
            plan_format_version: number;
            /** Format: date-time */
            readonly created_at: string;
        };
        /** @description One validation problem, located at a node, edge, or field. */
        FlowValidationIssue: {
            /** @example missing_catalog_reference */
            code: string;
            message: string;
            node_id?: string | null;
            edge_id?: string | null;
            field?: string | null;
        };
        FlowValidationResult: {
            valid: boolean;
            issues: components["schemas"]["FlowValidationIssue"][];
        };
        /** @description Publish the current draft graph as an immutable version and activate it for a route entry point. */
        PublishFlowRequest: {
            /** @example voice */
            channel: string;
            /** @example main */
            entry_code: string;
            /** @description Expected current draft version (optimistic concurrency against the draft being published). */
            version: number;
            /** @description If set, publish only proceeds when the binding's current active version matches (guards against concurrent publish/rollback clobber). */
            expected_current_flow_version_id?: components["schemas"]["UUIDv7"];
        };
        /** @description Re-activate a prior published version for a route entry point. */
        RollbackFlowRequest: {
            /** @example voice */
            channel: string;
            /** @example main */
            entry_code: string;
            /** @example 1 */
            to_version_number: number;
            /** @description If set, rollback only proceeds when the binding's current active version matches (guards against concurrent clobber). */
            expected_current_flow_version_id?: components["schemas"]["UUIDv7"];
        };
        /** @description Deterministic simulation of the draft graph against synthetic input. Does not mutate live reservations or agent state. */
        SimulateFlowRequest: {
            interaction_input: {
                [key: string]: unknown;
            };
            /** @example voice */
            channel?: string;
            /** @example main */
            entry_code?: string;
            /**
             * Format: date-time
             * @description Pins the simulator's virtual clock start so wait/timeout paths replay deterministically.
             */
            virtual_clock_start?: string;
            /** @description Reservation outcomes applied in order so offers resolve deterministically. */
            scripted_reservation_outcomes?: components["schemas"]["SimulateScriptedReservationOutcome"][];
            /** @description Recorded/mocked effect-node outputs keyed by node id. */
            scripted_effect_outputs?: {
                [key: string]: unknown;
            };
        };
        TraceStep: {
            index: number;
            node_id: string;
            node_kind: string;
            /** @enum {string} */
            status: "ok" | "error" | "skipped" | "suspended";
            /** @description The output port the node took (e.g. true/false, accepted/timeout). */
            port?: string | null;
            input?: {
                [key: string]: unknown;
            } | null;
            output?: {
                [key: string]: unknown;
            } | null;
            catalog_refs?: string[];
            effect_status?: string | null;
            /** @description Execution (CPU) time of the step in ms — NOT virtual wait time advanced by wait/reservation. */
            duration_ms?: number | null;
            error?: string | null;
            /** @description The id of the control-flow body region this step ran inside (loop/parallel/try_catch), if any. */
            region?: string | null;
            /** @description Zero-based loop iteration index for steps inside a loop_for/loop_while body. */
            iteration?: number | null;
            /** @description Zero-based branch index for steps inside a parallel branch. */
            branch?: number | null;
            /** @description True when this step's domain failure was caught by an enclosing try_catch. */
            caught?: boolean | null;
        };
        /** @description An ordered runtime or simulation trace explaining a routing outcome. */
        Trace: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /** @enum {string} */
            kind: "runtime" | "simulation";
            route_request_id?: components["schemas"]["UUIDv7"];
            flow_version_id?: components["schemas"]["UUIDv7"];
            /** @description Set for simulation traces (which pin a draft, not a published version). */
            flow_id?: components["schemas"]["UUIDv7"];
            outcome?: string | null;
            steps: components["schemas"]["TraceStep"][];
            /** Format: date-time */
            readonly created_at: string;
        };
        /** @description A simulation trace plus the resolved virtual clock start (so replay is exact even when the request omitted it). */
        SimulateFlowResponse: {
            /**
             * Format: date-time
             * @description The virtual clock origin actually used (echoed from the request, or server-chosen when omitted).
             */
            virtual_clock_start: string;
            trace: components["schemas"]["Trace"];
        };
        /** @description Admin/test entry point to drive a route through the published flow for a channel + entry code. */
        CreateRouteRequest: {
            /** @example voice */
            channel: string;
            /** @example main */
            entry_code: string;
            interaction_input?: {
                [key: string]: unknown;
            };
        };
        /** @description A captured value submitted to a route waiting at an interactive-input node. */
        SubmitRouteInput: {
            /** @description The input node to answer. Optional — defaults to the node the route is currently parked at (its resume cursor). */
            node_id?: string;
            /** @description The captured value (digits, text, "approved"/"rejected", a survey score, …). */
            value: unknown;
        };
        /** @description The interaction spine — reservations, events, and traces reference it. */
        RouteRequest: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            channel: string;
            entry_code: string;
            flow_version_id?: components["schemas"]["UUIDv7"];
            flow_code?: string | null;
            /** @enum {string} */
            status: "pending" | "running" | "waiting" | "completed" | "failed" | "cancelled";
            failure_code?: components["schemas"]["RoutingFailureCode"];
            /** @description The trace for this route request, once execution has produced one. */
            trace_id?: components["schemas"]["UUIDv7"];
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at?: string;
        };
        /** @description An offer of a route request to an agent, with its lifecycle state. */
        Reservation: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            route_request_id: components["schemas"]["UUIDv7"];
            agent_id: components["schemas"]["UUIDv7"];
            /** @enum {string} */
            state: "offered" | "accepted" | "rejected" | "timeout" | "cancelled" | "completed";
            attempt: number;
            /** Format: date-time */
            offered_at: string;
            /** Format: date-time */
            expires_at: string;
            /** Format: date-time */
            resolved_at?: string | null;
            reason?: string | null;
            /** Format: date-time */
            readonly created_at: string;
            /** Format: date-time */
            readonly updated_at?: string;
        };
        /** @description A queue in the catalog. Queues hold interactions waiting to be assigned to an agent. Queues have a channel type, priority, and an after-contact work (ACW) timer. */
        Queue: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example queue_billing
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example CRM-Q-BILLING
             */
            external_id?: string | null;
            /** @example Billing Queue */
            name: string;
            /** @description Channel types this queue accepts (e.g. ["voice", "chat"]). */
            channel_types: components["schemas"]["ChannelType"][];
            /**
             * @description Queue priority. Higher values indicate higher priority. Used by the routing engine to break ties among equally-skilled agents.
             * @example 5
             */
            priority: number;
            /**
             * @description After-contact work (ACW / WrapUp) timer in seconds. When an interaction ends, the agent enters WrapUp for this many seconds.
             * @example 60
             */
            acw_sec: number;
            /** @example true */
            enabled: boolean;
            /** @example 1 */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateQueueRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example queue_billing
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example CRM-Q-BILLING
             */
            external_id?: string | null;
            /** @example Billing Queue */
            name: string;
            channel_types: components["schemas"]["ChannelType"][];
            /** @example 5 */
            priority: number;
            /** @example 60 */
            acw_sec: number;
            /** @default true */
            enabled: boolean;
        };
        UpdateQueueRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example queue_billing
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example CRM-Q-BILLING
             */
            external_id?: string | null;
            name?: string;
            channel_types?: components["schemas"]["ChannelType"][];
            priority?: number;
            acw_sec?: number;
            enabled?: boolean;
            /**
             * @description Optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
        };
        /**
         * @description Supported channel types in v0.1.
         * @enum {string}
         */
        ChannelType: "voice" | "chat" | "email";
        /** @description A channel in the catalog. Channels represent a logical communication medium (voice, chat, email). They link to a default queue for unrouted interactions. */
        Channel: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example channel_voice_primary
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example CRM-CH-VOICE
             */
            external_id?: string | null;
            /** @example Main Voice Channel */
            name: string;
            channel_type: components["schemas"]["ChannelType"];
            /** @description Default queue for interactions arriving on this channel when no flow route is configured. Null means no default queue. */
            default_queue_id?: (string & components["schemas"]["UUIDv7"]) | null;
            /** @example true */
            enabled: boolean;
            /**
             * @description Monotonically increasing optimistic-lock version counter (CAT-08, OQ-1/A4). Server-owned. Mismatch on update returns HTTP 409.
             * @example 1
             */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateChannelRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example channel_voice_primary
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example CRM-CH-VOICE
             */
            external_id?: string | null;
            /** @example Main Voice Channel */
            name: string;
            channel_type: components["schemas"]["ChannelType"];
            default_queue_id?: (string & components["schemas"]["UUIDv7"]) | null;
            /** @default true */
            enabled: boolean;
        };
        /** @description Request body for updating a channel. Include `version` from the last GET response for optimistic concurrency (CAT-08). */
        UpdateChannelRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example channel_voice_primary
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example CRM-CH-VOICE
             */
            external_id?: string | null;
            /**
             * @description Current optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
            name?: string;
            channel_type?: components["schemas"]["ChannelType"];
            default_queue_id?: (string & components["schemas"]["UUIDv7"]) | null;
            enabled?: boolean;
        };
        /** @description An adapter registry row. Adapters represent integration points (e.g. a FreeSWITCH bridge, a LiveKit gateway). In v0.1 this is a catalog row only — no SDK contract, no execution. Real adapter execution lands in a later milestone. */
        Adapter: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example adapter_freeswitch_dc1
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example MDM-ADAPTER-FS-DC1
             */
            external_id?: string | null;
            /**
             * @description Display name for this adapter configuration.
             * @example FreeSWITCH Bridge - DC1
             */
            name: string;
            /**
             * @description Identifier for the adapter kind (e.g. "freeswitch", "livekit", "twilio"). Free text — the platform does not restrict values in v0.1.
             * @example freeswitch
             */
            adapter_type: string;
            /**
             * @description Free-form JSONB configuration blob. Shape is adapter-type-specific. No vendor-specific fixed columns — everything goes in this field.
             * @example {
             *       "host": "sip.example.com",
             *       "port": 5060
             *     }
             */
            config?: {
                [key: string]: unknown;
            };
            /** @example true */
            enabled: boolean;
            /**
             * @description Monotonically increasing optimistic-lock version counter (CAT-08, OQ-1/A4). Server-owned. Mismatch on update returns HTTP 409.
             * @example 1
             */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateAdapterRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example adapter_freeswitch_dc1
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example MDM-ADAPTER-FS-DC1
             */
            external_id?: string | null;
            /** @example FreeSWITCH Bridge - DC1 */
            name: string;
            /** @example freeswitch */
            adapter_type: string;
            /** @description Free-form JSONB configuration blob. Null to leave empty. */
            config?: {
                [key: string]: unknown;
            } | null;
            /** @default true */
            enabled: boolean;
        };
        /** @description Request body for updating an adapter. Include `version` from the last GET response for optimistic concurrency (CAT-08). */
        UpdateAdapterRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example adapter_freeswitch_dc1
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example MDM-ADAPTER-FS-DC1
             */
            external_id?: string | null;
            /**
             * @description Current optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
            name?: string;
            adapter_type?: string;
            /** @description Free-form JSONB configuration blob. Null to clear. */
            config?: {
                [key: string]: unknown;
            } | null;
            enabled?: boolean;
        };
        /** @description A configurable reason an agent can enter the Break state. Each reason has a `routable` flag that determines whether the agent is eligible for routing while on break. The `IsRoutable` domain helper checks this (STATE-10). */
        BreakReason: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            /**
             * @description User-facing canonical identifier (D04_1-01). Required, immutable
             *     after create. Composite UNIQUE (org_id, code). Used as the upsert
             *     key for bulk import (Phase 5) and cross-reference target for
             *     nested relationships and future DSL references.
             * @example break_lunch
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system
             *     (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
             *     key for bulk import (use `code` for that). Partial unique
             *     within an org when present. v0.1 supports at most one
             *     external source per entity per org; multi-source
             *     disambiguation deferred to v0.2 via `external_source TEXT`.
             * @example HR-BREAK-LUNCH
             */
            external_id?: string | null;
            /**
             * @description Display label shown on the agent desktop.
             * @example Lunch
             */
            name: string;
            /**
             * @description Whether an agent on this break reason is eligible for routing. `true` → agent can still receive interactions (e.g. "Short break"). `false` → agent is unavailable (e.g. "Lunch", "Training").
             * @example false
             */
            routable: boolean;
            /**
             * @description Sort position in the break reason picker. Lower values appear first.
             * @example 1
             */
            display_order: number;
            /** @example true */
            enabled: boolean;
            /** @example 1 */
            version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        CreateBreakReasonRequest: {
            /**
             * @description User-facing canonical identifier. Required on create, immutable
             *     after (passing a different code in PATCH returns HTTP 422 with
             *     ErrorCode=immutable_field).
             * @example break_lunch
             */
            code: string;
            /**
             * @description Optional caller-assigned identifier from an external system. See entity schema.
             * @example HR-BREAK-LUNCH
             */
            external_id?: string | null;
            /** @example Lunch */
            name: string;
            /** @example false */
            routable: boolean;
            /** @example 1 */
            display_order: number;
            /** @default true */
            enabled: boolean;
        };
        UpdateBreakReasonRequest: {
            /**
             * @description Must equal the stored value (immutable post-create in v0.1, D04_1-02).
             *     Passing a different value returns HTTP 422 with
             *     ErrorCode=immutable_field. The field is accepted in the PATCH
             *     body to preserve symmetry with the Create*Request shape; absent
             *     or matching values are no-ops. Rename support deferred to v0.2.
             * @example break_lunch
             */
            code?: string;
            /**
             * @description Mutable external-system mapping (D04_1-07). Pass a non-empty
             *     string to (re)bind to an external row; pass an empty string
             *     `""` to CLEAR the binding (server sets external_id to SQL NULL);
             *     omit the field to leave the existing binding unchanged. Phase 5
             *     fix H2 — `null` JSON values are indistinguishable from omission
             *     in the current oapi-codegen pointer encoding (both decode to a
             *     nil `*string`), so the empty-string sentinel is the documented
             *     v0.1 way to clear. A future v0.2 release may add a tri-state
             *     wrapper that allows literal `null` to clear, at which point the
             *     empty-string sentinel will be deprecated but still honoured.
             * @example HR-BREAK-LUNCH
             */
            external_id?: string | null;
            name?: string;
            routable?: boolean;
            display_order?: number;
            enabled?: boolean;
            /**
             * @description Optimistic-lock version. Mismatch → HTTP 409.
             * @example 1
             */
            version: number;
        };
        /**
         * @description Agent availability status values (STATE-01).
         *
         *     **Agent-toggleable:**
         *     - `Ready` — available to receive interactions
         *     - `NotReady` — unavailable (no sub-reasons; contrast with Break)
         *     - `Break` — temporary unavailability with a configurable reason
         *
         *     **System-set:**
         *     - `Engaged` — currently handling an interaction (system-initiated only)
         *     - `WrapUp` — post-interaction after-contact work period (system-initiated)
         *     - `Offline` — agent is logged out or timed out (system-initiated)
         * @enum {string}
         */
        AgentStatus: "Ready" | "NotReady" | "Break" | "Engaged" | "WrapUp" | "Offline";
        /**
         * @description The state the agent transitions to after the WrapUp period ends (STATE-06). Set by the agent while Engaged. Applied by the system when WrapUp expires.
         * @enum {string}
         */
        PostInteractionState: "ready" | "not_ready";
        /** @description The full agent state record (STATE-01). Returned by the status PATCH on success to avoid a follow-up GET (OQ-3C resolution). */
        AgentState: {
            /** @description The agent this state row belongs to. */
            agent_id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            status: components["schemas"]["AgentStatus"];
            /** @description The channel type the agent is currently engaged on (STATE-05). Non-null only when `status == Engaged`. Null in all other states. */
            engaged_channel?: (string & components["schemas"]["ChannelType"]) | null;
            /** @description The break reason for the current Break state (STATE-04). Non-null only when `status == Break`. Null in all other states. */
            break_reason_id?: (string & components["schemas"]["UUIDv7"]) | null;
            /** @description The target state after WrapUp expires (STATE-06). Set while Engaged. Null when not in Engaged or WrapUp state. */
            post_interaction_state?: (string & components["schemas"]["PostInteractionState"]) | null;
            /**
             * Format: date-time
             * @description Server-owned TTL for the WrapUp state (STATE-07). When now() passes this timestamp, the background goroutine fires the WrapUp → `post_interaction_state` transition. Null in all other states.
             * @example 2026-05-15T00:05:00Z
             */
            wrapup_until?: string | null;
            /**
             * @description Monotonically increasing version counter (STATE-08). Incremented on every state mutation. UI uses this to detect stale polling responses.
             * @example 3
             */
            state_version: number;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly updated_at: string;
        };
        /**
         * @description Request body for agent-state transitions (STATE-02, STATE-03, STATE-04,
         *     STATE-05). Single transition per call (OQ-3A resolution).
         *
         *     **Allowed agent-initiated transitions** (STATE-02):
         *     - `NotReady` → `Ready`
         *     - `Ready` → `NotReady`
         *     - `Ready` → `Break` (requires `break_reason_id`)
         *     - `Break` → `Ready`
         *     - `Break` → `NotReady`
         *     - `WrapUp` → `Ready`
         *     - `WrapUp` → `NotReady`
         *
         *     **System-only transitions** (cannot be triggered via this endpoint):
         *     - `Ready` → `Engaged` (requires `engaged_channel`)
         *     - `Engaged` → `WrapUp`
         *     - `WrapUp` → `Ready` / `WrapUp` → `NotReady` (via TTL goroutine)
         *     - `* → Offline`, `Offline → NotReady` (login/logout events)
         *
         *     A transition not in the allowed matrix returns HTTP 409 with
         *     `InvalidTransitionErrorResponse` (STATE-03, D-37).
         */
        PatchAgentStatusRequest: {
            /** @description The target state for this transition. */
            to: components["schemas"]["AgentStatus"];
            /** @description Required when `to == Break` (STATE-04). Must reference a `break_reason` belonging to the same org. Cross-org break reason IDs return HTTP 422 (not a not_found — the entity may exist, it's just unauthorized for this org). */
            break_reason_id?: (string & components["schemas"]["UUIDv7"]) | null;
            /** @description May be set while `status == Engaged` (STATE-06). Specifies the target state when WrapUp expires. Ignored on other transitions. */
            post_interaction_state?: (string & components["schemas"]["PostInteractionState"]) | null;
            /** @description Admin override (D-84). When `true`, bypasses the transition matrix (skips HTTP 409 `invalid_transition`). Cross-row probes still run: a missing or cross-org `break_reason_id` still returns HTTP 422 (D-84 Pitfall 3). v0.1 stub auth allows any caller to pass `force=true`; v1 AUTH phase restricts to `org_admin` role. Use sparingly — every `force=true` call emits a WARN-level audit log entry (`state.force.applied`). */
            force?: boolean;
        };
        /**
         * @description The catalog entity type being imported.
         * @enum {string}
         */
        ImportEntityType: "agents" | "skills" | "queues" | "channels" | "adapters" | "break_reasons";
        /** @description A failed row in a bulk import batch (IMP-04). The original row content is NOT echoed (deferred to v0.2 IMP-11 error CSV download — OQ-2A resolution). Row numbers are 1-based; row 1 is the first data row after the header in CSV, or the first array item for JSON. */
        BulkImportFailedRow: {
            /**
             * @description 1-based row number of the failed record.
             * @example 3
             */
            row: number;
            /**
             * @description The field name that caused the failure, if field-level.
             * @example email
             */
            field?: string | null;
            /**
             * @description Always `import_failed` for row-level errors (D-36).
             * @enum {string}
             */
            error: "import_failed";
            /**
             * @description Human-readable explanation of the failure.
             * @example invalid email format
             */
            reason: string;
        };
        /**
         * @description HTTP 207 multi-status body for bulk import (IMP-05, D-37).
         *
         *     NOT an error envelope — separate top-level schema. The `failed[]` items
         *     use the error envelope shape internally for consistency, but the
         *     top-level response is its own schema.
         *
         *     HTTP 200 when all rows succeed (no `failed` entries). HTTP 207 when any
         *     rows fail AND at least one row succeeds. HTTP 422 when ALL rows fail
         *     (no rows succeeded).
         */
        BulkImportResult: {
            /** @description UUIDv7 IDs of successfully created/updated records. */
            succeeded: components["schemas"]["UUIDv7"][];
            /** @description Structured errors for each failed row. Empty when all rows succeed. */
            failed: components["schemas"]["BulkImportFailedRow"][];
            /**
             * @description True when this response is a persisted prior result returned because the client repeated the Idempotency-Key (D5-13). False or absent on a first-time POST. v0.1 KNOWN LIMITATION: when true, `succeeded` is always an empty array because the server stores only counters; rely on `failed[]` for forensics (see operation description).
             * @default false
             */
            readonly idempotent_replay: boolean | null;
        };
        /** @description A persisted import job record (IMP-06). Created by the import endpoint; queryable via `GET /v1/orgs/{org_id}/imports/{id}`. */
        ImportJob: {
            id: components["schemas"]["UUIDv7"];
            org_id: components["schemas"]["UUIDv7"];
            entity_type: components["schemas"]["ImportEntityType"];
            /**
             * @description Lifecycle of the import (D5-26). `pending` — the row was created before processing began; in v0.1 callers normally observe only `completed` or `failed` because the request is synchronous. `failed` may be set by the crash-recovery sweep (D5-11) when a process restart leaves a pending row stranded.
             * @enum {string}
             */
            status: "pending" | "completed" | "failed";
            /** @example 100 */
            total_rows: number;
            /** @example 97 */
            succeeded_rows: number;
            /** @example 3 */
            failed_rows: number;
            /** @description Structured errors for failed rows; same shape as BulkImportResult.failed. */
            errors?: components["schemas"]["BulkImportFailedRow"][] | null;
            /**
             * Format: date-time
             * @example 2026-05-15T00:00:00Z
             */
            readonly created_at: string;
        };
        /**
         * @description Phase 5 (IMP-01) per-row JSON shape for `entity=adapters`. `config`
         *     is a free-form JSONB blob passed through to storage with no
         *     validation (D-72 Phase 3 contract).
         */
        ImportAdapterRequest: {
            code: string;
            external_id?: string | null;
            name: string;
            adapter_type: string;
            /** @description Free-form vendor configuration; no fixed schema. */
            config?: {
                [key: string]: unknown;
            };
            /** @default true */
            enabled: boolean;
        };
        /**
         * @description Phase 5 (IMP-01) per-row JSON shape for `entity=agents`. Mirror of
         *     CreateAgentRequest with FK references by `code` instead of UUID.
         *     `external_id` is optional integration-mapping (mutable per D04_1-07).
         */
        ImportAgentRequest: {
            /** @example emp_0042 */
            code: string;
            /** @example HR-EMP-0042 */
            external_id?: string | null;
            /** @example Mai Linh */
            name: string;
            /**
             * Format: email
             * @example mai@example.com
             */
            email: string;
            /** @default true */
            enabled: boolean;
            /** @description Skill assignments referenced by `skill_code`. MERGE semantics (D5-18) — existing assignments not in this list are LEFT INTACT; proficiency for codes present in this list is set to the value supplied (D5-19 import wins). Skill removal via import is deferred to v0.2. */
            skills?: {
                skill_code: string;
                proficiency: number;
            }[];
        };
        /**
         * @description Phase 5 (IMP-01) per-row JSON shape for `entity=break_reasons`.
         *     `name` is a mutable display label (Phase 04.1 dropped
         *     UNIQUE(org_id, name) — IDENT-03).
         */
        ImportBreakReasonRequest: {
            code: string;
            external_id?: string | null;
            name: string;
            /** @default true */
            routable: boolean;
            /** @default 0 */
            display_order: number;
            /** @default true */
            enabled: boolean;
        };
        /**
         * @description Phase 5 (IMP-01) per-row JSON shape for `entity=channels`. FK
         *     `default_queue_code` references an existing queue by code; cross-row
         *     FK probe is performed in the same chunk transaction.
         */
        ImportChannelRequest: {
            code: string;
            external_id?: string | null;
            name: string;
            channel_type: string;
            default_queue_code?: string | null;
            /** @default true */
            enabled: boolean;
        };
        /** @description Phase 5 (IMP-01) per-row JSON shape for `entity=queues`. */
        ImportQueueRequest: {
            code: string;
            external_id?: string | null;
            name: string;
            /** @description Multi-valued — in CSV use pipe-delimited per D5-04 (`voice|chat`); in JSON use the array form. */
            channel_types: string[];
            /** @default 5 */
            priority: number;
            /** @default 0 */
            acw_sec: number;
            /** @default true */
            enabled: boolean;
        };
        /** @description Phase 5 (IMP-01) per-row JSON shape for `entity=skills`. */
        ImportSkillRequest: {
            code: string;
            external_id?: string | null;
            name: string;
            description?: string | null;
            skill_type: string;
            /** @default true */
            enabled: boolean;
        };
        /** @description Liveness check response (D-17). */
        HealthResponse: {
            /**
             * @example alive
             * @enum {string}
             */
            status: "alive";
        };
        /**
         * @description Status of an individual readiness check.
         * @enum {string}
         */
        ReadinessCheck: "ok" | "degraded";
        /** @description Deep readiness check response (D-17). */
        ReadinessResponse: {
            /**
             * @example ok
             * @enum {string}
             */
            status: "ok" | "degraded";
            checks: {
                db: components["schemas"]["ReadinessCheck"];
                redis: components["schemas"]["ReadinessCheck"];
                migrations: {
                    /**
                     * @description Current applied migration version from the schema_migrations table.
                     * @example 1
                     */
                    version: number;
                };
            };
        };
    };
    responses: {
        /** @description Invalid request body, missing required fields, type mismatch, malformed IDs, or invalid `X-Org-Id` header. */
        BadRequest: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                /**
                 * @example {
                 *       "error": "invalid_body",
                 *       "reason": "malformed_json",
                 *       "request_id": "01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e"
                 *     }
                 */
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Missing, malformed, or non-UUIDv7 `X-Org-Id` header. */
        InvalidOrgID: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                /**
                 * @example {
                 *       "error": "invalid_org_id",
                 *       "reason": "missing_header"
                 *     }
                 */
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Resource not found, or cross-org probe (FOUND-08 leakage guard — a correct ID in a different org returns 404, not 403). */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                /**
                 * @example {
                 *       "error": "not_found",
                 *       "reason": "no_such_agent",
                 *       "request_id": "01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e"
                 *     }
                 */
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Request body exceeds the 50 MB / 10,000 row import limit. Use the v0.2 async pathway for larger imports. */
        RequestEntityTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                /**
                 * @example {
                 *       "error": "invalid_body",
                 *       "reason": "request_too_large_use_async_pathway",
                 *       "request_id": "01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e"
                 *     }
                 */
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Unexpected server error. */
        InternalServerError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                /**
                 * @example {
                 *       "error": "internal",
                 *       "reason": "unexpected_error",
                 *       "request_id": "01901b2c-7f3a-7abc-8d4e-5f6a7b8c9d0e"
                 *     }
                 */
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
    };
    parameters: {
        /**
         * @description Organization UUIDv7. Present in the path for REST semantics. The
         *     authoritative `org_id` used for DB scoping is always read from the
         *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
         *     is not used for data access (FOUND-08 leakage guard: a hostile client
         *     cannot drive cross-org behavior by editing the URL because the code
         *     never reads `{org_id}` from the path).
         */
        OrgIdPath: components["schemas"]["UUIDv7"];
        /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
        EntityIdPath: components["schemas"]["UUIDv7"];
        /** @description Client-generated UUIDv7 (RFC 9562 §5.7) used to make POST retry-safe (D5-13). A repeated request with the same key in the same org returns the persisted prior result with `idempotent_replay: true` (D5-27). Optional — absent header means a fresh job row is created each call. */
        IdempotencyKeyHeader: string;
        /** @description Import job UUIDv7 returned by the POST /catalog/import endpoint. */
        ImportJobIdPath: components["schemas"]["UUIDv7"];
        /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
        CursorQuery: string;
        /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
        LimitQuery: number;
        /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
        IncludeDisabledQuery: boolean;
        /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
        NameSearchQuery: string;
        /** @description The catalog entity type being imported. */
        EntityTypeQuery: components["schemas"]["ImportEntityType"];
        /** @description CSV schema version for import validation (IMP-08). Required when `Content-Type: text/csv`. Mismatched version returns HTTP 400 with supported versions listed. Example: `v0.1`. */
        SchemaVersionQuery: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    GetHealthz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Server is alive. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    GetReadyz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All systems ready. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadinessResponse"];
                };
            };
            /** @description One or more dependency checks failed. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadinessResponse"];
                };
            };
        };
    };
    GetOpenAPISpec: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OpenAPI 3.0 YAML spec. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/yaml": string;
                };
            };
        };
    };
    GetDocs: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Interactive documentation HTML page. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/html": string;
                };
            };
        };
    };
    GetExprFunctions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The expression function catalog. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExprFunctionCatalog"];
                };
            };
        };
    };
    ListAgents: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of agents (flat, no skills). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["AgentListItem"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateAgent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateAgentRequest"];
            };
        };
        responses: {
            /** @description Agent created. Returns the full agent record with embedded skills. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Agent"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). For agents only, an optimistic
             *     concurrency conflict on PATCH may also surface here (CAT-08,
             *     ErrorCode=version_conflict). Clients should branch on
             *     ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | components["schemas"]["VersionConflictErrorResponse"];
                };
            };
            /**
             * @description Semantically invalid request — structurally valid JSON that fails a business-rule check (D-75, ROADMAP Phase 3 CRIT 4, Codex C2 iter 3). Two flavors share this status: (a) `invalid_reference` — a `skills[].skill_id` references a skill
             *         that does not exist in the caller's org (CAT-03).
             *     (b) `invalid_value` — a `skills[].proficiency` value is outside
             *         the inclusive range 1-10.
             *     The `field` slot in ErrorResponse identifies which request body path failed (e.g. `skills[0].proficiency`).
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetAgent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent with embedded skills. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Agent"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteAgent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateAgent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateAgentRequest"];
            };
        };
        responses: {
            /** @description Agent updated. Returns the full updated agent with skills. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Agent"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (CAT-08, D-37, ErrorCode=version_conflict)
             *     with the current server-side agent record returned in `current`,
             *     OR a uniqueness collision raised by a PATCH that supplied an
             *     external_id already bound to another agent in the org
             *     (ErrorCode=duplicate_external_id; Phase 5 fix H1 — UPDATE 23505
             *     previously surfaced as 500 instead of 409). Clients should branch
             *     on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Agent"];
                    });
                };
            };
            /**
             * @description Business-rule violation. ErrorCode disambiguates: (a)
             *     `immutable_field` — PATCH attempted to change a field that is
             *     immutable in v0.1 (e.g., `code`; rename support deferred to
             *     v0.2 per D04_1-02); (b) `invalid_reference` — a `skills[].skill_id`
             *     references a skill that does not exist in the caller's org
             *     (CAT-03, PUT-semantics replacement); (c) `invalid_value` — a
             *     `skills[].proficiency` value is outside the inclusive range
             *     1-10. The `field` slot in ErrorResponse identifies which
             *     request body path failed (e.g. `code` or `skills[0].proficiency`).
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetAgentStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current agent state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentState"];
                };
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    PatchAgentStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PatchAgentStatusRequest"];
            };
        };
        responses: {
            /** @description State transition succeeded. Returns the full updated agent state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentState"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /** @description Invalid state transition. `from` and `to` identify the requested transition that is not in the allowed matrix (STATE-03, D-37). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvalidTransitionErrorResponse"];
                };
            };
            /** @description Semantically invalid request — e.g. `break_reason_id` not found or cross-org (STATE-04). Structurally valid JSON but business-rule violation. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListSkills: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of skills. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["Skill"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateSkill: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateSkillRequest"];
            };
        };
        responses: {
            /** @description Skill created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Skill"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). Clients should branch on
             *     ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetSkill: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Skill record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Skill"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteSkill: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Skill disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateSkill: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateSkillRequest"];
            };
        };
        responses: {
            /** @description Skill updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Skill"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (D-37, ErrorCode=version_conflict)
             *     with the current server-side skill record returned in `current`,
             *     OR a uniqueness collision raised by a PATCH that supplied an
             *     external_id already bound to another skill in the org
             *     (ErrorCode=duplicate_external_id; Phase 5 fix H1). Clients should
             *     branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Skill"];
                    });
                };
            };
            /**
             * @description Business-rule violation: `code` is immutable post-create
             *     (D04_1-02); PATCH attempting to change it returns
             *     ErrorCode=immutable_field. Rename support deferred to v0.2.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListFlows: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of flow drafts. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["Flow"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateFlowRequest"];
            };
        };
        responses: {
            /** @description Flow draft created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Flow"];
                };
            };
            400: components["responses"]["BadRequest"];
            /** @description Duplicate `code` within the org (ErrorCode=duplicate_code). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Flow draft record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Flow"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Flow draft soft-deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateFlowRequest"];
            };
        };
        responses: {
            /** @description Flow draft updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Flow"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (ErrorCode=version_conflict) with the
             *     current server-side flow record returned in `current`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Flow"];
                    };
                };
            };
            /** @description Immutable field violation (ErrorCode=immutable_field) when `code` differs from the stored value. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ValidateFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Validation result (valid=false with issues is still a 200). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowValidationResult"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    SimulateFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SimulateFlowRequest"];
            };
        };
        responses: {
            /** @description Deterministic simulation trace. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SimulateFlowResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /** @description Draft graph is invalid and cannot be simulated. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowValidationResult"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListFlowTraces: {
        parameters: {
            query?: {
                limit?: number;
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Traces for the flow, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        traces: components["schemas"]["Trace"][];
                    };
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    PublishFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PublishFlowRequest"];
            };
        };
        responses: {
            /** @description Published version created and binding activated. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowPublishResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /** @description Optimistic-concurrency conflict on the draft being published. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Draft graph is invalid (publish requires validation to pass). */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowValidationResult"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    RollbackFlow: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RollbackFlowRequest"];
            };
        };
        responses: {
            /** @description Prior version re-activated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowPublishResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    ListFlowVersions: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Published versions, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["FlowVersion"][];
                    };
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    GetFlowVersion: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Published flow version. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FlowVersion"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    ListRouteRequests: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                status?: "pending" | "running" | "waiting" | "completed" | "failed" | "cancelled";
                channel?: string;
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of route requests. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["RouteRequest"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateRouteRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRouteRequest"];
            };
        };
        responses: {
            /** @description Route request created (inspect status/failure_code for the routing outcome). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RouteRequest"];
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    GetRouteRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Route request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RouteRequest"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    GetRouteRequestTrace: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Runtime trace. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Trace"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    GetReservation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reservation. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Reservation"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    AcceptReservation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reservation accepted (or idempotent no-op for the already-accepted winner). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Reservation"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description Reservation is no longer offered (lost an accept/timeout race). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    RejectReservation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reservation rejected. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Reservation"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description Reservation is no longer offered. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    SubmitRouteInput: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SubmitRouteInput"];
            };
        };
        responses: {
            /** @description Input accepted; the route resumed (run to completion or re-parked). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RouteRequest"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description Route is not waiting for input (already resumed, completed, or raced). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetTrace: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Trace. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Trace"];
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    ListRouteRequestReservations: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reservations for the route request, oldest attempt first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["Reservation"][];
                    };
                };
            };
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CompleteReservation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reservation completed (agent moves to WrapUp via agent-state rules). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Reservation"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description Reservation is not in the accepted state. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListFlowEntryBindings: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Active bindings for the org. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["FlowEntryBinding"][];
                    };
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListQueues: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of queues. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["Queue"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateQueue: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateQueueRequest"];
            };
        };
        responses: {
            /** @description Queue created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Queue"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). Clients should branch on
             *     ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetQueue: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Queue record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Queue"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteQueue: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Queue disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateQueue: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateQueueRequest"];
            };
        };
        responses: {
            /** @description Queue updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Queue"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (D-37, ErrorCode=version_conflict)
             *     with the current server-side queue record returned in `current`,
             *     OR a uniqueness collision raised by a PATCH that supplied an
             *     external_id already bound to another queue in the org
             *     (ErrorCode=duplicate_external_id; Phase 5 fix H1). Clients should
             *     branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Queue"];
                    });
                };
            };
            /**
             * @description Business-rule violation: `code` is immutable post-create
             *     (D04_1-02); PATCH attempting to change it returns
             *     ErrorCode=immutable_field. Rename support deferred to v0.2.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListChannels: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of channels. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["Channel"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateChannel: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateChannelRequest"];
            };
        };
        responses: {
            /** @description Channel created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Channel"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). Clients should branch on
             *     ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Semantically invalid request — structurally valid JSON that fails a business-rule check (D-75, D-76). For CreateChannel: the supplied `default_queue_id` does not exist (or is disabled) in the caller's org. Returns `ErrorCode=invalid_reference`. The `field` slot in ErrorResponse identifies which request body path failed (e.g. `default_queue_id`). */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetChannel: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Channel record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Channel"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteChannel: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Channel disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateChannel: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateChannelRequest"];
            };
        };
        responses: {
            /** @description Channel updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Channel"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (D-37, ErrorCode=version_conflict)
             *     with the current server-side channel record returned in `current`,
             *     OR a uniqueness collision raised by a PATCH that supplied an
             *     external_id already bound to another channel in the org
             *     (ErrorCode=duplicate_external_id; Phase 5 fix H1). Clients should
             *     branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Channel"];
                    });
                };
            };
            /**
             * @description Business-rule violation. ErrorCode disambiguates: (a)
             *     `immutable_field` — PATCH attempted to change a field that is
             *     immutable in v0.1 (e.g., `code`; rename support deferred to
             *     v0.2 per D04_1-02); (b) `invalid_reference` — the supplied
             *     `default_queue_id` does not exist (or is disabled) in the
             *     caller's org. The `field` slot in ErrorResponse identifies
             *     which request body path failed (e.g. `code` or `default_queue_id`).
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListAdapters: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of adapters. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["Adapter"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateAdapter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateAdapterRequest"];
            };
        };
        responses: {
            /** @description Adapter created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Adapter"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). Phase 04.1 added these
             *     uniqueness constraints to adapters — pre-04.1 adapters had only
             *     PRIMARY KEY(id) uniqueness so 23505 was impossible.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetAdapter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Adapter record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Adapter"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteAdapter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Adapter disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateAdapter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateAdapterRequest"];
            };
        };
        responses: {
            /** @description Adapter updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Adapter"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (D-37, ErrorCode=version_conflict)
             *     with the current server-side adapter record returned in `current`,
             *     OR a uniqueness collision raised by a PATCH that supplied an
             *     external_id already bound to another adapter in the org
             *     (ErrorCode=duplicate_external_id; Phase 5 fix H1). Clients should
             *     branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["Adapter"];
                    });
                };
            };
            /**
             * @description Business-rule violation: `code` is immutable post-create
             *     (D04_1-02); PATCH attempting to change it returns
             *     ErrorCode=immutable_field. Rename support deferred to v0.2.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    ListBreakReasons: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor returned as `next_cursor` from the previous list response. Omit to fetch the first page. */
                cursor?: components["parameters"]["CursorQuery"];
                /** @description Maximum number of items to return per page. Defaults to 25. Maximum 100. */
                limit?: components["parameters"]["LimitQuery"];
                /** @description When `true`, include disabled entities (`enabled=false`) in list responses. Default `false` (CAT-09). */
                include_disabled?: components["parameters"]["IncludeDisabledQuery"];
                /** @description Case-insensitive substring search on the `name` field (ILIKE `%?%`). Returns entities whose name contains the value anywhere (not prefix-only — OQ-4B resolution). Add `pg_trgm` index for performance at larger catalog sizes. */
                name?: components["parameters"]["NameSearchQuery"];
            };
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated list of break reasons. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList"] & {
                        items?: components["schemas"]["BreakReason"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            500: components["responses"]["InternalServerError"];
        };
    };
    CreateBreakReason: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateBreakReasonRequest"];
            };
        };
        responses: {
            /** @description Break reason created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakReason"];
                };
            };
            400: components["responses"]["BadRequest"];
            /**
             * @description Duplicate `code` within the org (ErrorCode=duplicate_code), or
             *     duplicate `external_id` within the org when set
             *     (ErrorCode=duplicate_external_id). Phase 04.1 dropped the
             *     previous `UNIQUE(org_id, name)` constraint and replaced it
             *     with the universal `(org_id, code)` identity model (D04_1-09).
             *     Clients should branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetBreakReason: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Break reason record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakReason"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    DeleteBreakReason: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Break reason disabled. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
    UpdateBreakReason: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Entity UUIDv7 primary key. Must be a valid UUIDv7; UUIDv4 or lower returns HTTP 400 `invalid_id`. */
                id: components["parameters"]["EntityIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateBreakReasonRequest"];
            };
        };
        responses: {
            /** @description Break reason updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakReason"];
                };
            };
            400: components["responses"]["BadRequest"];
            404: components["responses"]["NotFound"];
            /**
             * @description Optimistic-concurrency conflict (D-37, ErrorCode=version_conflict)
             *     with the current server-side break_reason record returned in
             *     `current`, OR a uniqueness collision raised by a PATCH that
             *     supplied an external_id already bound to another break_reason in
             *     the org (ErrorCode=duplicate_external_id; Phase 5 fix H1). Clients
             *     should branch on ErrorCode, not parse the description text.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"] | (components["schemas"]["VersionConflictErrorResponse"] & {
                        current?: components["schemas"]["BreakReason"];
                    });
                };
            };
            /**
             * @description Business-rule violation: `code` is immutable post-create
             *     (D04_1-02); PATCH attempting to change it returns
             *     ErrorCode=immutable_field. Rename support deferred to v0.2.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    BulkImportCatalog: {
        parameters: {
            query: {
                /** @description The catalog entity type being imported. */
                entity: components["parameters"]["EntityTypeQuery"];
                /** @description CSV schema version for import validation (IMP-08). Required when `Content-Type: text/csv`. Mismatched version returns HTTP 400 with supported versions listed. Example: `v0.1`. */
                schema_version?: components["parameters"]["SchemaVersionQuery"];
            };
            header?: {
                /** @description Client-generated UUIDv7 (RFC 9562 §5.7) used to make POST retry-safe (D5-13). A repeated request with the same key in the same org returns the persisted prior result with `idempotent_replay: true` (D5-27). Optional — absent header means a fresh job row is created each call. */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKeyHeader"];
            };
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": unknown[];
                "text/csv": string;
            };
        };
        responses: {
            /** @description All rows imported successfully. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BulkImportResult"];
                };
            };
            /** @description Multi-status — some rows succeeded, some failed (IMP-05, D-37). The `succeeded` array contains IDs of created/updated records; `failed` contains structured errors for each failed row. */
            207: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BulkImportResult"];
                };
            };
            /** @description Invalid request — malformed JSON/CSV body, missing required fields, unsupported `schema_version` (IMP-08), or missing `entity` query parameter. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            413: components["responses"]["RequestEntityTooLarge"];
            /** @description All rows failed — no rows succeeded. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BulkImportResult"];
                };
            };
            500: components["responses"]["InternalServerError"];
        };
    };
    GetImportJob: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Organization UUIDv7. Present in the path for REST semantics. The
                 *     authoritative `org_id` used for DB scoping is always read from the
                 *     `X-Org-Id` header by the `OrgContext` middleware — this path parameter
                 *     is not used for data access (FOUND-08 leakage guard: a hostile client
                 *     cannot drive cross-org behavior by editing the URL because the code
                 *     never reads `{org_id}` from the path).
                 */
                org_id: components["parameters"]["OrgIdPath"];
                /** @description Import job UUIDv7 returned by the POST /catalog/import endpoint. */
                import_id: components["parameters"]["ImportJobIdPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Import job record. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ImportJob"];
                };
            };
            400: components["responses"]["InvalidOrgID"];
            404: components["responses"]["NotFound"];
            500: components["responses"]["InternalServerError"];
        };
    };
}
