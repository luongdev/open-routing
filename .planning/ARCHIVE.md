# GSD Archive

v0.1 Catalog Foundation was archived on 2026-06-02.

Current shipped behavior lives in `openspec/specs/`. Completed OpenSpec changes live under `openspec/changes/archive/`.

Use `.planning/` only as historical implementation context. Start future milestone work from a new OpenSpec change, not from unchecked GSD plan rows.
