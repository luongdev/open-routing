---
phase: 01-foundation-polyglot-monorepo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/pnpm-workspace.yaml
  - web/package.json
  - web/turbo.json
  - web/tsconfig.base.json
  - web/eslint.config.js
  - web/.gitignore
  - web/.npmrc
  - web/apps/admin/package.json
  - web/apps/admin/tsconfig.json
  - web/apps/admin/src/index.ts
  - web/apps/embed/package.json
  - web/apps/embed/tsconfig.json
  - web/apps/embed/src/index.ts
  - web/packages/ui/package.json
  - web/packages/ui/tsconfig.json
  - web/packages/ui/src/index.ts
  - web/pnpm-lock.yaml
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "pnpm workspace at web/ resolves three packages: @open-routing/admin, @open-routing/embed, @open-routing/ui (D-12, FOUND-01)"
    - "cd web && pnpm typecheck succeeds against all three packages — workspace is ready for Plan 08 CI to wire into the FOUND-09 web-typecheck job"
    - "cd web && pnpm lint succeeds against all three packages — workspace is ready for Plan 08 CI to wire into the FOUND-09 web-lint job"
    - "Turborepo lives ONLY in web/turbo.json — Go side never references turbo (D-09)"
    - "Each package src/index.ts is an empty stub (export {}) — D-12 forbids Vite/Lit/Shoelace in Phase 1"
    - "No Vite config, no Lit components, no Shoelace integration ship in Phase 1; pnpm typecheck/lint stay meaningful for FOUND-09 frontend jobs (D-13)"
  artifacts:
    - path: "web/pnpm-workspace.yaml"
      provides: "Declares apps/* and packages/* as workspace members"
      contains: "packages:"
    - path: "web/package.json"
      provides: "Workspace root with typecheck/lint scripts delegating to turbo"
      contains: "\"turbo\":"
    - path: "web/turbo.json"
      provides: "Turborepo pipelines for typecheck + lint (D-09)"
      contains: "\"typecheck\""
    - path: "web/tsconfig.base.json"
      provides: "Shared strict TS config every per-package tsconfig extends"
      contains: "\"strict\": true"
    - path: "web/apps/admin/src/index.ts"
      provides: "Empty stub per D-12 (Phase 6 fills with Vite + Lit)"
      contains: "export {}"
  key_links:
    - from: "web/apps/admin/tsconfig.json"
      to: "web/tsconfig.base.json"
      via: "extends: ../../tsconfig.base.json"
      pattern: "\"extends\": \"../../tsconfig.base.json\""
    - from: "web/package.json scripts.typecheck"
      to: "web/turbo.json tasks.typecheck"
      via: "turbo CLI dispatching per-package tsc --noEmit"
      pattern: "\"typecheck\": \"turbo typecheck\""
---

<objective>
Bootstrap the pnpm workspace stub under `web/` per D-12. Phase 1 ships only the minimal scaffold sufficient to make CI's `web-typecheck` and `web-lint` jobs (Plan 08) pass — three packages (`@open-routing/admin`, `@open-routing/embed`, `@open-routing/ui`), each with an empty `index.ts`. No Vite, no Lit, no Shoelace — those land in Phase 6 (`admin` + `ui`) and Phase 7 (`embed`).

Purpose: Satisfies the FOUND-01 directory layout half ("`web/` (pnpm workspace containing `apps/admin`, `apps/embed`, `packages/ui`)") and stages the inputs Plan 08 needs for the FOUND-09 frontend CI half (`pnpm -F '*' typecheck` and `pnpm -F '*' lint` runnable from `web/`; Plan 08 owns the CI workflow that actually gates merges on FOUND-09). Locks the workspace shape so Phases 6 and 7 only need to add Vite/Lit code into existing packages — no restructure required later.

Output: pnpm install succeeds; `pnpm typecheck` and `pnpm lint` from `web/` succeed (no errors); `pnpm-lock.yaml` committed.

This plan is independent of Plan 01 — `web/` has no dependencies on `services/api/` and runs in Wave 1 in parallel with Plan 01.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md
@.planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md
@.planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md

<interfaces>
<!-- pnpm workspace shape locked by D-12 / FOUND-01 -->

Workspace members (web/pnpm-workspace.yaml resolves via glob):
  apps/*
  packages/*

Per-package npm names (locked):
  @open-routing/admin    at web/apps/admin/
  @open-routing/embed    at web/apps/embed/
  @open-routing/ui       at web/packages/ui/

Per-package scripts (every package exposes the same shape):
  typecheck: "tsc --noEmit"
  lint:      "eslint src --max-warnings 0"

Workspace-root scripts (web/package.json):
  typecheck: "turbo typecheck"
  lint:      "turbo lint"

Locked devDependencies at workspace root (Research §Standard Stack §pnpm side, lines 156-163):
  turbo                                  ^2.0.0
  typescript                             ^5.6.0
  eslint                                 ^9.0.0
  @typescript-eslint/parser              ^8.0.0
  @typescript-eslint/eslint-plugin       ^8.0.0
  prettier                               ^3.0.0   (optional, recommended)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pnpm workspace root files</name>
  <files>web/pnpm-workspace.yaml, web/package.json, web/.npmrc, web/turbo.json, web/tsconfig.base.json, web/eslint.config.js, web/.gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Example 8: pnpm workspace root files" (lines 1394-1454) — copy verbatim
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-09 (Turborepo inside web/ ONLY), §D-12 (minimal scaffold files), §D-13 (no Vite/Lit/Shoelace)
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L13 (lines 793-860)
  </read_first>
  <action>
    Create `web/` directory and seven root files.

    File 1 — `web/pnpm-workspace.yaml` — exactly two glob lines:
    ```yaml
    packages:
      - 'apps/*'
      - 'packages/*'
    ```

    File 2 — `web/package.json` — copy from Research §Example 8 lines 1404-1420, with `"type": "module"` added (ESLint v9 flat config requires ESM):
    ```json
    {
      "name": "open-routing-web",
      "private": true,
      "type": "module",
      "packageManager": "pnpm@10.33.0",
      "scripts": {
        "typecheck": "turbo typecheck",
        "lint": "turbo lint",
        "format": "prettier --write '**/*.{ts,tsx,js,json,md}'"
      },
      "devDependencies": {
        "turbo": "^2.0.0",
        "typescript": "^5.6.0",
        "eslint": "^9.0.0",
        "@typescript-eslint/parser": "^8.0.0",
        "@typescript-eslint/eslint-plugin": "^8.0.0",
        "prettier": "^3.0.0"
      }
    }
    ```
    The `packageManager` field locks pnpm to v10.33.0 per Research §Environment Availability line 1555.

    File 3 — `web/.npmrc` — workspace defaults:
    ```
    strict-peer-dependencies=false
    auto-install-peers=true
    shamefully-hoist=false
    ```
    `shamefully-hoist=false` preserves pnpm's strict isolated `node_modules` layout (prevents phantom dep risk).

    File 4 — `web/turbo.json` — copy verbatim from Research §Example 8 lines 1422-1436:
    ```json
    {
      "$schema": "https://turbo.build/schema.json",
      "tasks": {
        "typecheck": {
          "dependsOn": ["^typecheck"],
          "outputs": []
        },
        "lint": {
          "outputs": []
        }
      }
    }
    ```
    The `dependsOn: ["^typecheck"]` ensures typecheck runs in topological order — when `apps/admin` later imports from `packages/ui`, ui's typecheck completes first.

    File 5 — `web/tsconfig.base.json` — copy verbatim from Research §Example 8 lines 1438-1454, with `lib` array added so DOM globals resolve in stubs:
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "noImplicitOverride": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "isolatedModules": true,
        "verbatimModuleSyntax": true,
        "lib": ["ES2022", "DOM", "DOM.Iterable"]
      }
    }
    ```

    File 6 — `web/eslint.config.js` — flat config per Claude's Discretion bullet 3, ESM (matches `"type": "module"`):
    ```js
    import tsParser from '@typescript-eslint/parser';
    import tsPlugin from '@typescript-eslint/eslint-plugin';

    export default [
      {
        files: ['**/*.ts'],
        languageOptions: {
          parser: tsParser,
          parserOptions: {
            ecmaVersion: 2022,
            sourceType: 'module',
            project: false,
          },
        },
        plugins: {
          '@typescript-eslint': tsPlugin,
        },
        rules: {
          ...tsPlugin.configs.recommended.rules,
        },
      },
      {
        ignores: ['**/node_modules/**', '**/dist/**', '**/.turbo/**'],
      },
    ];
    ```
    Use `project: false` (not `project: true`) — empty `index.ts` files in Phase 1 do not justify the type-aware lint overhead. Phase 6/7 can switch to type-aware lint once real TS code exists.

    File 7 — `web/.gitignore`:
    ```
    node_modules/
    .turbo/
    dist/
    *.tsbuildinfo
    .DS_Store
    ```
  </action>
  <verify>
    <automated>test -f web/pnpm-workspace.yaml &amp;&amp; test -f web/package.json &amp;&amp; test -f web/turbo.json &amp;&amp; test -f web/tsconfig.base.json &amp;&amp; test -f web/eslint.config.js &amp;&amp; test -f web/.npmrc &amp;&amp; test -f web/.gitignore &amp;&amp; grep -q "apps/" web/pnpm-workspace.yaml &amp;&amp; grep -q "packages/" web/pnpm-workspace.yaml &amp;&amp; grep -q 'open-routing-web' web/package.json &amp;&amp; grep -q '"type": "module"' web/package.json &amp;&amp; grep -q '"turbo":' web/package.json &amp;&amp; grep -q 'turbo.build/schema.json' web/turbo.json &amp;&amp; grep -q '"strict": true' web/tsconfig.base.json &amp;&amp; grep -q '"noUncheckedIndexedAccess": true' web/tsconfig.base.json &amp;&amp; grep -q 'tsPlugin' web/eslint.config.js</automated>
  </verify>
  <done>Seven workspace root files exist with required content; pnpm-workspace.yaml declares apps/* + packages/*; package.json declares ESM + workspace devDeps; turbo.json has typecheck + lint tasks; tsconfig.base.json has strict-mode config</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create three sub-packages (admin, embed, ui) with empty index.ts stubs</name>
  <files>web/apps/admin/package.json, web/apps/admin/tsconfig.json, web/apps/admin/src/index.ts, web/apps/embed/package.json, web/apps/embed/tsconfig.json, web/apps/embed/src/index.ts, web/packages/ui/package.json, web/packages/ui/tsconfig.json, web/packages/ui/src/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L13 §"web/apps/admin/" (lines 862-888), §"embed" (lines 890-892), §"packages/ui" (lines 894-896)
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-12 (exact file list per package — package.json, tsconfig.json, src/index.ts; index.ts exports nothing), §D-13 (no Vite/Lit/Shoelace in Phase 1)
    - web/tsconfig.base.json (created in Task 1) — per-package tsconfig extends this
  </read_first>
  <action>
    Create three sub-packages, each with exactly three files in the same shape. The package names are LOCKED per D-12 / interfaces block. Do NOT add a `main`, `module`, `exports`, `types`, `files`, or `dependencies` field — they will land in Phase 6/7 when real code arrives.

    Package 1 — `web/apps/admin/`:

    `web/apps/admin/package.json`:
    ```json
    {
      "name": "@open-routing/admin",
      "private": true,
      "version": "0.0.0",
      "type": "module",
      "scripts": {
        "typecheck": "tsc --noEmit",
        "lint": "eslint src --max-warnings 0"
      }
    }
    ```

    `web/apps/admin/tsconfig.json`:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "rootDir": "src",
        "outDir": "dist"
      },
      "include": ["src"]
    }
    ```
    `outDir` is set so `tsc --noEmit` still respects it for diagnostics path resolution. Per D-13 no real build runs; `--noEmit` skips writing dist/.

    `web/apps/admin/src/index.ts`:
    ```ts
    export {};
    ```
    The `export {}` makes the file a module (without it, TypeScript treats a bare file as a script, which conflicts with `isolatedModules: true` from tsconfig.base.json). PER D-12: "`index.ts` exports nothing" — this is the canonical empty-module idiom.

    Package 2 — `web/apps/embed/`: mirror admin shape exactly with `"name": "@open-routing/embed"` in package.json. tsconfig.json + src/index.ts are byte-identical to admin.

    Package 3 — `web/packages/ui/`: mirror admin shape exactly with `"name": "@open-routing/ui"` in package.json. tsconfig.json's `extends` path stays `"../../tsconfig.base.json"` (depth from `web/packages/ui/` to `web/` is the same two levels as `web/apps/admin/` to `web/`). src/index.ts byte-identical.

    Anti-pattern guard: do NOT add `lit`, `@shoelace-style/shoelace`, `vite`, or `@open-routing/ui` as dependencies of `admin` or `embed` in this plan. D-13 explicitly forbids them in Phase 1. The Phase 6 plan adds `admin -> ui` workspace dependency via `"@open-routing/ui": "workspace:*"`.
  </action>
  <verify>
    <automated>test -f web/apps/admin/package.json &amp;&amp; test -f web/apps/admin/tsconfig.json &amp;&amp; test -f web/apps/admin/src/index.ts &amp;&amp; test -f web/apps/embed/package.json &amp;&amp; test -f web/apps/embed/tsconfig.json &amp;&amp; test -f web/apps/embed/src/index.ts &amp;&amp; test -f web/packages/ui/package.json &amp;&amp; test -f web/packages/ui/tsconfig.json &amp;&amp; test -f web/packages/ui/src/index.ts &amp;&amp; grep -q '"@open-routing/admin"' web/apps/admin/package.json &amp;&amp; grep -q '"@open-routing/embed"' web/apps/embed/package.json &amp;&amp; grep -q '"@open-routing/ui"' web/packages/ui/package.json &amp;&amp; grep -q '"tsc --noEmit"' web/apps/admin/package.json &amp;&amp; grep -q '"extends": "../../tsconfig.base.json"' web/apps/admin/tsconfig.json &amp;&amp; grep -q '"extends": "../../tsconfig.base.json"' web/packages/ui/tsconfig.json &amp;&amp; grep -q 'export {}' web/apps/admin/src/index.ts &amp;&amp; grep -q 'export {}' web/apps/embed/src/index.ts &amp;&amp; grep -q 'export {}' web/packages/ui/src/index.ts &amp;&amp; ! grep -q 'lit' web/apps/admin/package.json &amp;&amp; ! grep -q 'shoelace' web/apps/admin/package.json &amp;&amp; ! grep -q 'vite' web/apps/admin/package.json</automated>
  </verify>
  <done>Three packages with three files each (9 files total); all package.json names match the locked @open-routing/* pattern; all index.ts are `export {}`; no lit/shoelace/vite in any package.json</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run pnpm install, generate lockfile, validate pnpm typecheck + pnpm lint succeed</name>
  <files>web/pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-30 web-typecheck + web-lint CI jobs
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Pattern 5: GitHub Actions matrix" §web-typecheck job (lines 868-882) and §web-lint job (lines 884-896) — both run `pnpm install --frozen-lockfile` first, requiring a committed lockfile
    - web/package.json + all per-package package.json files created in Tasks 1-2 — sources of truth for pnpm install
  </read_first>
  <action>
    Run pnpm install in the `web/` workspace to produce `web/pnpm-lock.yaml`, then verify both pipeline scripts succeed end-to-end.

    Step 1 — Verify pnpm version. `pnpm --version` must return 10.x. If pnpm is unavailable on PATH, run `corepack enable pnpm` first (corepack ships with Node 22 per Research §Environment Availability). If pnpm still fails, stop and surface an error — the workspace cannot proceed without it.

    Step 2 — `cd web && pnpm install`. This resolves all `devDependencies` from root package.json, links the three sub-packages into `node_modules/`, and writes `pnpm-lock.yaml`. The lockfile MUST be created — Plan 08's CI workflow uses `pnpm install --frozen-lockfile` which fails without one.

    Step 3 — `cd web && pnpm typecheck`. This runs `turbo typecheck`, which dispatches `tsc --noEmit` in each of the three packages. Expected outcome: all three packages report success with zero errors (the `export {}` stub is the simplest valid TS file). Capture stdout into the task log.

    Step 4 — `cd web && pnpm lint`. This runs `turbo lint`, dispatching `eslint src --max-warnings 0` in each package. Expected outcome: zero warnings, zero errors. The eslint flat config in `web/eslint.config.js` will scan each package's `src/` directory; `export {};` triggers no rules.

    Anti-pattern guards:
    - Do NOT delete `pnpm-lock.yaml` after generation — CI depends on it (Plan 08 runs `pnpm install --frozen-lockfile`).
    - Do NOT add `--ignore-workspace-cycles` or `--shamefully-hoist` flags — the default strict pnpm behavior is the locked stance.
    - If a `peer dependency missing` warning appears for `eslint` / `@typescript-eslint/*`, ensure `auto-install-peers=true` is set in `.npmrc` (Task 1); if still failing, document the warning in the SUMMARY and continue.

    If `pnpm install` reports a network failure (no registry access in the agent env), record the error in the SUMMARY and proceed without lock — but flag this as a known gap. CI will not pass until the lockfile exists. The agent should still execute Step 3/Step 4 from a previously-installed local node_modules if present, otherwise mark as "pending CI environment".
  </action>
  <verify>
    <automated>test -f web/pnpm-lock.yaml &amp;&amp; (cd web &amp;&amp; pnpm typecheck) &amp;&amp; (cd web &amp;&amp; pnpm lint)</automated>
  </verify>
  <done>pnpm-lock.yaml exists; `pnpm typecheck` exits 0 with all three packages reporting success; `pnpm lint` exits 0 with no warnings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ web/pnpm-lock.yaml | Supply-chain trust — every package version installed by pnpm must be slopcheck-verified |
| filesystem ↔ pnpm | pnpm-lock.yaml integrity is what makes CI reproducible — if the lockfile is missing or tampered, CI installs different code than dev |
| eslint config ↔ user code | ESLint runs JS that can execute arbitrary code; flat config sources are limited to the two @typescript-eslint/* packages declared in devDependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | web/package.json npm installs | mitigate | Research §Package Legitimacy Audit (lines 220-251) ran slopcheck against `typescript`, `turbo`, `eslint`, `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin`, `prettier`: all [OK]. No SLOP packages. No blocking checkpoint required |
| T-1-99 | Information disclosure | web/pnpm-lock.yaml | accept | Lockfile is committed and public; contains version hashes only, no secrets. Locked-version installs prevent CI from silently upgrading to a malicious package release |
</threat_model>

<verification>
After all tasks complete, run as a single smoke sequence (mirrors what Plan 08 CI will run):

```bash
test -d web/apps/admin && test -d web/apps/embed && test -d web/packages/ui
test -f web/pnpm-workspace.yaml && test -f web/pnpm-lock.yaml
cd web
pnpm install --frozen-lockfile    # CI mode — proves the lockfile is consistent with package.jsons
pnpm typecheck                     # both turbo tasks succeed
pnpm lint
```

The frozen-lockfile install is the most important check: it proves Plan 08's `web-typecheck` and `web-lint` CI jobs will pass.
</verification>

<success_criteria>
- All 17 files in `files_modified` exist on disk
- `web/pnpm-workspace.yaml` lists `apps/*` and `packages/*`
- `web/package.json` has all six locked devDependencies (turbo, typescript, eslint, @typescript-eslint/parser, @typescript-eslint/eslint-plugin, prettier)
- Each sub-package has `name`, `private: true`, `type: module`, and the two scripts (`typecheck`, `lint`)
- Each `src/index.ts` is the single line `export {};`
- `cd web && pnpm install` produces `pnpm-lock.yaml`
- `cd web && pnpm typecheck` exits 0 with no errors across all three packages
- `cd web && pnpm lint` exits 0 with zero warnings across all three packages
- No `lit`, `@shoelace-style/shoelace`, or `vite` appears anywhere in `web/` package.json files (D-13 verification)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-polyglot-monorepo/01-02-SUMMARY.md` capturing:
- Three workspace member packages and their npm names
- Exact pinned dependency versions resolved into pnpm-lock.yaml (top-line summary; full list in lockfile)
- Output of `pnpm typecheck` (last line) and `pnpm lint` (last line) for each package
- Confirmation that no Vite/Lit/Shoelace appears (D-13 honored)
- Any peer-dependency warnings observed during install (for Phase 6/7 awareness)
</output>
