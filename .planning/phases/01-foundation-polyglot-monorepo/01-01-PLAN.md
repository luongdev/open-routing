---
phase: 01-foundation-polyglot-monorepo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .editorconfig
  - .gitattributes
  - .env.example
  - docker-compose.yml
  - Taskfile.yml
  - README.md
  - openapi/.gitkeep
  - migrations/000001_create_scaffold.up.sql
  - migrations/000001_create_scaffold.down.sql
  - services/api/go.mod
  - services/api/go.sum
  - services/api/sqlc.yaml
  - services/api/.golangci.yml
  - services/api/.air.toml
  - services/api/internal/config/config.go
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-06
  - FOUND-10

must_haves:
  truths:
    - "Repo root has services/api/, web/, openapi/, migrations/ directories (FOUND-01)"
    - "docker compose up brings up postgres:17 and redis:7-alpine with healthchecks (FOUND-10, D-25)"
    - "migrations/000001_create_scaffold.up.sql defines _scaffold table per D-18 with org_id UUID NOT NULL and UNIQUE(org_id, external_id) (FOUND-02, FOUND-06)"
    - "Migrations live at /migrations/ at repo root with NNNNNN_name.up.sql / down.sql naming (D-10)"
    - "Single Go module at services/api/go.mod with path github.com/luongdev/open-routing/services/api; no root go.mod, no go.work (D-22)"
    - "Taskfile.yml at repo root contains the locked target list — test, lint, gen, migrate-up, migrate-down, migrate-create, dev, build, ci (D-08)"
    - "task dev flow: docker compose up -d postgres redis → wait healthchecks → optional migrate-up reminder → air hot-reload (D-24)"
    - "docker-compose.yml contains exactly postgres:17 + redis:7-alpine — no api service, no Jaeger/OTel collector (D-15, D-25)"
    - "Postgres + Redis healthchecks use pg_isready and redis-cli ping (D-26)"
    - "Wave-0 CLI tools (task, migrate, air) installed and on PATH"
  artifacts:
    - path: "services/api/go.mod"
      provides: "Go module manifest pinning all locked stack dependencies"
      contains: "module github.com/luongdev/open-routing/services/api"
    - path: "migrations/000001_create_scaffold.up.sql"
      provides: "First migration; satisfies FOUND-02 + FOUND-06"
      contains: "UNIQUE (org_id, external_id)"
    - path: "docker-compose.yml"
      provides: "postgres:17 + redis:7-alpine services with healthchecks"
      contains: "image: postgres:17"
    - path: "Taskfile.yml"
      provides: "Top-level orchestrator with migrate-up, migrate-down, migrate-create, dev, test, lint, typecheck, gen, build, ci targets"
      contains: "migrate-up:"
    - path: "services/api/sqlc.yaml"
      provides: "sqlc config with sql_package: pgx/v5"
      contains: "sql_package: \"pgx/v5\""
  key_links:
    - from: "Taskfile.yml task migrate-up"
      to: "migrate CLI binary"
      via: "task → migrate -path migrations -database \"$DATABASE_URL\" up"
      pattern: "migrate -path migrations"
    - from: "docker-compose.yml postgres service"
      to: "$DATABASE_URL in Taskfile.yml env"
      via: "5432 port binding + openrouting credentials"
      pattern: "POSTGRES_USER: openrouting"
    - from: "services/api/sqlc.yaml schema field"
      to: "../../migrations"
      via: "relative path to repo-root migrations/ dir"
      pattern: "schema: \"../../migrations\""
---

<objective>
Bootstrap repo-root scaffolding for the polyglot monorepo: directory layout, root-level config files, docker-compose with postgres + redis healthchecks, the first golang-migrate SQL migration creating `_scaffold` (D-18), the Go module at `services/api/`, the Taskfile orchestrator (D-08), sqlc/golangci/air configs, the env-loader package, and the `.env.example` checked-in template. Also installs the three Wave-0 CLI binaries (task, migrate, air) that subsequent plans require.

Purpose: Every later plan in this phase depends on the module path, the migrations layout, the docker-compose Postgres+Redis pair, and the Taskfile target names — establishing these correctly here prevents reworking five other plans. Implements FOUND-01 (monorepo layout), FOUND-02 (org_id UUID NOT NULL on every org-scoped table), FOUND-06 (UNIQUE(org_id, external_id) composite constraint), FOUND-10 (single-command bring-up via `task dev` which runs `docker compose up`).

Output: Repo skeleton ready for code; `docker compose up -d postgres redis` passes healthchecks within 30s; `task --list` shows all targets; `task migrate-up` applies migration 000001 against the live compose database and produces a `schema_migrations` row with `version=1, dirty=false`.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md
@.planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md
@.planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md
@.planning/phases/01-foundation-polyglot-monorepo/01-VALIDATION.md

<interfaces>
<!-- Locked module path from CONTEXT.md D-22 and Research §Recommended Project Structure -->
<!-- This module path is referenced by every other plan in Phase 1. Do NOT alter. -->

Go module path:
  module github.com/luongdev/open-routing/services/api
  go 1.23

Locked stack imports (Research §Standard Stack, lines 128-153):
  github.com/go-chi/chi/v5                                                v5.2.3
  github.com/jackc/pgx/v5                                                 v5.9.2
  github.com/jackc/pgx/v5/pgxpool                                         (same module)
  github.com/jackc/pgx/v5/stdlib                                          (database/sql shim for migrate)
  github.com/jackc/pgx/v5/pgconn                                          (used in DBTX signature)
  github.com/golang-migrate/migrate/v4                                    v4.19.1
  github.com/golang-migrate/migrate/v4/database/postgres                  (driver)
  github.com/golang-migrate/migrate/v4/source/file                        (file:// source)
  github.com/google/uuid                                                  v1.6.0
  github.com/pganalyze/pg_query_go/v6                                     v6 (CGO required)
  github.com/redis/go-redis/v9                                            v9.x
  github.com/stretchr/testify                                             v1.10+
  github.com/testcontainers/testcontainers-go                             v0.40+
  github.com/testcontainers/testcontainers-go/modules/postgres            v0.40+
  go.opentelemetry.io/otel                                                v1.x
  go.opentelemetry.io/otel/sdk                                            v1.x
  go.opentelemetry.io/otel/sdk/trace                                      (sdktrace)
  go.opentelemetry.io/otel/sdk/resource                                   (resource.New)
  go.opentelemetry.io/otel/semconv/v1.26.0                                (semconv.ServiceName)
  go.opentelemetry.io/otel/propagation                                    (TraceContext, Baggage)
  go.opentelemetry.io/otel/exporters/stdout/stdouttrace                   v1.x
  go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp         v1.x
  go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp           v0.x (latest)
  log/slog                                                                stdlib

Config struct shape (this plan creates internal/config/config.go):
  type Config struct {
      DatabaseURL    string  // DATABASE_URL — required
      RedisURL       string  // REDIS_URL — required
      OTelExporter   string  // OTEL_EXPORTER — default "stdout", accepts "otlp" (D-14)
      OTLPEndpoint   string  // OTEL_EXPORTER_OTLP_ENDPOINT — required when OTelExporter=="otlp"
      OTLPProtocol   string  // OTEL_EXPORTER_OTLP_PROTOCOL — default "http/protobuf"
      ListenAddr     string  // LISTEN_ADDR — default ":8080"
      ValidationMode string  // ORGDB_VALIDATION_MODE — default "panic" (dev/test), "error" in prod (drives db.ValidationMode enum)
  }
  func Load() (*Config, error)
  func MustLoad() *Config // panics on error; used by cmd/migrate main()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Wave-0 CLI binaries (task, migrate, air)</name>
  <files>(no repo files; environment install only)</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Environment Availability" (lines 1548-1566) — confirms which tools are missing and pinned versions
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-08 (Taskfile is the top-level orchestrator), §D-10/D-11 (migrate CLI), §D-24 (air for hot-reload)
    - .planning/phases/01-foundation-polyglot-monorepo/01-VALIDATION.md §"Wave 0 Requirements" (lines 60-71)
  </read_first>
  <action>
    Install three CLI binaries required by later tasks in this phase. Per Research §"Environment Availability" lines 1556-1559 these are the pinned versions:

    1. `task` (Taskfile runner, D-08): run `go install github.com/go-task/task/v3/cmd/task@latest`. Verify with `task --version` returning v3.50+ output. If already installed, skip install but still verify.

    2. `migrate` CLI (golang-migrate, D-10/D-11): run `go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@v4.19.1`. The `-tags 'postgres'` build tag is REQUIRED — without it the binary does not include the postgres driver and `migrate -database "postgres://..."` returns "unknown driver". Verify with `migrate -version` returning `v4.19.1`.

    3. `air` (Go hot-reload, D-24): run `go install github.com/air-verse/air@v1.61.7`. Per Research §Pitfall 8 lines 1078-1080, air v1.65+ requires Go 1.25; pin v1.61.x for Go 1.23 compatibility. Verify with `air -v` returning v1.61.x.

    Print each tool's version after install so the task log captures the verification. Do NOT modify go.mod/go.sum for these — they are CLI binaries installed to `$GOPATH/bin`, not project dependencies.

    If `$GOPATH/bin` is not on `$PATH`, print a warning instructing the user to add it (`export PATH="$PATH:$(go env GOPATH)/bin"`) and continue; the binaries are installed regardless.
  </action>
  <verify>
    <automated>command -v task &amp;&amp; command -v migrate &amp;&amp; command -v air &amp;&amp; task --version &amp;&amp; migrate -version &amp;&amp; air -v</automated>
  </verify>
  <done>All three commands print versions: task v3.50+, migrate v4.19.1, air v1.61.x</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create repo-root hygiene files (.gitignore, .editorconfig, .gitattributes, .env.example, openapi/.gitkeep, README.md)</name>
  <files>.gitignore, .editorconfig, .gitattributes, .env.example, openapi/.gitkeep, README.md</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md L1 §".gitignore" (lines 131-143), §".editorconfig and .gitattributes" (lines 145-156), §"openapi/.gitkeep" (lines 158-161)
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Recommended Project Structure" (lines 336-410) for full tree
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-Discretion bullet 6 (.editorconfig + .gitattributes recommended)
    - Existing repo root `.gitignore` (currently ignores `.planning/`) — must preserve that line
  </read_first>
  <action>
    Create six hygiene files at repo root.

    `.gitignore` — extend the existing file (which currently has `.planning/` ignored). Append blocks for Go, Node, env files, IDE, OS. Required entries from PATTERNS.md L1 lines 136-143:
      - Go: `*.exe`, `*.test`, `*.out`, `bin/`, `services/api/bin/`, `.air-tmp/`, `services/api/.air-tmp/`
      - Node: `node_modules/`, `pnpm-store/`, `.turbo/`, `*.log`
      - IDE: `.idea/`, `.vscode/`
      - Env: `.env`, `.env.*`, `!.env.example` (negate so .env.example IS committed)
      - OS: `.DS_Store`, `Thumbs.db`
    Preserve the existing `.planning/` line; do NOT remove it.

    `.editorconfig` — per PATTERNS.md L1 lines 150-155:
      - `root = true`
      - Default block `[*]`: `indent_style = space`, `indent_size = 2`, `end_of_line = lf`, `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`
      - Override `[*.go]`: `indent_style = tab`
      - Override `[Makefile]`: `indent_style = tab`
      - Override `[*.{yml,yaml,json}]`: `indent_size = 2` (explicit even though it matches default)

    `.gitattributes` — per PATTERNS.md L1 line 156: single line `* text=auto eol=lf` plus `*.sh text eol=lf`.

    `.env.example` — checked-in template documenting every env var the API reads. Content (one var per line with comment):
      ```
      # PostgreSQL connection (used by API, cmd/migrate, sqlc)
      DATABASE_URL=postgres://openrouting:openrouting@localhost:5432/openrouting?sslmode=disable
      # Redis connection (used by /readyz)
      REDIS_URL=redis://localhost:6379/0
      # OTel exporter target — D-14
      OTEL_EXPORTER=stdout
      # OTel OTLP endpoint (required when OTEL_EXPORTER=otlp) — D-14
      OTEL_EXPORTER_OTLP_ENDPOINT=
      # OTel OTLP protocol — D-14
      OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
      # HTTP listen address
      LISTEN_ADDR=:8080
      # orgDB validation mode: panic (dev/test) or error (prod) — per D-02
      ORGDB_VALIDATION_MODE=panic
      ```
      Match field names to the Config struct in `services/api/internal/config/config.go` (created in Task 7).

    `openapi/.gitkeep` — empty file; the `openapi/` directory must exist at repo root per FOUND-01 even though Phase 2 fills `openapi.yaml`. Per PATTERNS.md L1 lines 158-161.

    `README.md` — minimal stub. Header: `# open-routing` (single line). Subsection `## Quickstart` listing four commands as a numbered list: (1) `cp .env.example .env`, (2) `docker compose up -d postgres redis`, (3) `task migrate-up`, (4) `task dev`. Subsection `## Structure` showing the four top-level dirs with one-line descriptions: `services/api/` (Go API), `web/` (pnpm workspace stub for admin + embed + ui), `openapi/` (OpenAPI 3.1 spec — Phase 2), `migrations/` (golang-migrate SQL). Keep it short (≤ 40 lines).
  </action>
  <verify>
    <automated>test -f .gitignore &amp;&amp; test -f .editorconfig &amp;&amp; test -f .gitattributes &amp;&amp; test -f .env.example &amp;&amp; test -f openapi/.gitkeep &amp;&amp; test -f README.md &amp;&amp; grep -q '^\.env$' .gitignore &amp;&amp; grep -q '^\.planning/' .gitignore &amp;&amp; grep -q '\[\*\.go\]' .editorconfig &amp;&amp; grep -q 'DATABASE_URL=postgres' .env.example &amp;&amp; grep -q 'OTEL_EXPORTER=stdout' .env.example &amp;&amp; grep -q 'ORGDB_VALIDATION_MODE=panic' .env.example</automated>
  </verify>
  <done>All six files exist; .gitignore preserves `.planning/` and blocks `.env`; .editorconfig has Go tab override; .env.example documents all 7 env vars</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create docker-compose.yml with postgres:17 and redis:7-alpine healthchecks</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Pattern 6: docker-compose.yml (FOUND-10 + D-25)" (lines 922-960) — copy verbatim
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-25 (compose contains only postgres + redis, no api service), §D-26 (native compose healthchecks)
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §"Shared Pattern S10" (lines 994-1000)
  </read_first>
  <action>
    Create `docker-compose.yml` at repo root with exactly two services per D-25: `postgres` and `redis`. No `api` service in Phase 1 (devs run `air` natively for fast iteration). Copy the spec from Research §Pattern 6 lines 922-956 verbatim:

    - `postgres` service:
      - `image: postgres:17`
      - environment: `POSTGRES_DB: openrouting`, `POSTGRES_USER: openrouting`, `POSTGRES_PASSWORD: openrouting` (dev-only; commented as such)
      - `ports: - "5432:5432"`
      - `volumes: - openrouting_pg_data:/var/lib/postgresql/data` (named volume preserves data across compose down/up)
      - healthcheck: `test: ["CMD-SHELL", "pg_isready -U openrouting -d openrouting"]`, `interval: 5s`, `timeout: 3s`, `retries: 5`, `start_period: 10s`

    - `redis` service:
      - `image: redis:7-alpine`
      - `ports: - "6379:6379"`
      - healthcheck: `test: ["CMD", "redis-cli", "ping"]`, `interval: 5s`, `timeout: 3s`, `retries: 5`

    - `volumes:` block at file root declaring named volume `openrouting_pg_data`.

    Do NOT add an `api` service (D-25). Do NOT add a Jaeger/OTel collector container (D-15). The deferred ideas list explicitly forbids `web-admin`/`web-embed` Vite services in Phase 1.

    Per ASVS V14.3.2 / T-1-07: do not hardcode production secrets. The dev-only password is acceptable here because the .env.example documents the override path, and `.gitignore` prevents committing real `.env`.

    Validate after creation by running `docker compose config` (parses + validates the file syntactically). If `docker compose` is unavailable in the agent environment, skip the runtime check but still verify the file content.
  </action>
  <verify>
    <automated>test -f docker-compose.yml &amp;&amp; grep -q 'image: postgres:17' docker-compose.yml &amp;&amp; grep -q 'image: redis:7-alpine' docker-compose.yml &amp;&amp; grep -q 'pg_isready -U openrouting -d openrouting' docker-compose.yml &amp;&amp; grep -q 'redis-cli' docker-compose.yml &amp;&amp; grep -q 'openrouting_pg_data' docker-compose.yml &amp;&amp; ! grep -qE '^\s*api:' docker-compose.yml</automated>
  </verify>
  <done>Two services present with healthchecks; named volume declared; no `api` service; file parses (if docker compose available)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create first migration files (000001_create_scaffold.up.sql / .down.sql)</name>
  <files>migrations/000001_create_scaffold.up.sql, migrations/000001_create_scaffold.down.sql</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Example 9: First migration creating `_scaffold` (D-18)" (lines 1456-1478) — copy verbatim
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-18 (full _scaffold schema), §D-10 (golang-migrate filename convention NNNNNN_name.up.sql / .down.sql)
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L2 (lines 165-191)
  </read_first>
  <action>
    Create the `migrations/` directory at repo root and add the first sequential migration pair. Filename convention per D-10 and golang-migrate docs: `NNNNNN_name.up.sql` / `NNNNNN_name.down.sql` with six-digit zero-padded sequential version.

    `migrations/000001_create_scaffold.up.sql` — copy verbatim from Research §Example 9 lines 1462-1471:
    ```sql
    CREATE TABLE _scaffold (
        id          UUID PRIMARY KEY,
        org_id      UUID NOT NULL,
        external_id TEXT NOT NULL,
        name        TEXT NOT NULL,
        created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
        UNIQUE (org_id, external_id)
    );

    CREATE INDEX idx_scaffold_org_id ON _scaffold (org_id);
    ```

    Required content (each line is a verification anchor):
    - `CREATE TABLE _scaffold (` — table name uses leading underscore per D-18 (intentional; Phase 3 drops it).
    - `org_id      UUID NOT NULL,` — satisfies FOUND-02 (every org-scoped table has org_id UUID NOT NULL). Threat T-1-02 mitigated at schema level.
    - `UNIQUE (org_id, external_id)` — satisfies FOUND-06 composite uniqueness. NEVER use a global `UNIQUE (external_id)` per FOUND-06.
    - `id          UUID PRIMARY KEY,` — UUIDv7+ enforced at app layer per D-19 (no DB-side check; pg_uuidv7 extension not used).
    - `CREATE INDEX idx_scaffold_org_id` — non-functional but echoes the canonical "every org-scoped table indexes org_id" pattern Phase 3 will follow.

    `migrations/000001_create_scaffold.down.sql` — single statement: `DROP TABLE _scaffold;`. No `IF EXISTS` clause — migrate's rollback semantics already expect the table to exist.

    Add no other migrations. Phase 3 creates `000002_create_catalog.up.sql` and the first line of Phase 3's migration is `DROP TABLE _scaffold;` per CONTEXT.md §code_context.
  </action>
  <verify>
    <automated>test -f migrations/000001_create_scaffold.up.sql &amp;&amp; test -f migrations/000001_create_scaffold.down.sql &amp;&amp; grep -E 'org_id\s+UUID\s+NOT\s+NULL' migrations/000001_create_scaffold.up.sql &amp;&amp; grep -E 'UNIQUE\s*\(org_id,\s*external_id\)' migrations/000001_create_scaffold.up.sql &amp;&amp; grep -E 'CREATE\s+INDEX\s+idx_scaffold_org_id' migrations/000001_create_scaffold.up.sql &amp;&amp; grep -q 'DROP TABLE _scaffold' migrations/000001_create_scaffold.down.sql</automated>
  </verify>
  <done>Both migration files exist; up.sql contains org_id UUID NOT NULL, UNIQUE(org_id, external_id), idx_scaffold_org_id; down.sql drops the table</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Initialize Go module and pin all stack dependencies (services/api/go.mod, go.sum)</name>
  <files>services/api/go.mod, services/api/go.sum</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-22 (single Go module at services/api/go.mod; module path `github.com/luongdev/open-routing/services/api`), §D-23 (internal package layout)
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Standard Stack" §Core lines 128-142 (every Go dep + pinned version), §"Installation (Go module)" lines 187-207
    - .planning/phases/01-foundation-polyglot-monorepo/01-DISCUSSION-LOG.md §Q13 (lines 161-172) — user's choice confirmed
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L3 §"services/api/go.mod" (lines 196-207)
  </read_first>
  <action>
    Create `services/api/` directory then initialize the Go module with the locked path from D-22.

    Step 1 — `cd services/api && go mod init github.com/luongdev/open-routing/services/api`.

    Step 2 — In `services/api/`, run `go get` for every locked stack dependency at the versions Research §Standard Stack pinned:
    ```
    go get github.com/go-chi/chi/v5@v5.2.3
    go get github.com/jackc/pgx/v5@v5.9.2
    go get github.com/golang-migrate/migrate/v4@v4.19.1
    go get github.com/google/uuid@v1.6.0
    go get github.com/redis/go-redis/v9
    go get github.com/pganalyze/pg_query_go/v6
    go get go.opentelemetry.io/otel
    go get go.opentelemetry.io/otel/sdk
    go get go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp
    go get go.opentelemetry.io/otel/exporters/stdout/stdouttrace
    go get go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp
    go get go.opentelemetry.io/otel/semconv/v1.26.0
    go get github.com/testcontainers/testcontainers-go
    go get github.com/testcontainers/testcontainers-go/modules/postgres
    go get github.com/stretchr/testify
    ```

    Step 3 — Run `go mod tidy` to lock the transitive dependency versions in go.sum.

    Per Research §Pitfall 8 lines 1078-1080, Go toolchain must be 1.23.x; if go.mod ends up with `go 1.25` (from a newer local toolchain selecting it), edit the directive to `go 1.23` and re-run `go mod tidy`.

    CGO note (Research §Pitfall 10 / Standard Stack): `pg_query_go/v6` requires CGO. The first `go get` invocation will compile the embedded Postgres parser (~3 min cold). This is one-time per machine; subsequent builds use the build cache. Do NOT add `CGO_ENABLED=0` anywhere.

    Per RESEARCH.md §Package Legitimacy Audit (lines 220-251): all 16 Go packages were slopcheck-verified. Three packages flagged [SUS] (pgx/v5, sqlc-dev/sqlc, go-redis/v9) are false positives from the "package age" heuristic. NO checkpoint required. The audit confirmed `pgx-zero-log` was a SLOP probe and is not in this stack.

    Add a `services/api/tools.go` file with build tag `//go:build tools` listing CLI dev-deps that are needed at install time but not at build time:
    ```go
    //go:build tools

    package tools

    // The sqlc CLI and migrate CLI are installed separately (Task 1 or Wave 0).
    // This file exists to document the dev-tool surface; it currently has no imports.
    ```
    Reason: documents the tooling boundary without requiring `go run` indirection. If subsequent phases add `oapi-codegen` they import it here.
  </action>
  <verify>
    <automated>test -f services/api/go.mod &amp;&amp; test -f services/api/go.sum &amp;&amp; grep -q '^module github.com/luongdev/open-routing/services/api$' services/api/go.mod &amp;&amp; grep -E '^go\s+1\.23' services/api/go.mod &amp;&amp; grep -q 'github.com/go-chi/chi/v5 v5.2.3' services/api/go.mod &amp;&amp; grep -q 'github.com/jackc/pgx/v5 v5.9.2' services/api/go.mod &amp;&amp; grep -q 'github.com/golang-migrate/migrate/v4 v4.19.1' services/api/go.mod &amp;&amp; grep -q 'github.com/google/uuid v1.6.0' services/api/go.mod &amp;&amp; grep -q 'github.com/pganalyze/pg_query_go/v6' services/api/go.mod &amp;&amp; grep -q 'go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp' services/api/go.mod &amp;&amp; grep -q 'github.com/testcontainers/testcontainers-go' services/api/go.mod &amp;&amp; (cd services/api &amp;&amp; go vet ./...)</automated>
  </verify>
  <done>go.mod has module path D-22, Go 1.23, all 16 locked dependencies; go.sum exists; `go vet ./...` passes (proves CGO compiled successfully)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create sqlc.yaml, .golangci.yml, .air.toml (per-service dev configs)</name>
  <files>services/api/sqlc.yaml, services/api/.golangci.yml, services/api/.air.toml</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Example 3: sqlc.yaml configuration" (lines 1141-1163) — copy verbatim
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L3 §"services/api/sqlc.yaml" (lines 208-234), §".golangci.yml" (lines 236-256), §".air.toml" (lines 258-273)
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §"Claude's Discretion" bullets 1 (golangci-lint set) and 2 (air config)
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §Pitfall 8 (lines 1075-1080) — air version pin rationale
  </read_first>
  <action>
    Create three per-service config files inside `services/api/`.

    `services/api/sqlc.yaml` — copy verbatim from Research §Example 3 lines 1147-1162:
    ```yaml
    version: "2"
    sql:
      - engine: "postgresql"
        queries: "internal/db/queries"
        schema: "../../migrations"
        gen:
          go:
            package: "generated"
            out: "internal/db/generated"
            sql_package: "pgx/v5"
            emit_json_tags: true
            emit_prepared_queries: false
            emit_interface: false
            emit_exact_table_names: false
            emit_empty_slices: true
            emit_pointers_for_null_types: true
    ```
    Load-bearing fields:
    - `sql_package: "pgx/v5"` — emits `DBTX` interface that `orgDB` (plan 03) will implement.
    - `schema: "../../migrations"` — relative path from `services/api/` to repo-root `migrations/`. Phase 3 keeps this unchanged.
    - `queries: "internal/db/queries"` — Plan 03 creates this directory and its `scaffold.sql`.
    - `out: "internal/db/generated"` — Plan 03 commits the generated files per D-23 "sqlc output (committed)".

    `services/api/.golangci.yml` — per Claude's Discretion bullet 1, enable `default + errcheck, govet, staticcheck, gosec, gocritic`:
    ```yaml
    run:
      timeout: 5m
    linters:
      enable:
        - errcheck
        - govet
        - staticcheck
        - gosec
        - gocritic
    issues:
      exclude-rules:
        - path: _test\.go
          linters: [gosec, errcheck]
        - path: internal/db/generated/
          linters: [gosec, errcheck, gocritic, staticcheck]
    ```
    Exclusion `internal/db/generated/` prevents lint noise on machine-generated sqlc output (which has known patterns like ignored errcheck returns).

    `services/api/.air.toml` — per Claude's Discretion bullet 2 and Pitfall 8:
    ```toml
    root = "."
    tmp_dir = ".air-tmp"

    [build]
      bin = ".air-tmp/api"
      cmd = "go build -o .air-tmp/api ./cmd/api"
      include_ext = ["go", "yml", "yaml"]
      exclude_dir = ["internal/db/generated", ".air-tmp", "bin"]
      delay = 500
    ```
    Excludes `internal/db/generated/` so sqlc regeneration does not trigger a rebuild loop. Pinned to air v1.61.x compatibility (no v1.65-specific config fields).

    Do NOT pre-create `internal/db/queries/` or `internal/db/generated/` directories — Plan 03 owns those. sqlc generate will fail until Plan 03 runs; that is expected and not a Plan 01 concern.
  </action>
  <verify>
    <automated>test -f services/api/sqlc.yaml &amp;&amp; test -f services/api/.golangci.yml &amp;&amp; test -f services/api/.air.toml &amp;&amp; grep -q 'sql_package: "pgx/v5"' services/api/sqlc.yaml &amp;&amp; grep -q 'schema: "../../migrations"' services/api/sqlc.yaml &amp;&amp; grep -q 'queries: "internal/db/queries"' services/api/sqlc.yaml &amp;&amp; grep -E '^\s*- gosec' services/api/.golangci.yml &amp;&amp; grep -E '^\s*- gocritic' services/api/.golangci.yml &amp;&amp; grep -q 'internal/db/generated' services/api/.golangci.yml &amp;&amp; grep -q 'bin = ".air-tmp/api"' services/api/.air.toml &amp;&amp; grep -q 'cmd = "go build -o .air-tmp/api ./cmd/api"' services/api/.air.toml</automated>
  </verify>
  <done>Three config files exist; sqlc.yaml has sql_package: pgx/v5 + schema points at repo-root migrations; golangci-lint enables 5 linters + excludes generated/; air.toml builds ./cmd/api</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Create env-loader package (services/api/internal/config/config.go)</name>
  <files>services/api/internal/config/config.go</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L4 §"services/api/internal/config/config.go" (lines 280-294)
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-14 (OTel env var names), §D-30 (env names in CI), §D-02 (validation mode panic/error)
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Runtime State Inventory" (lines 988-998) — confirms .env is the only state source
    - .env.example created in Task 2 — env var names must match exactly
  </read_first>
  <action>
    Create `services/api/internal/config/config.go` implementing the 12-factor env loader. The struct shape is locked by D-14 / D-02 and consumed by plans 03, 04, 06.

    Package declaration: `package config`. No external dependencies — use stdlib `os` only.

    Define exported struct `Config`:
    ```go
    type Config struct {
        DatabaseURL    string
        RedisURL       string
        OTelExporter   string
        OTLPEndpoint   string
        OTLPProtocol   string
        ListenAddr     string
        ValidationMode string
    }
    ```

    Define exported `Load() (*Config, error)`:
    - Read each env var with `os.Getenv` for optional and `os.LookupEnv` for required-with-error.
    - `DATABASE_URL` and `REDIS_URL` are required — return `fmt.Errorf("config: %s is required", key)` if missing (use `errors.Join` if multiple are missing, so error message lists all gaps in one shot).
    - Defaults per CONTEXT.md / Patterns L4:
      - `OTelExporter` defaults to `"stdout"`; accepts `"otlp"` (D-14). Any other value → return error `"config: OTEL_EXPORTER must be 'stdout' or 'otlp'"`.
      - When `OTelExporter == "otlp"`, `OTLPEndpoint` is required; missing → return error.
      - `OTLPProtocol` defaults to `"http/protobuf"`.
      - `ListenAddr` defaults to `":8080"`.
      - `ValidationMode` defaults to `"panic"`; accepts `"error"`. Any other value → error `"config: ORGDB_VALIDATION_MODE must be 'panic' or 'error'"`.

    Define exported `MustLoad() *Config`: calls `Load()` and `log.Fatal` on error. Used by `cmd/migrate/main.go` (Plan 03) and `cmd/api/main.go` (Plan 06).

    Define unexported helpers `getEnvOrDefault(key, def string) string` and `requireEnv(key string) (string, error)` for DRY.

    Implementation rule: this file MUST NOT import `internal/db` or any other internal package (avoid import cycles). It is leaf-level.

    Run `go vet ./internal/config/...` to verify the file compiles. No unit test file needed in this plan; subsequent plans depending on Config can add tests if needed.
  </action>
  <verify>
    <automated>test -f services/api/internal/config/config.go &amp;&amp; grep -q '^package config$' services/api/internal/config/config.go &amp;&amp; grep -q 'type Config struct' services/api/internal/config/config.go &amp;&amp; grep -q 'DatabaseURL' services/api/internal/config/config.go &amp;&amp; grep -q 'OTelExporter' services/api/internal/config/config.go &amp;&amp; grep -q 'ValidationMode' services/api/internal/config/config.go &amp;&amp; grep -q 'func Load()' services/api/internal/config/config.go &amp;&amp; grep -q 'func MustLoad()' services/api/internal/config/config.go &amp;&amp; (cd services/api &amp;&amp; go vet ./internal/config/...)</automated>
  </verify>
  <done>config.go exists with Config struct (7 fields), Load(), MustLoad(); go vet passes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Create Taskfile.yml with locked target list (D-08)</name>
  <files>Taskfile.yml</files>
  <read_first>
    - .planning/phases/01-foundation-polyglot-monorepo/01-RESEARCH.md §"Example 7: Taskfile structure (root)" (lines 1313-1392) — copy verbatim with target adjustments
    - .planning/phases/01-foundation-polyglot-monorepo/01-CONTEXT.md §D-08 (target list LOCKED: gen, migrate-up, migrate-down, migrate-create, dev, test, lint, typecheck, build, ci), §D-09 (Turborepo lives in web/), §D-11 (api never auto-migrates), §D-24 (dev flow)
    - .planning/phases/01-foundation-polyglot-monorepo/01-PATTERNS.md §L1 §"Taskfile.yml" (lines 110-119)
    - docker-compose.yml created in Task 3 — postgres/redis service names and env values
    - .env.example created in Task 2 — DATABASE_URL/REDIS_URL string format
  </read_first>
  <action>
    Create `Taskfile.yml` at repo root. Copy Research §Example 7 lines 1316-1392 as the base, with these explicit target definitions (target list LOCKED by D-08; do not rename or add others without user approval):

    `version: '3'`

    `env:` block:
    - `DATABASE_URL: 'postgres://openrouting:openrouting@localhost:5432/openrouting?sslmode=disable'` — matches docker-compose.yml credentials.
    - `REDIS_URL: 'redis://localhost:6379/0'`

    `tasks:` map containing exactly these entries (each with `desc:` and `cmds:`):

    1. `default`: `cmds: [task: --list]` — running bare `task` shows all targets.

    2. `gen`: codegen. Phase 1 cmd: `cd services/api && sqlc generate`. Phase 2 will add oapi-codegen and openapi-typescript steps; leave a comment `# Phase 2: add oapi-codegen + openapi-typescript`.

    3. `migrate-up`: `migrate -path migrations -database "$DATABASE_URL" up`.

    4. `migrate-down`: `migrate -path migrations -database "$DATABASE_URL" down 1` (D-08 — rolls back exactly one).

    5. `migrate-create`: `migrate create -ext sql -dir migrations -seq {{.NAME}}` with `requires: vars: [NAME]` (taskfile syntax for required var). Sequential numbering per Open Question #5 / D-10.

    6. `dev` (D-24 flow): two cmds — first `docker compose up -d postgres redis`, then `cd services/api && air -c .air.toml`. Do NOT add a wait loop for healthchecks here — the second command blocks foreground so devs notice if compose failed.

    7. `test`: `cd services/api && go test -race -count=1 ./...`. The `-count=1` disables the Go test cache so changes always re-run.

    8. `test:quick`: `cd services/api && go test -count=1 -short ./internal/...` — used by VALIDATION.md sampling rate (lines 18-22). Tests use `if testing.Short() { t.Skip() }` to opt into the quick path (Plan 07 testsupport adds this convention).

    9. `lint`: three cmds — `cd services/api && go vet ./...`, `cd services/api && golangci-lint run`, `cd web && pnpm -F '*' lint`.

    10. `typecheck`: `cd web && pnpm -F '*' typecheck`.

    11. `ci`: D-30 composition — `cmds:` calling `[{task: lint}, {task: typecheck}, {task: test}]`. The CI workflow runs these in parallel jobs, but `task ci` locally runs them sequentially.

    12. `build`: two cmds — `cd services/api && go build -o ../../bin/api ./cmd/api` and `cd services/api && go build -o ../../bin/migrate ./cmd/migrate`.

    Anti-pattern guard: do NOT add `auto-migrate` to any target. D-11 forbids the API auto-running migrations. Devs invoke `task migrate-up` explicitly.

    After writing, run `task --list` to confirm the file parses and all 12 named targets appear in the output (`task --list` reads Taskfile.yml from the cwd).
  </action>
  <verify>
    <automated>test -f Taskfile.yml &amp;&amp; grep -q "^version: '3'" Taskfile.yml &amp;&amp; grep -q 'DATABASE_URL:' Taskfile.yml &amp;&amp; grep -q 'REDIS_URL:' Taskfile.yml &amp;&amp; grep -E '^\s*gen:' Taskfile.yml &amp;&amp; grep -E '^\s*migrate-up:' Taskfile.yml &amp;&amp; grep -E '^\s*migrate-down:' Taskfile.yml &amp;&amp; grep -E '^\s*migrate-create:' Taskfile.yml &amp;&amp; grep -E '^\s*dev:' Taskfile.yml &amp;&amp; grep -E '^\s*test:' Taskfile.yml &amp;&amp; grep -E '^\s*lint:' Taskfile.yml &amp;&amp; grep -E '^\s*typecheck:' Taskfile.yml &amp;&amp; grep -E '^\s*ci:' Taskfile.yml &amp;&amp; grep -E '^\s*build:' Taskfile.yml &amp;&amp; grep -q 'migrate -path migrations -database' Taskfile.yml &amp;&amp; grep -q 'docker compose up -d postgres redis' Taskfile.yml &amp;&amp; grep -q 'sqlc generate' Taskfile.yml &amp;&amp; task --list | grep -E '(migrate-up|migrate-down|dev|test|lint|ci|build)'</automated>
  </verify>
  <done>Taskfile.yml exists with all 11 locked D-08 targets; `task --list` parses and shows them</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem ↔ git | Secrets in `.env` and dev DB credentials must never enter version control |
| docker-compose host ↔ container | Postgres listens on host port 5432; dev-only password protects only localhost |
| go.dev proxy ↔ services/api/go.mod | Supply-chain trust — every imported package must be slopcheck-verified |
| filesystem ↔ task runner | Taskfile.yml shell-interpolates env vars; malicious `$DATABASE_URL` could enable command injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering / Information disclosure | migrations/000001_create_scaffold.up.sql | mitigate | `org_id UUID NOT NULL` + `UNIQUE (org_id, external_id)` enforced at schema level; verified by grep gate in Task 4 verify command |
| T-1-06 | Spoofing / Elevation | docker-compose.yml | mitigate | No host volume mounts beyond named `openrouting_pg_data`; ephemeral container, no test artifacts persisted to host filesystem |
| T-1-07 | Information disclosure | docker-compose.yml + .env.example + .gitignore | mitigate | Dev-only credentials hardcoded in compose with comment marking dev-only; `.env` ignored by git; `.env.example` checked in as documentation only |
| T-1-SC | Tampering | services/api/go.mod package installs | mitigate | Research §Package Legitimacy Audit (lines 220-251) ran slopcheck against every package; 16 of 17 [OK]/[SUS] (verified false positives), 1 [SLOP] probe (`pgx-zero-log`, never in stack); no blocking checkpoint required — see audit table |
</threat_model>

<verification>
After all tasks complete, run as a single smoke sequence (CI parity):

```bash
# Tooling installed
command -v task && command -v migrate && command -v air

# Structure exists
test -d services/api && test -d migrations && test -d openapi
test -f services/api/go.mod && test -f docker-compose.yml && test -f Taskfile.yml

# Module is correct
grep -q '^module github.com/luongdev/open-routing/services/api$' services/api/go.mod

# Migration enforces org isolation
grep -E 'org_id\s+UUID\s+NOT\s+NULL' migrations/000001_create_scaffold.up.sql
grep -E 'UNIQUE\s*\(org_id,\s*external_id\)' migrations/000001_create_scaffold.up.sql

# Compose validates
docker compose config >/dev/null

# Go module compiles (CGO has finished pg_query_go)
(cd services/api && go vet ./...)

# Taskfile parses
task --list >/dev/null

# End-to-end: compose up, migrate up, schema check
docker compose up -d postgres redis
# Wait up to 30s for healthcheck
until docker compose ps postgres --status running --format '{{.Health}}' | grep -q healthy; do sleep 2; done
task migrate-up
# Inspect schema_migrations
docker compose exec -T postgres psql -U openrouting -d openrouting -c "SELECT version, dirty FROM schema_migrations;" | grep -E '^\s+1\s+\|\s+f'
docker compose down
```
</verification>

<success_criteria>
- All 16 files in `files_modified` exist on disk with the content described in tasks
- `services/api/go.mod` has `module github.com/luongdev/open-routing/services/api` and `go 1.23`
- `migrations/000001_create_scaffold.up.sql` contains `org_id UUID NOT NULL` and `UNIQUE (org_id, external_id)` (FOUND-02 + FOUND-06)
- `docker compose up -d postgres redis` produces two healthy containers within 30s (FOUND-10 partial — Taskfile `dev` is the full single-command bring-up)
- `task migrate-up` (with compose running) applies migration 000001 and creates a `schema_migrations` row with `version=1, dirty=false`
- `task --list` shows all 11 D-08 targets (`gen`, `migrate-up`, `migrate-down`, `migrate-create`, `dev`, `test`, `test:quick`, `lint`, `typecheck`, `ci`, `build`)
- `go vet ./...` from `services/api/` passes (proves all deps including CGO `pg_query_go` compile)
- `.gitignore` preserves the existing `.planning/` line and blocks `.env` while allowing `.env.example`
</success_criteria>

<output>
Create `.planning/phases/01-foundation-polyglot-monorepo/01-01-SUMMARY.md` capturing:
- All artifacts created and the exact module path used (`github.com/luongdev/open-routing/services/api`)
- Pinned dependency versions written to `services/api/go.mod`
- Status of `task migrate-up` against live compose (schema_migrations.version observed)
- Wave-0 CLI versions installed (`task`, `migrate`, `air`)
- Any deviations from CONTEXT.md decisions (must be zero) with rationale if any occurred
</output>
