---
phase: 05-bulk-import-go
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - openapi/openapi.yaml
  - services/api/internal/db/sqlcheck.go
  - migrations/000003_create_import_jobs.up.sql
  - migrations/000003_create_import_jobs.down.sql
  - services/api/internal/api/server.gen.go
  - services/api/internal/api/types.gen.go
  - services/api/internal/api/spec.gen.go
  - web/packages/ui/src/api/generated.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-03
  - IMP-04
  - IMP-05
  - IMP-06
  - IMP-07
  - IMP-08
tags:
  - openapi
  - codegen
  - migration
  - sqlcheck

must_haves:
  truths:
    - "openapi.yaml carries 6 Import*Request schemas keyed on code (not external_id)"
    - "BulkImportCatalog operation accepts optional Idempotency-Key header"
    - "BulkImportResult schema has idempotent_replay field (nullable boolean, default false)"
    - "ImportJob schema has required status enum [pending, completed, failed]"
    - "Spec description text on BulkImportCatalog references (org_id, code) upsert key"
    - "migration 000003 creates import_jobs table with org_id + status CHECK + partial indexes"
    - "sqlcheck.go tenantTables map includes import_jobs entry"
    - "task gen produces no diff after edits (codegen committed alongside spec)"
  artifacts:
    - path: "openapi/openapi.yaml"
      provides: "6 Import*Request schemas + idempotent_replay + Idempotency-Key header + status enum"
      contains: "ImportAgentRequest"
    - path: "migrations/000003_create_import_jobs.up.sql"
      provides: "import_jobs table schema with partial unique on idempotency_key and partial index on pending status"
      contains: "CREATE TABLE import_jobs"
    - path: "migrations/000003_create_import_jobs.down.sql"
      provides: "DROP TABLE import_jobs"
      contains: "DROP TABLE IF EXISTS import_jobs"
    - path: "services/api/internal/db/sqlcheck.go"
      provides: "tenantTables[\"import_jobs\"]"
      contains: "\"import_jobs\""
    - path: "services/api/internal/api/types.gen.go"
      provides: "ImportAgentRequest et al. + ImportJobStatus + BulkImportCatalogParams.IdempotencyKey + BulkImportResult.IdempotentReplay"
      contains: "ImportAgentRequest"
  key_links:
    - from: "openapi/openapi.yaml"
      to: "services/api/internal/api/types.gen.go"
      via: "task gen (oapi-codegen)"
      pattern: "ImportAgentRequest"
    - from: "openapi/openapi.yaml"
      to: "web/packages/ui/src/api/generated.ts"
      via: "pnpm gen:api (openapi-typescript)"
      pattern: "ImportAgentRequest"
    - from: "migrations/000003_create_import_jobs.up.sql"
      to: "services/api/internal/db/sqlcheck.go"
      via: "tenantTables allowlist"
      pattern: "import_jobs"
---

<objective>
Land Phase 5 Wave 0 scaffolding: amend openapi.yaml with the 6 Import*Request
schemas keyed on `code`, the Idempotency-Key header parameter, the
ImportJob.status enum, and the BulkImportResult.idempotent_replay field;
create migration 000003 for `import_jobs`; register `import_jobs` in
SQLChecker's tenantTables allowlist; regenerate codegen.

Purpose: Phase 5's handlers, sqlc queries, tests, and middleware all depend
on the spec and migration changes landing first. Wave 0 produces no
behaviour but unlocks every downstream wave by writing the contract and the
DB shape.

Output: openapi.yaml deltas + migration 000003 + sqlcheck.go tenantTables
entry + regenerated codegen (types.gen.go, server.gen.go, spec.gen.go,
generated.ts). All on a single commit so downstream plans inherit the
contract.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-bulk-import-go/05-CONTEXT.md
@.planning/phases/05-bulk-import-go/05-RESEARCH.md
@.planning/phases/05-bulk-import-go/05-PATTERNS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-PHASE5-AMEND-CHECKLIST.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-01-SUMMARY.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-02-SUMMARY.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-03-SUMMARY.md
@.planning/phases/04-agent-state-machine-go/04-PATTERNS.md
@openapi/openapi.yaml
@migrations/000002_catalog_v0_1.up.sql
@migrations/000002_catalog_v0_1.down.sql
@services/api/internal/db/sqlcheck.go
@Taskfile.yml

<interfaces>
<!-- Existing OpenAPI landmarks that Phase 5 amends in place -->

openapi/openapi.yaml landmarks (verified line numbers; treat as approximate):
- 78: components: (root)
- 96: schemas: (root)
- 1454: ImportEntityType (enum agents|skills|queues|channels|adapters|break_reasons — UNCHANGED)
- 1465: BulkImportFailedRow (UNCHANGED)
- 1496: BulkImportResult (ADD idempotent_replay field — nullable boolean, default false, readOnly)
- 1523: ImportJob (ADD required status enum [pending, completed, failed] — D5-26)
- 1621: parameters: (root section — ADD IdempotencyKeyHeader here)
- 1783: paths:
- 3050-3169: POST /v1/orgs/{org_id}/catalog/import + GET .../imports/{import_id}
  - 3067: description text says "Upsert semantics: ... keyed by (org_id, external_id)" — REWRITE to (org_id, code)
  - 3089-3097: requestBody.content.application/json.schema = `type: array, items: {}, maxItems: 500` — KEEP items: {} per F2 (no oneOf — oapi-codegen v2 cannot accessor-emit oneOf request bodies)
- 3209-3213: tags section — Import tag description says "external_id" — REWRITE to "code"

Existing schema shape to mirror (Phase 04.1 CreateAgentRequest pattern, after 04.1 Plan 02 ship):
- CreateAgentRequest is the closest analog: required [code, name, email]; code field has `pattern: '^[a-z][a-z0-9_]{0,63}$'`; external_id is nullable; skills nested array.

migrations/000002_catalog_v0_1.up.sql landmarks (Phase 04.1 amended):
- Line 19: `BEGIN;` envelope start
- Line 214: `COMMIT;` envelope end
- agent_states block around lines 320-360 — shape pattern for new tables

services/api/internal/db/sqlcheck.go landmarks:
- Lines 32-42: `var tenantTables = map[string]struct{}{ ... }`
- Line 34: `"agents": {}, // CAT-01`
- Line 41: `"agent_states": {}, // STATE-01 (Phase 4; denormalized org_id...)` — Phase 4 added this; Phase 5 follows the same pattern.

Taskfile.yml landmarks:
- gen (line 18): orchestrates `task go:gen` + `task ts:gen` — runs oapi-codegen + openapi-typescript
- db:reset (line 43): drops + re-applies all migrations
- test (line 59): `cd services/api && go test -race -count=1 ./...`
- lint (line 70): golangci-lint
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend openapi.yaml with Phase 5 deltas (6 Import*Request schemas + Idempotency-Key header + ImportJob.status enum + BulkImportResult.idempotent_replay + description rewrites)</name>
  <files>openapi/openapi.yaml</files>
  <read_first>
    - openapi/openapi.yaml (specifically lines 1454-1562, 1620-1700, 3050-3215 for the schemas/parameters/operation we modify)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §F2 (oapi-codegen oneOf infeasibility — KEEP `items: {}`)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pattern 10 (Idempotency-Key spec shape)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (openapi.yaml MODIFIED section — exact yaml delta table)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-15, D5-25, D5-26, D5-27 (locked decisions for spec additions)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-02-SUMMARY.md (Phase 04.1 yaml-delta pattern — informs the mechanical edit style)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md D04_1-01, D04_1-03, D04_1-07 (code regex + external_id integration-mapping semantics)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PHASE5-AMEND-CHECKLIST.md (blocks 1, 2, 3, 4, 5, 6, 8 — concrete before/after text the spec deltas must reflect)
  </read_first>
  <action>
    Edit `openapi/openapi.yaml` in place. The edit produces ten coherent deltas; do them all in one Read/Edit pass before running `task gen`.

    DELTA 1 — Rewrite operation description at line ~3067 (BulkImportCatalog `description: |` block).
    - Find the line: `**Upsert semantics:** Rows are upserted keyed by `(org_id, external_id)`` and rewrite to `**Upsert semantics:** Rows are upserted keyed by `(org_id, code)` — the universal user-facing canonical identifier introduced by Phase 04.1 (D04_1-01)`.
    - Append a new paragraph in the same description block: `**Idempotency:** Clients MAY pass an `Idempotency-Key` UUIDv7 header to make a POST retry-safe. A repeated request with the same key in the same org returns the persisted prior result with `idempotent_replay: true` (D5-13 / D5-27). Note: in v0.1 the replay response carries an empty `succeeded` array because the server stores only counters + errors JSONB — original succeeded IDs are not reconstructable. Callers that need full replay should use the non-idempotent path or wait for v0.2.`

    DELTA 2 — Rewrite Import tag description at line ~3209-3213.
    - Find: any text in the `Import` tag description referencing `external_id` as the import key; rewrite to say `code` is the upsert key per Phase 04.1.

    DELTA 3 — KEEP `application/json` request body as `type: array, items: {}, maxItems: 500` (lines ~3092-3097). Add an explanatory comment in the description (above `items: {}`): "Per-row shape is one of the six Import*Request schemas in components.schemas, selected by the `?entity=` query parameter. Polymorphic `oneOf` is not used because oapi-codegen v2 does not emit accessor helpers for `oneOf` request bodies (verified — issue #1620)."

    DELTA 4 — Add the optional `Idempotency-Key` header parameter to the BulkImportCatalog operation. Reference an existing components.parameters entry (DELTA 9). Insert the `- $ref: "#/components/parameters/IdempotencyKeyHeader"` line into the operation's `parameters:` array (the operation already has OrgIdPath via the path-level parameters block; add a new per-operation `parameters:` block at the operation level beneath `operationId`/`summary`, or append to the existing parameters list — match Phase 04.1 yaml conventions). The parameter is `required: false`.

    DELTA 5 — Extend `BulkImportResult` schema (line ~1496-1521). Add a new property after `failed`:
    ```yaml
        idempotent_replay:
          type: boolean
          default: false
          nullable: true
          readOnly: true
          description: >
            True when this response is a persisted prior result returned because the
            client repeated the Idempotency-Key (D5-13). False or absent on a
            first-time POST. v0.1 KNOWN LIMITATION: when true, `succeeded` is
            always an empty array because the server stores only counters; rely
            on `failed[]` for forensics (see operation description).
    ```
    Do NOT add `idempotent_replay` to the `required` list (existing 207 / 422 consumers must keep parsing without it).

    DELTA 6 — Extend `ImportJob` schema (line ~1523-1562). Add `status` to `required:` list. Insert a new property:
    ```yaml
        status:
          type: string
          enum:
            - pending
            - completed
            - failed
          description: >
            Lifecycle of the import (D5-26). `pending` — the row was created
            before processing began; in v0.1 callers normally observe only
            `completed` or `failed` because the request is synchronous.
            `failed` may be set by the crash-recovery sweep (D5-11) when a
            process restart leaves a pending row stranded.
    ```

    DELTA 7 — Six new request schemas in `components.schemas` (mirrors Phase 04.1's `CreateAgentRequest` shape with FK fields swapped to `*_code` and an optional `external_id` integration-mapping field). Insert in alphabetical order beneath the existing Import* schemas (after `ImportJob` at line ~1562 and before infrastructure schemas at line ~1564):
    ```yaml
        ImportAgentRequest:
          type: object
          description: |
            Phase 5 (IMP-01) per-row JSON shape for `entity=agents`. Mirror of
            CreateAgentRequest with FK references by `code` instead of UUID.
            `external_id` is optional integration-mapping (mutable per D04_1-07).
          required: [code, name, email]
          properties:
            code:        { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$', example: "emp_0042" }
            external_id: { type: string, nullable: true, example: "HR-EMP-0042" }
            name:        { type: string, example: "Mai Linh" }
            email:       { type: string, format: email, example: "mai@example.com" }
            enabled:     { type: boolean, default: true }
            skills:
              type: array
              description: >
                Skill assignments referenced by `skill_code`. MERGE semantics
                (D5-18) — existing assignments not in this list are LEFT INTACT;
                proficiency for codes present in this list is set to the value
                supplied (D5-19 import wins). Skill removal via import is
                deferred to v0.2.
              items:
                type: object
                required: [skill_code, proficiency]
                properties:
                  skill_code:  { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
                  proficiency: { type: integer, minimum: 1, maximum: 10 }
        ImportSkillRequest:
          type: object
          description: Phase 5 (IMP-01) per-row JSON shape for `entity=skills`.
          required: [code, name, skill_type]
          properties:
            code:        { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            external_id: { type: string, nullable: true }
            name:        { type: string }
            description: { type: string, nullable: true }
            skill_type:  { type: string }
            enabled:     { type: boolean, default: true }
        ImportQueueRequest:
          type: object
          description: Phase 5 (IMP-01) per-row JSON shape for `entity=queues`.
          required: [code, name, channel_types]
          properties:
            code:        { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            external_id: { type: string, nullable: true }
            name:        { type: string }
            channel_types:
              type: array
              items: { type: string }
              description: >
                Multi-valued — in CSV use pipe-delimited per D5-04
                (`voice|chat`); in JSON use the array form.
            priority:    { type: integer, default: 5 }
            acw_sec:     { type: integer, default: 0 }
            enabled:     { type: boolean, default: true }
        ImportChannelRequest:
          type: object
          description: |
            Phase 5 (IMP-01) per-row JSON shape for `entity=channels`. FK
            `default_queue_code` references an existing queue by code; cross-row
            FK probe is performed in the same chunk transaction.
          required: [code, name, channel_type]
          properties:
            code:               { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            external_id:        { type: string, nullable: true }
            name:               { type: string }
            channel_type:       { type: string }
            default_queue_code: { type: string, nullable: true, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            enabled:            { type: boolean, default: true }
        ImportAdapterRequest:
          type: object
          description: |
            Phase 5 (IMP-01) per-row JSON shape for `entity=adapters`. `config`
            is a free-form JSONB blob passed through to storage with no
            validation (D-72 Phase 3 contract).
          required: [code, name, adapter_type]
          properties:
            code:         { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            external_id:  { type: string, nullable: true }
            name:         { type: string }
            adapter_type: { type: string }
            config:
              type: object
              description: Free-form vendor configuration; no fixed schema.
              additionalProperties: true
            enabled:      { type: boolean, default: true }
        ImportBreakReasonRequest:
          type: object
          description: |
            Phase 5 (IMP-01) per-row JSON shape for `entity=break_reasons`.
            `name` is a mutable display label (Phase 04.1 dropped
            UNIQUE(org_id, name) — IDENT-03).
          required: [code, name]
          properties:
            code:          { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
            external_id:   { type: string, nullable: true }
            name:          { type: string }
            routable:      { type: boolean, default: true }
            display_order: { type: integer, default: 0 }
            enabled:       { type: boolean, default: true }
    ```

    DELTA 8 — Reference the 6 new schemas from the JSON request body so they appear in clients despite the `items: {}` polymorphic-array shape. Add `x-types-only-references` block in the operation description or inline as a yaml comment listing each schema name — OR (preferred) add a no-op `x-import-row-types` extension to the requestBody object containing all 6 schema names. Keep the actual `items: {}` shape exactly as-is so generated code matches RESEARCH §F2.

    DELTA 9 — Add the reusable `IdempotencyKeyHeader` parameter under `components.parameters` (line ~1621). Insert in alphabetical order:
    ```yaml
        IdempotencyKeyHeader:
          name: Idempotency-Key
          in: header
          required: false
          description: >
            Client-generated UUIDv7 (RFC 9562 §5.7) used to make POST retry-safe
            (D5-13). A repeated request with the same key in the same org returns
            the persisted prior result with `idempotent_replay: true` (D5-27).
            Optional — absent header means a fresh job row is created each call.
          schema:
            type: string
            format: uuid
            example: "01919e1b-7c80-7c80-8000-0123456789ab"
    ```

    DELTA 10 — Verify spec lints. Do not run `task gen` yet (that's Task 3); do a structural sanity scan by re-Reading the modified file ranges and confirming yaml indentation matches surrounding entries.

    NB: Stay within OpenAPI 3.0 (the spec is locked to 3.0 per CONTRACT-01 deviation note). Do NOT use `oneOf` on the request body (RESEARCH §F2). Do NOT add new endpoint paths — the existing two operations stand.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && grep -c '^    ImportAgentRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^    ImportSkillRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^    ImportQueueRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^    ImportChannelRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^    ImportAdapterRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^    ImportBreakReasonRequest:' openapi/openapi.yaml | grep -qx 1 && grep -c '^        IdempotencyKeyHeader:' openapi/openapi.yaml | grep -qx 1 && grep -c 'idempotent_replay:' openapi/openapi.yaml | grep -qx 1 && grep -cE '^        status:$' openapi/openapi.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '(org_id, code)' openapi/openapi.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && ! grep -q 'keyed by `(org_id, external_id)`' openapi/openapi.yaml && echo OPENAPI_DELTAS_OK</automated>
  </verify>
  <done>10 deltas applied: 6 new Import*Request schemas + IdempotencyKeyHeader parameter + BulkImportResult.idempotent_replay + ImportJob.status enum + description rewrites (code, not external_id) + request body retains `items: {}`. Spec still parses as OpenAPI 3.0.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 000003 (import_jobs table + partial indexes) and add import_jobs to SQLChecker tenantTables</name>
  <files>
    migrations/000003_create_import_jobs.up.sql,
    migrations/000003_create_import_jobs.down.sql,
    services/api/internal/db/sqlcheck.go
  </files>
  <read_first>
    - migrations/000002_catalog_v0_1.up.sql (BEGIN/COMMIT envelope; UUIDv7 column + UNIQUE composite indices; agent_states block around lines 320-360)
    - migrations/000002_catalog_v0_1.down.sql (DROP TABLE order — Phase 5's down is single-statement so order is moot)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (migrations/000003 section — verbatim DDL + index strategy)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pattern 8 (migration DDL) and §Open Question Q4 (rationale for new 000003 instead of amending 000002)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-09, D5-10, D5-11, D5-13 (table fields driven by chunk semantics + idempotency + sweep)
    - services/api/internal/db/sqlcheck.go lines 32-42 (tenantTables map)
    - .planning/phases/04-agent-state-machine-go/04-PATTERNS.md (Phase 4 added `"agent_states": {}` — Phase 5 follows the same one-line append pattern)
  </read_first>
  <action>
    Author two new migration files and append a single line to sqlcheck.go.

    1) Create `migrations/000003_create_import_jobs.up.sql` verbatim from RESEARCH §Pattern 8 / PATTERNS §migrations/000003 section. Wrap in `BEGIN; ... COMMIT;` envelope. Table columns (UUID PK + denormalised org_id + entity_type with CHECK list of the 6 values + status with CHECK [pending|completed|failed] DEFAULT 'pending' + total_rows INTEGER DEFAULT 0 + succeeded_rows INTEGER DEFAULT 0 + failed_rows INTEGER DEFAULT 0 + errors JSONB NULLABLE + idempotency_key TEXT NULLABLE + created_at + updated_at). Three indexes:
       - `ix_import_jobs_org_created` on `(org_id, created_at DESC, id DESC)` for admin list queries (forward-compat).
       - `ix_import_jobs_pending_updated` partial index on `(updated_at) WHERE status = 'pending'` so the sweep query scans a tiny subset (D5-11).
       - `uq_import_jobs_org_idempotency` partial UNIQUE on `(org_id, idempotency_key) WHERE idempotency_key IS NOT NULL` so the key is uniqueness-enforced only when present (D5-13).
       NO `enabled` column (event history; mirror agent_states deviation per Phase 4 PATTERNS line 1009). NO `version` column (counters monotonically increase). NO FK constraint anywhere (D-80 invariant — app-layer probes only). `org_id` denormalised so SQLChecker sees it on every query.

    2) Create `migrations/000003_create_import_jobs.down.sql` with `BEGIN; DROP TABLE IF EXISTS import_jobs; COMMIT;`. v0.1 down migration is documentation-only; production uses `task db:reset` (D-25 / D04_1-12).

    3) In `services/api/internal/db/sqlcheck.go`, locate the `var tenantTables = map[string]struct{}{` block (lines 32-42) and insert ONE NEW LINE in alphabetical order (between `"agents"` and `"break_reasons"`):
       ```go
           "import_jobs":   {}, // IMP-06 (Phase 5; denormalised org_id per RESEARCH §Pattern 8)
       ```
       Inserted entry must follow the formatting of the surrounding entries exactly (column alignment of `{}` and trailing comment).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f migrations/000003_create_import_jobs.up.sql && test -f migrations/000003_create_import_jobs.down.sql && grep -c 'CREATE TABLE import_jobs' migrations/000003_create_import_jobs.up.sql | grep -qx 1 && grep -c 'ix_import_jobs_pending_updated' migrations/000003_create_import_jobs.up.sql | grep -qx 1 && grep -c 'uq_import_jobs_org_idempotency' migrations/000003_create_import_jobs.up.sql | grep -qx 1 && grep -cE "^[[:space:]]+\"import_jobs\":[[:space:]]+\\{\\}" services/api/internal/db/sqlcheck.go | grep -qx 1 && grep -c 'DROP TABLE IF EXISTS import_jobs' migrations/000003_create_import_jobs.down.sql | grep -qx 1 && echo MIGRATION_AND_SQLCHECK_OK</automated>
  </verify>
  <done>migrations/000003 up + down exist; up creates import_jobs with partial unique on idempotency_key and partial index on pending status; sqlcheck.go tenantTables registers `"import_jobs": {}` in alphabetical order.</done>
</task>

<task type="auto">
  <name>Task 3: Run task gen, commit codegen drift, verify no manual edits regressed</name>
  <files>
    services/api/internal/api/server.gen.go,
    services/api/internal/api/types.gen.go,
    services/api/internal/api/spec.gen.go,
    web/packages/ui/src/api/generated.ts
  </files>
  <read_first>
    - Taskfile.yml (lines 18-42 — `gen` task chain: `go:gen` runs oapi-codegen; `ts:gen` runs openapi-typescript)
    - .planning/phases/02-openapi-contract-codegen/02-VERIFICATION.md (Phase 2 codegen drift gate — CI rejects any uncommitted regen)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-03-SUMMARY.md (Phase 04.1 codegen regen pattern — task gen + commit produced no diff at plan exit)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (Codegen Output Files table — lists which files Phase 5 expects to regen)
    - openapi/openapi.yaml (the file edited in Task 1; codegen reads it)
  </read_first>
  <action>
    1) Run `task gen` from the repo root (`cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && task gen`). This invokes both `go:gen` (oapi-codegen — regenerates services/api/internal/api/*.gen.go) and `ts:gen` (openapi-typescript — regenerates web/packages/ui/src/api/generated.ts).
    2) `git diff --stat` to inventory regenerated files. Confirm the diff covers: server.gen.go (BulkImportCatalogParams gains IdempotencyKey *uuid pointer), types.gen.go (6 new Import*Request structs + ImportJobStatus enum constants + BulkImportResult.IdempotentReplay *bool pointer), spec.gen.go (embedded yaml bytes), and generated.ts (TS shapes for the 6 new request types + idempotent_replay).
    3) Verify that `BulkImportCatalogJSONBody` REMAINS `[]interface{}` in `services/api/internal/api/types.gen.go` (RESEARCH §F2 — `oneOf` not used; eager-decoded array elements are dispatched per-row via re-marshal in later plans). If codegen suddenly produces a richer typed body, something in the spec went wrong (likely an accidental `oneOf`) — STOP, re-check Task 1's `items: {}` invariant, fix, re-run.
    4) Run `git diff --exit-code services/api/internal/api/ web/packages/ui/src/api/` after `task gen` to confirm the working tree matches what regen produced (this is what the CI codegen-drift gate checks). Exit code must be 0 after a fresh `task gen`.
    5) Sanity-grep the new types: `grep -c 'type ImportAgentRequest ' services/api/internal/api/types.gen.go` returns 1; `grep -c 'IdempotentReplay ' services/api/internal/api/types.gen.go` returns ≥1; `grep -c 'ImportJobStatus' services/api/internal/api/types.gen.go` returns ≥3 (type + 3 enum constants).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && task gen 2>&1 | tail -20 && grep -c 'type ImportAgentRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'type ImportSkillRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'type ImportQueueRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'type ImportChannelRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'type ImportAdapterRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'type ImportBreakReasonRequest ' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'IdempotentReplay' services/api/internal/api/types.gen.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'BulkImportCatalogJSONBody = \[\]interface{}' services/api/internal/api/types.gen.go | grep -qx 1 && git diff --exit-code services/api/internal/api/ web/packages/ui/src/api/ && echo CODEGEN_DRIFT_CLEAN</automated>
  </verify>
  <done>task gen runs clean; 6 new Import*Request types + IdempotentReplay + ImportJobStatus enum present in types.gen.go; BulkImportCatalogJSONBody still `[]interface{}` (F2 invariant); git diff --exit-code returns 0 after regen (CI codegen-drift gate is green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted CSV/JSON crosses here on POST /catalog/import — request body, headers, query params. |
| API → DB | sqlc-generated parameterised queries on OrgDB — SQLChecker enforces org_id presence. |
| DB schema → migration runner | golang-migrate applies SQL files in numeric order; the `schema_migrations` table tracks version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | openapi.yaml polymorphic body schema | mitigate | Keep `items: {}` per RESEARCH §F2; document the dispatch-by-`?entity=` contract in operation description so client and server agree on how to identify per-row type. Drift-gate (CI) catches any spec change that breaks codegen. |
| T-05-01-02 | Information Disclosure | components.schemas exposes per-entity import shapes | accept | Schemas mirror existing CreateXRequest — no new sensitive fields. Idempotency-Key is a client-supplied UUIDv7 with no server secret. |
| T-05-01-03 | Denial of Service | Crash-sweep partial index size | mitigate | `ix_import_jobs_pending_updated` is a PARTIAL index on `WHERE status = 'pending'`; on a healthy system pending row count is bounded by concurrent imports (single-replica v0.1). Index size stays trivial. |
| T-05-01-04 | Tampering | SQLChecker bypass via missing tenantTables entry | mitigate | Adding `"import_jobs": {}` to tenantTables means every sqlc query against the new table is preflighted for `org_id = $N` (Pitfall 2). Without this entry the first generated query panics in dev/test (catches the gap immediately). |
| T-05-01-05 | Information Disclosure | uq_import_jobs_org_idempotency leaking idempotency keys across orgs | mitigate | Composite UNIQUE includes `org_id` in the leading position; partial filter `WHERE idempotency_key IS NOT NULL` so NULLs don't trip the unique. Cross-org probe impossible. |
| T-05-01-SC | Tampering | npm/Go module installs | mitigate | Task 3 (`task gen`) uses already-installed oapi-codegen + openapi-typescript pinned in tools.go / package.json — no new package fetches in Wave 0. (RESEARCH §Package Legitimacy Audit: zero new deps.) |
</threat_model>

<verification>
- `task gen` exits 0 and produces NO diff against the working tree (CI codegen-drift gate is green).
- `git diff --exit-code services/api/internal/api/ web/packages/ui/src/api/` returns 0 after `task gen`.
- All 6 Import*Request types present in types.gen.go.
- `BulkImportCatalogJSONBody` remains `[]interface{}` (F2 invariant — fail loudly if regression).
- migration 000003 up + down exist and parse as SQL.
- sqlcheck.go tenantTables map has the `import_jobs` entry in alphabetical order.
- `services/api/internal/api/types.gen.go` contains `IdempotentReplay` (pointer) and `ImportJobStatus` typed enum.
- The spec description for BulkImportCatalog reads "keyed by `(org_id, code)`" — NOT external_id (verified by grep).
</verification>

<success_criteria>
Wave 0 success when:
- [x] openapi.yaml carries the 10 deltas described in Task 1 (6 schemas + parameter + idempotent_replay + status enum + 2 description rewrites + items: {} retained)
- [x] migrations/000003_create_import_jobs.{up,down}.sql exist and DDL matches PATTERNS §migrations/000003
- [x] sqlcheck.go tenantTables map registers `import_jobs`
- [x] task gen runs clean; no manual edits required; CI codegen-drift gate green
- [x] BulkImportCatalogJSONBody stays `[]interface{}` (F2 invariant)
- [x] Single commit captures spec + migration + sqlcheck + regen (downstream plans inherit the contract atomically)
</success_criteria>

<output>
Create `.planning/phases/05-bulk-import-go/05-01-SUMMARY.md` capturing:
- Exact line ranges modified in openapi.yaml
- Migration 000003 up/down DDL inventory (3 indexes; 0 FKs; partial unique on idempotency)
- sqlcheck.go diff (one-line append)
- task gen output digest + confirmation that types.gen.go gained the 6 Import*Request types + IdempotentReplay + ImportJobStatus
- Confirmation that `BulkImportCatalogJSONBody = []interface{}` did NOT change (F2 invariant)
</output>
</content>
</invoke>