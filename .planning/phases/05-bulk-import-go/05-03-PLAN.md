---
phase: 05-bulk-import-go
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
  - 05-02
files_modified:
  - services/api/internal/db/queries/import_jobs.sql
  - services/api/internal/db/queries/agent_skills.sql
  - services/api/internal/db/queries/agent_states.sql
  - services/api/internal/db/queries/skills.sql
  - services/api/internal/db/generated/import_jobs.sql.go
  - services/api/internal/db/generated/agent_skills.sql.go
  - services/api/internal/db/generated/agent_states.sql.go
  - services/api/internal/db/generated/skills.sql.go
  - services/api/internal/db/generated/models.go
  - services/api/internal/db/generated/db.go
autonomous: true
requirements:
  - IMP-03
  - IMP-06
tags:
  - sqlc
  - codegen
  - import-jobs

must_haves:
  truths:
    - "import_jobs.sql ships 5 queries: InsertImportJob, FinaliseImportJob, GetImportJob, LookupImportJobByIdempotencyKey, SweepCrashedImportJobs"
    - "agent_skills.sql gains MergeAgentSkill :exec with ON CONFLICT (agent_id, skill_id) DO UPDATE SET proficiency = EXCLUDED.proficiency"
    - "agent_states.sql gains InsertAgentStateOnConflictNothing :exec — Phase 4 InsertAgentState is left unchanged"
    - "skills.sql gains ResolveSkillCodes :many (batch lookup by code in org)"
    - "sqlc generates Go bindings without manual edit; task gen produces no diff"
    - "Every new sqlc query mentions org_id in WHERE/INSERT/UPDATE — except SweepCrashedImportJobs which is org-agnostic by intent and runs under db.WithBypass(ctx, \"import_crash_sweep\")"
  artifacts:
    - path: "services/api/internal/db/queries/import_jobs.sql"
      provides: "5 import_jobs CRUD + sweep queries"
      contains: "InsertImportJob"
    - path: "services/api/internal/db/queries/agent_skills.sql"
      provides: "MergeAgentSkill :exec (PATCH semantics for Phase 5 imports)"
      contains: "MergeAgentSkill"
    - path: "services/api/internal/db/queries/agent_states.sql"
      provides: "InsertAgentStateOnConflictNothing :exec — for re-import safety"
      contains: "InsertAgentStateOnConflictNothing"
    - path: "services/api/internal/db/queries/skills.sql"
      provides: "ResolveSkillCodes :many — batch lookup for chunk-level skill resolution"
      contains: "ResolveSkillCodes"
    - path: "services/api/internal/db/generated/import_jobs.sql.go"
      provides: "sqlc-generated Go bindings"
      contains: "func (q *Queries) InsertImportJob"
  key_links:
    - from: "services/api/internal/db/queries/import_jobs.sql"
      to: "services/api/internal/db/generated/import_jobs.sql.go"
      via: "sqlc generate"
      pattern: "InsertImportJob|FinaliseImportJob|GetImportJob"
    - from: "services/api/internal/db/queries/agent_skills.sql"
      to: "services/api/internal/db/generated/agent_skills.sql.go"
      via: "sqlc generate"
      pattern: "MergeAgentSkill"
    - from: "services/api/internal/db/queries/skills.sql"
      to: "services/api/internal/db/generated/skills.sql.go"
      via: "sqlc generate"
      pattern: "ResolveSkillCodes"
---

<objective>
Author the 8 new sqlc queries Phase 5 owns (Phase 04.1 already shipped the
6 `UpsertXByCode` + 6 `GetXByCode` queries; Phase 5 invokes them).
Regenerate Go bindings via `sqlc generate` (driven by `task gen`).

Purpose: Wave 2 (imports package scaffold) and Wave 3 (per-entity row
processors) need typed `*Queries` methods to call. Wave 1 is single-plan
because all 8 queries are mechanically appended to existing files; they
have no behaviour, just signatures.

Output: 4 modified .sql files + 4 regenerated .sql.go files + models.go +
db.go updates. Zero handler/middleware code changes. Single commit.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bulk-import-go/05-CONTEXT.md
@.planning/phases/05-bulk-import-go/05-RESEARCH.md
@.planning/phases/05-bulk-import-go/05-PATTERNS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-03-SUMMARY.md
@.planning/phases/05-bulk-import-go/05-01-PLAN.md
@services/api/internal/db/queries/import_jobs.sql
@services/api/internal/db/queries/agent_skills.sql
@services/api/internal/db/queries/agent_states.sql
@services/api/internal/db/queries/skills.sql
@services/api/internal/db/queries/agents.sql
@services/api/sqlc.yaml
@Taskfile.yml

<interfaces>
<!-- Existing sqlc query files and patterns Phase 5 extends -->

services/api/internal/db/queries/agents.sql:
- Line 29: InsertAgent :one (basic INSERT — shape template for InsertImportJob)
- Line 83: UpdateAgent :one (version-checked UPDATE — shape template for FinaliseImportJob, minus the version field)
- Line 119: GetAgentByCode :one (Phase 04.1 — shape template for LookupImportJobByIdempotencyKey)
- Line 127: UpsertAgentByCode :one (Phase 04.1 — Phase 5 invokes; does NOT extend)

services/api/internal/db/queries/agent_skills.sql current query inventory (verified):
- Line 29: InsertAgentSkill :one (basic INSERT without ON CONFLICT)
- Line 34: DeleteAgentSkills :execrows (Phase 3 replaceAgentSkills DELETE half)
- Line 42: ListSkillsForAgent :many
- Line 59: SkillsPresentInOrg :many (batch lookup by id — template for ResolveSkillCodes)

services/api/internal/db/queries/agent_states.sql current inventory:
- Line 1: InsertAgentState :one (Phase 4 — no ON CONFLICT; Phase 5 must NOT change this)
- Line 9: GetAgentStateByAgentId :one
- Line 17: UpdateAgentStateStatus :one
- Line 46: ForceUpdateAgentStateStatus :one
- Line 69: ListExpiringWrapUps :many (shape template for SweepCrashedImportJobs RETURNING pattern)
- Line 80: ExpireWrapUp :one

services/api/internal/db/queries/skills.sql current inventory:
- Line 26: InsertSkill :one
- Line 93: GetSkillByCode :one (Phase 04.1)
- Line 101: UpsertSkillByCode :one (Phase 04.1)

services/api/sqlc.yaml: configures `engine: postgresql`, queries: per-file list. Phase 5 adds `queries/import_jobs.sql` to the list (verify YAML format and append; mirror agent_states.sql precedent).

Taskfile.yml gen task chain: `sqlc generate` runs INSIDE `task go:gen` BEFORE oapi-codegen. Both run via `task gen`.

migrations/000003_create_import_jobs.up.sql (created in Plan 05-01) is the schema source for the new queries; sqlc reads it via the schema path in sqlc.yaml.

Phase 5 PATTERNS section (db/queries/agent_skills.sql MODIFIED) — verbatim MergeAgentSkill SQL.
Phase 5 PATTERNS section (db/queries/skills.sql MODIFIED) — verbatim ResolveSkillCodes SQL.
Phase 5 PATTERNS section (db/queries/agent_states.sql MODIFIED) — verbatim InsertAgentStateOnConflictNothing SQL.
Phase 5 PATTERNS section (db/queries/import_jobs.sql NEW) — verbatim 5-query inventory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the new import_jobs.sql file with 5 queries (InsertImportJob, FinaliseImportJob, GetImportJob, LookupImportJobByIdempotencyKey, SweepCrashedImportJobs)</name>
  <files>
    services/api/internal/db/queries/import_jobs.sql,
    services/api/sqlc.yaml
  </files>
  <read_first>
    - services/api/internal/db/queries/agents.sql lines 29-39 (InsertAgent + GetAgent — INSERT and SELECT-by-id+org_id idioms)
    - services/api/internal/db/queries/agents.sql lines 83-110 (UpdateAgent — UPDATE with WHERE id=$ AND org_id=$ idiom)
    - services/api/internal/db/queries/agent_states.sql lines 69-106 (ListExpiringWrapUps + ExpireWrapUp — sweep RETURNING pattern + bypass-marked queries)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (import_jobs.sql NEW section — full 5-query inventory verbatim)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pattern 7 (5-query SQL listing)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-10, D5-11, D5-13 (job lifecycle + crash sweep + idempotency semantics)
    - migrations/000003_create_import_jobs.up.sql (the SCHEMA the queries read — created in Plan 05-01; column types must match)
    - services/api/sqlc.yaml (queries: list — append import_jobs.sql in alphabetical order)
  </read_first>
  <action>
    1) Create `services/api/internal/db/queries/import_jobs.sql` with 5 queries. Use the verbatim SQL from PATTERNS §import_jobs.sql section.

       Query 1 — InsertImportJob :one. INSERT row with status='pending', total_rows from caller, succeeded_rows=0, failed_rows=0, errors=NULL, idempotency_key=sqlc.narg('idempotency_key'). RETURNING the full row (every column the model needs). The `(org_id, idempotency_key) WHERE idempotency_key IS NOT NULL` partial UNIQUE enforces dedup; on conflict the INSERT raises 23505 with constraint name `uq_import_jobs_org_idempotency` — the handler's Idempotency-Key replay path looks up by `LookupImportJobByIdempotencyKey` BEFORE attempting INSERT, so the 23505 is a defensive backstop only.

       Query 2 — FinaliseImportJob :one. UPDATE status=$1, succeeded_rows=$2, failed_rows=$3, errors=$4, updated_at=NOW() WHERE id=$5 AND org_id=$6 RETURNING the full row. No version check (counters are monotonic, not optimistic-locked).

       Query 3 — GetImportJob :one. SELECT all columns WHERE id=$1 AND org_id=$2. FOUND-08 isolation holds because the WHERE is composite-org-scoped (cross-org lookup returns pgx.ErrNoRows → handler maps to 404).

       Query 4 — LookupImportJobByIdempotencyKey :one. SELECT all columns WHERE org_id=$1 AND idempotency_key=$2. Returns pgx.ErrNoRows on miss — handler propagates to "proceed normally; persist key on the new row" path.

       Query 5 — SweepCrashedImportJobs :many. UPDATE import_jobs SET status='failed', errors='[{"row":0,"error":"import_failed","reason":"server_crash"}]'::jsonb, updated_at=NOW() WHERE status='pending' AND updated_at < NOW() - INTERVAL '24 hours' RETURNING id, org_id. The WHERE clause is INTENTIONALLY org-agnostic (sweep runs across all orgs) — this is the SOLE query in the codebase that omits org_id from WHERE. Phase 5 sweep goroutine wraps the ctx with `db.WithBypass(ctx, "import_crash_sweep")` so SQLChecker's D-04 preflight accepts the query.

       Every query MUST have a header doc comment referencing the relevant D5-NN decision ID (for grep-discoverability), the relevant invariant, and the SQLChecker disposition. Mirror agent_states.sql's heavy commenting style.

    2) Append `queries/import_jobs.sql` to the `services/api/sqlc.yaml` `queries:` list. Insert in alphabetical order (between break_reasons and queues if the YAML keeps alphabetical, or wherever the convention puts it — read sqlc.yaml first).

    3) Do NOT run `sqlc generate` yet — that's Task 4.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f services/api/internal/db/queries/import_jobs.sql && grep -c '^-- name: InsertImportJob :one' services/api/internal/db/queries/import_jobs.sql | grep -qx 1 && grep -c '^-- name: FinaliseImportJob :one' services/api/internal/db/queries/import_jobs.sql | grep -qx 1 && grep -c '^-- name: GetImportJob :one' services/api/internal/db/queries/import_jobs.sql | grep -qx 1 && grep -c '^-- name: LookupImportJobByIdempotencyKey :one' services/api/internal/db/queries/import_jobs.sql | grep -qx 1 && grep -c '^-- name: SweepCrashedImportJobs :many' services/api/internal/db/queries/import_jobs.sql | grep -qx 1 && grep -c 'import_jobs.sql' services/api/sqlc.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && echo IMPORT_JOBS_SQL_OK</automated>
  </verify>
  <done>import_jobs.sql exists with 5 named queries; sqlc.yaml references it; SweepCrashedImportJobs is explicitly documented as the lone org-agnostic query that requires WithBypass.</done>
</task>

<task type="auto">
  <name>Task 2: Append MergeAgentSkill :exec to agent_skills.sql and InsertAgentStateOnConflictNothing :exec to agent_states.sql</name>
  <files>
    services/api/internal/db/queries/agent_skills.sql,
    services/api/internal/db/queries/agent_states.sql
  </files>
  <read_first>
    - services/api/internal/db/queries/agent_skills.sql lines 29-79 (existing 4 queries — InsertAgentSkill + DeleteAgentSkills + ListSkillsForAgent + SkillsPresentInOrg)
    - services/api/internal/db/queries/agent_states.sql lines 1-7 (existing InsertAgentState; Phase 5 must NOT modify this query — adds a SEPARATE one)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pattern 7 (verbatim MergeAgentSkill + InsertAgentStateOnConflictNothing SQL)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (agent_skills.sql MODIFIED + agent_states.sql MODIFIED sections)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-18, D5-19 (PATCH-like merge semantics; import value wins on proficiency conflict)
    - .planning/phases/04-agent-state-machine-go/04-PATTERNS.md (Hazard 7 — every new agent must seed agent_states; Phase 5 inheritance reminder)
  </read_first>
  <action>
    1) APPEND to `services/api/internal/db/queries/agent_skills.sql` (after the existing `SkillsPresentInOrg :many` query at line ~79):
       ```sql
       -- name: MergeAgentSkill :exec
       -- D5-18 + D5-19: skill assignment MERGE on import (Phase 5).
       -- ON CONFLICT (agent_id, skill_id) DO UPDATE so existing assignments
       -- get the new proficiency from the import (D5-19 import wins).
       --
       -- Existing skills NOT in the payload are LEFT INTACT (D5-18 PATCH-like)
       -- — achieved by simply NOT issuing DELETE statements. Phase 5 row
       -- processors MUST NOT call catalog.replaceAgentSkills (DELETE-ALL +
       -- INSERT-N — wrong semantic for import; RESEARCH Pitfall 10).
       --
       -- :exec (NOT :one) — ON CONFLICT UPDATE makes the RETURNING value
       -- ambiguous about which version landed. Phase 5 row processor does
       -- not need the row back; only that the upsert succeeded.
       INSERT INTO agent_skills (agent_id, skill_id, org_id, proficiency)
       VALUES ($1, $2, $3, $4)
       ON CONFLICT (agent_id, skill_id) DO UPDATE
       SET proficiency = EXCLUDED.proficiency;
       ```

       Parameter ordering MUST match agent_skills.sql convention (agent_id first, skill_id second, org_id third, proficiency fourth — verify against existing InsertAgentSkill at line 29 to confirm; if InsertAgentSkill uses a different order, mirror its order for consistency).

    2) APPEND to `services/api/internal/db/queries/agent_states.sql` (after `ExpireWrapUp :one` at the end of the file):
       ```sql
       -- name: InsertAgentStateOnConflictNothing :exec
       -- Phase 5 import (D5-18 / Phase 4 Hazard 7 carry-forward): seed state
       -- for NEW agents only; re-imports MUST NOT regress the state machine
       -- to Offline. Differs from InsertAgentState (line 1) which is
       -- unconditional. Adding ON CONFLICT to the existing query would
       -- change Phase 4 CreateAgent semantics — Phase 5 authors this as a
       -- separate sqlc query.
       --
       -- :exec — on conflict, no row is returned; the row processor does
       -- not need to read back state. The only contract is that the row
       -- exists when this query completes (whether INSERT'd or pre-existing).
       INSERT INTO agent_states (agent_id, org_id, status, state_version)
       VALUES ($1, $2, $3, 1)
       ON CONFLICT (agent_id) DO NOTHING;
       ```

    3) Do NOT run `sqlc generate` yet.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && grep -c '^-- name: MergeAgentSkill :exec' services/api/internal/db/queries/agent_skills.sql | grep -qx 1 && grep -c '^-- name: InsertAgentStateOnConflictNothing :exec' services/api/internal/db/queries/agent_states.sql | grep -qx 1 && grep -c 'ON CONFLICT (agent_id, skill_id) DO UPDATE' services/api/internal/db/queries/agent_skills.sql | grep -qx 1 && grep -c 'ON CONFLICT (agent_id) DO NOTHING' services/api/internal/db/queries/agent_states.sql | grep -qx 1 && grep -c '^-- name: InsertAgentState :one' services/api/internal/db/queries/agent_states.sql | grep -qx 1 && echo NEW_QUERIES_APPENDED_OK</automated>
  </verify>
  <done>MergeAgentSkill :exec + InsertAgentStateOnConflictNothing :exec added; original Phase 4 InsertAgentState query is untouched (Phase 4 CreateAgent semantics preserved).</done>
</task>

<task type="auto">
  <name>Task 3: Append ResolveSkillCodes :many to skills.sql for chunk-level batch skill resolution</name>
  <files>
    services/api/internal/db/queries/skills.sql
  </files>
  <read_first>
    - services/api/internal/db/queries/skills.sql lines 26-101 (existing queries; ResolveSkillCodes appends AFTER UpsertSkillByCode at line 101)
    - services/api/internal/db/queries/agent_skills.sql lines 59-79 (SkillsPresentInOrg :many — closest structural cousin: `ANY($1::uuid[]) + org_id = $2`, top-level FROM on tenant table)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pattern 7 (verbatim ResolveSkillCodes SQL)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (skills.sql MODIFIED section)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-16, D5-17 (chunk-level skill code resolution)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pitfall 6 (N+1 lookup avoidance — batch ResolveSkillCodes is the mitigation)
  </read_first>
  <action>
    APPEND to `services/api/internal/db/queries/skills.sql` (after `UpsertSkillByCode :one` at the end of the file):
    ```sql
    -- name: ResolveSkillCodes :many
    -- D5-16 + D5-17 (post-04.1): batch lookup of skill UUIDs by `code`.
    -- Used by the agent row processor (Phase 5) to resolve nested skill
    -- references once per chunk via Phase 04.1's `code` column. Returns
    -- (id, code) pairs; missing codes are absent from the result set —
    -- handler distinguishes "unknown skill" by set difference (mirror
    -- agent_skills.go:88 SkillsPresentInOrg miss-detection pattern).
    --
    -- RESEARCH Pitfall 6: N+1 lookup is the trap this query exists to
    -- avoid. 50 rows × 3 skills each = 150 round trips without batching;
    -- one ANY($1::text[]) call collapses it to 1 round trip per chunk.
    --
    -- SQLChecker compliance: top-level FROM skills (a tenant alias) +
    -- WHERE org_id = $1 → org_id IS in WHERE → D-02 passes.
    SELECT id, code
    FROM skills
    WHERE org_id = $1 AND code = ANY($2::text[]);
    ```

    Parameter ordering: org_id is $1 (matches Phase 04.1 convention where org_id leads), code-array is $2.

    Verify the query passes SQLChecker by reading the SQLChecker shape: tenant table referenced in FROM + org_id literal in WHERE → preflight green.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && grep -c '^-- name: ResolveSkillCodes :many' services/api/internal/db/queries/skills.sql | grep -qx 1 && grep -c 'FROM skills' services/api/internal/db/queries/skills.sql | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c 'code = ANY' services/api/internal/db/queries/skills.sql | grep -qx 1 && echo RESOLVE_SKILL_CODES_OK</automated>
  </verify>
  <done>ResolveSkillCodes :many appended to skills.sql; query mentions org_id in WHERE (SQLChecker green) and uses `ANY($2::text[])` for batch input.</done>
</task>

<task type="auto">
  <name>Task 4: Run task gen, regenerate sqlc bindings + oapi-codegen artifacts, commit zero-diff state</name>
  <files>
    services/api/internal/db/generated/import_jobs.sql.go,
    services/api/internal/db/generated/agent_skills.sql.go,
    services/api/internal/db/generated/agent_states.sql.go,
    services/api/internal/db/generated/skills.sql.go,
    services/api/internal/db/generated/models.go,
    services/api/internal/db/generated/db.go
  </files>
  <read_first>
    - Taskfile.yml (lines 18-42 — `gen` chain: sqlc generate + oapi-codegen + openapi-typescript)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-03-SUMMARY.md (Phase 04.1 plan 03 ran the same `task gen` flow after adding code-keyed queries — same wave shape)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (Codegen Output Files table — files Phase 5 expects to regen)
  </read_first>
  <action>
    1) Run `cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && task gen`. This invokes sqlc + oapi-codegen + openapi-typescript in sequence.

    2) Confirm sqlc generated:
       - `services/api/internal/db/generated/import_jobs.sql.go` (NEW file) — exports `InsertImportJob`, `FinaliseImportJob`, `GetImportJob`, `LookupImportJobByIdempotencyKey`, `SweepCrashedImportJobs` methods on *Queries.
       - `services/api/internal/db/generated/agent_skills.sql.go` (MODIFIED) — adds `MergeAgentSkill` method.
       - `services/api/internal/db/generated/agent_states.sql.go` (MODIFIED) — adds `InsertAgentStateOnConflictNothing` method.
       - `services/api/internal/db/generated/skills.sql.go` (MODIFIED) — adds `ResolveSkillCodes` method.
       - `services/api/internal/db/generated/models.go` (MODIFIED) — adds `ImportJob` struct mirroring the 11 columns of import_jobs.

    3) Confirm `BulkImportCatalogJSONBody = []interface{}` in `services/api/internal/api/types.gen.go` is UNCHANGED (Plan 05-01 invariant; if Wave 0 left it as `[]interface{}` it should still be `[]interface{}`).

    4) `git diff --exit-code services/api/internal/db/generated/ services/api/internal/api/` — must return 0 after the regen (no manual edits required; CI codegen-drift gate green).

    5) Run `cd services/api && go vet ./...` to catch any compile error introduced by the new generated bindings. Vet should be CLEAN (the generated code uses pgx types consistent with the rest of the package).

    6) Run `cd services/api && go test -count=1 -short ./internal/db/...` to confirm nothing in db/ regressed.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && task gen 2>&1 | tail -10 && test -f services/api/internal/db/generated/import_jobs.sql.go && grep -c 'func (q \*Queries) InsertImportJob' services/api/internal/db/generated/import_jobs.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) FinaliseImportJob' services/api/internal/db/generated/import_jobs.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) GetImportJob' services/api/internal/db/generated/import_jobs.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) LookupImportJobByIdempotencyKey' services/api/internal/db/generated/import_jobs.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) SweepCrashedImportJobs' services/api/internal/db/generated/import_jobs.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) MergeAgentSkill' services/api/internal/db/generated/agent_skills.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) InsertAgentStateOnConflictNothing' services/api/internal/db/generated/agent_states.sql.go | grep -qx 1 && grep -c 'func (q \*Queries) ResolveSkillCodes' services/api/internal/db/generated/skills.sql.go | grep -qx 1 && grep -c 'type ImportJob struct' services/api/internal/db/generated/models.go | grep -qx 1 && git diff --exit-code services/api/internal/db/generated/ services/api/internal/api/ && cd services/api && go vet ./... && echo SQLC_REGEN_CLEAN</automated>
  </verify>
  <done>All 8 new generated methods exist + ImportJob model struct present; task gen produces no diff against committed state; go vet is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query file → sqlc CLI → Go binding | Build-time codegen; failures surface as compile errors, not runtime risk. |
| Generated Queries methods → pgx pool | sqlc-emitted Exec/Query funcs use parameterised args — no string concatenation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Generated import_jobs queries bypassing SQLChecker via missing tenantTables entry | mitigate | Plan 05-01 Task 2 added `"import_jobs": {}` to tenantTables. Every Queries method routes through OrgDB.Exec/Query/QueryRow which runs preflight — if the entry were missing, the first INSERT/SELECT panics in dev/test. |
| T-05-03-02 | Tampering | SweepCrashedImportJobs as the lone org-agnostic UPDATE | mitigate | SQLChecker REJECTS the query in normal contexts. The crash-sweep goroutine (Plan 05-05) wraps ctx with `db.WithBypass(ctx, "import_crash_sweep")` which emits a structured slog audit. Only this single sweep callsite can pass the query. |
| T-05-03-03 | Tampering | MergeAgentSkill silently overwriting an existing org's agent_skills.org_id | accept | Composite PK is (agent_id, skill_id); the ON CONFLICT clause overwrites proficiency only. agent_id and skill_id are themselves UNIQUE per org by Phase 3 contract; cross-org agent_id collision is impossible because UUIDv7 keys + per-org isolation. |
| T-05-03-04 | Tampering | InsertAgentStateOnConflictNothing accidentally regressing Phase 4 callers | mitigate | Phase 5 authors this as a SEPARATE sqlc query; Phase 4's `InsertAgentState` query body is untouched. The diff is purely additive. Smoke check: `grep -c '^-- name: InsertAgentState :one' agent_states.sql` returns 1 (the original). |
| T-05-03-05 | Information Disclosure | ResolveSkillCodes leaking skill ids across orgs | mitigate | Query mentions `org_id = $1` in WHERE; SQLChecker preflight requires it. Caller must pass orgID from `orgkey.OrgIDFromContext(ctx)`. |
| T-05-03-SC | Tampering | sqlc binary swap | mitigate | sqlc CLI version pinned in `tools.go` (Phase 1 D-25 inheritance). No new module installs in this wave. |
</threat_model>

<verification>
- 4 .sql files modified; one new (import_jobs.sql).
- 8 new generated methods on `*Queries`.
- `task gen` produces no diff against tree (CI gate green).
- `cd services/api && go vet ./...` clean.
- `cd services/api && go test -count=1 -short ./internal/db/...` green.
- `BulkImportCatalogJSONBody` in types.gen.go unchanged (F2 invariant from Plan 05-01).
- Phase 4 `InsertAgentState :one` query untouched (Phase 5 added a SEPARATE OnConflictNothing variant).
</verification>

<success_criteria>
Wave 1 success when:
- [x] 5 import_jobs queries authored + sqlc-generated
- [x] MergeAgentSkill :exec authored + sqlc-generated; InsertAgentSkill stays untouched
- [x] InsertAgentStateOnConflictNothing :exec authored + sqlc-generated; InsertAgentState stays untouched
- [x] ResolveSkillCodes :many authored + sqlc-generated
- [x] task gen produces no diff after regen (CI codegen-drift gate green)
- [x] go vet clean across the api module
</success_criteria>

<output>
Create `.planning/phases/05-bulk-import-go/05-03-SUMMARY.md` capturing:
- Exact query inventory per file (counts of new + unchanged queries)
- Generated method list per *.sql.go file
- ImportJob model field types (UUIDv7 PK, denormalised org_id, status enum-as-text, timestamps)
- Confirmation that `BulkImportCatalogJSONBody = []interface{}` is unchanged
- Confirmation that `InsertAgentState :one` (Phase 4) is unchanged
</output>
</content>
</invoke>