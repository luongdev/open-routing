---
phase: 05-bulk-import-go
plan: 07
type: execute
wave: 5
depends_on:
  - 05-06
files_modified:
  - services/api/internal/imports/handlers_test.go
  - services/api/internal/imports/idempotency_test.go
  - services/api/internal/imports/jobs_test.go
  - services/api/internal/imports/testutil_test.go
  - services/api/internal/imports/testdata/agents-basic.json
  - services/api/internal/imports/testdata/agents-basic.csv
  - services/api/internal/imports/testdata/agents-with-skills.json
  - services/api/internal/imports/testdata/agents-with-skills.csv
  - services/api/internal/imports/testdata/skills-basic.csv
  - services/api/internal/imports/testdata/queues-basic.csv
  - services/api/internal/imports/testdata/channels-basic.csv
  - services/api/internal/imports/testdata/adapters-basic.csv
  - services/api/internal/imports/testdata/break_reasons-basic.csv
  - services/api/internal/imports/testdata/windows-excel-agents.csv
  - services/api/internal/imports/testdata/partial-success.csv
  - services/api/internal/imports/testdata/full-failure.csv
  - services/api/internal/imports/testdata/oversized-501-rows.csv
  - services/api/internal/imports/testdata/invalid-utf8.csv
  - services/api/internal/imports/testdata/invalid-code-format.csv
  - services/api/test/isolation/imports_test.go
  - services/api/test/isolation/imports_migration_idempotent_test.go
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
  - IMP-05
  - IMP-06
  - IMP-07
  - IMP-08
tags:
  - integration
  - isolation
  - testdata
  - migration

must_haves:
  truths:
    - "handlers_test.go covers entity × format matrix: 6 entities × {JSON, CSV} happy paths (≥ 12 cases)"
    - "windows-excel-agents.csv (UTF-8 BOM + CRLF + embedded quotes) imports successfully (IMP-02 / Pitfall 1)"
    - "Invalid UTF-8 CSV returns 400 csv_not_utf8 (D5-08)"
    - "Idempotent double-run (same payload, no Idempotency-Key) produces no duplicates (IMP-03 — code upsert)"
    - "Idempotency-Key replay returns idempotent_replay=true + empty succeeded[] (D5-13)"
    - "Partial-success CSV → 207 with structured failed[] (IMP-04 / IMP-05)"
    - "All-failure CSV → 422 (IMP-05)"
    - "Oversized CSV (501 rows) → 413 (IMP-07 / D5-22)"
    - "Wrong ?schema_version returns 400 with supported versions listed (IMP-08)"
    - "GET /imports/{id} round-trip: POST returns id; GET returns ImportJob with status + counters + errors (IMP-06)"
    - "Two-org isolation: orgA POST → orgB GET → 404 (FOUND-08 / IMP-06)"
    - "Two-org same-code import both succeed (Phase 04.1 invariant mirrored)"
    - "Migration 000003 is idempotent (re-running creates no schema drift)"
    - "Agent import seeds agent_states.status=Offline ON CONFLICT DO NOTHING (Hazard 7)"
    - "Re-import does NOT regress agent_states (Pitfall 3)"
  artifacts:
    - path: "services/api/internal/imports/handlers_test.go"
      provides: "entity × format matrix integration tests"
      contains: "TestBulkImport_JSON_Agents_HappyPath_200"
    - path: "services/api/internal/imports/idempotency_test.go"
      provides: "Idempotency-Key replay + double-run dedup tests"
      contains: "TestBulkImport_IdempotencyKey_Replay"
    - path: "services/api/internal/imports/jobs_test.go"
      provides: "import_jobs lifecycle integration test"
      contains: "TestBulkImport_GetImportJob_RoundTrip"
    - path: "services/api/internal/imports/testdata/windows-excel-agents.csv"
      provides: "UTF-8 BOM + CRLF + embedded-quote golden file (Pitfall 1 mandatory)"
      contains: ""
    - path: "services/api/test/isolation/imports_test.go"
      provides: "two-org isolation probes for import path"
      contains: "TestImport_CrossOrg"
    - path: "services/api/test/isolation/imports_migration_idempotent_test.go"
      provides: "migration 000003 idempotency smoke"
      contains: "TestMigration_ImportJobs_Idempotent"
  key_links:
    - from: "services/api/internal/imports/handlers_test.go"
      to: "services/api/internal/imports/handler_import.go"
      via: "end-to-end through httptest server + composite ApiHandlers + middleware chain"
      pattern: "BulkImportCatalog"
    - from: "services/api/internal/imports/testdata/windows-excel-agents.csv"
      to: "services/api/internal/imports/parser_csv.go (stripBOM)"
      via: "test reads file, posts as body; stripBOM consumes BOM"
      pattern: "0xEF 0xBB 0xBF"
    - from: "services/api/test/isolation/imports_test.go"
      to: "services/api/test/isolation/catalog_test.go (TestCatalog_CrossOrgSameCode_BothSucceed at line 375)"
      via: "mirror pattern for the import path"
      pattern: "freshOrg"
---

<objective>
Land Phase 5's full integration test surface: the entity × format matrix
(6 entities × {JSON, CSV} happy paths + per-entity error paths), the
testdata golden files (including the mandatory `windows-excel-agents.csv`
with UTF-8 BOM + CRLF + embedded-quote field — Pitfall 1 non-negotiable),
the idempotency + replay round-trip, the import_jobs lifecycle round-trip,
the cross-org isolation probes (mirror Phase 04.1's
`TestCatalog_CrossOrgSameCode_BothSucceed`), and the migration 000003
idempotency smoke.

Purpose: Wave 4 (Plan 05-06) shipped the handlers; Wave 5 / Plan 05-07
proves they work end-to-end with realistic data and prevents regressions
on the locked invariants. The phase-gate plan (Plan 05-08) depends on
this suite running green before declaring Phase 5 shippable.

Output: ~ 17 new test files / test data files; ≥ 20 new integration test
cases; no new production code.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-bulk-import-go/05-CONTEXT.md
@.planning/phases/05-bulk-import-go/05-RESEARCH.md
@.planning/phases/05-bulk-import-go/05-PATTERNS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-05-SUMMARY.md
@.planning/phases/05-bulk-import-go/05-04-PLAN.md
@.planning/phases/05-bulk-import-go/05-05-PLAN.md
@.planning/phases/05-bulk-import-go/05-06-PLAN.md
@services/api/test/isolation/main_test.go
@services/api/test/isolation/isolation_test.go
@services/api/test/isolation/catalog_test.go
@services/api/test/isolation/state_test.go
@services/api/test/isolation/migration_idempotent_test.go
@services/api/internal/state/agent_states_test.go
@services/api/internal/imports/testutil_test.go

<interfaces>
<!-- Existing isolation suite helpers Phase 5 reuses -->

services/api/test/isolation/main_test.go (9.1KB) — TestMain bringing up testcontainer Postgres + migrations + sharedPool; isolation tests share the pool across orgIDs (per-org DELETE for parallel safety).

services/api/test/isolation/isolation_test.go (10.3KB) — shared helpers used by all isolation_*.go files:
- `freshOrg(t)` — mints a fresh orgID + seeds the org_id-aware fixtures.
- `baseURL()` — returns the httptest server base URL.
- `requireContainer(t)` — skips if Docker unavailable.
- `postEntity(t, urlBase, entityPath, orgID, body)` — POST helper with X-Org-Id header.
- `getEntityStatus(t, urlBase, entityPath, orgID, id)` — GET helper.

services/api/test/isolation/catalog_test.go (17.4KB) — Phase 04.1 catalog isolation tests. `TestCatalog_CrossOrgSameCode_BothSucceed` at line 375 is the prototype for Phase 5's mirror.

services/api/test/isolation/state_test.go (9.1KB) — Phase 4 state isolation tests. Cross-org break_reason FK probe pattern.

services/api/test/isolation/migration_idempotent_test.go (4.5KB) — Phase 04.1 migration idempotency template — Phase 5 mirrors for 000003.

services/api/internal/state/agent_states_test.go (816 lines) — Phase 4 state package integration test shape — Phase 5's handlers_test.go follows the same `httptest.NewServer` + miniredis + testcontainers Postgres pattern.

services/api/internal/imports/testutil_test.go (from Plan 05-04 + Plan 05-05) — extended in this wave to add:
- `postImportJSON(t, th, entity, rows []any) (*http.Response, []byte)` helper.
- `postImportCSV(t, th, entity, csvBody []byte) (*http.Response, []byte)` helper.
- `getImportJob(t, th, jobID) (*http.Response, []byte)` helper.
- `importPath(orgID)` / `importJobPath(orgID, jobID)` URL builders.
- `cleanImportTables` already added in Plan 05-04 — reuse.

services/api/internal/imports/handler_import.go (Plan 05-06) — end-to-end handler tests exercise it via httptest server.

Phase 5 RESEARCH §Validation Architecture (test coverage matrix) — listed earlier in this context for the exhaustive integration test list.

testdata file conventions (Phase 5 chooses): `internal/imports/testdata/<entity>-<scenario>.{json,csv}`. The `windows-excel-agents.csv` MUST contain BOM (0xEF 0xBB 0xBF) + CRLF (\r\n) + at least one cell with an embedded double-quote (`""` escape) + at least one cell with an embedded comma (within quotes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend testutil_test.go with HTTP helpers + import the testdata files + build the entity × format matrix in handlers_test.go (≥ 12 happy-path tests + ≥ 6 error-path tests)</name>
  <files>
    services/api/internal/imports/handlers_test.go,
    services/api/internal/imports/testutil_test.go,
    services/api/internal/imports/testdata/agents-basic.json,
    services/api/internal/imports/testdata/agents-basic.csv,
    services/api/internal/imports/testdata/agents-with-skills.json,
    services/api/internal/imports/testdata/agents-with-skills.csv,
    services/api/internal/imports/testdata/skills-basic.csv,
    services/api/internal/imports/testdata/queues-basic.csv,
    services/api/internal/imports/testdata/channels-basic.csv,
    services/api/internal/imports/testdata/adapters-basic.csv,
    services/api/internal/imports/testdata/break_reasons-basic.csv,
    services/api/internal/imports/testdata/partial-success.csv,
    services/api/internal/imports/testdata/full-failure.csv,
    services/api/internal/imports/testdata/invalid-code-format.csv
  </files>
  <read_first>
    - services/api/internal/imports/testutil_test.go (current state — has cleanImportTables + newTestImports per Plan 05-04 Task 4)
    - services/api/internal/state/testutil_test.go (full template for HTTP helpers + composite ApiHandlers wiring + httptest.NewServer)
    - services/api/internal/state/agent_states_test.go (test patterns: setupTest helper, request helpers, assertion style)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (handlers_test.go NEW section + test coverage matrix)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Validation Architecture (test inventory)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-15..D5-20 + D5-25 (Import*Request shapes)
  </read_first>
  <action>
    1) Extend `services/api/internal/imports/testutil_test.go` to add:
       - Full composite ApiHandlers wiring + httptest.NewServer construction (mirror state/testutil_test.go lines 67-158). The TestImports struct gains an `HTTP *httptest.Server` field. `newTestImports(t)` builds an Importer, builds a catalog.Handlers stub (or the real one — read state/testutil_test.go to see how Phase 4 handled this), builds a state.Server (or stub), wires the three into an ApiHandlers composite literal, registers with `server.NewMux` (passing all required deps), wraps in httptest.NewServer.
       - HTTP helper functions:
         - `func importPath(orgID uuid.UUID) string { return "/v1/orgs/"+orgID.String()+"/catalog/import" }`
         - `func importJobPath(orgID, jobID uuid.UUID) string { return "/v1/orgs/"+orgID.String()+"/imports/"+jobID.String() }`
         - `func postImportJSON(t testing.TB, th *TestImports, entity api.ImportEntityType, rows []any) (*http.Response, []byte)` — marshals rows, POSTs with Content-Type: application/json + X-Org-Id, reads body, returns.
         - `func postImportJSONWithIdempotencyKey(t, th, entity, rows []any, key uuid.UUID)` — same plus Idempotency-Key header.
         - `func postImportCSV(t, th, entity, csvBody []byte) (*http.Response, []byte)` — POSTs with Content-Type: text/csv + ?schema_version=v0.1 + X-Org-Id.
         - `func getImportJob(t, th, jobID uuid.UUID) (*http.Response, []byte)` — GET helper.
         - `func loadTestData(t, filename string) []byte` — read from testdata/ dir; relative to test file location.

    2) Create the testdata files (UTF-8 LF unless noted otherwise; CSV header on line 1):
       - `agents-basic.json`: JSON array of 3 agent records with code, name, email, enabled. Codes follow regex (`emp_001`, `emp_002`, `emp_003`).
       - `agents-basic.csv`: 4 lines total. Header: `code,name,email,enabled`. Three data rows.
       - `agents-with-skills.json`: JSON array of 2 agents; each has `skills: [{skill_code: "skill_voice", proficiency: 7}, ...]`. Pre-condition: tests must seed the referenced skills before POSTing.
       - `agents-with-skills.csv`: 3 lines. Header: `code,name,email,skills`. Two data rows; skills column uses `skill_voice:7|skill_chat:5` syntax (D5-17).
       - `skills-basic.csv`: 4 lines. Header: `code,name,skill_type`. Three skills (skill_voice, skill_chat, skill_email).
       - `queues-basic.csv`: 3 lines. Header: `code,name,channel_types,priority,acw_sec`. Two queues with `voice|chat` multi-value channel_types.
       - `channels-basic.csv`: 3 lines. Header: `code,name,channel_type,default_queue_code`. Two channels referencing pre-seeded queues by code.
       - `adapters-basic.csv`: 3 lines. Header: `code,name,adapter_type,config`. Config is JSON-encoded `"{""endpoint"":""https://x""}"` (escaped quotes per RFC 4180).
       - `break_reasons-basic.csv`: 4 lines. Header: `code,name,routable,display_order`. Three break_reasons; two with the SAME name "Lunch" but different codes (proves Phase 04.1 IDENT-03 — name no longer unique).
       - `partial-success.csv`: 6 lines. Header: `code,name,email`. 4 rows: row 1 valid, row 2 invalid (bad email), row 3 valid, row 4 duplicate code of row 1.
       - `full-failure.csv`: 4 lines. Header: `code,name,email`. 3 rows ALL invalid (e.g. all bad code formats).
       - `invalid-code-format.csv`: 3 lines. Header: `code,name`. Two rows: `INVALID UPPER,Foo` and `valid_code,Bar`. Expected: row 1 fails with invalid_code_format; row 2 succeeds → 207.

    3) Create `services/api/internal/imports/handlers_test.go` with the entity × format matrix. Each test function uses `newTestImports(t)` for fresh fixtures + `defer cleanImportTables`.

       Happy-path tests (≥ 12):
       - `TestBulkImport_JSON_Agents_HappyPath_200` — POST agents-basic.json → 200 + Succeeded has 3 UUIDs + Failed empty + import_jobs row exists with status=completed.
       - `TestBulkImport_CSV_Agents_HappyPath_200` — POST agents-basic.csv → 200 + same shape.
       - `TestBulkImport_JSON_Agents_WithSkills_200` — pre-seed skills via direct SQL; POST agents-with-skills.json → 200 + each agent's agent_skills join row written.
       - `TestBulkImport_CSV_Agents_WithSkills_200` — same with CSV format.
       - `TestBulkImport_JSON_Skills_HappyPath_200` — POST skills as JSON.
       - `TestBulkImport_CSV_Skills_HappyPath_200` — POST skills-basic.csv.
       - `TestBulkImport_JSON_Queues_HappyPath_200` — POST queues with channel_types array.
       - `TestBulkImport_CSV_Queues_HappyPath_200` — POST queues-basic.csv with pipe-delimited channel_types.
       - `TestBulkImport_JSON_Channels_HappyPath_200` — pre-seed queues; POST channels JSON.
       - `TestBulkImport_CSV_Channels_HappyPath_200` — POST channels-basic.csv (FK code-lookup probe).
       - `TestBulkImport_JSON_Adapters_HappyPath_200` — POST adapters with config JSONB.
       - `TestBulkImport_CSV_Adapters_HappyPath_200` — POST adapters-basic.csv.
       - `TestBulkImport_JSON_BreakReasons_HappyPath_200` — POST break_reasons.
       - `TestBulkImport_CSV_BreakReasons_HappyPath_200` — POST break_reasons-basic.csv. CRITICAL: includes TWO rows with SAME name "Lunch" → both succeed (IDENT-03).

       Error-path tests (≥ 6):
       - `TestBulkImport_FailedRow_Structure_207` — POST partial-success.csv → 207 + Failed has structured `{Row, Field, Error: import_failed, Reason}` entries.
       - `TestBulkImport_HTTPStatus_AllFail_422` — POST full-failure.csv → 422.
       - `TestBulkImport_InvalidCodeFormat_PerRow` — POST invalid-code-format.csv → 207 (1 succeed, 1 fail with `Field: "code", Reason: "invalid_code_format"`).
       - `TestBulkImport_MissingRequiredColumn_400` — POST CSV missing `code` column → 400 missing_columns.
       - `TestBulkImport_UnknownColumn_400` — POST CSV with extra `salary` column → 400 unknown_columns.
       - `TestBulkImport_UnknownEntity_400` — POST with `?entity=invalid` → 400 unsupported_entity.
       - `TestBulkImport_Reimport_PreservesAgentStateMachine` — POST agents-basic.csv twice; assert agent_states row's status stays at its post-first-import value across the second import (Hazard 7 carry-forward).
       - `TestBulkImport_SeedsAgentStatesForNewAgents` — POST agents-basic.csv; assert every imported agent has a corresponding agent_states row with status='Offline' + state_version=1.
       - `TestBulkImport_AgentImport_SkillMerge_PreservesExisting` — pre-seed agent with 2 skill assignments (skill_voice + skill_chat); POST agents-with-skills.csv that only mentions skill_voice; assert BOTH assignments still present + skill_voice.proficiency updated to imported value (D5-18 + D5-19).
       - `TestBulkImport_AgentImport_UnknownSkillCode_PerRow` — POST agents-with-skills.json referencing a skill_code that doesn't exist; assert 207 with `Field: "skills[N].skill_code", Reason: "unknown_skill"` for the offending row.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f services/api/internal/imports/handlers_test.go && grep -c '^func TestBulkImport_' services/api/internal/imports/handlers_test.go | awk '{ if ($1 >= 16) exit 0; else exit 1 }' && ls services/api/internal/imports/testdata/*.csv | wc -l | awk '{ if ($1 >= 8) exit 0; else exit 1 }' && ls services/api/internal/imports/testdata/*.json | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c 'postImportJSON' services/api/internal/imports/testutil_test.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'postImportCSV' services/api/internal/imports/testutil_test.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && cd services/api && go vet ./internal/imports/... && go test -count=1 -short -run 'TestBulkImport_' ./internal/imports/... && echo HANDLERS_TEST_OK</automated>
  </verify>
  <done>handlers_test.go ships ≥ 16 integration test cases; testutil_test.go ships HTTP helpers; ≥ 8 testdata CSV files + ≥ 2 JSON files exist; all tests pass (or skip cleanly on Docker unavailable).</done>
</task>

<task type="auto">
  <name>Task 2: Author Pitfall-1 mandatory windows-excel-agents.csv + invalid-utf8.csv + oversized-501-rows.csv testdata + the paired integration tests (BOM + CRLF + UTF-8 fail + oversize)</name>
  <files>
    services/api/internal/imports/testdata/windows-excel-agents.csv,
    services/api/internal/imports/testdata/invalid-utf8.csv,
    services/api/internal/imports/testdata/oversized-501-rows.csv,
    services/api/internal/imports/handlers_test.go
  </files>
  <read_first>
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Pitfall 1 + §Pattern 3 + §A4 (BOM handling)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (testdata directory inventory — windows-excel-agents.csv NON-NEGOTIABLE)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-08 (UTF-8 only after BOM strip), IMP-02 (BOM + CRLF + embedded quotes)
    - services/api/internal/imports/parser_csv.go (stripBOM + newCSVReader from Plan 05-04 Task 2)
  </read_first>
  <action>
    1) Create `services/api/internal/imports/testdata/windows-excel-agents.csv`:
       - First 3 bytes: 0xEF 0xBB 0xBF (UTF-8 BOM).
       - Line endings: CRLF (\r\n) throughout.
       - Header: `code,name,email,enabled`.
       - 3 data rows. Row 2 has an embedded comma inside a quoted field: `emp_002,"Smith, John",john@example.com,true`. Row 3 has an embedded quote: `emp_003,"Mary ""The Boss"" Doe",mary@example.com,true`.
       - The file MUST be authored exactly to byte specification. Recommend authoring via a small Go script (one-off in this task) that writes `[]byte{0xEF, 0xBB, 0xBF}` followed by ASCII content with `\r\n` separators. Verify byte count + first 3 bytes via `xxd -l 8 services/api/internal/imports/testdata/windows-excel-agents.csv` after authoring.

    2) Create `services/api/internal/imports/testdata/invalid-utf8.csv`:
       - Header line valid UTF-8: `code,name\n`.
       - Data row 1 contains an invalid UTF-8 byte sequence: e.g., `emp_001,\xc3\x28` (an invalid 2-byte sequence). Author via byte writer.

    3) Create `services/api/internal/imports/testdata/oversized-501-rows.csv`:
       - Header: `code,name,email`.
       - 501 data rows of valid format (e.g., `emp_0001,Alice,a@x.com`, …, `emp_0501,Alice501,a501@x.com`).
       - Generate via a small Go helper at test setup time (avoids checking in a giant file). Alternative: check in a smaller file (say 11 rows) and have the test programmatically duplicate to 501 — simpler. Use the programmatic approach to keep the repo lean.

    4) Append to `services/api/internal/imports/handlers_test.go` (or create a separate `handlers_csv_test.go` file if handlers_test.go is getting unwieldy):
       - `TestBulkImport_WindowsExcel_BOM_CRLF_200` — POST windows-excel-agents.csv → 200; assert all 3 agents inserted; assert the embedded-comma and embedded-quote fields round-trip correctly (read back agent.name == "Smith, John" and "Mary \"The Boss\" Doe").
       - `TestBulkImport_InvalidUTF8_400` — POST invalid-utf8.csv → 400 + Reason: "csv_not_utf8".
       - `TestBulkImport_Oversize501Rows_413` — generate 501-row body via test helper; POST → 413 + Reason mentioning the row cap.
       - `TestBulkImport_Oversize50MBBody_413` — generate 51 MB body via test helper (use `strings.Repeat`); POST → 413 + the body-size cap message. (This validates Plan 05-02's BodyLimit middleware in the real chain.)
       - `TestBulkImport_SchemaVersionMismatch_400` — POST with `?schema_version=v0.2` (CSV body); assert 400 + Reason mentions supported versions.
       - `TestBulkImport_SchemaVersionMissing_400` — POST CSV WITHOUT `?schema_version`; assert 400.
       - `TestBulkImport_NestedQuotedNewline_RoundTrips` — author an inline byte buffer (no separate testdata file) with a quoted newline inside a field; POST → 200; assert the field round-trips.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f services/api/internal/imports/testdata/windows-excel-agents.csv && test -f services/api/internal/imports/testdata/invalid-utf8.csv && head -c 3 services/api/internal/imports/testdata/windows-excel-agents.csv | xxd -p | grep -qx 'efbbbf' && file services/api/internal/imports/testdata/windows-excel-agents.csv | grep -qE 'CRLF|with CRLF' && grep -c '^func TestBulkImport_WindowsExcel_' services/api/internal/imports/handlers_test.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_InvalidUTF8_' services/api/internal/imports/handlers_test.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_Oversize' services/api/internal/imports/handlers_test.go | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_SchemaVersion' services/api/internal/imports/handlers_test.go | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && cd services/api && go test -count=1 -short -run 'TestBulkImport_WindowsExcel_|TestBulkImport_InvalidUTF8_|TestBulkImport_Oversize|TestBulkImport_SchemaVersion|TestBulkImport_NestedQuotedNewline_' ./internal/imports/... && echo CSV_TEST_OK</automated>
  </verify>
  <done>windows-excel-agents.csv with BOM + CRLF + embedded quotes/commas verified by xxd + file commands; invalid-utf8.csv triggers 400 csv_not_utf8; oversize tests trigger 413; schema_version tests trigger 400; all CSV-spec tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Author idempotency_test.go + jobs_test.go integration tests for replay round-trip + import_jobs lifecycle</name>
  <files>
    services/api/internal/imports/idempotency_test.go,
    services/api/internal/imports/jobs_test.go
  </files>
  <read_first>
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (idempotency_test.go NEW + jobs_test.go NEW sections + test case lists)
    - .planning/phases/05-bulk-import-go/05-CONTEXT.md D5-10, D5-13 (job lifecycle + replay semantics)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Validation Architecture (test inventory)
    - services/api/internal/imports/idempotency.go (lookupIdempotentReplay + rehydrateBulkImportResult — Plan 05-04)
    - services/api/internal/imports/jobs.go (createJob + finaliseJob — Plan 05-04)
  </read_first>
  <action>
    1) Create `services/api/internal/imports/idempotency_test.go`:
       - `TestBulkImport_IdempotencyKey_MissProceeds_NewJobCreated` — POST agents-basic.json with Idempotency-Key K1; assert 200 + idempotent_replay nil/false + a new import_jobs row exists with idempotency_key=K1.
       - `TestBulkImport_IdempotencyKey_HitReturnsReplay_NoNewWork` — POST twice with same K1 + same body; assert second response has idempotent_replay=true + succeeded:[] (D5-13 KNOWN LIMITATION) + failed:[] (since first run succeeded); assert only ONE import_jobs row exists.
       - `TestBulkImport_IdempotencyKey_HitReturnsPriorFailures` — pre-seed an import_jobs row for K2 with status=completed + failed=[3 entries]; POST with K2; assert response Failed[] mirrors the persisted entries (D5-12 zero-transformation guarantee).
       - `TestBulkImport_IdempotencyKey_DifferentBodySameKey_StillReturnsPriorResult` — POST with K3 + body A; assert 200; POST with K3 + DIFFERENT body B; assert response matches FIRST result (replay; no re-execution).
       - `TestBulkImport_IdempotencyKey_DifferentOrgSameKey_BothProceed` — POST as orgA with K4; POST as orgB with K4; assert BOTH process (composite UNIQUE on (org_id, idempotency_key); cross-org isolation).
       - `TestBulkImport_NoIdempotencyKey_DoubleRunCreatesTwoJobs_NoDupesByCode` — POST agents-basic.json TWICE without any Idempotency-Key; assert TWO distinct import_jobs rows but ONE set of unique agents (Phase 04.1's (org_id, code) UNIQUE — IMP-03).

    2) Create `services/api/internal/imports/jobs_test.go`:
       - `TestBulkImport_GetImportJob_RoundTrip` — POST → response carries job id; GET .../imports/{id} → 200 + ImportJob shape with status=completed + counters + nil errors.
       - `TestBulkImport_GetImportJob_PartialFailure_ReturnsErrors` — POST partial-success.csv → 207; GET .../imports/{id} → 200 + status=completed + Failed[] populated.
       - `TestBulkImport_GetImportJob_NotFound_404` — random UUID; GET → 404 with Reason: "import_job_not_found".
       - `TestBulkImport_CreateJob_BeforeChunk1` — instrument the createJob call to assert it ran BEFORE any chunk DB writes (D5-10 — admin GET can see the job mid-import). The simplest assertion: in a deliberately failing test scenario (force a panic AFTER createJob + BEFORE finaliseJob via test hook), verify the import_jobs row exists with status=pending AFTER recovery.
       - `TestBulkImport_FinaliseJob_OnAllSucceed_UpdatesStatus` — POST happy path; assert AFTER response, import_jobs row has status=completed + total_rows matches + succeeded_rows=total + failed_rows=0.
       - `TestBulkImport_FinaliseJob_OnPartial_UpdatesCounters` — POST partial-success.csv; assert after response, import_jobs row has status=completed + succeeded_rows + failed_rows match.

    These tests run end-to-end via the httptest server from Task 1's testutil extension; they reuse cleanImportTables fixtures.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f services/api/internal/imports/idempotency_test.go && test -f services/api/internal/imports/jobs_test.go && grep -c '^func TestBulkImport_IdempotencyKey_' services/api/internal/imports/idempotency_test.go | awk '{ if ($1 >= 5) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_NoIdempotencyKey_' services/api/internal/imports/idempotency_test.go | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_GetImportJob_' services/api/internal/imports/jobs_test.go | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && grep -c '^func TestBulkImport_FinaliseJob_' services/api/internal/imports/jobs_test.go | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && cd services/api && go test -count=1 -short -run 'TestBulkImport_IdempotencyKey_|TestBulkImport_NoIdempotencyKey_|TestBulkImport_GetImportJob_|TestBulkImport_FinaliseJob_' ./internal/imports/... && echo IDEMPOTENCY_JOBS_TEST_OK</automated>
  </verify>
  <done>idempotency_test.go ships ≥ 5 IdempotencyKey cases + ≥ 1 NoIdempotencyKey double-run case; jobs_test.go ships ≥ 3 GetImportJob cases + ≥ 2 FinaliseJob cases; all pass.</done>
</task>

<task type="auto">
  <name>Task 4: Author cross-org isolation tests (services/api/test/isolation/imports_test.go) + migration 000003 idempotency test (services/api/test/isolation/imports_migration_idempotent_test.go)</name>
  <files>
    services/api/test/isolation/imports_test.go,
    services/api/test/isolation/imports_migration_idempotent_test.go
  </files>
  <read_first>
    - services/api/test/isolation/main_test.go (TestMain + shared pool + httptest server bringup)
    - services/api/test/isolation/isolation_test.go (helper inventory — freshOrg, baseURL, postEntity, etc.)
    - services/api/test/isolation/catalog_test.go (`TestCatalog_CrossOrgSameCode_BothSucceed` at line 375 — Phase 5 mirrors this exact pattern for import path)
    - services/api/test/isolation/state_test.go (Phase 4 cross-org probe pattern)
    - services/api/test/isolation/migration_idempotent_test.go (Phase 04.1 migration idempotency template — Phase 5 mirrors)
    - .planning/phases/05-bulk-import-go/05-PATTERNS.md (services/api/test/isolation/imports_test.go NEW + imports_migration_idempotent_test.go NEW sections)
    - .planning/phases/05-bulk-import-go/05-RESEARCH.md §Validation Architecture FOUND-08 + Pitfall 4.4 (code collisions across orgs)
  </read_first>
  <action>
    1) Create `services/api/test/isolation/imports_test.go` mirroring `catalog_test.go` structure (header doc comment + freshOrg-based fixture setup + table-driven scenarios where applicable):
       - `TestImport_CrossOrgGetJob_404` — orgA POSTs agents-basic.json → captures jobID; orgB GETs `/v1/orgs/{orgB}/imports/{jobID}`; assert 404 (FOUND-08).
       - `TestImport_CrossOrgSameCode_BothSucceed` — orgA imports an agent with code `emp_001`; orgB imports a DIFFERENT agent with the SAME code `emp_001`; assert BOTH return 200 + their own agent rows in their own org. Mirrors the Phase 04.1 schema invariant `UNIQUE (org_id, code)` for the import path. Tests Pitfall 4.4 (code collisions during bulk import).
       - `TestImport_CrossOrg_Channel_DefaultQueueCode_invalid_reference` — orgA seeds a queue with code `q_main`; orgB attempts to import a channel referencing `default_queue_code=q_main`; assert per-row failure with `Reason: "invalid_reference"` (FK probe fails because orgB has no `q_main` queue). Mirrors Phase 4's cross-org break_reason probe pattern.
       - `TestImport_CrossOrgSameIdempotencyKey_BothProceed` — orgA POST with Idempotency-Key K; orgB POST with same K; assert BOTH process to their own import_jobs rows (composite UNIQUE on (org_id, idempotency_key) — Phase 04.1 / D5-13).

    2) Create `services/api/test/isolation/imports_migration_idempotent_test.go` mirroring `migration_idempotent_test.go`:
       - `TestMigration_ImportJobs_Idempotent` — bring up a fresh testcontainer; apply all migrations (000001 + 000002 + 000003); assert import_jobs table exists; re-apply migration 000003 (using golang-migrate force or a SQL-replay helper); assert zero schema drift; verify the partial indexes still exist + the CHECK constraints still match.
       - Approach: simplest implementation re-runs the migration file via a SQL-level `psql -f` equivalent rather than the migration runner (the runner short-circuits on schema_migrations version match). Use `pgx.Exec` to apply the raw SQL string from the file; assert PostgreSQL errors with `relation "import_jobs" already exists` are caught and counted as "idempotent" (or that the SQL uses `IF NOT EXISTS` consistently — if the migration uses bare `CREATE TABLE`, idempotency must be tested differently; consult the migration_idempotent_test.go pattern).

       Alternative: use `task db:reset` semantics — drop the schema, apply 000001+000002+000003, snapshot pg_catalog; then drop import_jobs, re-apply 000003 alone, snapshot pg_catalog; assert snapshots are byte-equivalent. This is heavier but more reliable.

    The exact pattern depends on what Phase 04.1's migration_idempotent_test.go did — read that file first and mirror its approach exactly. Phase 04.1's version is the canonical reference.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && test -f services/api/test/isolation/imports_test.go && test -f services/api/test/isolation/imports_migration_idempotent_test.go && grep -c '^func TestImport_CrossOrg' services/api/test/isolation/imports_test.go | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && grep -c '^func TestImport_CrossOrgSameCode_BothSucceed' services/api/test/isolation/imports_test.go | grep -qx 1 && grep -c '^func TestMigration_ImportJobs_Idempotent' services/api/test/isolation/imports_migration_idempotent_test.go | grep -qx 1 && cd services/api && go vet ./test/isolation/... && go test -count=1 -short -run 'TestImport_CrossOrg|TestMigration_ImportJobs_Idempotent' ./test/isolation/... && echo ISOLATION_TESTS_OK</automated>
  </verify>
  <done>imports_test.go ships ≥ 3 cross-org probes including the same-code-both-succeed mirror of Phase 04.1; imports_migration_idempotent_test.go ships the migration replay test; all isolation tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testdata file → CSV parser | Untrusted bytes (BOM, CRLF, invalid UTF-8) cross here; parser must reject malformed and accept Pitfall-1 shapes. |
| orgA POST → orgB GET | Cross-org probe boundary — handler must enforce composite WHERE for FOUND-08. |
| testcontainers Postgres → CI runner | Docker is required; tests skip cleanly when unavailable (Phase 1 convention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | testdata files inadvertently checked in with editor-applied UTF-8 BOM | mitigate | The windows-excel-agents.csv MUST have BOM by design (Pitfall 1); other CSV files MUST NOT. Pre-commit byte check: head -c 3 of all CSV files (except windows-excel-*); assert no 0xEF 0xBB 0xBF. Implemented as a verify-step in this plan. |
| T-05-07-02 | Information Disclosure | Cross-org tests using shared sharedPool without proper cleanup | mitigate | Each test uses `freshOrg(t)` to mint a fresh orgID + cleanImportTables(orgID) in defer. Per-org DELETE is parallel-safe (Phase 4 invariant). |
| T-05-07-03 | Tampering | Migration idempotency test masking a real DDL bug | mitigate | The test runs the EXACT SQL from migration 000003 against a fresh + a pre-applied DB; both must produce identical pg_catalog state. Any "already exists" mid-test is asserted; bare CREATE TABLE without `IF NOT EXISTS` would fail loudly. |
| T-05-07-04 | Denial of Service | 51 MB body test exhausting CI memory | mitigate | Use `strings.Repeat(",,,\n", 13631488)` style large string assembly INSIDE the test body (free'd after); avoids committing a 51 MB testdata file. Body is constructed once per test and streamed via httptest. |
| T-05-07-05 | Tampering | Tests bypassing the body-limit middleware by going around server.NewMux | mitigate | All integration tests go through the httptest server constructed by newTestImports, which uses server.NewMux (Plan 05-06 Task 4 — BodyLimit injected). Direct Importer calls in unit tests bypass the middleware on purpose; only integration tests prove the chain. |
| T-05-07-06 | Information Disclosure | testdata containing PII | accept | All testdata uses synthetic codes/names/emails (`emp_001`, `alice@example.com`). No real customer data. |
| T-05-07-SC | Tampering | Test setup downloading new test data files | mitigate | All testdata files are checked into the repo; no test downloads at runtime. The 501-row CSV is generated programmatically inside the test, not fetched. |
</threat_model>

<verification>
- `cd services/api && go test -count=1 -short ./internal/imports/... ./test/isolation/...` runs all Phase 5 tests green.
- `find services/api/internal/imports/testdata -name '*.csv' | xargs ls` shows ≥ 8 CSV files including windows-excel-agents.csv.
- BOM byte check: `head -c 3 services/api/internal/imports/testdata/windows-excel-agents.csv | xxd -p` returns `efbbbf`.
- All other CSV testdata files lack BOM (test enforces).
- ≥ 16 happy-path tests in handlers_test.go.
- ≥ 6 error-path tests including BOM round-trip + invalid UTF-8 + oversize.
- ≥ 5 idempotency tests + ≥ 5 jobs lifecycle tests.
- ≥ 3 cross-org isolation tests + ≥ 1 migration idempotency test.
- Total new test cases: ≥ 35.
</verification>

<success_criteria>
Wave 5 / Plan 05-07 success when:
- [x] handlers_test.go ships entity × format matrix coverage (≥ 12 happy + ≥ 4 error)
- [x] windows-excel-agents.csv exists with verified UTF-8 BOM + CRLF; the BOM-round-trip test passes
- [x] Invalid UTF-8, oversize, schema version, missing/unknown columns, embedded quote/newline cases all pass
- [x] Idempotency replay round-trip works (≥ 5 cases including hit/miss/cross-org)
- [x] import_jobs lifecycle round-trip works (≥ 5 cases)
- [x] Cross-org isolation: same-code-both-succeed, get-cross-org-404, channel-FK-invalid-reference, idempotency-key-cross-org (4 cases)
- [x] Migration 000003 idempotency test passes
- [x] No new production code; all changes are tests + testdata
</success_criteria>

<output>
Create `.planning/phases/05-bulk-import-go/05-07-SUMMARY.md` capturing:
- Total test case count broken down by area (handlers, csv-edge, idempotency, jobs, isolation, migration)
- testdata file manifest with byte sizes + BOM-presence verification
- Phase 04.1 invariants explicitly mirrored (TestImport_CrossOrgSameCode_BothSucceed)
- Hazard 7 + Pitfall 3 regression test confirmations
</output>
</content>
</invoke>