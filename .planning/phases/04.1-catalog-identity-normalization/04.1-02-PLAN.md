---
phase: 04.1-catalog-identity-normalization
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - openapi/openapi.yaml
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04, IDENT-06]
must_haves:
  truths:
    - "All 6 primary entity schemas (Agent, Skill, Queue, Channel, Adapter, BreakReason) declare code as a required property with pattern '^[a-z][a-z0-9_]{0,63}$' and a description referencing D04_1-01"
    - "All 6 entity schemas demote external_id to nullable + optional (type: string, nullable: true; removed from required list)"
    - "AgentListItem is the only ListItem schema present; it gains code + demoted external_id (the other 5 entities reuse the base entity schema for list responses — per RESEARCH Pitfall 6)"
    - "All 6 Create*Request schemas require code and accept optional external_id"
    - "All 6 Update*Request schemas declare code as optional (NOT in required) with the same regex pattern and a description explaining the immutability rule (D04_1-15)"
    - "ErrorCode enum gains exactly 3 new values: duplicate_code, duplicate_external_id, immutable_field (positioned alphabetically or appended)"
    - "CreateAdapter gains a NEW 409 response block (currently absent — RESEARCH Pitfall 7)"
    - "All 6 Update*Request endpoints add a 422 response block referencing immutable_field"
    - "info.description block gains an Identity Model paragraph linking the design review docs (D04_1-17)"
  artifacts:
    - path: "openapi/openapi.yaml"
      provides: "Phase 04.1 contract with code/external_id schema deltas + new error codes + new 409/422 response shapes"
      contains: "duplicate_code"
  key_links:
    - from: "openapi/openapi.yaml ErrorCode enum"
      to: "services/api/internal/api/types.gen.go (regenerated in Plan 03)"
      via: "oapi-codegen emits ErrorCodeDuplicateCode/ErrorCodeDuplicateExternalID/ErrorCodeImmutableField as typed constants"
      pattern: "api\\.ErrorCodeDuplicate(Code|ExternalId)|api\\.ErrorCodeImmutableField"
    - from: "openapi/openapi.yaml Create*Request schemas"
      to: "Plan 04 handler bodies"
      via: "code field in required list drives generated struct Code string (NOT *string for create)"
      pattern: "required:[^\\]]*code"
---

<objective>
Amend `openapi/openapi.yaml` in place to introduce the universal `code` field across all 6 primary catalog entities, demote `external_id` to optional/nullable, add 3 new `ErrorCode` enum values (`duplicate_code`, `duplicate_external_id`, `immutable_field`), add a 409 response shape to `CreateAdapter` (currently absent — RESEARCH Pitfall 7), add a 422 response to all 6 `Update*` endpoints for the `immutable_field` case, and update `info.description` with an Identity Model paragraph linking the design review docs.

Purpose: The spec is the contract source of truth (CONTRACT-01). Every codegen target (Go server stubs via `task gen`, TS client via `pnpm gen:api`) reads from this file. Without these edits, `api.ErrorCodeDuplicateCode` does not exist as a typed constant, `CreateAgentRequest.Code` does not appear as a required Go struct field, and the CI codegen-drift gate fails on every subsequent plan.

Output: A single amended `openapi/openapi.yaml` file with ~41 deltas (per RESEARCH §Pitfall 6 corrected count). Spec is parallel-safe with Plan 01 (migration) because they touch different files; Plan 03 depends on both being committed before running `task gen`.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md
@openapi/openapi.yaml

<interfaces>
Existing OpenAPI 3.0 contract structure (openapi/openapi.yaml — 93KB, ~3000 lines):

- `info.description` (lines 6-44): existing developer-facing prose. Phase 04.1 appends Identity Model section per D04_1-17.
- `components.schemas.ErrorCode` (lines ~98-116): enum of 12 values. Phase 04.1 appends 3 new values.
- Entity schemas (each ~60-80 lines):
  - `Agent` (~lines 266-334): currently `required: [id, org_id, external_id, name, email, enabled, version, created_at, updated_at]`. external_id is `type: string` (no nullable). skills array at end.
  - `Skill`, `Queue`, `Channel`, `Adapter`, `BreakReason`: same shape but different per-entity properties (no skills array).
- `AgentListItem` (~line 336): single ListItem schema — same as Agent without `skills[]`. NO other ListItem schemas exist (RESEARCH Pitfall 6 — confirmed by grep).
- `Create*Request` schemas (each ~30 lines): `required: [external_id, name, ...]`; Phase 04.1 swaps `code` in, demotes `external_id` to nullable optional.
- `Update*Request` schemas (each ~30 lines): `required: [version]`; properties include external_id (currently nullable false) + entity-specific fields. Phase 04.1 adds optional `code` with description explaining immutability.
- Path operations (~6 entity Create paths + 6 Update paths + 6 Get/List paths):
  - `CreateAgent` 409 currently uses `oneOf: [ErrorResponse, VersionConflictErrorResponse]` (D-37 union) — keep oneOf for agents
  - `CreateSkill409`, `CreateQueue409`, `CreateChannel409`, `CreateBreakReason409`: flat `$ref: ErrorResponse`
  - `CreateAdapter`: NO 409 response today (RESEARCH Pitfall 7) — must ADD it as flat `$ref: ErrorResponse`
  - `UpdateAgent`, `UpdateChannel`: have 422 (for invalid_reference); the same 422 shape applies to immutable_field — update description
  - `UpdateSkill`, `UpdateQueue`, `UpdateAdapter`, `UpdateBreakReason`: no 422 today — must ADD

Naming conventions: ErrorCode enum values use snake_case. Schema names use PascalCase. Path operationIds use camelCase.

Codegen targets (regenerated in Plan 03 by `task gen`):
- `services/api/internal/api/server.gen.go` (oapi-codegen — chi routes + StrictServerInterface)
- `services/api/internal/api/types.gen.go` (oapi-codegen — typed request/response structs + ErrorCode constants)
- `services/api/internal/api/spec.gen.go` (oapi-codegen — embeds the yaml bytes for runtime /openapi.yaml serving)
- `web/packages/ui/src/api/generated.ts` (openapi-typescript — TS client surface)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend ErrorCode enum with 3 new values + add Identity Model section to info.description</name>
  <files>openapi/openapi.yaml</files>
  <read_first>
    - openapi/openapi.yaml (read `info.description` block at top of file, lines ~6-44, AND `components.schemas.ErrorCode` enum block at lines ~98-116 — these are the 2 sections this task touches)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md (D04_1-16 — three new ErrorCode values; D04_1-17 — info.description link)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md §"openapi/openapi.yaml" lines 149-178 (ErrorCode enum extension pattern) and lines 348-359 (info.description Identity Model paragraph)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md §"Pattern 3" lines 376-438 (constraint-name introspect note + 3 new ErrorCode names)
  </read_first>
  <action>
    Edit `openapi/openapi.yaml` in place. This task is intentionally LIMITED to two surgical edits:

    **Edit 1 — `info.description` block (around lines 6-44):**

    The existing `info.description` is a YAML `|`-style multi-line string. Locate the closing line of the existing description (the last indented line before the next top-level key like `openapi:` or `servers:` or another `info:` field). APPEND immediately before the description block closes, preserving the existing indentation level (typically 4 spaces inside the `description:` block):

    ```
        ## Identity Model

        Catalog entities use `code` (lowercase, `^[a-z][a-z0-9_]{0,63}$`) as the
        user-facing canonical identifier (D04_1-01). `code` is required on every
        primary entity (agents, skills, queues, channels, adapters,
        break_reasons), composite-unique within an org, and immutable
        post-create in v0.1 (D04_1-02 — rename support deferred to v0.2 via a
        dedicated rename endpoint). `external_id` is demoted to an OPTIONAL
        integration-mapping field with partial unique `(org_id, external_id)
        WHERE external_id IS NOT NULL` (D04_1-06). Cross-AI peer-review design
        rationale: `.planning/phases/03-catalog-crud-go/03-CATALOG-IDENTITY-REVIEW.md`
        and the response artifact `03-CATALOG-IDENTITY-REVIEW-RESPONSE.md`.
    ```

    Use exactly the indentation level of existing description content (read the file first to confirm; YAML breakage on indentation mismatch is the most common failure mode).

    **Edit 2 — `components.schemas.ErrorCode` enum:**

    Locate `components.schemas.ErrorCode.enum` (around lines 98-116 — currently 12 values). APPEND 3 new values, alphabetically positioned for diff readability:

    - `duplicate_code` — insert AFTER `cross_org` and BEFORE `import_failed` (alphabetical)
    - `duplicate_external_id` — insert AFTER `duplicate_code` (so the two duplicate_* values cluster)
    - `immutable_field` — insert AFTER `import_failed` and BEFORE `internal` (alphabetical)

    Each new enum value MUST appear on its own `      - <value>` line matching the existing 6-space indent (verify by reading the existing enum block).

    Update the schema's `description` field (if one exists above the enum) to mention "Phase 04.1 added `duplicate_code`, `duplicate_external_id`, and `immutable_field`." OR add a new `description:` if absent.

    Do NOT remove or rename any existing enum values. Existing `version_conflict` REMAINS (still used by CAT-08 optimistic concurrency on PATCH).

    Do NOT touch any other schema, response, or path operation in this task. Those are Task 2 + Task 3.
  </action>
  <verify>
    <automated>
      cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54
      grep -c '      - duplicate_code$' openapi/openapi.yaml | xargs -I{} test {} -eq 1 \
        && grep -c '      - duplicate_external_id$' openapi/openapi.yaml | xargs -I{} test {} -eq 1 \
        && grep -c '      - immutable_field$' openapi/openapi.yaml | xargs -I{} test {} -eq 1 \
        && grep -c '      - version_conflict$' openapi/openapi.yaml | xargs -I{} test {} -eq 1 \
        && grep -c '03-CATALOG-IDENTITY-REVIEW' openapi/openapi.yaml | xargs -I{} test {} -ge 1 \
        && grep -c 'Identity Model' openapi/openapi.yaml | xargs -I{} test {} -ge 1 \
        && (cd services/api && go run github.com/redocly/cli@1.25 lint ../../openapi/openapi.yaml 2>&1 || npx -y @redocly/cli@1.25.0 lint openapi/openapi.yaml 2>&1) | grep -E '(0 problems|✓|errors)' | head -1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '      - duplicate_code$' openapi/openapi.yaml` returns exactly 1 (the new enum value is present with the correct 6-space indent).
    - `grep -c '      - duplicate_external_id$' openapi/openapi.yaml` returns exactly 1.
    - `grep -c '      - immutable_field$' openapi/openapi.yaml` returns exactly 1.
    - `grep -c '      - version_conflict$' openapi/openapi.yaml` returns exactly 1 (existing value preserved).
    - `grep -c '03-CATALOG-IDENTITY-REVIEW' openapi/openapi.yaml` returns ≥ 1 (Identity Model link present).
    - `grep -c 'Identity Model' openapi/openapi.yaml` returns ≥ 1 (heading appears in info.description).
    - Redocly lint (or equivalent OAS3 validator) reports no NEW errors compared to baseline; pre-existing warnings tolerated. The CI codegen-drift job in Plan 06 enforces the full clean lint pass.
  </acceptance_criteria>
  <done>
    `openapi/openapi.yaml` contains the 3 new ErrorCode enum values and the Identity Model paragraph in `info.description` with a link to `.planning/phases/03-catalog-crud-go/03-CATALOG-IDENTITY-REVIEW.md` and its RESPONSE artifact. All 7 grep gates pass. Spec is still parseable as OAS 3.0 (verified by Redocly lint reporting no NEW errors). Task 2 (entity schemas) and Task 3 (path responses) can now proceed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add code + demote external_id across 6 entity schemas + 1 ListItem + 6 Create*Request + 6 Update*Request</name>
  <files>openapi/openapi.yaml</files>
  <read_first>
    - openapi/openapi.yaml (read each of the 19 schema blocks: Agent, Skill, Queue, Channel, Adapter, BreakReason, AgentListItem, CreateAgentRequest, CreateSkillRequest, CreateQueueRequest, CreateChannelRequest, CreateAdapterRequest, CreateBreakReasonRequest, UpdateAgentRequest, UpdateSkillRequest, UpdateQueueRequest, UpdateChannelRequest, UpdateAdapterRequest, UpdateBreakReasonRequest — use grep to locate each, then targeted Read with offset/limit)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md (D04_1-13, D04_1-14, D04_1-15 — entity / Create / Update schema rules)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md §"openapi/openapi.yaml" lines 179-296 (entity + Create + Update template with full YAML examples)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md §"Pitfall 4: Update*Request.Code distinguishing nil vs empty vs equal-to-stored" (lines 558-583) and §"Pitfall 6: AgentListItem is the ONLY ListItem schema" (lines 594-610)
  </read_first>
  <action>
    Edit `openapi/openapi.yaml` in place.

    **For each of the 6 entity schemas (`Agent`, `Skill`, `Queue`, `Channel`, `Adapter`, `BreakReason`):**

    a. Locate the schema's `required:` array. ADD `code` (insert before `name` for diff readability). For agents/skills/queues/channels: REMOVE `external_id` from the required list (these 4 had it required pre-04.1). For adapters and break_reasons: external_id was NOT in required pre-04.1; just leave it absent.

    b. Locate the schema's `properties:` block. Position is critical (D04_1-13 readability — code AFTER org_id BEFORE external_id):

       - INSERT a new property block after the existing `org_id:` property:
         ```yaml
             code:
               type: string
               pattern: '^[a-z][a-z0-9_]{0,63}$'
               description: |
                 User-facing canonical identifier (D04_1-01). Required, immutable
                 after create. Composite UNIQUE (org_id, code). Used as the upsert
                 key for bulk import (Phase 5) and cross-reference target for
                 nested relationships and future DSL references.
               example: emp_0042
         ```
         (Use the same per-entity example: agents=`emp_0042`, skills=`skill_voice_tier1`, queues=`queue_billing`, channels=`channel_voice_primary`, adapters=`adapter_freeswitch_dc1`, break_reasons=`break_lunch`.)

       - For agents/skills/queues/channels (per D04_1-08 description text): locate the existing `external_id:` property block (currently `type: string`, sometimes with `description` and `example`) and CHANGE to:
         ```yaml
             external_id:
               type: string
               nullable: true
               description: |
                 Optional caller-assigned identifier from an external system
                 (HR, CRM, etc.). Used for sync mapping only — NOT the upsert
                 key for bulk import (use `code` for that). Partial unique
                 within an org when present. v0.1 supports at most one
                 external source per entity per org; multi-source
                 disambiguation deferred to v0.2 via `external_source TEXT`.
               example: HR-EMP-0042
         ```
         (Per-entity examples: agents=`HR-EMP-0042`, skills=`HR-SKILL-VOICE`, queues=`CRM-Q-BILLING`, channels=`CRM-CH-VOICE`. Use existing example if present, otherwise these.)

       - For adapters: ADD `external_id` as a NEW property block (it did not exist pre-04.1), positioned between `code` and `name`, with the same nullable+description structure. Example: `MDM-ADAPTER-FS-DC1`.

       - For break_reasons: ADD `external_id` as a NEW property block, positioned between `code` and `name`. Example: `HR-BREAK-LUNCH`.

    **For `AgentListItem` schema (the ONLY *ListItem schema — RESEARCH Pitfall 6):**

    Same treatment as the `Agent` entity schema: add `code` to `required:` and `properties:`, demote `external_id`. Use the same example values as the `Agent` schema.

    Do NOT search for or modify `SkillListItem`, `QueueListItem`, `ChannelListItem`, `AdapterListItem`, `BreakReasonListItem` — they do NOT exist as separate schemas (those entities reuse the base schema for list responses).

    **For each of the 6 `Create*Request` schemas (`CreateAgentRequest`, `CreateSkillRequest`, ..., `CreateBreakReasonRequest`):**

    a. Locate the schema's `required:` array. REMOVE `external_id` if present (was previously required for 4 entities). ADD `code` (insert BEFORE `name` in the list for diff readability).

    b. Locate the schema's `properties:` block. INSERT a new property block at the TOP of properties (just below `properties:`):
       ```yaml
           code:
             type: string
             pattern: '^[a-z][a-z0-9_]{0,63}$'
             description: |
               User-facing canonical identifier. Required on create, immutable
               after (passing a different code in PATCH returns HTTP 422 with
               ErrorCode=immutable_field).
             example: emp_0042
       ```

    c. If the schema has an existing `external_id:` property block, CHANGE it to add `nullable: true` and update the description text to "Optional caller-assigned identifier from an external system. See entity schema." If the schema has NO existing external_id property (adapters, break_reasons), ADD one between `code` and `name` with the same nullable + description structure.

    **For each of the 6 `Update*Request` schemas (`UpdateAgentRequest`, `UpdateSkillRequest`, ..., `UpdateBreakReasonRequest`):**

    a. Do NOT add `code` to the `required:` list (D04_1-15 + RESEARCH Open Question #1 recommendation — code is optional on PATCH; handler enforces immutability).

    b. Locate the schema's `properties:` block. INSERT a new property block at the TOP of properties:
       ```yaml
           code:
             type: string
             pattern: '^[a-z][a-z0-9_]{0,63}$'
             description: |
               Must equal the stored value (immutable post-create in v0.1, D04_1-02).
               Passing a different value returns HTTP 422 with
               ErrorCode=immutable_field. The field is accepted in the PATCH
               body to preserve symmetry with the Create*Request shape; absent
               or matching values are no-ops. Rename support deferred to v0.2.
             example: emp_0042
       ```

       Critically: do NOT add `nullable: true`. The Update*Request property must be optional (NOT in required) and non-nullable so oapi-codegen v2 emits `Code *string` (nil = absent in PATCH body). This is the exact semantic the handler relies on per RESEARCH Pitfall 4.

    c. For `external_id` in `Update*Request`: if absent, ADD as a new property between `code` and the next field; if present, ADD `nullable: true` (handler treats nil as "no change", JSON null as "clear binding"). The 4 entities that had external_id required pre-04.1 already have it in Update*Request — just add the nullable flag and update the description.

    **No other edits in this task.** Path response shapes (409/422) are Task 3.

    **Hazard alert:** YAML indentation is the most common failure mode. Every new property block above is indented 6 spaces from the `properties:` line (4 + 2). The `required:` array entries are indented 6 spaces too (`      - code`). Verify by reading a small section after editing each schema (use grep to count and Read with offset/limit to inspect).
  </action>
  <verify>
    <automated>
      cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54
      grep -c "pattern: '\\^\\[a-z\\]\\[a-z0-9_\\]{0,63}\\$'" openapi/openapi.yaml | xargs -I{} test {} -ge 19 \
        && grep -cE '^    AgentListItem:' openapi/openapi.yaml | xargs -I{} test {} -eq 1 \
        && grep -cE '^    SkillListItem:' openapi/openapi.yaml | xargs -I{} test {} -eq 0 \
        && grep -cE '^    (Agent|Skill|Queue|Channel|Adapter|BreakReason):' openapi/openapi.yaml | xargs -I{} test {} -ge 6 \
        && grep -cE '^    Create(Agent|Skill|Queue|Channel|Adapter|BreakReason)Request:' openapi/openapi.yaml | xargs -I{} test {} -eq 6 \
        && grep -cE '^    Update(Agent|Skill|Queue|Channel|Adapter|BreakReason)Request:' openapi/openapi.yaml | xargs -I{} test {} -eq 6 \
        && grep -cE '^      nullable: true$' openapi/openapi.yaml | xargs -I{} test {} -ge 6 \
        && grep -c 'D04_1-01' openapi/openapi.yaml | xargs -I{} test {} -ge 6 \
        && grep -c 'D04_1-02' openapi/openapi.yaml | xargs -I{} test {} -ge 6 \
        && grep -cE '^          example: emp_0042' openapi/openapi.yaml | xargs -I{} test {} -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - The pattern `^[a-z][a-z0-9_]{0,63}$` appears at least 19 times in the file (6 entity schemas + 1 AgentListItem + 6 Create*Request + 6 Update*Request = 19 schemas declaring `code` with regex).
    - `AgentListItem` schema exists exactly once at the components.schemas level (6-space indent at line start).
    - NO `SkillListItem`, `QueueListItem`, `ChannelListItem`, `AdapterListItem`, `BreakReasonListItem` schemas exist (confirms RESEARCH Pitfall 6 was honored — no false ListItems added).
    - All 6 entity schemas (`Agent`, `Skill`, `Queue`, `Channel`, `Adapter`, `BreakReason`) are present at components.schemas level.
    - All 6 `Create*Request` and all 6 `Update*Request` schemas present.
    - `nullable: true` appears at least 6 times (on the 4 demoted external_ids in entity schemas, plus 2 new ones for adapters/break_reasons, plus all 6 Create + 6 Update — total grows; minimum 6 = at least the entity-schema demotions).
    - `D04_1-01` (code identity decision) is referenced in at least 6 places (description blocks in entity schemas).
    - `D04_1-02` (immutability decision) is referenced in at least 6 places (Update*Request description blocks).
    - Agent `code` example `emp_0042` appears at least once (sanity check that example values were inserted, not just regex patterns).
    - Redocly lint reports no NEW errors. The CI codegen-drift gate in Plan 06 enforces full clean lint pass.
  </acceptance_criteria>
  <done>
    All 19 schemas affected by Phase 04.1 (6 entities + 1 ListItem + 6 Create + 6 Update) declare `code` with the regex pattern. external_id is demoted to nullable optional on all 4 entities that had it pre-04.1; adapters and break_reasons gain a NEW nullable external_id property. AgentListItem is the only ListItem (confirms RESEARCH Pitfall 6). All 10 grep gates pass. Task 3 (path responses) is unblocked.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 409 to CreateAdapter, add 422 to all 6 Update endpoints, refresh 409 descriptions on existing entity Create paths</name>
  <files>openapi/openapi.yaml</files>
  <read_first>
    - openapi/openapi.yaml (read each of the 12 path operations: POST /v1/orgs/{org_id}/{agents,skills,queues,channels,adapters,break_reasons}, PATCH /v1/orgs/{org_id}/{agents,skills,queues,channels,adapters,break_reasons}/{id} — use grep to locate `CreateAgent` / `UpdateAgent` operationIds and read the response blocks)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md (D04_1-16 — three new wire shapes: 409 duplicate_code, 409 duplicate_external_id, 422 immutable_field)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md §"openapi/openapi.yaml" lines 298-371 (Response shape extensions — full YAML examples for CreateAdapter 409 and Update* 422)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md §"Pitfall 7: CreateAdapter has NO 409 response currently" (lines 612-622) and §"Pitfall 10: oneOf 409 wire shape for CreateAgent vs FLAT for others" (lines 642-654)
  </read_first>
  <action>
    Edit `openapi/openapi.yaml` in place.

    **Edit A: Refresh existing 409 response descriptions on 5 entity Create paths (CreateAgent, CreateSkill, CreateQueue, CreateChannel, CreateBreakReason).**

    For each, locate the existing `"409":` response block under the Create operation's `responses:` section. Update the `description:` text to mention BOTH duplicate flavors (the schema reference stays the same — flat `$ref: ErrorResponse` for skill/queue/channel/break_reason; `oneOf` union for agent per Pitfall 10):

    ```yaml
            "409":
              description: |
                Duplicate `code` within the org (ErrorCode=duplicate_code), or
                duplicate `external_id` within the org when set
                (ErrorCode=duplicate_external_id). For agents only, an optimistic
                concurrency conflict on PATCH may also surface here (CAT-08,
                ErrorCode=version_conflict). Clients should branch on
                ErrorCode, not parse the description text.
              # (schema unchanged — keep existing $ref or oneOf shape)
    ```

    Preserve the existing `content:` block under each 409 verbatim — Phase 04.1 only updates the description, not the schema reference.

    For `CreateAgent` specifically: keep the existing `oneOf: [ErrorResponse, VersionConflictErrorResponse]` (D-37 union — `agents.go` handler still uses `body.FromErrorResponse(...)` to populate it). Do NOT collapse it to flat.

    **Edit B: Add NEW 409 response to CreateAdapter (RESEARCH Pitfall 7 — currently absent).**

    Locate the `CreateAdapter` path operation (typically POST `/v1/orgs/{org_id}/adapters` — search for `operationId: CreateAdapter` or `CreateAdapter` heading). Locate the `responses:` block. Adapter currently has only `"201":`, `"400":`, `"500":` (no 409). INSERT a new 409 block AFTER the `"400":` block and BEFORE the `"500":` block:

    ```yaml
            "409":
              description: |
                Duplicate `code` within the org (ErrorCode=duplicate_code), or
                duplicate `external_id` within the org when set
                (ErrorCode=duplicate_external_id). Phase 04.1 added these
                uniqueness constraints to adapters — pre-04.1 adapters had only
                PRIMARY KEY(id) uniqueness so 23505 was impossible.
              content:
                application/json:
                  schema:
                    $ref: "#/components/schemas/ErrorResponse"
    ```

    Use the FLAT `$ref` shape (matches CreateSkill / CreateQueue / CreateChannel / CreateBreakReason — NOT the oneOf used by CreateAgent — RESEARCH Pitfall 10).

    **Edit C: Add NEW 422 response to all 6 Update endpoints (UpdateAgent, UpdateSkill, UpdateQueue, UpdateChannel, UpdateAdapter, UpdateBreakReason).**

    For UpdateAgent and UpdateChannel: a 422 response block already exists (for `invalid_reference` / FK violation). UPDATE the description to also mention immutable_field:

    ```yaml
            "422":
              description: |
                Business-rule violation. ErrorCode disambiguates: (a)
                `immutable_field` — PATCH attempted to change a field that is
                immutable in v0.1 (e.g., `code`; rename support deferred to
                v0.2 per D04_1-02); (b) `invalid_reference` — referenced
                entity in another org or non-existent; (c) `invalid_value` —
                value out of range (e.g., proficiency outside 1-10).
              content:
                application/json:
                  schema:
                    $ref: "#/components/schemas/ErrorResponse"
    ```

    Keep the existing `content:` block (schema ref to ErrorResponse) verbatim.

    For UpdateSkill, UpdateQueue, UpdateAdapter, UpdateBreakReason: 422 response is ABSENT today. INSERT a new 422 block AFTER the `"409":` block (or after `"404":` if no 409 exists — verify per file) and BEFORE `"500":`:

    ```yaml
            "422":
              description: |
                Business-rule violation: `code` is immutable post-create
                (D04_1-02); PATCH attempting to change it returns
                ErrorCode=immutable_field. Rename support deferred to v0.2.
              content:
                application/json:
                  schema:
                    $ref: "#/components/schemas/ErrorResponse"
    ```

    **No other edits in this task.** Plan 03 will regenerate codegen artifacts; the contract is now complete for that purpose.
  </action>
  <verify>
    <automated>
      cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54
      # Count operations referencing immutable_field across Update endpoints
      grep -c 'immutable_field' openapi/openapi.yaml | xargs -I{} test {} -ge 7 \
        && grep -c 'duplicate_code' openapi/openapi.yaml | xargs -I{} test {} -ge 7 \
        && grep -c 'duplicate_external_id' openapi/openapi.yaml | xargs -I{} test {} -ge 7 \
        && (cd services/api && go run github.com/redocly/cli@1.25 lint ../../openapi/openapi.yaml 2>&1 || npx -y @redocly/cli@1.25.0 lint openapi/openapi.yaml 2>&1) | grep -E '(0 problems|errors)' | head -3 \
        && grep -B2 'PATCH attempted to change' openapi/openapi.yaml | grep -c '"422":' | xargs -I{} test {} -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'immutable_field' openapi/openapi.yaml` returns ≥ 7 (1 enum value + at least 6 response descriptions referencing it across Update endpoints).
    - `grep -c 'duplicate_code' openapi/openapi.yaml` returns ≥ 7 (1 enum value + 6 Create endpoint 409 descriptions including new CreateAdapter).
    - `grep -c 'duplicate_external_id' openapi/openapi.yaml` returns ≥ 7 (1 enum value + 6 Create endpoint 409 descriptions).
    - Redocly lint reports no NEW errors (or "0 problems" if previously clean); existing warnings tolerated.
    - At least one `"422":` block has a `description:` containing the substring `PATCH attempted to change` (proves the immutable_field text was inserted, not just the enum value referenced).
    - The full set of changes from Tasks 1+2+3 lands in a single committable file; Plan 03 (sqlc + codegen) consumes this committed state.
  </acceptance_criteria>
  <done>
    `openapi/openapi.yaml` has new 409 response on CreateAdapter, refreshed 409 descriptions on the 5 other Create endpoints, and new 422 responses (or updated descriptions) on all 6 Update endpoints. All 5 grep gates pass. Redocly lint clean (or no NEW errors). The contract source is locked for Plan 03's `task gen` regeneration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec source → codegen output | `openapi/openapi.yaml` is read by oapi-codegen (Go) and openapi-typescript (TS) in Plan 03; spec changes silently break compile if syntax is wrong |
| Spec source → CI codegen-drift gate | CONTRACT-04 — CI fails if `git diff` after `task gen` is non-empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06 | Tampering | YAML indentation drift on schema additions | mitigate | Read each section before edit; verify with grep gates after edit. Redocly lint catches structural breakage. |
| T-04.1-07 | Spoofing | Enum value collision with v0.2 milestone | accept | Future ErrorCode values (e.g., `rate_limited`, `quota_exceeded`) will append; Phase 04.1's 3 values are positioned alphabetically and reserved by spec. |
| T-04.1-08 | Tampering | Removing existing ErrorCode values (e.g., version_conflict) breaks CAT-08 callers | mitigate | Task 1 acceptance criterion: `version_conflict` must still appear; verify via grep gate. NO removal of existing enum values. |
| T-04.1-09 | Information Disclosure | Description text exposing internal decision-ID format (D04_1-XX) to public API consumers | accept | OpenAPI docs are admin/operator-facing; D04_1-XX references provide traceability without leaking secrets. v0.2 may sanitize. |
| T-04.1-SC | Tampering | Redocly CLI install via `npx -y @redocly/cli@1.25.0` | mitigate | Redocly is already an established phase-2 dependency (verified in `.github/workflows/ci.yml`); legitimacy audit covered in Phase 2. No new package install in Phase 04.1. |
</threat_model>

<verification>
1. `grep -c "pattern: '\\^\\[a-z\\]\\[a-z0-9_\\]{0,63}\\$'" openapi/openapi.yaml` returns ≥ 19 (all schemas with code regex).
2. `grep -c '      - duplicate_code$'` returns exactly 1.
3. `grep -c '      - duplicate_external_id$'` returns exactly 1.
4. `grep -c '      - immutable_field$'` returns exactly 1.
5. `grep -c 'immutable_field' openapi/openapi.yaml` returns ≥ 7 (enum + Update*Request descriptions + response descriptions).
6. `grep -c '03-CATALOG-IDENTITY-REVIEW' openapi/openapi.yaml` returns ≥ 1 (Identity Model link).
7. Redocly CLI lint reports no NEW errors.

Plan 03 runs the full `task gen && git diff --exit-code` codegen-drift check that ultimately validates this plan's output drives correct Go/TS regeneration.
</verification>

<success_criteria>
`openapi/openapi.yaml` now declares `code` on all 6 entity schemas (Agent, Skill, Queue, Channel, Adapter, BreakReason), the single AgentListItem, all 6 Create*Request, and all 6 Update*Request schemas with the regex `^[a-z][a-z0-9_]{0,63}$`. `external_id` is demoted to nullable optional everywhere (4 entities lose `required: external_id`; adapters and break_reasons gain a new nullable external_id property). The `ErrorCode` enum carries 3 new values. CreateAdapter has a new 409 response. All 6 Update endpoints have a 422 response mentioning immutable_field. `info.description` carries an Identity Model paragraph linking the design review docs.

All grep gates in `<verification>` pass. Redocly lint passes (or reports only pre-existing warnings — to be verified clean during Plan 06's `task gen && git diff --exit-code` gate).

The spec is now READY for Plan 03 to run `task gen` and commit the regenerated Go + TS artifacts.
</success_criteria>

<output>
Create `.planning/phases/04.1-catalog-identity-normalization/04.1-02-SUMMARY.md` capturing:
- File modified: `openapi/openapi.yaml` (single file, ~41 deltas)
- Enum values added: `duplicate_code`, `duplicate_external_id`, `immutable_field`
- Schemas modified: Agent + Skill + Queue + Channel + Adapter + BreakReason + AgentListItem (7 read schemas) + 6 Create*Request + 6 Update*Request = 19 schemas with code property
- Path operations modified: 6 Create*Request (refreshed 409 descriptions + NEW 409 for CreateAdapter) + 6 Update*Request (NEW or refreshed 422 mentioning immutable_field)
- Key clarifications honored: AgentListItem is the ONLY *ListItem schema (RESEARCH Pitfall 6); CreateAgent keeps its oneOf 409 wire shape (Pitfall 10); CreateAdapter's 409 uses the flat shape
- Hazards for downstream plans: Plan 03's `task gen` MUST regenerate cleanly — any indentation error or missing comma will fail the codegen-drift CI gate
- Sources: `openapi/openapi.yaml`, `04.1-CONTEXT.md` D04_1-13..17, `04.1-RESEARCH.md` §Pitfalls 4/6/7/10, `04.1-PATTERNS.md` §openapi.yaml lines 149-371
</output>
