# Phase 5 CONTEXT.md Amend Checklist (Post Phase 04.1 Merge)

**Sequence-locked per D04_1-25:** Execute AFTER Phase 04.1 merges to main, on the Phase 5 branch `gsd/phase-05-bulk-import-go` (NOT on the Phase 04.1 branch).

**Provenance:** Authored by Plan 04.1-06 / Task 3 as the executable spec for the post-merge CONTEXT amend. The orchestrator (or a future agent) consults this checklist exactly once after Phase 04.1 lands on main; the amend is a single commit `docs(05): update CONTEXT for code-based identity post Phase 04.1`.

**Source of truth for renames:** `.planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md` §decisions D04_1-01..D04_1-26 (especially D04_1-01 "code is the universal user-facing canonical identifier" and D04_1-25 "Phase 5 CONTEXT.md amended in place AFTER Phase 04.1 merges to main").

---

## Setup

1. Switch to the Phase 5 branch and rebase on the merged Phase 04.1 main:

   ```bash
   git fetch origin
   git checkout gsd/phase-05-bulk-import-go
   git rebase origin/main  # pick up Phase 04.1 changes
   ```

2. Verify file presence:

   ```bash
   test -f .planning/phases/05-bulk-import-go/05-CONTEXT.md || { echo "FATAL: 05-CONTEXT.md missing"; exit 1; }
   test -f .planning/phases/05-bulk-import-go/05-RESEARCH.md || { echo "FATAL: 05-RESEARCH.md missing"; exit 1; }
   ```

---

## Concrete Rename Instructions

Each line below is keyed to a specific decision ID. The "before" text is the exact wording on the current Phase 5 branch (verified at commit BASE during Plan 04.1-06 — line numbers are approximate and may shift if other commits land first; grep for the "before" string to locate the target).

The amend MUST honor the canonical identity contract introduced in Phase 04.1:
- `code` is the **user-facing canonical identifier** for upserts and references (D04_1-01, D04_1-25).
- `external_id` becomes **optional integration-mapping** (D04_1-06, D04_1-07).
- The Phase 5 import upsert key is **`(org_id, code)`** via `UpsertXByCode` (authored in Phase 04.1 Plan 03).

### Block 1 — `<domain>` "In scope" (line ~42)

**Before:**
> `ImportAgentRequest`, `ImportSkillRequest`, ..., `ImportBreakReasonRequest` (mirror `Create*Request` but reference FKs by `external_id`); add ...

**After:**
> `ImportAgentRequest`, `ImportSkillRequest`, ..., `ImportBreakReasonRequest` (mirror `Create*Request` but reference FKs by `code` — the universal user-facing identifier introduced by Phase 04.1 / D04_1-01; `external_id` remains as optional integration-mapping metadata per D04_1-07); add ...

### Block 2 — D5-15 (line ~194)

**Before (verbatim):**
> **D5-15:** **Import uses per-entity `Import*Request` schemas — NOT `Create*Request`.** New `components.schemas`: `ImportAgentRequest`, `ImportSkillRequest`, `ImportQueueRequest`, `ImportChannelRequest`, `ImportAdapterRequest`, `ImportBreakReasonRequest`. Each mirrors its `Create*Request` BUT references FK relationships by the target entity's `external_id` (a human-readable caller-assigned code) instead of by `id` (server-minted UUIDv7). Rationale: "tư duy humanable" — admins coming from external systems don't have Open Routing UUIDs; they have their own stable codes (HR system employee_id, CRM skill_code, etc.). Forcing UUIDs would make CSV imports impossible without a pre-import lookup pass.

**After (verbatim — replace the entire decision block):**
> **D5-15:** **Import uses per-entity `Import*Request` schemas — NOT `Create*Request`.** New `components.schemas`: `ImportAgentRequest`, `ImportSkillRequest`, `ImportQueueRequest`, `ImportChannelRequest`, `ImportAdapterRequest`, `ImportBreakReasonRequest`. Each mirrors its `Create*Request` BUT references FK relationships by the target entity's `code` (the universal user-facing canonical identifier per Phase 04.1 / D04_1-01) instead of by `id` (server-minted UUIDv7). Rationale: "tư duy humanable" — admins coming from external systems don't have Open Routing UUIDs; they have their own stable codes. Phase 04.1 promoted `code` from a per-entity convention to a universal contract (composite `UNIQUE (org_id, code)` across all 6 catalog entities), making it the natural upsert key. `external_id` remains supported on `Create*Request` as optional integration-mapping metadata (D04_1-07), but the Phase 5 import upsert key is `code` via `UpsertXByCode` (authored in Phase 04.1 Plan 03). Forcing UUIDs would make CSV imports impossible without a pre-import lookup pass.

### Block 3 — D5-16 (line ~205)

**Before (verbatim):**
> **D5-16:** **JSON agent imports support nested `skills[]`.** `ImportAgentRequest.skills: [{skill_external_id: "SKILL_VOICE", proficiency: 7}, ...]`. Per row: lookup `(org_id, skill_external_id)` → skill UUID. Unknown skill → per-row failure `{field: "skills[0].skill_external_id", reason: "unknown_skill"}` — the agent itself does NOT import; the whole row fails (skills are intrinsic to a complete agent record). Note: this is a stricter decision than "import the agent without skills"; admins can re-run after creating the missing skill.

**After (verbatim — replace the entire decision block):**
> **D5-16:** **JSON agent imports support nested `skills[]`.** `ImportAgentRequest.skills: [{skill_code: "skill_voice", proficiency: 7}, ...]`. Per row: lookup `(org_id, skill_code)` → skill UUID. Unknown skill → per-row failure `{field: "skills[0].skill_code", reason: "unknown_skill"}` — the agent itself does NOT import; the whole row fails (skills are intrinsic to a complete agent record). Note: `skill_code` values follow Phase 04.1's regex `^[a-z][a-z0-9_]{0,63}$` (D04_1-03); illegal token shape → per-row failure `{field: "skills[N].skill_code", reason: "invalid_code_format"}` (handler validates via `validateCodeFormat` from Phase 04.1 Plan 03). Re-running after creating the missing skill is the documented recovery path.

### Block 4 — D5-17 (line ~214)

**Before (verbatim):**
> **D5-17:** **CSV agent imports support skills via a single `skills` column with `CODE:prof|CODE:prof` syntax.** Example cell: `"SKILL_VOICE:7|SKILL_CHAT:9|SKILL_EMAIL:5"`. Pipeline: trim → split by D5-04 separator priority → for each token, split on `:` → validate `[code, prof]`. Missing `:` or non-int proficiency → per-row `reason: invalid_skill_token`. Same `unknown_skill` resolution rule as D5-16. Document the convention in CSV column description.

**After (verbatim — replace the entire decision block):**
> **D5-17:** **CSV agent imports support skills via a single `skills` column with `code:prof|code:prof` syntax.** Example cell: `"skill_voice:7|skill_chat:9|skill_email:5"` (lowercase per Phase 04.1 D04_1-03 regex). Pipeline: trim → split by D5-04 separator priority → for each token, split on `:` → validate `[code, prof]`. Missing `:` → per-row `reason: invalid_skill_token`. Non-int proficiency → `reason: invalid_skill_token`. Token whose `code` part fails the `^[a-z][a-z0-9_]{0,63}$` regex → `reason: invalid_code_format` (handler reuses `validateCodeFormat` from Phase 04.1 Plan 03). Same `unknown_skill` resolution rule as D5-16. Document the lowercase convention in the CSV column description AND in the admin import-screen UI copy (Phase 6).

### Block 5 — D5-18 (line ~222)

**Before (verbatim):**
> **D5-18:** **Skill merge semantics on agent update (PATCH-like, NOT PUT).** When an import row updates an existing agent (`external_id` matches), the `skills` array MERGES into the agent's existing `agent_skills` join rows — existing skills NOT in the import payload are LEFT INTACT. This is a documented divergence from `UpdateAgentRequest` (which replaces the whole set, per Phase 2 OQ-1A). Rationale: bulk imports often arrive partial (HR system exports a delta); preserving existing assignments avoids unintended skill loss. **Trade-off:** admins cannot REMOVE a skill via import — they must use UI or the dedicated agent PATCH API.

**After (verbatim — replace the entire decision block):**
> **D5-18:** **Skill merge semantics on agent update (PATCH-like, NOT PUT).** When an import row updates an existing agent (matched by `code` via `UpsertAgentByCode` from Phase 04.1 Plan 03), the `skills` array MERGES into the agent's existing `agent_skills` join rows — existing skills NOT in the import payload are LEFT INTACT. This is a documented divergence from `UpdateAgentRequest` (which replaces the whole set, per Phase 2 OQ-1A). Rationale: bulk imports often arrive partial (HR system exports a delta); preserving existing assignments avoids unintended skill loss. **Trade-off:** admins cannot REMOVE a skill via import — they must use UI or the dedicated agent PATCH API. **Phase 04.1 alignment:** `code` is immutable post-create (D04_1-02); `external_id` may be added or rebound on update via `ImportAgentRequest.external_id` (optional field) — admins can use the import to attach a new external-system binding to a code-keyed agent.

### Block 6 — D5-25 (line ~288)

**Before (verbatim):**
> **D5-25:** Add 6 new request schemas: `ImportAgentRequest`, `ImportSkillRequest`, `ImportQueueRequest`, `ImportChannelRequest`, `ImportAdapterRequest`, `ImportBreakReasonRequest`. Each mirrors `Create*Request` with FK fields swapped to `*_external_id` (string). `ImportAgentRequest` also carries `skills: [{skill_external_id, proficiency}]`. Update the `BulkImportCatalog` `requestBody.content.application/json` schema from `items: {}` to `oneOf: [<6 schemas>]` (or per-entity discriminator — planner picks; `oneOf` is the OAS 3.0-compatible path since the spec was downgraded from 3.1).

**After (verbatim — replace the entire decision block):**
> **D5-25:** Add 6 new request schemas: `ImportAgentRequest`, `ImportSkillRequest`, `ImportQueueRequest`, `ImportChannelRequest`, `ImportAdapterRequest`, `ImportBreakReasonRequest`. Each mirrors `Create*Request` with FK fields swapped to `*_code` (string, regex `^[a-z][a-z0-9_]{0,63}$` per Phase 04.1 D04_1-03). Concretely: replace each `agent_external_id`, `skill_external_id`, `queue_external_id`, `channel_external_id`, `adapter_external_id`, `break_reason_external_id` with `agent_code`, `skill_code`, `queue_code`, `channel_code`, `adapter_code`, `break_reason_code` respectively. `ImportAgentRequest` also carries `skills: [{skill_code, proficiency}]` (was `{skill_external_id, proficiency}`). Each Import*Request retains an optional `external_id` field for integration-mapping (mutable per D04_1-07; not the FK target). Update the `BulkImportCatalog` `requestBody.content.application/json` schema from `items: {}` to `oneOf: [<6 schemas>]` (or per-entity discriminator — planner picks; `oneOf` is the OAS 3.0-compatible path since the spec was downgraded from 3.1). **Note (oapi-codegen oneOf concern):** Phase 5's 05-RESEARCH.md flagged that oapi-codegen v2 has limitations with oneOf in strict-server mode; the planner must address this (per-entity discriminator may be necessary). This is independent of the identity-model rename.

### Block 7 — D5-26 (line ~298) — NO RENAME, just verify

**Before (verbatim):**
> **D5-26:** Extend `ImportJob` schema with `status: { type: string, enum: [pending, completed, failed] }` (required). Forward-compat with v0.2 async; v0.1 returns `completed` or `failed` only (sync path), `pending` is only visible during a server-crash window or via the cleanup goroutine sweep.

**After:** **NO CHANGE.** D5-26 is about `ImportJob.status` enum and does not reference `external_id` or any identity primitive. Verify the block reads identically after the amend; if it does, leave it alone.

### Block 8 — `<references>` Trustworthy Source (line ~458)

**Before (verbatim):**
> - **4.4 (`external_id` Collisions During Bulk Import)** — covered by Phase 3's schema `UNIQUE (org_id, external_id)` per entity. Phase 5's integration test asserts org A and org B can both import the same `external_id` values without conflict.

**After (verbatim):**
> - **4.4 (`code` Collisions During Bulk Import)** — covered by Phase 04.1's schema `UNIQUE (org_id, code)` per entity (D04_1-01). Phase 5's integration test asserts org A and org B can both import the same `code` values without conflict (cross-org isolation; mirrors Phase 04.1 Plan 05's `TestCatalog_CrossOrgSameCode_BothSucceed` in `services/api/test/isolation/catalog_test.go`). The legacy `(org_id, external_id)` constraint was demoted to a partial unique index `WHERE external_id IS NOT NULL` (D04_1-06) and is enforced only when a row carries an explicit external-system mapping.

### Block 9 — `<code_context>` Integration Points / Phase 3 helpers (line ~549)

**Before (verbatim, multi-line):**
> - `repo.{Agent,Skill,...}.UpsertByExternalID(ctx, OrgDB, params)` — Phase 5's row processor calls this; Phase 3 owns the per-entity validation, the `(org_id, external_id)` upsert SQL, and the Redis cache invalidation (per CAT-11). If Phase 3 does NOT expose upsert helpers (only Create/Update REST handlers), Phase 5 must author parallel SQL — flagged for planner.
> - `repo.Skill.ResolveExternalIDs(ctx, OrgDB, codes []string) → map[string]uuid.UUID, []string unknown` — Phase 5's nested skill resolver (D5-16, D5-17) batches lookups.

**After (verbatim):**
> - `repo.{Agent,Skill,...}.UpsertByCode(ctx, OrgDB, params)` — Phase 5's row processor calls this. Phase 04.1 Plan 03 AUTHORED the `UpsertXByCode` sqlc queries for all 6 entities (`INSERT ... ON CONFLICT (org_id, code) DO UPDATE SET ... RETURNING *`); Phase 5 invokes them. Phase 3 owns the per-entity validation; Phase 04.1 owns the upsert SQL; Phase 3's Redis cache invalidation (per CAT-11) is reused — the import call site invokes the same write helper.
> - `repo.Skill.ResolveCodes(ctx, OrgDB, codes []string) → map[string]uuid.UUID, []string unknown` — Phase 5's nested skill resolver (D5-16, D5-17) batches lookups via Phase 04.1's `GetSkillByCode` query primitive (Plan 03).

### Block 10 — `<specifics>` "Tư duy humanable" (line ~577)

**Before (verbatim):**
> - **"Tư duy humanable"** — user emphasised: any field admins type into CSV/JSON should reference entities by their human-readable code (`external_id`), not server-minted UUIDs. Drove D5-15..D5-17.

**After (verbatim):**
> - **"Tư duy humanable"** — user emphasised: any field admins type into CSV/JSON should reference entities by their human-readable code, not server-minted UUIDs. Phase 04.1 promoted `code` from a per-entity convention to a universal contract (D04_1-01, composite `UNIQUE (org_id, code)` across all 6 catalog entities) — this is the canonical identifier Phase 5 imports reference. `external_id` remains supported as optional integration-mapping metadata (D04_1-07). Drove D5-15..D5-17.

---

## Optional follow-up: 05-RESEARCH.md

The Phase 5 research file `.planning/phases/05-bulk-import-go/05-RESEARCH.md` references `external_id` in FK contexts. Per D04_1-26, the 3 architecture-shaping discoveries (eager strict-server decode, oapi-codegen oneOf infeasibility, stale stubs.go path) are **independent of the identity model** and survive intact. However, any `UpsertByExternalID` / `external_id-based upsert` mentions should be renamed for consistency:

```bash
# Identify FK-context references that need renaming (do NOT mass-replace —
# review each hit because some are legitimate references to the optional
# external_id field surviving on Create*Request, not the FK upsert key):
grep -n 'external_id' .planning/phases/05-bulk-import-go/05-RESEARCH.md | grep -iE 'upsert|fk|reference|key'
```

Apply targeted edits (NOT a blanket `sed`) to each FK-context hit, with the rationale: "FK-context references are renamed `code`; demotion notes about the optional `external_id` field are preserved." Commit as a separate commit:

```bash
git add .planning/phases/05-bulk-import-go/05-RESEARCH.md
git commit -m "docs(05): update RESEARCH FK references to code post Phase 04.1"
```

---

## Internal Consistency Check

After the amend, verify the Decisions section is self-consistent:

```bash
# 1. The "humanable" phrase appears with `code` (not `external_id`):
grep -A1 'humanable' .planning/phases/05-bulk-import-go/05-CONTEXT.md

# 2. D5-15..D5-18 reference `code` as the FK upsert key:
sed -n '/D5-15:/,/D5-19:/p' .planning/phases/05-bulk-import-go/05-CONTEXT.md | grep -E 'code|external_id'

# 3. D5-25 lists `*_code` field names (not `*_external_id`):
grep -A6 'D5-25:' .planning/phases/05-bulk-import-go/05-CONTEXT.md | grep -E '_(code|external_id)'

# 4. ImportAgentRequest's nested skills use skill_code:
grep -E 'skill_(code|external_id)' .planning/phases/05-bulk-import-go/05-CONTEXT.md
```

Expected: every grep returns the `code` form; no `*_external_id` survivals in FK-context lines (legitimate `external_id` references in integration-mapping context — D04_1-07 demotion notes — are allowed).

---

## Commit

```bash
git add .planning/phases/05-bulk-import-go/05-CONTEXT.md
git commit -m "docs(05): update CONTEXT for code-based identity post Phase 04.1"
```

The commit message convention is **fixed by D04_1-25** — do not paraphrase.

---

## Done When

- [ ] Phase 5 CONTEXT.md decisions D5-15, D5-16, D5-17, D5-18, D5-25 read `code` (not `external_id`) for FK upsert key and nested-skill identity references.
- [ ] D5-26 unchanged (no `external_id` reference; no rename needed).
- [ ] §domain "In scope" / §references 4.4 / §code_context Integration Points / §specifics "tư duy humanable" all reference `code` as the canonical identifier.
- [ ] (Optional) 05-RESEARCH.md FK-context references renamed; demotion notes about the optional `external_id` field preserved.
- [ ] Internal consistency grep gates above all return the expected `code` form.
- [ ] Commit message on the Phase 5 branch matches verbatim: `docs(05): update CONTEXT for code-based identity post Phase 04.1`.
- [ ] Phase 5 branch is rebased on the merged Phase 04.1 + still passes any existing 05-CONTEXT.md parse/validate.
- [ ] STATE.md updated (separately, by the orchestrator/user) noting that Phase 5 can be unpaused once Phase 6 (admin app) is ready to consume the new identity contract.

---

## Why This Checklist Exists (D04_1-25 sequence lock)

The amend is sequence-locked AFTER Phase 04.1 merges because:

1. **The Phase 5 plans are NOT yet generated.** `/gsd-plan-phase 05` was paused so it could consume the post-04.1 identity contract. Amending CONTEXT.md before Phase 04.1 ships would create a chicken-and-egg ordering: Phase 5 CONTEXT would reference `UpsertXByCode` queries that hadn't landed on main yet, and the plan-phase would have no way to verify the contract.
2. **The Phase 04.1 branch should NOT carry Phase 5 changes.** D04_1-25 explicitly says the amend goes on the Phase 5 branch, not on the Phase 04.1 branch. Mixing them would entangle the PR review surface.
3. **The Phase 5 branch already exists and is preserved.** Per Phase 04.1 CONTEXT canonical_refs, `gsd/phase-05-bulk-import-go` was preserved during the Phase 04.1 work; the amend is a clean commit on top of the merged main.

If you skip this checklist and let `/gsd-plan-phase 05` resume directly: it will plan against the stale `external_id`-keyed contract and you'll be unable to author the import row processor against the Phase 04.1 `UpsertXByCode` primitives. The plans-and-then-fix-up loop is more expensive than the rename-the-decisions-first loop documented above.

---

*Plan 04.1-06 / Task 3 — Phase 5 amend checklist authored.*
*Source: D04_1-25 (Phase 04.1 CONTEXT.md §decisions Area 7 — sequence-locked).*
*Companion to: `.planning/phases/04.1-catalog-identity-normalization/04.1-REVIEW.md` (gate results + cross-AI deferral notice).*
