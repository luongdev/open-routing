---
phase: 04.1-catalog-identity-normalization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - migrations/000002_catalog_v0_1.up.sql
  - migrations/000002_catalog_v0_1.down.sql
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-07]
must_haves:
  truths:
    - "Every primary catalog table (agents, skills, queues, channels, adapters, break_reasons) carries a code TEXT NOT NULL column with composite UNIQUE (org_id, code)"
    - "external_id on agents/skills/queues/channels is demoted to TEXT NULL with a partial unique index ix_<entity>_org_external_id ON (org_id, external_id) WHERE external_id IS NOT NULL"
    - "adapters and break_reasons each gain BOTH a new code (required) column AND a new external_id (optional) column with the same UNIQUE + partial-unique pattern"
    - "break_reasons.UNIQUE(org_id, name) constraint is dropped (the underlying _org_id_name_key auto-index disappears with it)"
    - "Migration 000002_catalog_v0_1.up.sql remains a single BEGIN/COMMIT transaction and is idempotent: running the row_number dedup CTE twice on the same data is a no-op"
    - "task db:reset rebuilds the post-04.1 schema cleanly from a Phase 1 baseline (Phase 1 scaffold drops then Phase 04.1 schema applies)"
    - "Under `task db:reset`, the `code TEXT NOT NULL` column is created inline so no row exists with NULL or empty code — the `WHERE code IS NULL OR code = ''` backfill SQL is a defensive no-op (`UPDATE … RETURNING 0 rows`). The backfill is present for stale-DB rollforward safety per D-61 (editable migration; devs who already applied 000002 prior to Phase 04.1 amendment need the dedup to fire). PATTERNS.md two-step pattern (TEXT NULL + UPDATE + SET NOT NULL) is the production-rollforward-correct equivalent; v0.1 uses the inline-NOT-NULL + dead-on-fresh-reset form because production rows do not yet exist (pre-v0.1-ship A2)."
  artifacts:
    - path: "migrations/000002_catalog_v0_1.up.sql"
      provides: "Phase 04.1 schema with code on all 6 catalog tables, demoted external_id, dropped break_reasons.name unique"
      contains: "code TEXT NOT NULL"
    - path: "migrations/000002_catalog_v0_1.down.sql"
      provides: "Symmetric rollback (drop tables wholesale; D04_1-12 + D-77 carry-forward)"
      contains: "DROP TABLE IF EXISTS"
  key_links:
    - from: "migrations/000002_catalog_v0_1.up.sql"
      to: "auto-generated constraint name <entity>_org_id_code_key"
      via: "inline UNIQUE (org_id, code) in CREATE TABLE"
      pattern: "UNIQUE \\(org_id, code\\)"
    - from: "migrations/000002_catalog_v0_1.up.sql"
      to: "partial unique index ix_<entity>_org_external_id"
      via: "CREATE UNIQUE INDEX ... WHERE external_id IS NOT NULL"
      pattern: "WHERE external_id IS NOT NULL"
---

<objective>
Amend the single editable migration `000002_catalog_v0_1.up.sql` (per D-61) in place to introduce a universal user-facing `code TEXT NOT NULL UNIQUE (org_id, code)` column on every primary catalog entity (D04_1-01), demote `external_id` to `TEXT NULL` with a partial unique index (D04_1-06), and drop the fragile `break_reasons.UNIQUE(org_id, name)` constraint (D04_1-03 / IDENT-03). Update the symmetric down migration. NO migration 003 is created.

Purpose: Establish the schema contract that every other Phase 04.1 plan (OpenAPI deltas, sqlc query authoring, handler refactor, test fixtures) builds on. Without this DDL plus the auto-generated constraint names (`<entity>_org_id_code_key` and `ix_<entity>_org_external_id`), the constraint-name introspection in `catalog/errors.go` cannot distinguish `duplicate_code` (409) from `duplicate_external_id` (409) — the central contract change.

Output: Two amended `.sql` files; the up migration applies cleanly from a Phase 1 baseline (`task db:reset`) and the row_number dedup backfill CTEs are idempotent so re-running them is a no-op.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md
@.planning/phases/04.1-catalog-identity-normalization/04.1-VALIDATION.md
@migrations/000002_catalog_v0_1.up.sql
@migrations/000002_catalog_v0_1.down.sql

<interfaces>
Existing schema constraint shapes (from `migrations/000002_catalog_v0_1.up.sql`, repo-root `migrations/`, NOT `services/api/migrations/`):

- `agents` (lines 29-45): `external_id TEXT NOT NULL` + inline `UNIQUE (org_id, external_id)` → auto-name `agents_org_id_external_id_key`. Phase 04.1 demotes the column and replaces the UNIQUE.
- `skills` (50-66), `queues` (71-87), `channels` (96-111): same shape as agents.
- `adapters` (120-133): NO external_id column today; only `PRIMARY KEY(id)` for uniqueness. Phase 04.1 adds BOTH columns.
- `break_reasons` (139-157): `UNIQUE (org_id, name)` at line 149 (auto-name `break_reasons_org_id_name_key`); NO external_id today. Phase 04.1 drops the name unique AND adds both new columns.
- `agent_skills` (171-180) and `agent_states` (191-212): NOT touched by Phase 04.1 (D04_1-DEFER-03).
- All tables sit inside ONE `BEGIN; ... COMMIT;` envelope (line 19, line 214) — preserve.

Naming conventions in use (carry forward):
- Inline `UNIQUE (col_a, col_b)` → Postgres auto-names `<table>_<col_a>_<col_b>_key` (e.g. `agents_org_id_code_key`)
- Named explicit indexes use `ix_<table>_<descriptor>` (e.g. `ix_agents_org_external_id`, `ix_agents_org_created`, `ix_agents_org_name`)

Test harness reaches the migration via `migrate.NewWithDatabaseInstance("file://../../../../migrations", "postgres", driver)` in `services/api/test/isolation/main_test.go` — repo-root path, not `services/api/migrations/`. (CONTEXT.md and PATTERNS.md occasionally point at `services/api/migrations/` — the correct path is repo-root `migrations/`.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amend migration up — add code, demote external_id, drop break_reasons.UNIQUE(name), backfill via row_number window</name>
  <files>migrations/000002_catalog_v0_1.up.sql</files>
  <read_first>
    - migrations/000002_catalog_v0_1.up.sql (current state — preserve BEGIN/COMMIT envelope at lines 19/214, DROP TABLE _scaffold at 24, agent_skills 161-180, agent_states 191-212)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md (§"migrations/000002_catalog_v0_1.up.sql" lines 50-136 — full backfill SQL, per-entity special cases, idempotency hazard)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md (D04_1-01, D04_1-03, D04_1-05, D04_1-06, D04_1-09, D04_1-10, D04_1-11 — the locked DDL decisions)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md §"Pattern 1: PostgreSQL partial unique index" (lines 303-336), §"Pattern 4: Idempotent migration backfill" (lines 440-487), §"Pitfall 5: Migration backfill collision with task db:reset workflow" (lines 584-592)
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-61 — editable migration, D-77 — scaffold deletion carry-forward, D-63/D-64 — preserve cursor and name-search indexes)
  </read_first>
  <action>
    Edit `migrations/000002_catalog_v0_1.up.sql` in place (D-61 amend-in-place; do NOT create migration 003 — D04_1-09).

    Preserve verbatim: lines 1-25 (header comment + BEGIN + DROP TABLE _scaffold), lines 159-212 (agent_skills + agent_states blocks), line 214 (COMMIT).

    Apply the following edits to each of the 6 primary catalog tables. Per-entity differences are called out where they differ from the agents template.

    **Step 1: For agents (current lines 29-40), skills (50-61), queues (71-83), channels (96-107) — these 4 already have `external_id TEXT NOT NULL` + `UNIQUE (org_id, external_id)`:**

    1. Inside the CREATE TABLE column list, INSERT a new line `    code          TEXT NOT NULL,` positioned IMMEDIATELY AFTER `org_id` and BEFORE `external_id` (D04_1-13 readability ordering).
    2. CHANGE the existing `external_id   TEXT NOT NULL,` line to `    external_id   TEXT NULL,` (column alignment per existing 4-space block — preserve visual format).
    3. REPLACE the inline `UNIQUE (org_id, external_id)` line at the end of the CREATE TABLE with `UNIQUE (org_id, code)` (auto-named `<table>_org_id_code_key` — the constraint name that `catalog/errors.go` introspects).
    4. IMMEDIATELY AFTER the existing `CREATE INDEX ix_<table>_org_name ...` line (and BEFORE the next entity's `-- -----` comment header), INSERT a new `CREATE UNIQUE INDEX ix_<table>_org_external_id ON <table> (org_id, external_id) WHERE external_id IS NOT NULL;` line. The index name format `ix_<table>_org_external_id` (singular `external_id`, no plural, no `_key` suffix) is required because `mapPgError` matches `strings.HasPrefix(name, "ix_") && strings.HasSuffix(name, "_org_external_id")`.
    5. Preserve existing `CREATE INDEX ix_<table>_org_created ON <table> (org_id, created_at DESC, id DESC);` and `CREATE INDEX ix_<table>_org_name ON <table> (org_id, lower(name) text_pattern_ops) WHERE enabled = TRUE;` — these support cursor pagination D-63 and name-search D-64.

    **Step 2: For adapters (current lines 120-133) — this table has NO external_id and NO inline UNIQUE beyond PRIMARY KEY(id):**

    1. INSERT `    code          TEXT NOT NULL,` after `org_id` (between `org_id` and `name`).
    2. INSERT `    external_id   TEXT NULL,` AFTER `code` (between `code` and `name`).
    3. ADD `,` to the closing `updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()` line and APPEND a new inline `UNIQUE (org_id, code)` line.
    4. After the existing `CREATE INDEX ix_adapters_org_name ...` line, APPEND `CREATE UNIQUE INDEX ix_adapters_org_external_id ON adapters (org_id, external_id) WHERE external_id IS NOT NULL;`.

    **Step 3: For break_reasons (current lines 139-157) — this table has NO external_id but HAS `UNIQUE (org_id, name)`:**

    1. INSERT `    code          TEXT NOT NULL,` AFTER `org_id` (between `org_id` and `name`).
    2. INSERT `    external_id   TEXT NULL,` AFTER `code`.
    3. REPLACE `UNIQUE (org_id, name)` at line 149 with `UNIQUE (org_id, code)` (this drops the `break_reasons_org_id_name_key` auto-index entirely — IDENT-03 + D04_1-03).
    4. After the existing `CREATE INDEX ix_break_reasons_org_name ...` line, APPEND `CREATE UNIQUE INDEX ix_break_reasons_org_external_id ON break_reasons (org_id, external_id) WHERE external_id IS NOT NULL;`.

    **Step 4: Idempotent backfill block (D04_1-10, D04_1-11; insert IMMEDIATELY BEFORE the COMMIT on line 214, AFTER all CREATE TABLE + CREATE INDEX statements):**

    Insert a new section. The header comment MUST explicitly document the dead-on-fresh-reset / live-on-stale-DB semantics so future readers don't mistake the backfill for unreachable code:

    ```sql
    -- ---------------------------------------------------------------------------
    -- Phase 04.1 backfill (D04_1-10, D04_1-11): populate code from external_id
    -- (agents/skills/queues/channels) or name (adapters/break_reasons), then
    -- dedupe with row_number window. Idempotent — re-running on deduped data
    -- is a no-op.
    --
    -- IMPORTANT — execution semantics under `task db:reset` vs stale-DB rollforward:
    --   * Under `task db:reset` (fresh): the `code TEXT NOT NULL` column was
    --     created inline; no row exists with NULL or empty code. The
    --     `WHERE code IS NULL OR code = ''` predicate matches zero rows.
    --     Backfill is a defensive NO-OP on fresh reset. This is intentional.
    --   * Under stale-DB rollforward (a dev applied a pre-Phase-04.1 version of
    --     000002 locally before this amendment; D-61 editable-migration policy):
    --     the dev runs `task db:reset` to pick up the amendment; this branch
    --     never executes. The backfill ONLY fires if a custom migrate-up path
    --     is invoked that adds the `code` column without populating it
    --     (currently no such path exists in v0.1; this is forward-compatibility).
    --   * The PATTERNS.md two-step pattern (`TEXT NULL → UPDATE → SET NOT NULL`)
    --     is the production-rollforward-correct equivalent and SHOULD be used
    --     if a future production migration adds `code` to existing rows.
    --     v0.1 uses the inline-NOT-NULL form because pre-v0.1-ship reality (A2):
    --     no production rows exist yet. See RESEARCH §Pitfall 5.
    -- ---------------------------------------------------------------------------
    ```

    For each of the 4 entities with pre-existing external_id (agents, skills, queues, channels), emit this exact 3-step pattern (substitute the table name):

    ```sql
    -- Backfill is no-op on fresh task db:reset (code is NOT NULL inline);
    -- present for stale-DB rollforward safety per D-61.
    UPDATE agents
    SET code = CASE
        WHEN substring(external_id from 1 for 1) ~ '^[a-zA-Z]$'
            THEN lower(regexp_replace(external_id, '[^a-zA-Z0-9_]', '_', 'g'))
        ELSE 'r_' || lower(regexp_replace(external_id, '[^a-zA-Z0-9_]', '_', 'g'))
    END
    WHERE code IS NULL OR code = '';

    WITH dups AS (
        SELECT id,
               row_number() OVER (PARTITION BY org_id, code ORDER BY created_at, id) AS rn
        FROM agents
    )
    UPDATE agents a
    SET code = a.code || '_' || dups.rn::text
    FROM dups
    WHERE a.id = dups.id AND dups.rn > 1;
    ```

    For adapters and break_reasons (no pre-existing external_id), substitute `external_id` with `name` in the CASE expression (the regex-replace + lower-case sanitization treats `name` the same way; row_number dedup follows). Per RESEARCH §Pattern 4 (lines 478-481): use bare name derivation + row_number dedup, NOT the UUID-suffix recipe in CONTEXT `<specifics>` — symmetric across all 6 entities, easier to read.

    **Note on backfill execution under task db:reset (WARNING 9 reconciliation):**
    Because the `code` column is created `TEXT NOT NULL` INLINE in the `CREATE TABLE` block (Step 1 above), a fresh `task db:reset` results in tables that have ZERO rows. Every `UPDATE … WHERE code IS NULL OR code = ''` statement runs but matches no rows — these statements are intentionally no-ops on fresh reset. They exist for two reasons:
    1. **Stale-DB rollforward safety (D-61):** A dev who applied a pre-Phase-04.1 version of migration 000002 locally needs the backfill to populate `code` on existing rows BEFORE the inline `NOT NULL` constraint fires. The dev MUST run `task db:reset` to pick up the amended migration — at that point the table is dropped and recreated empty, so the backfill again no-ops. But the SQL is kept as documentation + forward-compatibility for any future custom migration path that adds `code` to an existing populated table.
    2. **Production correctness future-proofing:** When v0.2 adds the FIRST production-rollforward path that needs to backfill, the PATTERNS.md two-step pattern (`ALTER TABLE … ADD COLUMN code TEXT NULL; UPDATE … SET code = …; ALTER TABLE … ALTER COLUMN code SET NOT NULL;`) is the correct shape. v0.1 ships frozen migration 000002 with inline `NOT NULL`; no production rows exist yet (pre-v0.1-ship reality A2).

    The grep gate `row_number() OVER (PARTITION BY org_id, code` MUST match exactly 6 times (one per primary catalog table); the gate verifies the SQL is present in the file, not that it executes effectively on any particular database state.

    **Step 5: Update the header comment block (lines 1-17) to mention Phase 04.1 amendment.** Insert after line 17 (above `BEGIN;`):
    ```
    --   * (Phase 04.1) Every primary catalog table carries `code TEXT NOT NULL`
    --     with `UNIQUE (org_id, code)`; `external_id` is demoted to `TEXT NULL`
    --     with a partial unique index `ix_<entity>_org_external_id ... WHERE
    --     external_id IS NOT NULL`. break_reasons.UNIQUE(org_id, name) is
    --     dropped (name becomes mutable display label). Backfill at the end of
    --     this file derives code from external_id (or name for adapters and
    --     break_reasons) and dedupes collisions via row_number window
    --     (idempotent — re-runnable). Backfill is a defensive no-op under
    --     fresh `task db:reset` (code is NOT NULL inline); present for
    --     stale-DB rollforward safety per D-61. See
    --     .planning/phases/04.1-catalog-identity-normalization/.
    ```

    NO migration 003. NO new constraint names beyond the auto-named `<table>_org_id_code_key` and the explicit `ix_<table>_org_external_id`. Preserve the single `BEGIN; ... COMMIT;` transaction envelope.

    **Per D04_1-09 step 4:** `code` is NOT added to the name-search index (`ix_<table>_org_name` stays on `lower(name)`). NO new code-search index — the UNIQUE (org_id, code) auto-generates a B-tree index used for both uniqueness and point lookups (RESEARCH Pitfall 9).
  </action>
  <verify>
    <automated>
      cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54
      grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'code          TEXT NOT NULL' | xargs -I{} test {} -eq 6 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'UNIQUE (org_id, code)' | xargs -I{} test {} -eq 6 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'WHERE external_id IS NOT NULL' | xargs -I{} test {} -eq 6 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'external_id   TEXT NULL' | xargs -I{} test {} -eq 6 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'UNIQUE (org_id, name)' | xargs -I{} test {} -eq 0 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c "row_number() OVER (PARTITION BY org_id, code" | xargs -I{} test {} -eq 6 \
        && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'regexp_replace' | xargs -I{} test {} -eq 6 \
        && grep -cE '^BEGIN;$' migrations/000002_catalog_v0_1.up.sql | xargs -I{} test {} -eq 1 \
        && grep -cE '^COMMIT;$' migrations/000002_catalog_v0_1.up.sql | xargs -I{} test {} -eq 1 \
        && grep -c 'DROP TABLE IF EXISTS _scaffold' migrations/000002_catalog_v0_1.up.sql | xargs -I{} test {} -eq 1 \
        && grep -c 'no-op on fresh task db:reset' migrations/000002_catalog_v0_1.up.sql | xargs -I{} test {} -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - Filtering out comments (`grep -v '^--'`), exactly 6 occurrences of `code          TEXT NOT NULL` exist (one per primary catalog table; spacing matches the existing column-block 4-space style).
    - Filtering out comments, exactly 6 occurrences of `UNIQUE (org_id, code)` exist (one inline per table).
    - Filtering out comments, exactly 6 occurrences of `WHERE external_id IS NOT NULL` exist (one per partial unique index).
    - Filtering out comments, exactly 6 occurrences of `external_id   TEXT NULL` exist (4 demotions + 2 new columns for adapters/break_reasons).
    - Filtering out comments, ZERO occurrences of `UNIQUE (org_id, name)` remain (break_reasons constraint is dropped — IDENT-03).
    - Filtering out comments, exactly 6 occurrences of `row_number() OVER (PARTITION BY org_id, code` exist (idempotent dedup CTE per entity).
    - Filtering out comments, exactly 6 occurrences of `regexp_replace` exist (sanitization per entity).
    - Exactly one `BEGIN;` and one `COMMIT;` line at top-level — single transaction envelope preserved.
    - `DROP TABLE IF EXISTS _scaffold` is present exactly once (D-77 carry-forward).
    - The comment marker `no-op on fresh task db:reset` appears at least once in the migration file (WARNING 9 reconciliation — documents the dead-code-on-fresh-reset semantics for future readers).
    - File still parses as valid SQL (verified by subsequent `task db:reset` smoke test in Plan 06; this task does not run migrations).
  </acceptance_criteria>
  <done>
    The 6 entity DDL blocks in `migrations/000002_catalog_v0_1.up.sql` carry the new `code` column with `UNIQUE (org_id, code)`, `external_id` is `TEXT NULL` with a partial unique index per entity, break_reasons no longer has `UNIQUE (org_id, name)`, and the backfill block at the bottom (above COMMIT) is idempotent via the row_number window pattern with explicit documentation that it no-ops on fresh `task db:reset` (present for stale-DB rollforward per D-61). The grep gates above ALL pass. Header comment block notes the Phase 04.1 amendment with a link to `.planning/phases/04.1-catalog-identity-normalization/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update down migration with Phase 04.1 documentation comment (drop-table-wholesale shape preserved per D04_1-12)</name>
  <files>migrations/000002_catalog_v0_1.down.sql</files>
  <read_first>
    - migrations/000002_catalog_v0_1.down.sql (current state — preserve drop-table-wholesale pattern + _scaffold recreation D-77)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-PATTERNS.md (§"migrations/000002_catalog_v0_1.down.sql" lines 139-146 — explicit "down is drop-table-wholesale" guidance)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-CONTEXT.md (D04_1-12 — down migration is documentation-only; production uses task db:reset)
    - .planning/phases/04.1-catalog-identity-normalization/04.1-RESEARCH.md §"Pitfall 11: Down migration symmetry for the partial unique index" (lines 656-661)
  </read_first>
  <action>
    Edit `migrations/000002_catalog_v0_1.down.sql` in place.

    The existing down migration drops all tables wholesale and recreates the `_scaffold` table (D-77 carry-forward). Since columns/constraints disappear when the tables drop, NO SQL changes are required for Phase 04.1 (RESEARCH §Pitfall 11 + PATTERNS §down.sql).

    The ONLY edit is to the header comment block: add a note explaining that the down migration is intentionally drop-table-wholesale rather than column-additive, and that v0.1 uses `task db:reset` rather than down-up cycles.

    Locate the existing header comment block (typically lines 1-15 of `000002_catalog_v0_1.down.sql`). Append immediately before the `BEGIN;` line:

    ```
    --
    -- Phase 04.1 note: the down migration intentionally drops all catalog
    -- tables rather than reversing the Phase 04.1 column/constraint changes
    -- column-by-column. Rationale (D04_1-12, RESEARCH §Pitfall 11):
    --   * v0.1 testing uses `task db:reset` (drop DB, recreate, re-apply
    --     migrations from scratch) — down migration is documentation-only.
    --   * A column-additive down (drop code, restore external_id NOT NULL +
    --     UNIQUE(org_id, external_id), restore break_reasons.UNIQUE(org_id, name))
    --     would fail on any post-04.1 row with NULL external_id — the table
    --     drop sidesteps the problem.
    --   * The Phase 1 _scaffold table is recreated at the end so a Phase 1
    --     baseline is restored cleanly (D-77 carry-forward).
    ```

    Do NOT change any SQL statements. The DROP TABLE list + _scaffold recreation stays verbatim.

    If the existing down.sql does not already drop tables in dependency-safe order (children before parents — agent_skills before agents, agent_states before agents), add `DROP TABLE IF EXISTS agent_skills, agent_states;` BEFORE `DROP TABLE IF EXISTS agents, ...;` only if the existing file is not already correct. (Read the current down.sql first; do NOT change the existing drop list unless the order is wrong.)
  </action>
  <verify>
    <automated>
      cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54
      grep -c 'Phase 04.1 note' migrations/000002_catalog_v0_1.down.sql | xargs -I{} test {} -ge 1 \
        && grep -c 'DROP TABLE' migrations/000002_catalog_v0_1.down.sql | xargs -I{} test {} -ge 1 \
        && grep -c '_scaffold' migrations/000002_catalog_v0_1.down.sql | xargs -I{} test {} -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - The down migration contains the comment marker `Phase 04.1 note` documenting the drop-table-wholesale design.
    - `DROP TABLE` statements remain in the file (existing pattern preserved — at least one DROP TABLE line).
    - `_scaffold` table recreation block remains intact (D-77 carry-forward).
    - No new ALTER TABLE statements were added; the down migration stays purely drop-then-recreate-scaffold.
  </acceptance_criteria>
  <done>
    `migrations/000002_catalog_v0_1.down.sql` carries a header comment block explaining Phase 04.1's deliberate choice not to reverse column-by-column, with rationale referencing D04_1-12 + RESEARCH §Pitfall 11. SQL body unchanged from current state. Grep gates pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → Postgres 17 | golang-migrate writes schema changes; relies on transactional `BEGIN/COMMIT` envelope for atomicity |
| Backfill CTE → existing row data | UPDATE statements rewrite `code` from `external_id`/`name`; data is org-scoped (PARTITION BY org_id) so cross-org collision is impossible |
| Plan downstream (sqlc/handlers/tests) → constraint names | Plans 03-05 rely on auto-name `<entity>_org_id_code_key` and explicit `ix_<entity>_org_external_id` from this migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | Migration backfill CTE | mitigate | Window function `row_number() OVER (PARTITION BY org_id, code)` operates inside a single org partition; cannot leak codes across orgs. UPDATE is wrapped in the file-level BEGIN/COMMIT transaction. |
| T-04.1-02 | Denial of Service | Re-running migration on already-deduped data | mitigate | Idempotent dedup pattern: after the first run, every (org_id, code) partition has exactly one row with rn=1; second-run UPDATE matches zero rows and is a no-op. Plan 05 includes an explicit `TestMigration_Idempotent_BackfillProducesNoOpOnRerun` integration test. |
| T-04.1-03 | Information Disclosure | Backfilled `code` derived from `external_id` exposing external-system identifiers in the UI | accept | Pre-v0.1-ship reality (A2): no production rows exist; CI/dev seed data is intentionally exposed for debugging. v0.1 ships frozen migration; rebrand happens before first prod customer. |
| T-04.1-04 | Tampering | Editing the migration file after Phase 1 already applied it locally | mitigate | RESEARCH Pitfall 5 — local devs MUST run `task db:reset` after pulling Phase 04.1 (golang-migrate version-tracks, not content-tracks). Plan 06 includes a `task db:reset` smoke step. CI uses fresh testcontainer per run. |
| T-04.1-05 | Tampering | Drop migration leaving production schema in inconsistent state | accept | D04_1-12 + D-25 carry-forward: v0.1 down is documentation-only. Production rollback via `task db:reset` from Phase 1 baseline + replay. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | n/a | Phase 04.1 adds NO new external packages (verified by RESEARCH §Package Legitimacy Audit — all packages already in `services/api/go.mod`). |
</threat_model>

<verification>
1. `cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/frosty-khayyam-9c3d54 && grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'code          TEXT NOT NULL'` returns 6.
2. `grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'UNIQUE (org_id, code)'` returns 6.
3. `grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'WHERE external_id IS NOT NULL'` returns 6.
4. `grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c 'UNIQUE (org_id, name)'` returns 0 (break_reasons drop verified).
5. `grep -v '^--' migrations/000002_catalog_v0_1.up.sql | grep -c "row_number() OVER (PARTITION BY org_id, code"` returns 6.
6. `grep -cE '^BEGIN;$' migrations/000002_catalog_v0_1.up.sql && grep -cE '^COMMIT;$' migrations/000002_catalog_v0_1.up.sql` both return 1.
7. `grep -c 'Phase 04.1 note' migrations/000002_catalog_v0_1.down.sql` returns ≥ 1.
8. `grep -c 'no-op on fresh task db:reset' migrations/000002_catalog_v0_1.up.sql` returns ≥ 1 (WARNING 9 documentation marker).

Plan 06 runs the full `task db:reset` smoke test (requires Docker + Postgres container, not run in this plan's verify).
</verification>

<success_criteria>
The up migration (`migrations/000002_catalog_v0_1.up.sql`) carries a `code TEXT NOT NULL` column on every primary catalog entity, demoted `external_id TEXT NULL`, the partial unique index per entity, the dropped `break_reasons.UNIQUE(org_id, name)`, and the idempotent row_number-based backfill block — all inside the existing single `BEGIN; ... COMMIT;` transaction envelope. The backfill block carries an explicit documentation header explaining its no-op-on-fresh-reset / stale-DB-rollforward-safe semantics (WARNING 9 reconciliation).

The down migration (`migrations/000002_catalog_v0_1.down.sql`) is documented as drop-table-wholesale-by-design per Phase 04.1 with rationale linked to D04_1-12 + RESEARCH §Pitfall 11.

All 10 grep gates in `<verification>` pass.

Wave 0 is structurally ready for Plan 02 (OpenAPI yaml deltas — parallel-safe with this plan because they touch different files) and downstream Plan 03 (sqlc query authoring + `task gen` regen + codecheck.go helper — depends on this migration's auto-generated constraint names being committed).
</success_criteria>

<output>
Create `.planning/phases/04.1-catalog-identity-normalization/04.1-01-SUMMARY.md` capturing:
- Files modified: `migrations/000002_catalog_v0_1.up.sql`, `migrations/000002_catalog_v0_1.down.sql`
- Constraint names introduced (exact list, since Plan 04's `mapPgError` introspection depends on these): `agents_org_id_code_key`, `skills_org_id_code_key`, `queues_org_id_code_key`, `channels_org_id_code_key`, `adapters_org_id_code_key`, `break_reasons_org_id_code_key`, `ix_agents_org_external_id`, `ix_skills_org_external_id`, `ix_queues_org_external_id`, `ix_channels_org_external_id`, `ix_adapters_org_external_id`, `ix_break_reasons_org_external_id`
- Constraint removed: `break_reasons_org_id_name_key` (the auto-name from the now-dropped `UNIQUE (org_id, name)`)
- Patterns established: row_number window dedup as idempotent backfill (consumed by Plan 05 `TestMigration_Idempotent`)
- Backfill execution semantics: documented as no-op on fresh `task db:reset` (NOT NULL inline → no NULL rows match the WHERE predicate); kept for stale-DB rollforward safety per D-61. PATTERNS.md two-step pattern is the production-correct equivalent for v0.2.
- Hazards for downstream plans: developers with already-applied 000002 MUST run `task db:reset` (Plan 06 enforces); test fixtures still using hyphens must be edited (Plan 05 handles)
- Sources: `migrations/000002_catalog_v0_1.up.sql`, `04.1-CONTEXT.md`, `04.1-RESEARCH.md` §Pattern 1 + §Pattern 4 + §Pitfall 5 + §Pitfall 11, `04.1-PATTERNS.md` §"migrations/000002_catalog_v0_1.up.sql" + §"migrations/000002_catalog_v0_1.down.sql"
</output>
</content>
</invoke>