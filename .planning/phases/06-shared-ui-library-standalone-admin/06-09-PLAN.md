---
phase: 06-shared-ui-library-standalone-admin
plan: 09
type: execute
wave: 3
depends_on:
  - "06-05"
files_modified:
  - web/packages/ui/src/components/break-reasons/break-reason-list.ts
  - web/packages/ui/src/components/break-reasons/break-reason-list.test.ts
  - web/packages/ui/src/components/break-reasons/break-reason-detail.ts
  - web/packages/ui/src/components/break-reasons/break-reason-detail.test.ts
  - web/packages/ui/src/components/break-reasons/break-reason-form.ts
  - web/packages/ui/src/components/break-reasons/break-reason-form.test.ts
  - web/packages/ui/src/components/break-reasons/index.ts
  - web/packages/ui/src/components/index.ts
  - web/packages/ui/src/components/shell/catalog-shell.ts
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "or-break-reason-list renders columns: code, name, routable (✓/✗), display_order, enabled, updated_at, [⋮]"
    - "or-break-reason-detail routable field is sl-switch (default true); display_order is integer input"
    - "or-break-reason-form single-step wizard posts CreateBreakReasonRequest with routable + display_order"
    - "409 on break reason PATCH uses error.current without re-GET (Pitfall 9)"
    - "catalog-shell.ts routes /break-reasons, /break-reasons/new, /break-reasons/:id to real components"
    - "or-status-panel Plan 12 reads break reasons from this entity; routable field visible in list"
  artifacts:
    - path: "web/packages/ui/src/components/break-reasons/break-reason-list.ts"
      provides: "or-break-reason-list custom element"
      exports: ["OrBreakReasonList"]
    - path: "web/packages/ui/src/components/break-reasons/break-reason-detail.ts"
      provides: "or-break-reason-detail custom element"
      exports: ["OrBreakReasonDetail"]
    - path: "web/packages/ui/src/components/break-reasons/break-reason-form.ts"
      provides: "or-break-reason-form custom element"
      exports: ["OrBreakReasonForm"]
    - path: "web/packages/ui/src/components/break-reasons/index.ts"
      provides: "break-reasons barrel export"
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "shell with break-reason routes wired"
      contains: "or-break-reason-list"
  key_links:
    - from: "break-reason-list.ts"
      to: "routable column"
      via: "sl-icon check-lg / x-lg with tooltip"
      pattern: "routable"
    - from: "break-reason-detail.ts"
      to: "validateUpdateBreakReason"
      via: "ajv validator from validators/index.js"
      pattern: "validateUpdateBreakReason"
    - from: "catalog-shell.ts"
      to: "OrBreakReasonList, OrBreakReasonDetail, OrBreakReasonForm"
      via: "import and render in route config"
      pattern: "or-break-reason-list.*or-break-reason-detail"
---

<objective>
Build the BreakReasons entity CRUD components — or-break-reason-list, or-break-reason-detail, or-break-reason-form. BreakReasons have two entity-specific fields: routable (boolean switch, default true) and display_order (integer, lower = first in break picker). Wire break-reason routes into the shell router.

Purpose: BreakReasons feeds the status panel (Plan 12) — the break picker renders break reasons from this entity. The routable field controls whether agents on this break type can still receive interactions.
Output: Three break-reason Lit components + tests; break-reason routes in catalog-shell.ts.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@web/packages/ui/src/api/client.ts
@web/packages/ui/src/api/errors.ts

Prior plan outputs (read for pattern reference):
@.planning/phases/06-shared-ui-library-standalone-admin/06-07-SUMMARY.md
</context>

<interfaces>
<!-- BreakReason API shape -->
BreakReason entity from openapi.yaml:
```typescript
interface BreakReason {
  id: string;
  org_id: string;
  code: string;            // read-only post-create
  name: string;
  external_id?: string;
  routable: boolean;       // required, default true — agents on this break can receive interactions
  display_order: number;   // integer, required — lower values appear first in break picker
  enabled: boolean;
  version: number;
  created_at: string;
  updated_at: string;
}
interface CreateBreakReasonRequest {
  code: string; name: string; external_id?: string;
  routable: boolean; display_order: number; enabled?: boolean;
}
interface UpdateBreakReasonRequest {
  name?: string; external_id?: string;
  routable?: boolean; display_order?: number; enabled?: boolean;
  version: number;
}
```

List column header tooltip for "routable" (UI-SPEC §5.3 D6-V-10):
"Routable: agent can still receive interactions while on break"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: or-break-reason-list — columns with routable ✓/✗ and display_order</name>
  <files>
    web/packages/ui/src/components/break-reasons/break-reason-list.ts
    web/packages/ui/src/components/break-reasons/break-reason-list.test.ts
    web/packages/ui/src/components/break-reasons/index.ts
  </files>
  <read_first>
    - 06-07-SUMMARY.md (or-skill-list pattern — copy for break-reasons with adjusted columns)
    - UI-SPEC.md §5.3 "Per-entity column sets" — Break Reasons: code, name, routable(✓/✗), display_order, enabled, updated_at, [⋮]
    - UI-SPEC.md §5.3 — "display_order right-aligned; routable column header tooltip: 'Routable: agent can still receive interactions while on break'"
    - UI-SPEC.md §11 "Empty states" — Break Reasons: "No break reasons yet" / "Define why agents go on break. Each reason shows in the break picker."
    - CONTEXT.md D6-08 (per-component Shoelace imports)
  </read_first>
  <behavior>
    - Test 1: renders "No break reasons yet" empty state when items=[]
    - Test 2: renders routable column as ✓ (check-lg) or ✗ (x-lg) icon per break reason value
    - Test 3: "routable" column header has sl-tooltip with "Routable: agent can still receive interactions while on break"
    - Test 4: display_order column is right-aligned (style text-align:right)
    - Test 5: row click dispatches open-routing:navigate with /orgs/{orgId}/break-reasons/{id}
    - Test 6: [+ Create break reason] dispatches open-routing:navigate to /orgs/{orgId}/break-reasons/new
  </behavior>
  <action>
    Create web/packages/ui/src/components/break-reasons/break-reason-list.ts following or-skill-list pattern:

    @customElement('or-break-reason-list') class OrBreakReasonList extends LitElement.

    Properties: orgId (attribute 'org-id'), client (attribute: false).

    State: _search, _cursor, _cursorStack, _includeDisabled, _limit=25.

    Column set per UI-SPEC §5.3 D6-V-10 — Break Reasons:
    - code: monospace (--or-text-code, --or-color-code-fg)
    - name: plain text
    - routable: same icon pattern as enabled — check-lg in --sl-color-success-500 (routable=true), x-lg in --or-color-text-muted (routable=false). Column header MUST include sl-tooltip content="Routable: agent can still receive interactions while on break".
    - display_order: integer, right-aligned (style="text-align:right")
    - enabled: check-lg / x-lg icon
    - updated_at: relative time + sl-tooltip ISO datetime
    - [⋮]: sl-dropdown context menu

    Empty state per UI-SPEC §11:
    - No items: "No break reasons yet" heading / "Define why agents go on break. Each reason shows in the break picker." / [+ Create break reason]
    - Search active + no results: "No break reasons found matching '{_search}'." + [Clear search]

    Navigation: row click → open-routing:navigate to /orgs/{orgId}/break-reasons/{id}.
    CTA button → /orgs/{orgId}/break-reasons/new.

    Per-component Shoelace imports (D6-08): same set as skill-list plus tooltip.js for header tooltip.

    Write break-reason-list.test.ts with 6 behaviors.

    Create index.ts placeholder exporting OrBreakReasonList.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- --reporter=verbose --testPathPattern=break-reason-list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - routable column renders check-lg/x-lg icons (not boolean text)
    - routable column header has sl-tooltip per UI-SPEC D6-V-10
    - display_order is right-aligned
    - All 6 Vitest behaviors pass
    - No direct router import in break-reason-list.ts
  </acceptance_criteria>
  <done>or-break-reason-list renders all columns correctly; column header tooltip; all 6 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: or-break-reason-detail + or-break-reason-form (routable switch + display_order integer)</name>
  <files>
    web/packages/ui/src/components/break-reasons/break-reason-detail.ts
    web/packages/ui/src/components/break-reasons/break-reason-detail.test.ts
    web/packages/ui/src/components/break-reasons/break-reason-form.ts
    web/packages/ui/src/components/break-reasons/break-reason-form.test.ts
    web/packages/ui/src/components/break-reasons/index.ts
    web/packages/ui/src/components/index.ts
    web/packages/ui/src/components/shell/catalog-shell.ts
  </files>
  <read_first>
    - 06-07-SUMMARY.md (or-skill-detail pattern — copy; adjust fields for break-reasons)
    - UI-SPEC.md §5.4 "Break Reason" field order: code(RO), name, external_id, routable(switch), display_order(integer), enabled(switch)
    - UI-SPEC.md §5.4 — routable helper text: "When on, agents on this break can still receive routed interactions."
    - UI-SPEC.md §5.4 — display_order helper text: "Lower values appear first in the break picker."
    - UI-SPEC.md §5.5 D6-V-16 — Break Reason single-step: code, name, external_id, routable, display_order, enabled
    - CONTEXT.md D6-03 (409 use error.current, not response.json() — Pitfall 9)
    - CONTEXT.md D04_1-02 (code always or-code-input readonly=true in detail/edit)
  </read_first>
  <behavior>
    break-reason-detail tests:
    - Test 1: code field renders as or-code-input with readonly=true (D04_1-02)
    - Test 2: routable field renders as sl-switch with helper "When on, agents on this break can still receive routed interactions."
    - Test 3: display_order renders as sl-input type="number" min="0" step="1" with helper "Lower values appear first in the break picker."
    - Test 4: 409 PATCH response: uses error.current (not response.json()); renders or-conflict-banner
    - Test 5: Save disabled until dirty; footer shows version · updated · created

    break-reason-form tests:
    - Test 1: single-step wizard; stepper hidden; header "Create break reason"
    - Test 2: routable sl-switch defaults to checked=true (per UI-SPEC default: routable=true)
    - Test 3: display_order required; form blocked if empty
    - Test 4: successful POST navigates to /orgs/{orgId}/break-reasons/{newId}
  </behavior>
  <action>
    --- or-break-reason-detail ---

    Create web/packages/ui/src/components/break-reasons/break-reason-detail.ts:

    @customElement('or-break-reason-detail') class OrBreakReasonDetail extends LitElement.

    Properties: orgId, entityId, client.

    Load task: GET /v1/orgs/{orgId}/break-reasons/{entityId}.

    Form fields per UI-SPEC §5.4 Break Reason:
    1. code → <or-code-input .value=${br.code} .readonly=${true}> (D04_1-02)
    2. name → sl-input required
    3. external_id → sl-input optional, helper "Optional integration mapping. Pass empty to clear."
    4. routable → sl-switch with helper text "When on, agents on this break can still receive routed interactions."
       Bind .checked=${this._formData.routable} @sl-change=${e => this._formData = {...this._formData, routable: e.target.checked}}
    5. display_order → sl-input type="number" min="0" step="1" required, helper "Lower values appear first in the break picker."
    6. enabled → sl-switch

    Dirty tracking: compare _formData against loaded break reason.

    409 handling: if error && 'current' in error → update _serverState from error.current; render or-conflict-banner mode='crud'. Do NOT call response.json().

    Save: ajv validateUpdateBreakReason before PATCH.

    Top bar: [← Back to Break reasons] + spacer + [Delete] + [Disable]/[Enable].

    Footer metadata bar (D6-V-14).

    Per-component Shoelace imports: input, switch, button, dialog, alert, spinner, icon, icon-button, tooltip.

    --- or-break-reason-form ---

    Create web/packages/ui/src/components/break-reasons/break-reason-form.ts:

    @customElement('or-break-reason-form') class OrBreakReasonForm extends LitElement.

    Single-step wizard: steps=[{key:'basics', label:'Basics'}].

    Fields per UI-SPEC §5.5 D6-V-16 Break Reason:
    - code (or-code-input required)
    - name (sl-input required)
    - external_id (sl-input optional)
    - routable (sl-switch, .checked=${true} as default — routable=true by default per spec)
    - display_order (sl-input type="number" min="0" step="1" required)
    - enabled (sl-switch, default true)

    On complete: validateCreateBreakReason. POST /v1/orgs/{orgId}/break-reasons. On 2xx → navigate to /orgs/{orgId}/break-reasons/{id}. On 409 duplicate_code → inline code error.

    --- Barrel and shell routing ---

    Update index.ts: export OrBreakReasonList, OrBreakReasonDetail, OrBreakReasonForm.

    Update web/packages/ui/src/components/index.ts: add export * from './break-reasons/index.js'

    Update catalog-shell.ts:
    - Import OrBreakReasonList, OrBreakReasonDetail, OrBreakReasonForm from '../break-reasons/index.js'
    - Wire routes (note URL segment is 'break-reasons' matching server path):
      { path: '/orgs/:org_id/break-reasons', render: ({org_id}) => { this._currentOrgId = org_id ?? ''; return html`<or-break-reason-list .orgId=${org_id} .client=${this._client!}></or-break-reason-list>`; }}
      { path: '/orgs/:org_id/break-reasons/new', render: ({org_id}) => html`<or-break-reason-form .orgId=${org_id} .client=${this._client!}></or-break-reason-form>` }
      { path: '/orgs/:org_id/break-reasons/:id', render: ({org_id, id}) => html`<or-break-reason-detail .orgId=${org_id} .entityId=${id} .client=${this._client!}></or-break-reason-detail>` }
    Keep all other entity placeholder routes unchanged.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- --reporter=verbose --testPathPattern=break-reason 2>&1 | tail -20 && pnpm --filter @open-routing/admin build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - code field renders or-code-input readonly=true (D04_1-02)
    - routable field is sl-switch with correct helper text
    - display_order is sl-input type="number" with correct helper text
    - 409 uses error.current; or-conflict-banner renders (Pitfall 9 avoided)
    - routable defaults to true in create form
    - catalog-shell.ts has break-reason routes wired (grep or-break-reason-list exits 0)
    - All 9 Vitest behaviors pass (5 detail + 4 form)
    - pnpm --filter @open-routing/admin build exits 0
  </acceptance_criteria>
  <done>BreakReasons CRUD complete; routable+display_order fields correct; shell routes wired; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routable boolean → status panel | Break reasons feed or-status-panel; routable flag controls interaction routing during break |
| display_order integer → break picker | Integer ordering; server sanitizes; client validates non-negative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Tampering | routable field tampered via DevTools to wrong value | mitigate | ajv validateCreateBreakReason/validateUpdateBreakReason enforces boolean type; server validates independently; no operational bypass possible (routing decision at server) |
| T-06-09-02 | Tampering | display_order set to negative integer | mitigate | sl-input min="0" prevents negative in UX; ajv minimum:0 constraint; server validates; UI shows inline error on 422 |
| T-06-09-03 | Information Disclosure | 409 error.current exposes break reason version state | accept | Break reason data is org-scoped; X-Org-Id enforced; admin already has read access |
| T-06-09-SC | Tampering | No new npm installs in this plan | accept | Uses packages from Plan 06-01 legitimacy audit |
</threat_model>

<verification>
After both tasks:
- pnpm --filter @open-routing/ui test -- --testPathPattern=break-reason exits 0 (≥9 tests)
- grep "routable" web/packages/ui/src/components/break-reasons/break-reason-list.ts exits 0
- grep "or-break-reason-list" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- grep "validateUpdateBreakReason" web/packages/ui/src/components/break-reasons/break-reason-detail.ts exits 0
- pnpm --filter @open-routing/admin build exits 0
</verification>

<success_criteria>
1. or-break-reason-list renders routable (✓/✗), display_order (right-aligned), and column header tooltip
2. or-break-reason-detail routable as sl-switch (default true) + display_order integer + 409 via error.current
3. or-break-reason-form single-step wizard; routable defaults true; display_order required
4. catalog-shell.ts routes /break-reasons, /break-reasons/new, /break-reasons/:id to real components
5. All 9 unit tests pass; pnpm --filter @open-routing/admin build exits 0
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-09-SUMMARY.md` when done
</output>
