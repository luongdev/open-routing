---
phase: 06-shared-ui-library-standalone-admin
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/packages/ui/src/components/primitives/data-table.ts
  - web/packages/ui/src/components/primitives/cursor-paginator.ts
  - web/packages/ui/src/components/primitives/conflict-banner.ts
  - web/packages/ui/src/components/primitives/form-wizard.ts
  - web/packages/ui/src/components/primitives/code-input.ts
  - web/packages/ui/src/components/primitives/data-table.test.ts
  - web/packages/ui/src/components/primitives/conflict-banner.test.ts
  - web/packages/ui/src/components/primitives/index.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "<or-data-table> renders a <table> element with sticky <thead> and row click events"
    - "<or-cursor-paginator> shows Previous (disabled when cursorStack empty) and Next (disabled when hasMore false)"
    - "<or-conflict-banner mode='crud'> renders amber banner with serverValue diff and two action buttons"
    - "<or-conflict-banner mode='status'> renders invalid-transition copy with Refresh CTA"
    - "<or-form-wizard> renders stepper with step circles; single-step entities hide stepper"
    - "<or-code-input> enforces regex ^[a-z][a-z0-9_]{0,63}$ via setCustomValidity on validation"
    - "CI workflow has gen:validators drift gate and admin build smoke"
  artifacts:
    - path: "web/packages/ui/src/components/primitives/data-table.ts"
      provides: "reusable table for all 6 entity list pages and import result"
    - path: "web/packages/ui/src/components/primitives/conflict-banner.ts"
      provides: "409 CRUD and status inline banner per D6-03/D6-04"
    - path: "web/packages/ui/src/components/primitives/form-wizard.ts"
      provides: "multi-step wizard for Agent + Channel; single-step for others"
    - path: "web/packages/ui/src/components/primitives/code-input.ts"
      provides: "Phase 04.1 code field validator with regex enforcement"
  key_links:
    - from: "conflict-banner.ts"
      to: "or-agent-detail.ts (Wave 2)"
      via: "mode='crud' + serverValue + userValue properties"
      pattern: "or-conflict-banner"
    - from: "form-wizard.ts"
      to: "agent-form.ts (Wave 2)"
      via: "steps array property + wizard-completed event"
      pattern: "or-form-wizard"
---

<objective>
Create all shared primitive components that entity components depend on: `<or-data-table>`, `<or-cursor-paginator>`, `<or-conflict-banner>`, `<or-form-wizard>`, `<or-code-input>`. Also wire the CI drift gate (D6-31). These are Wave 1 because all entity plans (Wave 2+) import them.

Purpose: Shared primitives must exist before any entity component code. Conflict-banner (ADMIN-05) is testable independently of entity components. CI gate (D6-31) catches codegen drift on every PR.
Output: 5 primitive components with tests; CI extended with gen:validators + admin build.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md

<interfaces>
<!-- or-data-table API from UI-SPEC §5.10 -->
interface OrDataTable extends LitElement {
  @property({ type: Array }) columns: Array<{ key: string; label: string; width?: string; align?: 'left'|'right'|'center'; render?: (row: object) => TemplateResult }>;
  @property({ type: Array }) rows: object[];
  @property({ type: Boolean }) loading: boolean;
  // events: or-row-click CustomEvent<{row: object}>, or-row-action CustomEvent<{row: object, action: string}>
}

<!-- or-conflict-banner API from UI-SPEC §5.7 + CONTEXT D6-03 -->
interface OrConflictBanner extends LitElement {
  @property({ type: String }) mode: 'crud' | 'status';
  @property({ type: Object }) serverValue: object | { from: string; to: string };
  @property({ type: Object }) userValue: object;
  // events: open-routing:conflict-acknowledged CustomEvent<{ action: 'review' | 'discard' }>
}

<!-- or-form-wizard API from UI-SPEC §5.5 -->
interface OrFormWizard extends LitElement {
  @property({ type: Array }) steps: Array<{ key: string; label: string }>;
  @property({ type: Number }) currentStep: number;
  // events: open-routing:wizard-step-changed, open-routing:wizard-completed
}

<!-- or-code-input API from UI-SPEC §5.10 + CONTEXT D04_1-03 -->
// regex: ^[a-z][a-z0-9_]{0,63}$
// Wraps sl-input with pattern + setCustomValidity enforcement

<!-- or-cursor-paginator API from UI-SPEC §5.10 -->
interface OrCursorPaginator extends LitElement {
  @property({ type: Boolean }) hasMore: boolean;
  @property({ type: Array }) cursorStack: string[];
  @property({ type: Number }) limit: number;
  // events: or-page-changed CustomEvent<{ cursor: string|null, direction: 'next'|'prev', limit: number }>
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create or-data-table, or-cursor-paginator, or-code-input primitives</name>
  <files>
    web/packages/ui/src/components/primitives/data-table.ts
    web/packages/ui/src/components/primitives/cursor-paginator.ts
    web/packages/ui/src/components/primitives/code-input.ts
    web/packages/ui/src/components/primitives/data-table.test.ts
    web/packages/ui/src/components/primitives/index.ts
  </files>
  <read_first>
    - UI-SPEC §5.3 (data table header styling — --or-color-sidebar-bg background, sticky thead, row hover, row click navigates, code cell uses --or-text-code)
    - UI-SPEC §5.3 (row context menu [⋮] — sl-dropdown with sl-menu, Edit/Disable/Enable/Delete items)
    - UI-SPEC §5.10 (or-cursor-paginator — Previous disabled when stack empty; Next disabled when hasMore false; limit picker 10/25/50/100)
    - UI-SPEC §5.10 (or-code-input — wraps sl-input, pattern attr + setCustomValidity, valid/invalid border state)
    - CONTEXT.md D04_1-03 (code field regex: ^[a-z][a-z0-9_]{0,63}$)
    - RESEARCH.md §4 (Shoelace readiness — await customElements.whenDefined before accessing .value)
  </read_first>
  <behavior>
    - Test: or-data-table with rows=[{code:'a',name:'b'}] renders <tbody> with one <tr>
    - Test: or-data-table with loading=true renders [data-testid="loading"] element
    - Test: or-data-table row click dispatches 'or-row-click' with detail.row equal to the row object
    - Test: or-cursor-paginator with hasMore=false renders Next button as disabled
    - Test: or-cursor-paginator Previous button emits or-page-changed with direction='prev'
    - Test: or-code-input with value='valid_code' (matches regex) passes validation (no error state)
    - Test: or-code-input with value='123invalid' (starts with digit — fails regex) fires setCustomValidity with error message
  </behavior>
  <action>
    Create web/packages/ui/src/components/primitives/data-table.ts:
    @customElement('or-data-table') class OrDataTable extends LitElement.
    Shoelace imports: '@shoelace-style/shoelace/dist/components/dropdown/dropdown.js', '@shoelace-style/shoelace/dist/components/menu/menu.js', '@shoelace-style/shoelace/dist/components/menu-item/menu-item.js', '@shoelace-style/shoelace/dist/components/spinner/spinner.js', '@shoelace-style/shoelace/dist/components/icon/icon.js'
    Properties: columns (Array), rows (Array), loading (Boolean, default false).
    Template: when loading, render data-testid="loading" with skeleton rows. When loaded, render <table> with <thead><tr><th>…</th></tr></thead> and <tbody>.
    Row click: each <tr> has @click that dispatches 'or-row-click' CustomEvent({ detail: { row }, bubbles: true, composed: true }).
    Context menu [⋮] per row: last cell is a sl-dropdown with sl-menu sl-menu-items for Edit/Disable/Enable/Delete.
    CSS: thead sticky (position:sticky; top:0; z-index: var(--or-z-sticky, 10)); row hover background var(--or-color-row-hover, #fafafa); code cells use font-family: var(--or-font-mono), color: var(--or-color-code-fg).

    Create web/packages/ui/src/components/primitives/cursor-paginator.ts:
    @customElement('or-cursor-paginator') class OrCursorPaginator extends LitElement.
    Shoelace imports: '@shoelace-style/shoelace/dist/components/button/button.js', '@shoelace-style/shoelace/dist/components/select/select.js', '@shoelace-style/shoelace/dist/components/option/option.js'
    Properties: hasMore (Boolean), cursorStack (Array<string>, default []), limit (Number, default 25).
    @state() private _pageNum = 1 (incremented on next, decremented on prev).
    Previous button: disabled when cursorStack.length === 0. Click → pop last cursor from stack, emit 'or-page-changed' { cursor: poppedCursor, direction: 'prev', limit }.
    Next button: disabled when !hasMore. Click → push current position, emit 'or-page-changed' { cursor: NEXT_CURSOR_placeholder, direction: 'next', limit }.
    NOTE: cursorStack management is in the parent component (list). Paginator just emits events.
    Limit picker: sl-select size="small" with options 10/25/50/100; change emits 'or-page-changed' { cursor: null (reset), limit: newLimit }.
    Page indicator: "Page {_pageNum}".

    Create web/packages/ui/src/components/primitives/code-input.ts:
    @customElement('or-code-input') class OrCodeInput extends LitElement.
    Shoelace imports: '@shoelace-style/shoelace/dist/components/input/input.js'
    Properties: value (String), label (String), helperText (String), readonly (Boolean, default false), required (Boolean).
    const CODE_PATTERN = /^[a-z][a-z0-9_]{0,63}$/;
    const CODE_ERROR_MSG = 'Code must start with a lowercase letter and contain only lowercase letters, digits, and underscores (max 64 chars)';
    validate(): return CODE_PATTERN.test(this.value) — sets sl-input setCustomValidity('') for valid or setCustomValidity(CODE_ERROR_MSG) for invalid.
    Expose validate() as a public method so form components can call it.
    Events: 'or-code-input' CustomEvent({ detail: { value, valid }, bubbles: true, composed: true }) on every change.
    Read-only state (D04_1-02): when readonly=true, sl-input is disabled + shows lock icon (sl-icon name="lock") as suffix + helper text "Code cannot be changed after create." overrides helperText.

    Create data-table.test.ts with 3 tests + cursor-paginator tests (2) + code-input tests (2) — 7 tests total in data-table.test.ts file covering all three primitives (or split into separate test files per component).

    Update primitives/index.ts:
    export { OrOrgPicker } from './org-picker.js';
    export { OrDataTable } from './data-table.js';
    export { OrCursorPaginator } from './cursor-paginator.js';
    export { OrCodeInput } from './code-input.js';
    (OrConflictBanner and OrFormWizard added in Task 2)
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- data-table 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - data-table.ts: OrDataTable renders <table>, <thead>, <tbody>, dispatches 'or-row-click' on row click
    - cursor-paginator.ts: Previous button has disabled attribute when cursorStack is empty
    - code-input.ts: CODE_PATTERN is /^[a-z][a-z0-9_]{0,63}$/
    - code-input.ts: readonly=true renders sl-input with disabled attribute + lock icon
    - pnpm --filter @open-routing/ui test exits 0 (all primitive tests pass)
  </acceptance_criteria>
  <done>or-data-table, or-cursor-paginator, or-code-input primitives created with passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create or-conflict-banner, or-form-wizard, and extend CI workflow</name>
  <files>
    web/packages/ui/src/components/primitives/conflict-banner.ts
    web/packages/ui/src/components/primitives/form-wizard.ts
    web/packages/ui/src/components/primitives/conflict-banner.test.ts
    web/packages/ui/src/components/primitives/index.ts
    .github/workflows/ci.yml
  </files>
  <read_first>
    - UI-SPEC §5.7 (or-conflict-banner — crud mode: amber banner, diff table, Review+Discard buttons; status mode: transition copy, Refresh CTA; aria-live assertive on first appearance)
    - UI-SPEC §5.5 (or-form-wizard — stepper layout, step circles 24px, connector line, per-entity step config, single-step hides stepper)
    - CONTEXT.md D6-03 (409 handling — consume current/from+to from error body directly; no re-GET)
    - CONTEXT.md D6-04 (auto-update + inline banner UX — not toast/modal)
    - CONTEXT.md D6-31 (CI extension — gen:api + gen:validators drift gate + admin build smoke)
    - RESEARCH.md §5 (gen:validators drift gate command: pnpm --filter @open-routing/ui gen:api && pnpm --filter @open-routing/ui gen:validators && git diff --exit-code)
  </read_first>
  <behavior>
    - Test: or-conflict-banner mode='crud' renders element with background var(--or-color-conflict-bg)
    - Test: mode='crud' with serverValue={name:'Alice N'} and userValue={name:'Alice'} renders 'name' in diff section
    - Test: mode='status' with serverValue={from:'Engaged',to:'NotReady'} renders text 'Engaged' and 'NotReady'
    - Test: clicking 'Review and re-submit' dispatches 'open-routing:conflict-acknowledged' with detail.action='review'
    - Test: clicking 'Discard my changes' dispatches 'open-routing:conflict-acknowledged' with detail.action='discard'
  </behavior>
  <action>
    Create web/packages/ui/src/components/primitives/conflict-banner.ts:
    @customElement('or-conflict-banner') class OrConflictBanner extends LitElement.
    Shoelace imports: '@shoelace-style/shoelace/dist/components/alert/alert.js', '@shoelace-style/shoelace/dist/components/button/button.js', '@shoelace-style/shoelace/dist/components/icon/icon.js', '@shoelace-style/shoelace/dist/components/details/details.js'
    Properties:
    - @property({ type: String }) mode: 'crud' | 'status' = 'crud'
    - @property({ type: Object }) serverValue: Record<string, unknown> = {}
    - @property({ type: Object }) userValue: Record<string, unknown> = {}
    - @property({ type: Boolean, attribute: 'show-diff' }) showDiff = true

    CSS: background var(--or-color-conflict-bg, #fef3c7); border 2px solid var(--or-color-conflict-border, #f59e0b); border-radius var(--or-radius-md, 4px); padding 16px; margin-bottom 24px.
    aria-live="assertive" on the host element.

    CRUD mode render:
    Heading: "This {entity} was changed elsewhere" (generic — entity name from serverValue or label property if needed).
    Body: "Your edits are below — review the diff and re-submit, or discard your changes and load the server's version."
    Diff section: for each key in serverValue where serverValue[key] !== userValue[key], render a <details open> showing "Server now: {serverValue[key]}" (--or-color-diff-removed bg) and "Your edit: {userValue[key]}" (--or-color-diff-added bg).
    Buttons: [Review and re-submit] (primary) + [Discard my changes] (text, danger color).
    Button handlers: dispatch 'open-routing:conflict-acknowledged' CustomEvent({ detail: { action: 'review'|'discard' }, bubbles: true, composed: true }).

    Status mode render:
    Heading: "Transition isn't allowed"
    Body (template using from/to from serverValue): "Status is now {serverValue.from}. Your request to go to {serverValue.to} isn't allowed from {serverValue.from}. Wait for wrap-up to end, or use Force transition (admin)."
    Button: [Refresh transitions] (primary). Dispatches 'open-routing:conflict-acknowledged' { action: 'review' }.

    Create web/packages/ui/src/components/primitives/form-wizard.ts:
    @customElement('or-form-wizard') class OrFormWizard extends LitElement.
    Shoelace imports: '@shoelace-style/shoelace/dist/components/button/button.js', '@shoelace-style/shoelace/dist/components/dialog/dialog.js', '@shoelace-style/shoelace/dist/components/icon/icon.js'
    Properties:
    - @property({ type: Array }) steps: Array<{ key: string; label: string }> = []
    - @property({ type: Number }) currentStep = 0
    @state() private _dirty = false

    Single-step detection: steps.length <= 1 → hide stepper, show simple form layout.
    Multi-step stepper: renders step circles (24px) connected by lines per UI-SPEC §5.5.
    Each step: completed (filled primary-500 background + check icon), current (primary-500 border + step number), future (muted border + step number).
    Navigation buttons: Back (hidden on step 0), Next/Create (primary). On click Next: dispatch 'open-routing:wizard-step-changed'; on final step Next becomes 'Create {entity}' and dispatches 'open-routing:wizard-completed'.
    Cancel flow: if _dirty, dispatch event for parent to handle confirm dialog.
    Slot-based content: <slot name="step-{key}"> for each step's form content.

    Create conflict-banner.test.ts with 5 tests per behavior block.

    Update primitives/index.ts to add:
    export { OrConflictBanner } from './conflict-banner.js';
    export { OrFormWizard } from './form-wizard.js';

    Update .github/workflows/ci.yml:
    Find the existing 'frontend-typecheck-lint-unit' job (or equivalent frontend job).
    After the existing frontend steps, add:
    - name: "Gen API + validators drift gate"
      run: |
        pnpm --filter @open-routing/ui gen:api
        pnpm --filter @open-routing/ui gen:validators
        git diff --exit-code -- web/packages/ui/src/api/generated.ts web/packages/ui/src/validators/
    - name: "Admin production build smoke"
      run: pnpm --filter @open-routing/admin build
    Keep existing steps intact; only append the two new steps.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- conflict-banner 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - conflict-banner.ts: OrConflictBanner has aria-live="assertive" attribute
    - conflict-banner.ts: mode='crud' renders two buttons Review and Discard
    - conflict-banner.ts: mode='status' renders single Refresh button
    - 'open-routing:conflict-acknowledged' event has bubbles:true and composed:true
    - conflict-banner.test.ts: 5 tests pass
    - form-wizard.ts: steps.length <= 1 hides stepper visual
    - .github/workflows/ci.yml: contains 'gen:validators' and 'pnpm --filter @open-routing/admin build'
  </acceptance_criteria>
  <done>or-conflict-banner with CRUD+status modes tested; or-form-wizard with stepper; CI drift gate + admin build extended</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 409 error body → conflict-banner | Server-provided entity data rendered in the browser |
| CI workflow → generated files | gen:validators must not be injectable (Babel/ajv compile is deterministic from spec) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | XSS via conflict banner diff display | mitigate | Lit html`` template auto-escapes all interpolated values from serverValue/userValue; never pass raw HTML strings |
| T-06-04-02 | Tampering | CSV upload via or-import-page (future) | mitigate | Client sends file verbatim; server validates; no client-side CSV parsing in v0.1 (CONTEXT deferred) |
| T-06-04-03 | Elevation of Privilege | CSP eval risk from ajv standalone | accept | ajv standalone validators are pre-compiled static code — no eval() at runtime; CSP-safe by design |
</threat_model>

<verification>
After both tasks complete:
- pnpm --filter @open-routing/ui test exits 0 (all primitive tests pass including conflict-banner)
- grep "or-conflict-banner" .github/workflows/ci.yml exits non-zero (not in CI — but gen:validators IS in CI)
- grep "gen:validators" .github/workflows/ci.yml exits 0
- grep "@open-routing/admin build" .github/workflows/ci.yml exits 0
- pnpm --filter @open-routing/admin build exits 0
</verification>

<success_criteria>
1. OrConflictBanner renders CRUD 409 diff with per-field server-vs-user comparison
2. OrConflictBanner renders status 409 with from/to copy and Refresh CTA
3. 5 conflict-banner tests pass; 7 data-table/paginator/code-input tests pass
4. OrFormWizard renders stepper for multi-step; no stepper for single-step
5. CI workflow extended: gen:validators drift gate + admin build smoke
6. pnpm --filter @open-routing/ui test exits 0 (all 12+ primitive tests)
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-04-SUMMARY.md` when done
</output>
