---
phase: 06-shared-ui-library-standalone-admin
plan: 14
type: execute
wave: 5
depends_on:
  - "06-11"
  - "06-12"
  - "06-13"
files_modified:
  - web/packages/ui/src/components/shell/catalog-shell.ts
  - web/packages/ui/package.json
  - web/apps/admin/package.json
  - .github/workflows/ci.yml
  - web/packages/ui/src/index.ts
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06

must_haves:
  truths:
    - "packages/ui exports all 21 components via src/index.ts barrel"
    - "pnpm --filter @open-routing/ui test exits 0 (all unit tests across all plans)"
    - "pnpm --filter @open-routing/admin build exits 0 (production Vite bundle)"
    - "CI drift gate: pnpm gen:api && pnpm gen:validators && git diff --exit-code passes"
    - "packages/ui package.json exports map includes all subpath entries"
    - "catalog-shell sidebar has all 8 nav entries with correct icons per UI-SPEC §11.8"
    - "theme toggle cycles or-light → or-dark → or-brand and persists to localStorage"
    - "locale toggle switches EN ↔ VI and persists to localStorage"
  artifacts:
    - path: "web/packages/ui/src/index.ts"
      provides: "full barrel export: all 21+ components + API utilities + validators + themes"
    - path: ".github/workflows/ci.yml"
      provides: "CI with gen:api + gen:validators drift gate + admin build smoke"
    - path: "web/packages/ui/package.json"
      provides: "exports map with all subpaths"
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "complete shell with all 6 entities + status + import routes; sidebar 8 entries"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "gen:api + gen:validators"
      via: "pnpm run in frontend-typecheck-lint-unit job; git diff --exit-code drift gate"
      pattern: "gen:validators.*exit-code"
    - from: "packages/ui src/index.ts"
      to: "all component files"
      via: "export * from each entity + status + imports + primitives"
      pattern: "export.*components"
---

<objective>
Final integration plan: audit all 21 components are properly exported, verify the full unit test suite passes, wire any remaining shell gaps, update CI with drift gate + build smoke, and run the cross-entity verification. This plan closes Phase 6.

Purpose: Ensures no component is accidentally unreachable. CI enforcement locks in codegen discipline for Phase 7. The human checkpoint verifies the full admin flow end-to-end before marking phase done.
Output: Complete packages/ui with all exports; CI updated; admin built and verified by human.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@.planning/ROADMAP.md

Prior plan outputs (all summaries — for completeness check):
@.planning/phases/06-shared-ui-library-standalone-admin/06-01-SUMMARY.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-06-SUMMARY.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-11-SUMMARY.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix all component exports; update CI with drift gate + admin build</name>
  <files>
    web/packages/ui/src/index.ts
    web/packages/ui/package.json
    web/apps/admin/package.json
    .github/workflows/ci.yml
  </files>
  <read_first>
    - 06-01-SUMMARY.md (what scripts were added to packages/ui package.json — test, gen:api, gen:validators)
    - CONTEXT.md D6-31 (CI additions: gen:api + gen:validators drift gate + admin build smoke — NOT E2E Playwright in CI)
    - RESEARCH.md §1 "Standard Stack" (pnpm workspace; turbo pipeline)
    - web/turbo.json (existing pipeline for gen:api; need gen:validators + build tasks)
    - .github/workflows/ci.yml (existing structure — add to frontend-typecheck-lint-unit job)
    - 06-02-SUMMARY.md (gen:validators script name: pnpm --filter @open-routing/ui gen:validators)
  </read_first>
  <action>
    --- Complete barrel export audit ---

    Read web/packages/ui/src/index.ts. Verify it exports from:
    - ./api/client.js (createApiClient, ApiClient)
    - ./api/errors.js (ApiError, ErrorCodes, isApiError, parseApiError)
    - ./api/task.js (createApiTask)
    - ./components/agents/index.js (OrAgentList, OrAgentDetail, OrAgentForm)
    - ./components/skills/index.js (OrSkillList, OrSkillDetail, OrSkillForm)
    - ./components/queues/index.js (OrQueueList, OrQueueDetail, OrQueueForm)
    - ./components/channels/index.js (OrChannelList, OrChannelDetail, OrChannelForm)
    - ./components/adapters/index.js (OrAdapterList, OrAdapterDetail, OrAdapterForm)
    - ./components/break-reasons/index.js (OrBreakReasonList, OrBreakReasonDetail, OrBreakReasonForm)
    - ./components/status/index.js (OrStatusPanel)
    - ./components/imports/index.js (OrImportPage, OrImportResult)
    - ./components/shell/index.js (OrCatalogShell)
    - ./components/primitives/index.js (OrDataTable, OrCursorPaginator, OrCodeInput, OrConflictBanner, OrFormWizard, OrOrgPicker, OrQueuePicker)
    - ./validators/index.js (all validateCreate*, validateUpdate* exports)
    - ./themes/index.js (orLight, orDark, orBrand, ThemeName, Theme)
    - ./locales/locale-codes.js

    Add any missing exports. Ensure all use .js extensions (ESM resolution).

    --- packages/ui package.json exports map ---

    Verify exports map covers:
    - "." → ./src/index.ts (or dist/index.js for publish)
    - "./components" → ./src/components/index.ts
    - "./validators" → ./src/validators/index.ts
    - "./themes" → ./src/themes/index.ts
    - "./api" → ./src/api/index.ts (create if missing)

    --- CI drift gate + admin build smoke ---

    Read .github/workflows/ci.yml. Find the frontend-typecheck-lint-unit job. After existing steps, add:

    1. Drift gate step:
    ```yaml
    - name: Check gen:api + gen:validators drift
      working-directory: web
      run: |
        pnpm --filter @open-routing/ui run gen:api
        pnpm --filter @open-routing/ui run gen:validators
        git diff --exit-code web/packages/ui/src/api/generated.ts web/packages/ui/src/validators/
    ```
    Error message on failure: "Generated files are out of sync. Run pnpm gen:api && pnpm gen:validators and commit the results."

    2. Admin production build smoke:
    ```yaml
    - name: Admin production build smoke
      working-directory: web
      run: pnpm --filter @open-routing/admin build
    ```

    NOTE: Do NOT add Playwright to CI (D6-31 explicitly defers E2E to Phase 7).

    --- Turbo pipeline update ---

    Read web/turbo.json. Ensure the pipeline includes:
    - gen:validators (inputs: openapi/openapi.yaml, outputs: web/packages/ui/src/validators/**)
    - build for @open-routing/admin (dependsOn: ^build)

    Add if missing.

    --- apps/admin/package.json scripts audit ---

    Read web/apps/admin/package.json. Verify scripts include:
    - "dev": "vite"
    - "build": "tsc && vite build"
    - "preview": "vite preview"
    - "test": "vitest run" (unit tests if any in admin app)
    - "test:e2e": "playwright test"

    Add "test:e2e" if missing (D6-30 smoke test is in web/apps/admin/e2e/).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test 2>&1 | tail -10 && pnpm --filter @open-routing/admin build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/packages/ui/src/index.ts exports all 21 entity components + primitives + utilities + validators + themes
    - .github/workflows/ci.yml has gen:api + gen:validators drift gate step
    - .github/workflows/ci.yml has admin build smoke step
    - web/apps/admin/package.json has "test:e2e": "playwright test"
    - pnpm --filter @open-routing/ui test exits 0
    - pnpm --filter @open-routing/admin build exits 0
  </acceptance_criteria>
  <done>All exports complete; CI drift gate + admin build smoke added; full test suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of full admin SPA</name>
  <files></files>
  <what-built>
    Phase 6 complete admin SPA:
    - All 6 entity CRUD flows (agents, skills, queues, channels, adapters, break-reasons)
    - Agent status panel with 5s polling, break reason picker, force transition
    - Bulk import 3-step wizard + result page with 207 partial display
    - Theme toggle (light/dark/brand) + locale toggle (EN/VI)
    - Sidebar with 8 nav entries (6 entities + Bulk Import + Agent Status)
    - org-picker at / with UUIDv7 validation
  </what-built>
  <how-to-verify>
    Start the Go API and Vite dev server (two terminals):
      Terminal 1: cd services/api && task dev  (starts API on :8080)
      Terminal 2: pnpm --filter @open-routing/admin dev  (starts Vite on :5173)

    Navigate to http://localhost:5173

    **Tier 1 — Core navigation (required):**
    1. org-picker page renders with "Open Routing" heading + UUIDv7 input
    2. Enter invalid UUID → error shows on submit (not on keystroke)
    3. Enter valid test org UUID → navigates to /orgs/{id}/agents
    4. Sidebar renders 8 nav entries: Agents, Skills, Queues, Channels, Adapters, Break Reasons, Bulk Import, Agent Status
    5. Clicking each sidebar entry navigates to the correct route (verify URL changes)
    6. Browser back button works (returns to previous route)

    **Tier 2 — Entity CRUD (spot-check 2 entities):**
    7. Agents list: search input debounces (type quickly, one request fires after ~300ms)
    8. Create agent: 3-step wizard (Basics → Skills → Review); wizard stepper visible
    9. Skills list: renders skill_type column as chip/text; no config column
    10. Create skill: single-step wizard (no stepper); "Create skill" header

    **Tier 3 — Theme and locale:**
    11. Theme toggle in top bar: click cycles through light → dark → brand; page re-themes visually
    12. Refreshing page retains selected theme (localStorage persist)
    13. Locale toggle: click switches language strings in sidebar (Agents → Nhân viên for VI)

    **Tier 4 — Build verification:**
    14. pnpm --filter @open-routing/admin build exits 0 (production bundle compiles)
    15. pnpm --filter @open-routing/ui test exits 0 (all unit tests pass)

    Expected: Tiers 1-2 fully functional; Tier 3 visual only; Tier 4 automated green.
    If Go API is not running: Tier 2 entity lists will show error state (OK — structure verified by unit tests).
  </how-to-verify>
  <resume-signal>Type "approved" when Tiers 1 (all 6) + 4 (both automated checks) pass, even if API is not running. Describe any Tier 2/3 issues found. Phase 6 ships on your approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI drift gate → committed generated files | Generator runs in CI; git diff catches drift |
| Admin build output → deploy | pnpm build smoke confirms bundle compiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14-01 | Tampering | Generated file drift (API/validator out of sync with openapi.yaml) | mitigate | CI drift gate: pnpm gen:api + gen:validators + git diff --exit-code; PR blocked on drift |
| T-06-14-02 | Information Disclosure | Bundle includes dev-only code in production build | mitigate | Vite production mode strips import.meta.env.DEV; pnpm build smoke confirms no build errors |
| T-06-14-03 | Spoofing | localStorage theme/locale manipulated | accept | Theme/locale are UX preferences only; no auth data stored in localStorage; org_id in URL (not localStorage) |
| T-06-14-SC | Tampering | No new npm installs in this plan | accept | Uses packages from Plan 06-01 legitimacy audit |
</threat_model>

<verification>
After Task 1 (automated):
- pnpm --filter @open-routing/ui test exits 0
- pnpm --filter @open-routing/admin build exits 0
- grep "gen:validators" .github/workflows/ci.yml exits 0
- grep "git diff --exit-code" .github/workflows/ci.yml exits 0
- grep "@open-routing/admin build" .github/workflows/ci.yml exits 0

After Task 2 (human approved):
- Phase 6 marked complete in .planning/STATE.md
- ROADMAP.md Phase 6 plans all checked [x]
</verification>

<success_criteria>
1. packages/ui exports all 21 entity components (6×3) + 7 primitives + shell + status + imports
2. Full unit test suite (all plans) passes: pnpm --filter @open-routing/ui test exits 0
3. Production build passes: pnpm --filter @open-routing/admin build exits 0
4. CI drift gate added and enforced (gen:api + gen:validators + git diff)
5. Human verifies: org-picker → sidebar navigation → theme/locale toggle → browser back works
6. Phase 6 complete
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-14-SUMMARY.md` when done.
Then update .planning/STATE.md: mark Phase 6 shipped, record date 2026-05-18.
</output>
