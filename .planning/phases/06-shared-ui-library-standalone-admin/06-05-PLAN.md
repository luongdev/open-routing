---
phase: 06-shared-ui-library-standalone-admin
plan: 05
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - web/packages/ui/src/components/agents/agent-list.ts
  - web/packages/ui/src/components/agents/agent-detail.ts
  - web/packages/ui/src/components/agents/agent-form.ts
  - web/packages/ui/src/components/agents/agent-list.test.ts
  - web/packages/ui/src/components/agents/agent-detail.test.ts
  - web/packages/ui/src/components/agents/agent-form.test.ts
  - web/packages/ui/src/components/agents/index.ts
  - web/packages/ui/src/components/index.ts
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-03
  - ADMIN-05
  - CAT-01
  - CAT-03
  - CAT-08
  - STATE-08

must_haves:
  truths:
    - "<or-agent-list> fetches GET /v1/orgs/{org_id}/agents and renders rows in or-data-table"
    - "<or-agent-list> shows loading skeleton when task is pending"
    - "<or-agent-list> debounces name search 300ms and resets cursor per D6-V-11"
    - "<or-agent-detail> fetches agent by id and renders code as read-only with lock icon per D04_1-02"
    - "<or-agent-detail> on PATCH 409 renders or-conflict-banner with server current entity per D6-03"
    - "<or-agent-detail> skills sub-table: PATCH body includes skills array (empty = remove all per OQ-1A)"
    - "<or-agent-form> wizard create flow: 3 steps (Basics → Skills → Review)"
    - "pnpm --filter @open-routing/ui test exits 0 covering all agent component tests"
  artifacts:
    - path: "web/packages/ui/src/components/agents/agent-list.ts"
      provides: "<or-agent-list> with or-data-table, or-cursor-paginator, search, include-disabled toggle, Refresh"
    - path: "web/packages/ui/src/components/agents/agent-detail.ts"
      provides: "<or-agent-detail> 2-col layout: basic form (640px) + skills sub-table (flex 1)"
    - path: "web/packages/ui/src/components/agents/agent-form.ts"
      provides: "<or-agent-form> 3-step wizard via or-form-wizard for Agent create"
  key_links:
    - from: "agent-detail.ts"
      to: "or-conflict-banner"
      via: "409 response error.current consumed without re-GET per D6-03 + RESEARCH Pitfall 9"
      pattern: "or-conflict-banner"
    - from: "agent-list.ts"
      to: "createApiClient() GET /v1/orgs/{org_id}/agents"
      via: "@lit/task Task with args [orgId, search, cursor, includeDisabled]"
      pattern: "GET.*agents"
    - from: "agent-form.ts"
      to: "validateCreateAgent from validators/CreateAgentRequest.ts"
      via: "ajv-standalone validator called in wizard step Next handler"
      pattern: "validateCreateAgent"
---

<objective>
Build the complete Agents entity UI as the Wave 2 exemplar: list page (or-agent-list), detail/edit page (or-agent-detail with 2-column layout + skills sub-table), and 3-step wizard create (or-agent-form). This validates the entity-component pattern end-to-end; Waves 3-4 copy it.

Purpose: Agents is the most complex entity (skills sub-table, 3-step wizard, status panel link). Success here proves the pattern works for all 6 entities.
Output: 3 Lit components + 3 test files; agents entity fully functional in admin SPA.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-PATTERNS.md
@web/packages/ui/src/api/client.ts
@web/packages/ui/src/api/errors.ts
@web/packages/ui/src/api/task.ts

<interfaces>
<!-- From RESEARCH.md §3: Lit component pattern -->
import { LitElement, html, css } from 'lit';
import { customElement, property, state } from 'lit/decorators.js';
import { Task } from '@lit/task';
import type { ApiClient } from '../../api/client.js';
import type { components } from '../../api/generated.js';

type Agent = components['schemas']['Agent'];
type AgentStatus = components['schemas']['AgentStatus'];

<!-- From RESEARCH.md §3: component receives client as @property -->
@property({ type: Object }) client!: ApiClient;
// Shell constructs ONE client at boot; passes it as property to all entity components

<!-- From RESEARCH.md §9: 409 body consumption (Pitfall 9) -->
// openapi-fetch returns { data, error, response }
// For 409: check error field — openapi-fetch parses 4xx as error
// if (error && 'current' in error) { /* version_conflict */ }
// if (error && 'from' in error && 'to' in error) { /* invalid_transition */ }

<!-- From RESEARCH.md §12: component test pattern -->
const el = document.createElement('or-agent-list') as any;
el.orgId = 'test-org-id';
el.client = { GET: vi.fn().mockResolvedValue({ data: { items: [], has_more: false }, error: null }) };
document.body.appendChild(el);
await el.updateComplete;
// assert shadowRoot content

<!-- Agent entity columns per UI-SPEC §5.3 -->
Agents: code (mono), name, email, enabled (✓/✗), updated_at (relative + tooltip), [⋮]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create or-agent-list with data-table, search, pagination, and tests</name>
  <files>
    web/packages/ui/src/components/agents/agent-list.ts
    web/packages/ui/src/components/agents/agent-list.test.ts
  </files>
  <read_first>
    - RESEARCH.md §3 (full agent-list.ts component pattern — exact Task args pattern, pending/complete/error rendering)
    - RESEARCH.md §8 (Pitfall 8: stale closures in Task args — always reference reactive properties directly in args())
    - UI-SPEC §5.3 (list page layout — display heading, filter row, data table, pagination; per UI-SPEC column set for Agents: code/name/email/enabled/updated_at/⋮)
    - UI-SPEC §5.3 (search 300ms debounce, include-disabled checkbox, Refresh icon-button, skeleton loading state)
    - UI-SPEC §7.1 (loading pattern: skeleton rows 5 items; subsequent fetch: progress bar + buttons disabled)
    - PATTERNS.md row: agent-list.ts (Task pattern from api/task.ts)
  </read_first>
  <behavior>
    - Test: or-agent-list with mocked client.GET resolving {data:{items:[{code:'emp_0042',name:'Alice',email:'a@b.com',enabled:true,updated_at:'2026-05-17T00:00:00Z'}],has_more:false}} renders row with 'emp_0042'
    - Test: or-agent-list with client.GET returning pending Promise shows [data-testid="loading"]
    - Test: or-agent-list dispatches navigation when row is clicked (or-row-click from data-table)
    - Test: or-agent-list search input changes refetch with name param (check client.GET called with query.name)
  </behavior>
  <action>
    Create web/packages/ui/src/components/agents/agent-list.ts:

    @customElement('or-agent-list') class OrAgentList extends LitElement.

    Shoelace imports: input, checkbox, icon-button, button, badge, spinner, alert, select, option, tooltip, dropdown, menu, menu-item, icon.

    Properties:
    @property({ type: String, attribute: 'org-id' }) orgId = ''
    @property({ type: Object }) client!: ApiClient

    @state() private _search = ''
    @state() private _cursor: string | null = null
    @state() private _cursorStack: string[] = []
    @state() private _includeDisabled = false
    @state() private _limit = 25
    @state() private _searchDebounce?: ReturnType<typeof setTimeout>

    private _listTask = new Task(this, {
      task: async ([orgId, search, cursor, includeDisabled, limit]) => {
        const { data, error } = await this.client.GET('/v1/orgs/{org_id}/agents', {
          params: { path: { org_id: orgId }, query: { name: search || undefined, cursor: cursor ?? undefined, include_disabled: includeDisabled, limit } }
        });
        if (error) throw error;
        return data;
      },
      args: () => [this.orgId, this._search, this._cursor, this._includeDisabled, this._limit] as const,
    });

    Search handler: debounced 300ms; on change clear cursor + cursorStack (reset to page 1).
    Include-disabled toggle: clear cursor + cursorStack on change.
    Refresh button: call this._listTask.run() explicitly.

    Column definitions for agent list per UI-SPEC §5.3:
    [
      { key: 'code', label: 'code', render: (row) => html`<code class="code-cell">${row.code}</code>` },
      { key: 'name', label: 'name' },
      { key: 'email', label: 'email' },
      { key: 'enabled', label: 'enabled', render: (row) => row.enabled ? html`<sl-icon name="check-lg" style="color:var(--sl-color-success-500)"></sl-icon>` : html`<sl-icon name="x-lg" style="color:var(--or-color-text-muted)"></sl-icon>` },
      { key: 'updated_at', label: 'updated', render: (row) => html`<sl-tooltip content="${row.updated_at}"><span>${this._relativeTime(row.updated_at)}</span></sl-tooltip>` },
    ]

    _relativeTime(iso: string): string — simple relative time (e.g., "2h ago", "3d ago") without external library.

    Render structure (per UI-SPEC §5.3):
    - Page heading "Agents" (--or-text-display) + [+ Create agent] primary button (navigates)
    - Filter row: search sl-input (300ms debounce) + include-disabled sl-checkbox + Refresh sl-icon-button
    - this._listTask.render({ pending, complete, error })
    - complete: or-data-table with columns + rows; or-cursor-paginator below
    - pending: or-data-table with loading=true
    - error: sl-alert variant="danger" with error reason + request_id + Retry button

    Pagination: listen for 'or-page-changed' on or-cursor-paginator; update _cursor / _cursorStack / _limit.

    Empty states:
    - No items (no search): "No agents yet. Click [+ Create agent] to add your first." + CTA button
    - No items (search active): "No agents found matching '{_search}'." + [Clear search] button

    Navigation: clicking [+ Create agent] dispatches 'open-routing:navigate' CustomEvent with path '/orgs/{orgId}/agents/new'. Row click also dispatches 'open-routing:navigate'. Shell listens for this event and calls _routes.goto().
    NOTE: Components do NOT directly call _routes.goto() — they dispatch navigation events; shell wires them.

    Create agent-list.test.ts with 4 tests per the behavior block.
    Mock client: { GET: vi.fn() }. Use mockResolvedValue for data, mockReturnValue(new Promise(() => {})) for pending.
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- agent-list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - agent-list.ts exports class OrAgentList extends LitElement with @customElement('or-agent-list')
    - Task args function returns tuple with all 5 reactive state values as const
    - agent-list.ts does NOT call fetch() directly (ADMIN-04 — only client.GET)
    - Search debounce is 300ms (setTimeout with 300 in the handler)
    - pnpm --filter @open-routing/ui test -- agent-list exits 0 with 4 passing tests
  </acceptance_criteria>
  <done>OrAgentList with Task-driven API, search debounce, pagination, 4 tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create or-agent-detail (2-col layout + skills sub-table + 409) and or-agent-form wizard</name>
  <files>
    web/packages/ui/src/components/agents/agent-detail.ts
    web/packages/ui/src/components/agents/agent-form.ts
    web/packages/ui/src/components/agents/agent-detail.test.ts
    web/packages/ui/src/components/agents/agent-form.test.ts
    web/packages/ui/src/components/agents/index.ts
    web/packages/ui/src/components/index.ts
  </files>
  <read_first>
    - UI-SPEC §5.4.1 (agent-detail 2-column layout ≥1280px: 640px basic form + flex-1 skills sub-table; mobile/tablet stacks below)
    - UI-SPEC §5.4 (save flow steps 1-8; cancel flow dirty check; 409 conflict banner render; 404 state; footer metadata bar)
    - UI-SPEC §5.5 (wizard form for Agent: 3 steps — Basics/Skills/Review; per-entity step config table)
    - RESEARCH.md §7 (409 UX — CRUD body has error.current; Pitfall 9: check error field not response.json())
    - CONTEXT.md D04_1-02 (code is immutable — render as or-code-input with readonly=true + helper text "Code cannot be changed after create.")
    - CONTEXT.md D6-17 (wizard create: multi-step for Agent — Basics → Skills → Review)
    - RESEARCH.md §5 (form validation: import validateCreateAgent from '../../validators/CreateAgentRequest.js'; call before API)
  </read_first>
  <behavior>
    - Test: or-agent-detail with mocked client.GET returns agent, renders code field as read-only
    - Test: or-agent-detail PATCH with 409 response { error:'version_conflict', current:{name:'NewName',...} } shows or-conflict-banner with serverValue containing NewName
    - Test: or-agent-detail Save button is disabled until form is dirty
    - Test: or-agent-form step 1 validation — empty code field prevents Next (shows or-code-input error)
    - Test: or-agent-form on wizard-completed dispatches POST to client.POST /v1/orgs/{org_id}/agents
  </behavior>
  <action>
    Create web/packages/ui/src/components/agents/agent-detail.ts:

    @customElement('or-agent-detail') class OrAgentDetail extends LitElement.
    Shoelace imports: input, switch, button, icon, icon-button, spinner, alert, tooltip, dialog, select, option, badge.
    Properties: orgId, entityId (from attribute 'entity-id'), client.
    @state() private _entity: Agent | null, _loading, _saving, _dirty, _conflictServer: Agent | null, _fieldErrors, _deleteConfirmOpen.

    GET task: fetch /v1/orgs/{org_id}/agents/{id} on mount.
    Form state: _form mirrors entity fields; marking _dirty on any change (compare to loaded _entity).

    Layout: 2-column grid at ≥1280px: left 640px (form), right flex-1 (skills sub-table). CSS Grid or Flexbox.
    Form fields in left column (per UI-SPEC §5.4.1):
    - code: <or-code-input .readonly=${true} .value=${_entity.code}> — ALWAYS read-only after create (D04_1-02)
    - name: sl-input required
    - email: sl-input type="email"
    - external_id: sl-input (optional)
    - enabled: sl-switch
    Footer metadata: "version {N} · updated {relative} · created {date}" in muted text.
    Bottom bar: [Cancel] + [Save changes] (disabled when !_dirty or _saving)

    Skills sub-table in right column:
    Header: "Skills". Table: skill name | proficiency select (sl-select size="small" options 1-10) | remove button (sl-icon-button name="x-lg").
    [+ Add skill ▾] button: opens sl-dropdown with searchable list of org skills (debounced search to GET /v1/orgs/{org_id}/skills, max 100). Default proficiency=5 on add.
    Helper text: "Tip: changes here save with the form."

    Save flow per UI-SPEC §5.4:
    1. client-side ajv validate (import validateUpdateAgent from '../../validators/UpdateAgentRequest.js')
    2. PATCH with body { code (excluded — read only), name, email, external_id, enabled, version, skills: [{skill_id, proficiency}] }
    3. On 2xx: toast "Saved" + refresh form state
    4. On 409 version_conflict: const conflictError = error; if ('current' in conflictError) { _conflictServer = conflictError.current; } — render or-conflict-banner
    5. On 422: inline field errors
    6. On 5xx: danger alert with retry

    Delete: opens sl-dialog with confirm "Delete agent {agent.name}?"; on confirm → DELETE endpoint → dispatch 'open-routing:entity-deleted' + navigate back.

    Conflict banner: when _conflictServer is not null, render <or-conflict-banner mode="crud" .serverValue=${_conflictServer} .userValue=${_form}> above the form. Listen for 'open-routing:conflict-acknowledged' to handle review/discard.

    Create web/packages/ui/src/components/agents/agent-form.ts:
    @customElement('or-agent-form') class OrAgentForm extends LitElement.
    Uses or-form-wizard with steps: [{key:'basics',label:'Basics'},{key:'skills',label:'Skills'},{key:'review',label:'Review'}].
    Step 1 (Basics): or-code-input for code, sl-input for name/email/external_id, sl-switch for enabled.
    Step 2 (Skills): initial skill assignments (same UI as detail skills sub-table but for a new agent).
    Step 3 (Review): key-value summary of all entered data.
    On wizard-completed: client-side validate all fields with validateCreateAgent; POST to /v1/orgs/{org_id}/agents; on 201 dispatch 'open-routing:entity-created' + navigate to /orgs/{orgId}/agents/{newId}.
    On 409 duplicate_code: return to step 1 + inline error on code field.

    Create agent-detail.test.ts: 3 tests per behavior (readonly code, 409 banner, Save disabled).
    Create agent-form.test.ts: 2 tests per behavior (step 1 validation, POST on complete).

    Create agents/index.ts: export { OrAgentList } from './agent-list.js'; export { OrAgentDetail } from './agent-detail.js'; export { OrAgentForm } from './agent-form.js';

    Update src/components/index.ts:
    export * from './agents/index.js';
    export * from './primitives/index.js';
    // (other entities added by Waves 3-4 plans)
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- agent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - agent-detail.ts: code field renders <or-code-input .readonly=${true}> always (D04_1-02)
    - agent-detail.ts: on 409, _conflictServer is set from error.current WITHOUT a second GET (per D6-03 + Pitfall 9)
    - agent-detail.ts: includes 'import validateUpdateAgent' from validators
    - agent-form.ts: uses or-form-wizard with 3 steps (basics, skills, review)
    - agent-form.ts: includes 'import validateCreateAgent' from validators
    - pnpm --filter @open-routing/ui test exits 0 covering all 9 agent tests (4 list + 3 detail + 2 form)
    - grep -c "fetch(" web/packages/ui/src/components/agents/agent-list.ts returns 0 (raw fetch forbidden ADMIN-04)
  </acceptance_criteria>
  <done>Complete Agents entity UI: list + detail (2-col + 409) + 3-step wizard; 9 tests passing; pattern established for Wave 3-4 entities</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server 409 body → conflict-banner display | Server-provided current entity state rendered in browser; must not allow XSS |
| Client form data → PATCH body | User input must be validated before API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | XSS via 409 current entity in conflict-banner | mitigate | Lit html`` auto-escapes; all interpolated values from serverValue are string-typed and Lit-escaped |
| T-06-05-02 | Spoofing | org_id path tampering in API calls | mitigate | org_id sourced from router params (already UUIDv7-validated by shell); server enforces X-Org-Id |
| T-06-05-03 | Tampering | form input bypassing validation | mitigate | ajv-standalone validateCreateAgent/validateUpdateAgent called before every POST/PATCH; server is authoritative |
</threat_model>

<verification>
After both tasks complete:
- pnpm --filter @open-routing/ui test exits 0 covering all agent tests
- grep "readonly" web/packages/ui/src/components/agents/agent-detail.ts exits 0 (code field is readonly)
- grep -c "response.json()" web/packages/ui/src/components/agents/agent-detail.ts returns 0 (Pitfall 9 avoided)
- grep "or-conflict-banner" web/packages/ui/src/components/agents/agent-detail.ts exits 0
- pnpm --filter @open-routing/admin build exits 0
</verification>

<success_criteria>
1. OrAgentList renders agent rows from API, debounces search, handles pagination
2. OrAgentDetail renders 2-column layout; code field always read-only; 409 shows conflict-banner with server's current data (no re-GET)
3. OrAgentForm 3-step wizard: Step 1 validates with ajv before advancing; POST on complete
4. All 9 agent tests (4 list + 3 detail + 2 form) pass
5. Raw fetch() is NOT used anywhere (ADMIN-04 ESLint rule)
6. Pattern established for Wave 3-4 entity copy
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-05-SUMMARY.md` when done
</output>
