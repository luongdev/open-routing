---
phase: 06-shared-ui-library-standalone-admin
plan: 06
type: execute
wave: 2
depends_on:
  - "06-03"
  - "06-05"
files_modified:
  - web/packages/ui/src/components/shell/catalog-shell.ts
  - web/apps/admin/playwright.config.ts
  - web/apps/admin/e2e/smoke.spec.ts
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03

must_haves:
  truths:
    - "shell router replaces agent route placeholders with real OrAgentList/OrAgentDetail/OrAgentForm components"
    - "shell passes client instance (constructed at boot from URL org_id) as .client property to every entity component"
    - "browser back button from agent detail returns to agent list (history.pushState works in shadow DOM)"
    - "playwright.config.ts exists; pnpm --filter @open-routing/admin test:e2e --list shows smoke test"
  artifacts:
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "updated router with real agent components + client bootstrap from URL org_id"
    - path: "web/apps/admin/playwright.config.ts"
      provides: "Playwright config targeting localhost:5173"
    - path: "web/apps/admin/e2e/smoke.spec.ts"
      provides: "smoke test: nav to /, enter org_id, navigate agents, create agent, see in list"
  key_links:
    - from: "catalog-shell.ts"
      to: "createApiClient"
      via: "client constructed at connectedCallback with getOrgId reading from _currentOrgId state"
      pattern: "createApiClient"
    - from: "shell.ts"
      to: "OrAgentList, OrAgentDetail, OrAgentForm"
      via: "import and render in route config"
      pattern: "or-agent-list.*or-agent-detail"
---

<objective>
Wire the agent routes in the shell router with real components, bootstrap the API client from the URL org_id, and create the Playwright smoke test. This plan also connects the 'open-routing:navigate' event (dispatched by entity components) to shell._routes.goto().

Purpose: Without client bootstrap + route wiring, agent components render but have no API connectivity. The Playwright smoke test is the end-to-end validation gate.
Output: Working agents end-to-end; shell creates and passes client; smoke test runnable locally.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@web/packages/ui/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire agent routes in shell + bootstrap createApiClient from URL org_id</name>
  <files>
    web/packages/ui/src/components/shell/catalog-shell.ts
  </files>
  <read_first>
    - RESEARCH.md §2 (@lit-labs/router route config with render functions that receive params)
    - CONTEXT.md D6-09 (org_id from URL path → createApiClient getOrgId; one client at boot)
    - RESEARCH.md §2 (route guard pattern with enter() returning false to abort)
    - web/packages/ui/src/api/client.ts (createApiClient factory — getOrgId is called PER REQUEST)
    - PATTERNS.md row: web/apps/admin/src/index.ts (client is constructed in shell, not in index.ts)
    - RESEARCH.md §3 (component receives client as @property — shell constructs, passes down)
  </read_first>
  <action>
    Update web/packages/ui/src/components/shell/catalog-shell.ts to wire real agent routes:

    1. Import OrAgentList, OrAgentDetail, OrAgentForm from '../agents/index.js'
    2. Import createApiClient, type ApiClient from '../../api/client.js'
    3. Add @state() private _client: ApiClient | null = null
    4. Add @state() private _currentOrgId = ''

    Client bootstrap (in connectedCallback, after super.connectedCallback()):
    Read initial org from URL path: parse window.location.pathname for /orgs/:org_id/ segment.
    If found and valid UUIDv7: set _currentOrgId; create _client = createApiClient({ baseURL: '', getOrgId: () => this._currentOrgId }).
    Also listen for popstate to update _currentOrgId when user navigates back.

    Update Routes array — replace agent placeholder routes with real components:
    { path: '/orgs/:org_id/agents', render: ({org_id}) => {
        this._currentOrgId = org_id ?? '';
        return html`<or-agent-list .orgId=${org_id} .client=${this._client!}></or-agent-list>`;
    }}
    { path: '/orgs/:org_id/agents/new', render: ({org_id}) => html`<or-agent-form .orgId=${org_id} .client=${this._client!}></or-agent-form>` }
    { path: '/orgs/:org_id/agents/:id', render: ({org_id, id}) => html`<or-agent-detail .orgId=${org_id} .entityId=${id} .client=${this._client!}></or-agent-detail>` }
    { path: '/orgs/:org_id/agents/:id/status', render: ({org_id, id}) => html`<or-status-panel .orgId=${org_id} .agentId=${id} .client=${this._client!}></or-status-panel>` }
    Keep placeholder divs for other entities (filled by Wave 3-4 plans).

    Add 'open-routing:navigate' event listener (in connectedCallback):
    this.addEventListener('open-routing:navigate', (e: CustomEvent) => {
      this._routes.goto(e.detail.path);
    });

    Add 'open-routing:org-selected' event listener (in connectedCallback) to handle org-picker submission:
    this.addEventListener('open-routing:org-selected', (e: CustomEvent) => {
      const orgId = e.detail.orgId as string;
      this._currentOrgId = orgId;
      this._client = createApiClient({ baseURL: '', getOrgId: () => this._currentOrgId });
      this._routes.goto(`/orgs/${orgId}/agents`);
    });

    Update top bar "Switch org" button handler: navigate to '/' and clear _client + _currentOrgId.

    Ensure all entity routes update _currentOrgId when matched (so getOrgId() returns the right value if org changed via URL).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/admin build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - catalog-shell.ts imports createApiClient from '../../api/client.js'
    - catalog-shell.ts has _client: ApiClient state and creates it via createApiClient()
    - getOrgId function in createApiClient reads from _currentOrgId (not captured at creation)
    - Agent routes render OrAgentList/OrAgentDetail/OrAgentForm with .client property
    - 'open-routing:navigate' event listener calls this._routes.goto(e.detail.path)
    - pnpm --filter @open-routing/admin build exits 0
    - grep "createApiClient" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
  </acceptance_criteria>
  <done>Shell router wired with real agent components; createApiClient bootstrapped from URL org_id; navigate events handled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playwright smoke test setup + manual verification of agents flow</name>
  <files>
    web/apps/admin/playwright.config.ts
    web/apps/admin/e2e/smoke.spec.ts
  </files>
  <read_first>
    - RESEARCH.md §13 (Playwright two-org isolation test pattern)
    - CONTEXT.md D6-30 (smoke test: navigate to /, enter test org_id, navigate agents, create agent, see in list)
    - RESEARCH.md Standard Stack table (@playwright/test 1.60.0 — already installed by Plan 06-01)
  </read_first>
  <what-built>
    Created playwright.config.ts targeting http://localhost:5173.
    Created e2e/smoke.spec.ts with one test: navigate to /, enter org_id, click Continue, navigate to agents list, click + Create agent, complete step 1 (code+name+email) + step 2 (no skills) + step 3 (review), click Create agent, assert agent appears in list.
    playwright.config.ts: webServer: { command: 'pnpm --filter @open-routing/admin dev', url: 'http://localhost:5173', reuseExistingServer: true }.
    Install Playwright browsers: pnpm exec playwright install chromium.
  </what-built>
  <how-to-verify>
    1. Start the Go API: task dev (in services/api directory)
    2. Start the Vite dev server: pnpm --filter @open-routing/admin dev
    3. In a third terminal: pnpm --filter @open-routing/admin test:e2e
    4. Verify Playwright opens Chrome, navigates to localhost:5173
    5. Verify org-picker page appears with "Open Routing" heading
    6. Verify entering a valid org_id UUID and clicking Continue navigates to /orgs/{org_id}/agents
    7. Verify agents list loads (may be empty — OK for smoke test)
    8. If Go API is running: verify [+ Create agent] navigates to wizard
    Expected: Test passes or shows clear failure about the flow step. Build artifacts in web/apps/admin/dist/ from the build check above should also be valid.
  </how-to-verify>
  <resume-signal>Type "approved" when smoke test runs (pass or fail-with-clear-output) and admin builds successfully, or describe blocking issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path org_id → createApiClient getOrgId | Client reads org_id from URL; server enforces via X-Org-Id header middleware |
| Playwright test → localhost:5173 | E2E smoke test hits dev server; not production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Spoofing | org_id from URL path used in getOrgId() | mitigate | Shell UUIDv7 route guard rejects malformed org_ids before client construction; server enforces X-Org-Id |
| T-06-06-02 | Information Disclosure | Playwright test org_id hardcoded | accept | Test org_id is a test fixture UUID; no production data; dev-only test |
</threat_model>

<verification>
After checkpoint approval:
- pnpm --filter @open-routing/admin build exits 0
- grep "createApiClient" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- grep "or-agent-list" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- web/apps/admin/playwright.config.ts exists
- web/apps/admin/e2e/smoke.spec.ts exists
</verification>

<success_criteria>
1. Shell router wires all 4 agent routes with real components + client
2. createApiClient constructed with getOrgId reading _currentOrgId state
3. 'open-routing:navigate' events from entity components reach shell._routes.goto()
4. Playwright config and smoke test exist; test runs locally
5. pnpm --filter @open-routing/admin build exits 0
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-06-SUMMARY.md` when done
</output>
