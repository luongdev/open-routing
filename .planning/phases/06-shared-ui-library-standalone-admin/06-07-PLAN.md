---
phase: 06-shared-ui-library-standalone-admin
plan: 07
type: execute
wave: 3
depends_on:
  - "06-05"
files_modified:
  - web/packages/ui/src/components/skills/skill-list.ts
  - web/packages/ui/src/components/skills/skill-list.test.ts
  - web/packages/ui/src/components/skills/skill-detail.ts
  - web/packages/ui/src/components/skills/skill-detail.test.ts
  - web/packages/ui/src/components/skills/skill-form.ts
  - web/packages/ui/src/components/skills/skill-form.test.ts
  - web/packages/ui/src/components/skills/index.ts
  - web/packages/ui/src/components/index.ts
  - web/packages/ui/src/components/shell/catalog-shell.ts
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "or-skill-list renders skills with columns: code, name, skill_type, enabled, updated_at, [⋮]"
    - "or-skill-detail form fields render in order: code (read-only), name, external_id, description, skill_type, enabled"
    - "or-skill-form single-step wizard (stepper hidden) posts CreateSkillRequest on complete"
    - "409 on skill PATCH delivers conflict banner with error.current fields (no re-GET)"
    - "all skill components dispatch open-routing:navigate; none call router directly"
    - "catalog-shell.ts routes /orgs/:org_id/skills, /skills/new, /skills/:id to real skill components"
  artifacts:
    - path: "web/packages/ui/src/components/skills/skill-list.ts"
      provides: "or-skill-list custom element"
      exports: ["OrSkillList"]
    - path: "web/packages/ui/src/components/skills/skill-detail.ts"
      provides: "or-skill-detail custom element"
      exports: ["OrSkillDetail"]
    - path: "web/packages/ui/src/components/skills/skill-form.ts"
      provides: "or-skill-form custom element"
      exports: ["OrSkillForm"]
    - path: "web/packages/ui/src/components/skills/index.ts"
      provides: "skills barrel export"
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "shell with skill routes wired"
      contains: "or-skill-list"
  key_links:
    - from: "skill-list.ts"
      to: "or-data-table"
      via: "renders inside or-data-table primitive"
      pattern: "or-data-table"
    - from: "skill-detail.ts"
      to: "validateUpdateSkill"
      via: "ajv validator imported from validators/index.js"
      pattern: "validateUpdateSkill"
    - from: "catalog-shell.ts"
      to: "OrSkillList, OrSkillDetail, OrSkillForm"
      via: "import and render in route config"
      pattern: "or-skill-list.*or-skill-detail"
---

<objective>
Build the Skills entity CRUD components — or-skill-list, or-skill-detail, or-skill-form — following the Agents exemplar pattern from Plan 06-05. Skills is the simplest entity (no sub-table, single-step wizard). Wire skill routes into the shell router.

Purpose: Skills is the canonical "simple entity" pattern — establishes the non-Agent template for Queues, BreakReasons, Channels, Adapters in Plans 08-11.
Output: Three skill Lit components + tests; skill routes in catalog-shell.ts.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@web/packages/ui/src/api/client.ts
@web/packages/ui/src/api/errors.ts

Prior plan outputs (read for pattern reference):
@.planning/phases/06-shared-ui-library-standalone-admin/06-05-SUMMARY.md
</context>

<interfaces>
<!-- Copy the agent component patterns from 06-05. Skills follow the same shape. -->

From web/packages/ui/src/components/agents/agent-list.ts (pattern reference):
```typescript
// or-agent-list → copy pattern for or-skill-list
@customElement('or-skill-list')
export class OrSkillList extends LitElement {
  @property({ type: String, attribute: 'org-id' }) orgId = '';
  @property({ attribute: false }) client!: ApiClient;
  @state() private _search = '';
  @state() private _cursor: string | null = null;
  @state() private _cursorStack: string[] = [];
  @state() private _includeDisabled = false;
  @state() private _limit = 25;
  // @lit/task Task for fetch
  // dispatches open-routing:navigate CustomEvent (bubbles: true, composed: true)
}
```

From web/packages/ui/src/validators/ (generated by Plan 06-02):
```typescript
export { validateCreateSkill } from './create-skill-request.js';
export { validateUpdateSkill } from './update-skill-request.js';
```

Skill API shape (from openapi.yaml, mirrored by generated types):
```typescript
interface Skill {
  id: string;
  org_id: string;
  code: string;         // read-only post-create
  name: string;
  external_id?: string;
  description?: string;
  skill_type: string;   // required
  enabled: boolean;
  version: number;
  created_at: string;
  updated_at: string;
}
interface CreateSkillRequest { code: string; name: string; external_id?: string; description?: string; skill_type: string; enabled?: boolean; }
interface UpdateSkillRequest { name?: string; external_id?: string; description?: string; skill_type?: string; enabled?: boolean; version: number; }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: or-skill-list — list with columns, search, pagination, context menu</name>
  <files>
    web/packages/ui/src/components/skills/skill-list.ts
    web/packages/ui/src/components/skills/skill-list.test.ts
    web/packages/ui/src/components/skills/index.ts
  </files>
  <read_first>
    - 06-05-SUMMARY.md (or-agent-list implementation — copy pattern)
    - UI-SPEC.md §5.3 "Per-entity column sets" — Skills row: code, name, skill_type, enabled, updated_at, [⋮]
    - UI-SPEC.md §5.3 "Search input behavior" — 300ms debounce, reset cursor, min 1 char
    - UI-SPEC.md §5.3 "States" — loading skeleton, populated, empty (no search), empty (with search), error
    - UI-SPEC.md §5.3 "Row context menu [⋮]" — sl-dropdown with Edit/Disable/Enable/Delete items
    - RESEARCH.md §3 (or-data-table primitive API — must use it, not raw table)
    - CONTEXT.md D6-25 (no periodic polling; Refresh button only)
    - CONTEXT.md D6-08 (per-component Shoelace imports — list them explicitly)
  </read_first>
  <behavior>
    - Test 1: renders "No skills yet" empty state when task resolves with items=[]
    - Test 2: renders 3 rows when task resolves with 3 skills; each row has code in monospace and skill_type chip
    - Test 3: search input debounces 300ms and resets cursor to null
    - Test 4: context menu [⋮] click on a row opens sl-dropdown with Edit/Disable/Enable/Delete options
    - Test 5: dispatches open-routing:navigate with path /orgs/{orgId}/skills/{id} on row click
    - Test 6: "No skills found matching 'xyz'" empty state when search active and items=[]
  </behavior>
  <action>
    Create web/packages/ui/src/components/skills/skill-list.ts following the or-agent-list pattern from Plan 06-05:

    @customElement('or-skill-list') class OrSkillList extends LitElement.

    Properties: orgId (String, attribute 'org-id'), client (attribute: false, type ApiClient).

    State: _search (string), _cursor (string|null), _cursorStack (string[]), _includeDisabled (boolean), _limit (25).

    Use @lit/task Task with args () => [this.orgId, this._search, this._cursor, this._includeDisabled, this._limit] to fetch GET /v1/orgs/{orgId}/skills with query params name=_search (omit if empty), cursor, include_disabled, limit. Call this.client.GET().

    Column set per UI-SPEC §5.3 D6-V-10: code (monospace, --or-text-code --or-color-code-fg), name, skill_type (plain text chip — inline span with small border-radius and --or-color-skeleton-base bg), enabled (sl-icon check-lg / x-lg), updated_at (relative time + tooltip), [⋮] context menu.

    Context menu [⋮]: sl-dropdown + sl-menu with items Edit → navigate to /orgs/{orgId}/skills/{id}, Disable → PATCH enabled=false (if currently enabled), Enable → PATCH enabled=true (if currently disabled), Delete → opens confirm modal (use window.confirm for v0.1).

    Search: sl-input with sl-icon name="search" prefix, 300ms debounce using setTimeout/clearTimeout, clears _cursor on change.

    Include disabled: sl-checkbox, resets _cursor.

    Refresh button: sl-icon-button name="arrow-clockwise", calls task.run().

    Pagination: or-cursor-paginator component from Plan 06-04 with .hasMore=${response.has_more} .cursorStack=${_cursorStack} events for page-prev and page-next.

    Navigation: on row click dispatch CustomEvent('open-routing:navigate', { detail: { path: `/orgs/${this.orgId}/skills/${item.id}` }, bubbles: true, composed: true }).

    [+ Create skill] CTA: sl-button variant="primary" at top-right, dispatches open-routing:navigate to /orgs/${this.orgId}/skills/new.

    Empty states (UI-SPEC §5.3):
    - No items + no search: "No skills yet" heading + "Define a skill to assign to agents." body + [+ Create skill] CTA
    - No items + search active: "No skills found matching '{_search}'." + [Clear search] button
    - Error: sl-alert variant="danger" with "Couldn't load skills. {error.reason}. Request ID: {request_id}. [Retry]"
    - Loading: 5-row skeleton using CSS shimmer animation

    Per-component Shoelace imports (D6-08):
    import '@shoelace-style/shoelace/dist/components/input/input.js'
    import '@shoelace-style/shoelace/dist/components/checkbox/checkbox.js'
    import '@shoelace-style/shoelace/dist/components/button/button.js'
    import '@shoelace-style/shoelace/dist/components/icon-button/icon-button.js'
    import '@shoelace-style/shoelace/dist/components/dropdown/dropdown.js'
    import '@shoelace-style/shoelace/dist/components/menu/menu.js'
    import '@shoelace-style/shoelace/dist/components/menu-item/menu-item.js'
    import '@shoelace-style/shoelace/dist/components/alert/alert.js'
    import '@shoelace-style/shoelace/dist/components/icon/icon.js'
    import '@shoelace-style/shoelace/dist/components/tooltip/tooltip.js'
    import '@shoelace-style/shoelace/dist/components/spinner/spinner.js'

    Write skill-list.test.ts using Vitest + happy-dom covering the 6 behaviors above. Use vi.fn() to mock client.GET. Import urlpattern-polyfill from test-setup only (do NOT import in component).

    Create web/packages/ui/src/components/skills/index.ts exporting OrSkillList, OrSkillDetail (re-export placeholder until Task 2), OrSkillForm (re-export placeholder until Task 2).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- --reporter=verbose --testPathPattern=skill-list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - or-skill-list renders columns: code (monospace), name, skill_type, enabled (✓/✗), updated_at, [⋮]
    - 300ms debounce search resets cursor to null on change
    - Row click dispatches open-routing:navigate with /orgs/{orgId}/skills/{id}
    - [+ Create skill] CTA dispatches open-routing:navigate with /orgs/{orgId}/skills/new
    - 409 context menu Disable/Enable fires PATCH enabled=false/true without confirmation
    - All 6 Vitest behaviors pass
    - No direct router import in skill-list.ts
  </acceptance_criteria>
  <done>or-skill-list renders correctly with all states; 6 unit tests pass; no router coupling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: or-skill-detail + or-skill-form (single-step wizard)</name>
  <files>
    web/packages/ui/src/components/skills/skill-detail.ts
    web/packages/ui/src/components/skills/skill-detail.test.ts
    web/packages/ui/src/components/skills/skill-form.ts
    web/packages/ui/src/components/skills/skill-form.test.ts
    web/packages/ui/src/components/skills/index.ts
    web/packages/ui/src/components/index.ts
    web/packages/ui/src/components/shell/catalog-shell.ts
  </files>
  <read_first>
    - 06-05-SUMMARY.md (or-agent-detail implementation — copy pattern, minus skills sub-table)
    - UI-SPEC.md §5.4 "Skill" field order: code(RO), name, external_id, description(textarea), skill_type, enabled
    - UI-SPEC.md §5.4 "Save flow" steps 1-8 — especially step 5 (409: extract error.current, preserve dirty edits, render conflict banner)
    - UI-SPEC.md §5.4 "Cancel flow" — dirty guard with sl-dialog confirm
    - UI-SPEC.md §5.4 "Footer metadata bar" D6-V-14: version · updated · created
    - UI-SPEC.md §5.5 "Single-step behavior" — stepper hidden; header "Create skill"; bottom nav Cancel + Create only
    - CONTEXT.md D6-03 (409: consume error field from openapi-fetch; do NOT call response.json() again — Pitfall 9)
    - CONTEXT.md D6-17 (single-step wizard for Skill — same or-form-wizard component, steps=[{key:'basics',label:'Basics'}])
    - RESEARCH.md §9 "Pitfall 9: 409 body consumption" — openapi-fetch already parsed; use `error` not `response.json()`
    - CONTEXT.md D04_1-02 (code field ALWAYS rendered as or-code-input .readonly=${true} in detail/edit)
  </read_first>
  <behavior>
    skill-detail tests:
    - Test 1: renders code field as or-code-input with readonly=true always (per D04_1-02)
    - Test 2: Save button disabled until form is dirty; enabled after user edits any field
    - Test 3: 409 response uses error.current (NOT response.json()) and renders or-conflict-banner without re-GET
    - Test 4: Cancel with dirty form opens sl-dialog "Discard your changes? Unsaved edits will be lost."
    - Test 5: successful PATCH shows toast "Saved" + updates form state from response body

    skill-form tests:
    - Test 1: renders single-step form (stepper hidden, header "Create skill")
    - Test 2: validateCreateSkill runs before POST; shows inline error if code fails ^[a-z][a-z0-9_]{0,63}$ pattern
    - Test 3: 409 duplicate_code shows inline error on code field; no navigation
    - Test 4: successful POST dispatches open-routing:navigate to /orgs/{orgId}/skills/{newId}
  </behavior>
  <action>
    --- or-skill-detail ---

    Create web/packages/ui/src/components/skills/skill-detail.ts:

    @customElement('or-skill-detail') class OrSkillDetail extends LitElement.

    Properties: orgId (attribute 'org-id'), entityId (attribute 'entity-id'), client (attribute: false).

    Load task: @lit/task fetching GET /v1/orgs/{orgId}/skills/{entityId} via this.client.GET.

    Form fields per UI-SPEC §5.4 Skill field order:
    1. code → <or-code-input .value=${skill.code} .readonly=${true}> always (D04_1-02; cite this decision)
    2. name → sl-input required
    3. external_id → sl-input optional, helper "Optional integration mapping. Pass empty to clear."
    4. description → sl-textarea optional
    5. skill_type → sl-input required (text input; skill_type is freeform string in API)
    6. enabled → sl-switch

    Dirty tracking: @state() private _dirty = false; compare current form values against loaded skill in _handleInput().

    Footer metadata bar (D6-V-14): single line below form — "version {N}   ·   updated {relative}   ·   created {YYYY-MM-DD}"; wrap "updated" in sl-tooltip showing ISO datetime.

    Top bar: [← Back to Skills] sl-button variant="text" with chevron-left icon dispatching open-routing:navigate to /orgs/{orgId}/skills. Spacer. [Delete] sl-button variant="default" text color --sl-color-danger-500 (confirm modal). [Disable]/[Enable] sl-button variant="default".

    Save flow (UI-SPEC §5.4 steps 1-8):
    1. Spinner + disable buttons
    2. ajv validateUpdateSkill (imported from '../../validators/index.js') — if fail, inline field errors, abort
    3. PATCH /v1/orgs/{orgId}/skills/{entityId} with { ...formValues, version: skill.version }
    4. 2xx → toast "Saved" (use sl-alert auto-dismiss 3s); update local state from response body
    5. 409 → const { error } = response; if ('current' in error): extract error.current fields, update form state IN PLACE, render or-conflict-banner mode='crud' with diff (D6-03, Pitfall 9 — do NOT call response.json())
    6. 422 immutable_field → toast "Code cannot be changed after create"; reset code field
    7. 422 invalid_value → inline field error
    8. 5xx → sl-alert variant="danger" at top of form with reason + request_id + [Retry]

    Cancel flow: if !_dirty navigate back; if _dirty open sl-dialog with "Discard your changes?" + Keep editing / Discard buttons.

    Per-component Shoelace imports: input, textarea, switch, button, select, option, dialog, alert, spinner, icon, icon-button, tooltip.

    --- or-skill-form ---

    Create web/packages/ui/src/components/skills/skill-form.ts:

    @customElement('or-skill-form') class OrSkillForm extends LitElement.

    Properties: orgId (attribute 'org-id'), client (attribute: false).

    Use or-form-wizard component with steps=[{key:'basics', label:'Basics'}] — single step causes wizard to hide stepper and show "Create skill" header with Cancel + Create buttons only (UI-SPEC §5.5).

    Form fields (UI-SPEC §5.5 D6-V-16 Skill): code (or-code-input), name (sl-input required), external_id (sl-input optional), description (sl-textarea optional), skill_type (sl-input required), enabled (sl-switch, default true).

    On wizard-completed event:
    1. validateCreateSkill (from validators) — inline errors if fail
    2. POST /v1/orgs/{orgId}/skills via this.client.POST
    3. 2xx → toast "Created skill" + dispatch open-routing:navigate to /orgs/${orgId}/skills/${newId}
    4. 409 duplicate_code → inline error on code field "This code is already in use."
    5. 5xx → sl-alert variant="danger" with retry

    --- Update index.ts and shell routing ---

    Update web/packages/ui/src/components/skills/index.ts to export OrSkillList, OrSkillDetail, OrSkillForm.

    Update web/packages/ui/src/components/index.ts: add export * from './skills/index.js'

    Update web/packages/ui/src/components/shell/catalog-shell.ts:
    - Import OrSkillList, OrSkillDetail, OrSkillForm from '../skills/index.js'
    - Replace skill placeholder routes with real renders:
      { path: '/orgs/:org_id/skills', render: ({org_id}) => { this._currentOrgId = org_id ?? ''; return html`<or-skill-list .orgId=${org_id} .client=${this._client!}></or-skill-list>`; }}
      { path: '/orgs/:org_id/skills/new', render: ({org_id}) => html`<or-skill-form .orgId=${org_id} .client=${this._client!}></or-skill-form>` }
      { path: '/orgs/:org_id/skills/:id', render: ({org_id, id}) => html`<or-skill-detail .orgId=${org_id} .entityId=${id} .client=${this._client!}></or-skill-detail>` }
    Keep all other entity placeholder routes unchanged (filled by Plans 08-11).
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- --reporter=verbose --testPathPattern=skill 2>&1 | tail -20 && pnpm --filter @open-routing/admin build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - or-skill-detail: code field always or-code-input readonly=true (D04_1-02)
    - or-skill-detail: 409 uses error.current without re-GET (Pitfall 9 avoided)
    - or-skill-detail: or-conflict-banner renders with diff on 409
    - or-skill-detail: cancel with dirty form opens sl-dialog confirm
    - or-skill-form: single-step wizard (stepper hidden); stepper count 1 hides stepper per UI-SPEC §5.5
    - or-skill-form: validateCreateSkill runs before POST
    - catalog-shell.ts has or-skill-list, or-skill-detail, or-skill-form route renders
    - grep "or-skill-list" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
    - pnpm --filter @open-routing/admin build exits 0
    - All 9 Vitest behaviors pass (5 detail + 4 form)
  </acceptance_criteria>
  <done>Skills CRUD components complete; shell routes wired; all tests pass; admin builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → skill form | skill_type is freeform text; code validated by or-code-input pattern |
| 409 error body → UI render | error.current parsed by openapi-fetch; rendered into form fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | skill_type freeform text field | mitigate | Server validates skill_type against allowed enum; ajv validateCreateSkill client-side; 422 shows inline error |
| T-06-07-02 | Information Disclosure | 409 error.current field values rendered into DOM | accept | Values are org-scoped server data already visible to the admin; no cross-org leak (server enforces X-Org-Id) |
| T-06-07-03 | Elevation of Privilege | or-skill-form accessible without valid org_id | mitigate | Shell UUIDv7 route guard (from Plan 06-03) rejects malformed org_ids before component renders |
| T-06-07-SC | Tampering | npm installs (no new packages in this plan) | accept | No new package installs; using packages from Plan 06-01 audit |
</threat_model>

<verification>
After both tasks complete:
- pnpm --filter @open-routing/ui test -- --testPathPattern=skill exits 0 (≥9 tests)
- grep "or-skill-list" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- grep "validateUpdateSkill" web/packages/ui/src/components/skills/skill-detail.ts exits 0
- grep "readonly" web/packages/ui/src/components/skills/skill-detail.ts exits 0
- pnpm --filter @open-routing/admin build exits 0
</verification>

<success_criteria>
1. or-skill-list renders code/name/skill_type/enabled/updated_at/[⋮] columns with correct states
2. or-skill-detail form fields in spec order; code always read-only; 409 handled via error.current without re-GET
3. or-skill-form single-step wizard (stepper hidden); validateCreateSkill fires before POST
4. catalog-shell.ts wires /skills, /skills/new, /skills/:id to real skill components
5. All 9 unit tests pass; pnpm --filter @open-routing/admin build exits 0
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-07-SUMMARY.md` when done
</output>
