---
phase: 06-shared-ui-library-standalone-admin
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/packages/ui/src/components/shell/catalog-shell.ts
  - web/packages/ui/src/components/shell/shell-chrome.ts
  - web/packages/ui/src/components/primitives/org-picker.ts
  - web/packages/ui/src/components/shell/catalog-shell.test.ts
  - web/packages/ui/src/components/primitives/org-picker.test.ts
  - web/packages/ui/src/components/shell/index.ts
  - web/packages/ui/src/components/primitives/index.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
  - ADMIN-06

must_haves:
  truths:
    - "<or-catalog-shell> registers as a custom element (customElements.get('or-catalog-shell') !== undefined)"
    - "shell has reactive @property theme:'or-light'|'or-dark'|'or-brand' that calls _applyTheme() on update"
    - "shell's _applyTheme sets CSS custom properties on this.style (not document.documentElement) per D6-20"
    - "shell has 8 sidebar nav entries per D6-11: Agents, Skills, Queues, Channels, Adapters, Break Reasons, separator, Bulk Import, Agent Status"
    - "<or-org-picker> validates org_id on submit (not on keystroke) per D6-V-09"
    - "shell theme test: setting theme='or-dark' changes shell.style.getPropertyValue('--sl-color-primary-500') to #4faab2"
    - "shell has @lit-labs/router Routes instance with all entity routes defined"
  artifacts:
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "<or-catalog-shell> with theme, org-id, modules, locale properties; @lit-labs/router Routes; sidebar; outlet"
    - path: "web/packages/ui/src/components/shell/shell-chrome.ts"
      provides: "sidebar + topbar internal components"
    - path: "web/packages/ui/src/components/primitives/org-picker.ts"
      provides: "<or-org-picker> with UUIDv7 validation on-submit, localStorage persist"
  key_links:
    - from: "catalog-shell.ts"
      to: "orDark / orBrand token maps"
      via: "_applyTheme() iterates token map and calls this.style.setProperty per D6-20"
      pattern: "this\\.style\\.setProperty"
    - from: "catalog-shell.ts"
      to: "@lit-labs/router Routes"
      via: "private _routes = new Routes(this, [...])"
      pattern: "@lit-labs/router"
---

<objective>
Create the `<or-catalog-shell>` page-level component with @lit-labs/router integration, sidebar nav (8 entries per D6-11), top bar with theme toggle (D6-19), UUIDv7 route guard (D6-13), and `<or-org-picker>` root route component. This is the mounting point for the entire admin SPA.

Purpose: Every Wave 2+ entity component gets rendered inside the shell's outlet. Theme runtime switching (ADMIN-06) is entirely shell-driven.
Output: Working shell skeleton that passes theme-switch unit test; org-picker with UUID validation.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-RESEARCH.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-UI-SPEC.md
@web/packages/ui/src/api/errors.ts

<interfaces>
<!-- From RESEARCH.md §2: @lit-labs/router API -->
import { Routes } from '@lit-labs/router';
// Routes constructor: new Routes(host: LitElement, routes: RouteConfig[])
// RouteConfig: { path: string, enter?: (params) => Promise<boolean>, render: (params) => TemplateResult }
// Navigation: this._routes.goto(path: string)
// Outlet: this._routes.outlet() — must be in the shell's own render() method (RESEARCH Pitfall 7)

<!-- From RESEARCH.md §8: Theme pattern -->
// orLight, orDark, orBrand are Record<string, string> token maps from themes/index.ts
// _applyTheme(): iterate resolved token map, call this.style.setProperty(key, val) on host
// CSS custom properties cascade into child shadow roots (confirmed per UI-SPEC §1)

<!-- From UI-SPEC §5.1: Shell API -->
interface OrCatalogShell extends LitElement {
  @property({ type: String, attribute: 'org-id' }) orgId: string;
  @property({ type: String }) theme: 'or-light' | 'or-dark' | 'or-brand';
  @property({ type: String }) modules: string;  // comma-separated entity filter
  @property({ type: String }) locale: 'en' | 'vi';
}

<!-- UUIDv7 regex from CONTEXT.md D6-13 -->
const UUIDV7_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create or-catalog-shell with theme system and router skeleton</name>
  <files>
    web/packages/ui/src/components/shell/catalog-shell.ts
    web/packages/ui/src/components/shell/shell-chrome.ts
    web/packages/ui/src/components/shell/catalog-shell.test.ts
    web/packages/ui/src/components/shell/index.ts
  </files>
  <read_first>
    - RESEARCH.md §2 (@lit-labs/router Routes constructor, outlet() placement — Pitfall 7: outlet MUST be in shell's own render())
    - RESEARCH.md §3 (Lit 3 component pattern — @customElement, @property, @state, Task)
    - RESEARCH.md §4 (Shoelace per-component imports — import icon/icon-button/button/button-group/select/option/tooltip/drawer)
    - RESEARCH.md §8 (theme _applyTheme implementation + CSS cascade across Shadow DOM)
    - UI-SPEC §5.1 (shell layout: 56px topbar, 248px sidebar, 8 nav entries, icon names per entry)
    - UI-SPEC §2.2 (exact theme token values — used in tests to assert correct property values)
    - CONTEXT.md D6-20 (theme on shell host — NOT document.documentElement)
    - CONTEXT.md D6-13 (UUIDv7 route guard pattern with enter() callback)
    - RESEARCH.md §12 (component test pattern with happy-dom + await el.updateComplete)
  </read_first>
  <behavior>
    - Test: creating OrCatalogShell in happy-dom, setting theme='or-dark', awaiting updateComplete → shell.style.getPropertyValue('--sl-color-primary-500') equals '#4faab2'
    - Test: creating OrCatalogShell with theme='or-brand' → shell.style.getPropertyValue('--sl-color-primary-500') equals '#0d8b96'
    - Test: OrCatalogShell renders sidebar with aria-label 'Navigation' containing links for Agents, Skills, Queues, Channels, Adapters, Break Reasons, Bulk Import, Agent Status
    - Test: modules='agents,skills' hides Queues, Channels, Adapters, Break Reasons sidebar entries
  </behavior>
  <action>
    Create web/packages/ui/src/components/shell/catalog-shell.ts:

    @customElement('or-catalog-shell') class OrCatalogShell extends LitElement.
    Shoelace imports (per-component, no barrel):
    - '@shoelace-style/shoelace/dist/components/icon/icon.js'
    - '@shoelace-style/shoelace/dist/components/icon-button/icon-button.js'
    - '@shoelace-style/shoelace/dist/components/button/button.js'
    - '@shoelace-style/shoelace/dist/components/button-group/button-group.js'
    - '@shoelace-style/shoelace/dist/components/select/select.js'
    - '@shoelace-style/shoelace/dist/components/option/option.js'
    - '@shoelace-style/shoelace/dist/components/tooltip/tooltip.js'
    - '@shoelace-style/shoelace/dist/components/drawer/drawer.js'

    Properties:
    @property({ type: String, attribute: 'org-id' }) orgId = ''
    @property({ type: String }) theme: 'or-light' | 'or-dark' | 'or-brand' = 'or-light'
    @property({ type: String }) modules = ''  // empty = show all
    @property({ type: String }) locale: 'en' | 'vi' = 'en'
    @state() private _sidebarOpen = false

    Theme system (_applyTheme method):
    - Import orLight, orDark, orBrand token maps from '../../themes/index.js'
    - Import type { ThemeName } from '../../themes/index.js'
    - _THEME_TOKENS: Record<ThemeName, Record<string, string>> = { 'or-light': orLight, 'or-dark': orDark, 'or-brand': orBrand }
    - _applyTheme(): const tokens = _THEME_TOKENS[this.theme] ?? {}
      First clear all prior tokens (iterate the union of all token keys in all maps, removeProperty each).
      Then apply: for (const [key, val] of Object.entries(tokens)) this.style.setProperty(key, val)
      Persist: localStorage.setItem('or-theme', this.theme)
    - In connectedCallback: restore from localStorage if valid ThemeName value exists
    - Override updated(changed): if changed.has('theme') call _applyTheme()

    Router (private _routes = new Routes(this, [...])):
    Routes array (skeleton — entity components registered in later plans, use html`<div>Loading...</div>` as placeholder):
    - { path: '/', render: () => html`<or-org-picker></or-org-picker>` }
    - { path: '/orgs/:org_id/*', enter: async ({org_id}) => { if (!UUIDV7_PATTERN.test(org_id ?? '')) { this._routes.goto('/'); return false; } return true; } }
    - { path: '/orgs/:org_id/agents', render: ({org_id}) => html`<div data-route="agents" data-org-id="${org_id}">Agents list</div>` }
    - { path: '/orgs/:org_id/agents/new', render: ({org_id}) => html`<div data-route="agents-new">Agent create</div>` }
    - { path: '/orgs/:org_id/agents/:id', render: ({org_id, id}) => html`<div data-route="agent-detail">Agent detail</div>` }
    - { path: '/orgs/:org_id/agents/:id/status', render: ({org_id, id}) => html`<div data-route="agent-status">Agent status</div>` }
    - (Same placeholder pattern for skills, queues, channels, adapters, break-reasons)
    - { path: '/orgs/:org_id/imports/new', render: ({org_id}) => html`<div data-route="imports-new">Import page</div>` }
    - { path: '/orgs/:org_id/imports/:id', render: ({org_id, id}) => html`<div data-route="import-result">Import result</div>` }

    Sidebar nav entries (UI-SPEC §5.1):
    Array of nav entries with visibility filter (modules property):
    const NAV_ENTRIES = [
      { key: 'agents', label: 'Agents', icon: 'people-fill', path: '/orgs/{orgId}/agents' },
      { key: 'skills', label: 'Skills', icon: 'tag-fill', path: '/orgs/{orgId}/skills' },
      { key: 'queues', label: 'Queues', icon: 'funnel-fill', path: '/orgs/{orgId}/queues' },
      { key: 'channels', label: 'Channels', icon: 'broadcast-pin', path: '/orgs/{orgId}/channels' },
      { key: 'adapters', label: 'Adapters', icon: 'plug-fill', path: '/orgs/{orgId}/adapters' },
      { key: 'break-reasons', label: 'Break Reasons', icon: 'pause-circle-fill', path: '/orgs/{orgId}/break-reasons' },
      { key: '__divider__', label: '', icon: '', path: '' },  // divider
      { key: 'imports', label: 'Bulk Import', icon: 'upload', path: '/orgs/{orgId}/imports/new' },
      { key: 'status', label: 'Agent Status', icon: 'circle-fill', path: '/orgs/{orgId}/agents/status' },
    ]
    Filter: when modules is non-empty, visible entries = split by comma + always show divider and status/imports sections.
    When modules is empty: show all.

    Layout CSS (Shadow DOM):
    :host { display: flex; flex-direction: column; height: 100vh; }
    .topbar { height: 56px; display: flex; align-items: center; background: var(--or-color-topbar-bg, #fff); border-bottom: 1px solid var(--or-color-divider, #e5e5e5); }
    .body { display: flex; flex: 1; overflow: hidden; }
    .sidebar { width: 248px; background: var(--or-color-sidebar-bg, #f5f5f5); border-right: 1px solid var(--or-color-divider, #e5e5e5); overflow-y: auto; }
    .content { flex: 1; overflow-y: auto; padding: 32px 32px 24px; }

    Render method:
    return html`
      <div class="topbar">
        <sl-icon-button name="list" class="hamburger-toggle" @click=${() => this._sidebarOpen = !this._sidebarOpen}></sl-icon-button>
        <span class="wordmark" style="font-weight:600;margin-left:8px;">Open Routing</span>
        <div style="flex:1"></div>
        <sl-tooltip content="${this.orgId}">
          <code class="org-chip" @click=${() => navigator.clipboard?.writeText(this.orgId)}>
            org: ${this.orgId.slice(0,8)}…
          </code>
        </sl-tooltip>
        <sl-button-group label="Theme" style="margin:0 8px;">
          <sl-icon-button name="sun" title="Light theme" @click=${() => this.theme = 'or-light'}></sl-icon-button>
          <sl-icon-button name="moon" title="Dark theme" @click=${() => this.theme = 'or-dark'}></sl-icon-button>
          <sl-icon-button name="palette" title="Brand theme" @click=${() => this.theme = 'or-brand'}></sl-icon-button>
        </sl-button-group>
        <sl-button variant="text" @click=${() => this._routes.goto('/')}>Switch org</sl-button>
      </div>
      <div class="body">
        <nav class="sidebar" aria-label="Navigation">
          ${this._renderNav()}
        </nav>
        <main class="content">
          ${this._routes.outlet()}
        </main>
      </div>
    `;

    Create web/packages/ui/src/components/shell/shell-chrome.ts: minimal file — re-exports OrCatalogShell (shell-chrome.ts was considered as a separate sidebar component; keep inline in catalog-shell.ts for simplicity; shell-chrome.ts just re-exports).

    Create catalog-shell.test.ts with 4 tests per the <behavior> block:
    - import 'urlpattern-polyfill' is in test-setup.ts so no explicit import needed
    - import './catalog-shell.js' to register the element
    - import { orDark, orBrand } from '../../themes/index.js'
    - Test 1 (theme dark): el.theme = 'or-dark'; await el.updateComplete; expect(el.style.getPropertyValue('--sl-color-primary-500')).toBe('#4faab2')
    - Test 2 (theme brand): el.theme = 'or-brand'; await el.updateComplete; expect(el.style.getPropertyValue('--sl-color-primary-500')).toBe('#0d8b96')
    - Test 3 (sidebar entries): el.orgId = 'test'; await el.updateComplete; nav = el.shadowRoot.querySelector('[aria-label="Navigation"]'); expect nav to contain text 'Agents' and 'Break Reasons'
    - Test 4 (modules filter): el.modules = 'agents,skills'; await el.updateComplete; check hidden entries (Queues etc.) are hidden

    Create shell/index.ts: export { OrCatalogShell } from './catalog-shell.js';
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- catalog-shell 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/shell/catalog-shell.ts exports class OrCatalogShell extends LitElement with @customElement('or-catalog-shell')
    - catalog-shell.ts imports from '@lit-labs/router' Routes
    - catalog-shell.ts has private _applyTheme() method that calls this.style.setProperty
    - catalog-shell.ts does NOT contain 'document.documentElement' (forbidden per RESEARCH Pitfall 10)
    - catalog-shell.test.ts: pnpm --filter @open-routing/ui test -- catalog-shell exits 0 with 4 passing tests
    - theme test: or-dark sets --sl-color-primary-500 to #4faab2 on shell.style
  </acceptance_criteria>
  <done>OrCatalogShell registered; theme runtime switching tested; router skeleton wired; 8-entry sidebar rendered</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create or-org-picker with UUIDv7 validation</name>
  <files>
    web/packages/ui/src/components/primitives/org-picker.ts
    web/packages/ui/src/components/primitives/org-picker.test.ts
    web/packages/ui/src/components/primitives/index.ts
  </files>
  <read_first>
    - UI-SPEC §5.2 (or-org-picker visual layout — 480px card, wordmark, subtitle, input, helper text, Continue button 44px height, last-used affordance)
    - UI-SPEC §5.2 (validation timing: ON-SUBMIT not on keystroke per D6-V-09)
    - CONTEXT.md D6-10 (org-picker: UUIDv7 input, last-used from localStorage, malformed → toast)
    - CONTEXT.md D6-13 (UUIDv7 regex: /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i)
    - RESEARCH.md §4 (Shoelace event protocol — sl-input fires on every keystroke; sl-change on committed change; .value for reading)
    - RESEARCH.md §5 (Shoelace readiness — await customElements.whenDefined('sl-input') in firstUpdated)
  </read_first>
  <behavior>
    - Test: clicking Continue with valid UUIDv7 dispatches 'open-routing:org-selected' CustomEvent with { orgId }
    - Test: clicking Continue with invalid UUID sets input error state (sl-input's helpText changes to validation error message) and does NOT dispatch event
    - Test: lastUsedOrgId property pre-fills sl-input value
    - Test: valid submit stores orgId in localStorage under key 'or-last-org-id'
  </behavior>
  <action>
    Create web/packages/ui/src/components/primitives/org-picker.ts:

    @customElement('or-org-picker') class OrOrgPicker extends LitElement.
    Shoelace imports:
    - '@shoelace-style/shoelace/dist/components/input/input.js'
    - '@shoelace-style/shoelace/dist/components/button/button.js'
    - '@shoelace-style/shoelace/dist/components/spinner/spinner.js'

    Properties:
    @property({ type: String, attribute: 'last-used-org-id' }) lastUsedOrgId = ''
    @state() private _value = ''
    @state() private _error = ''
    @state() private _submitting = false

    const UUIDV7_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;

    firstUpdated: set _value = lastUsedOrgId from localStorage 'or-last-org-id' || this.lastUsedOrgId

    _handleSubmit(): validation is ON-SUBMIT per D6-V-09.
    1. Trim _value
    2. If not matches UUIDV7_PATTERN: set _error = 'Not a valid UUIDv7. Format: 8-4-4-4-12 hex characters with version digit 7.'; return
    3. localStorage.setItem('or-last-org-id', trimmed)
    4. dispatch 'open-routing:org-selected' CustomEvent({ detail: { orgId: trimmed }, bubbles: true, composed: true })

    Visual layout per UI-SPEC §5.2:
    Card 480px wide, centered with flex on :host. Display card internals per spec layout.
    :host { display: flex; justify-content: center; align-items: center; min-height: 100vh; }
    Card has padding 32px, border-radius 8px (--or-radius-lg), border 1px (--or-color-card-border), shadow (--or-shadow-lg).
    Include:
    - wordmark "Open Routing" (24px weight 600)
    - subtitle "Standalone admin console" (14px muted)
    - form label "Organization ID"
    - sl-input with .value binding, @sl-input handler to update _value, ?help-text error on invalid, aria-label
    - helper text below input (when no error): "UUIDv7 format. Example: 01901b2c-7f3a-7abc-8d4e-..."
    - Continue button: full width, 44px min-height, @click=${_handleSubmit}, ?disabled=${_submitting}
    - Last used section: render only when localStorage has 'or-last-org-id' and it differs from current _value
      "Last used: {truncated}" + [Use] sl-button variant="text" @click to fill input

    Create org-picker.test.ts with 4 tests per behavior block.
    Import './org-picker.js' to register element.
    Use vi.stubGlobal for localStorage mock in tests that check storage.
    Test 1 (valid submit dispatches event): set _value via property, click Continue, listen for 'open-routing:org-selected', assert detail.orgId is the UUID.
    Test 2 (invalid submit shows error): set _value to 'not-a-uuid', click Continue, await updateComplete, assert el.shadowRoot shows error text.
    Test 3 (lastUsedOrgId prefills): create element with lastUsedOrgId='01901b2c-...', await updateComplete, assert sl-input value is the UUID.
    Test 4 (valid submit saves to localStorage): mock localStorage.setItem, submit valid UUID, assert it was called with 'or-last-org-id'.

    Create primitives/index.ts: export { OrOrgPicker } from './org-picker.js';
    (other primitives added by Plan 06-07)
  </action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/.claude/worktrees/eloquent-curie-dd5175 && pnpm --filter @open-routing/ui test -- org-picker 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/primitives/org-picker.ts exports class OrOrgPicker with @customElement('or-org-picker')
    - UUIDV7_PATTERN regex is /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i
    - Validation fires on-submit (button click), NOT on sl-input sl-input event
    - pnpm --filter @open-routing/ui test -- org-picker exits 0 with 4 passing tests
    - org-picker.ts does NOT call document.documentElement (forbidden — theme is shell responsibility)
    - 'open-routing:org-selected' event has bubbles:true and composed:true (Shadow DOM crossing for Phase 7)
  </acceptance_criteria>
  <done>OrOrgPicker component with UUIDv7 on-submit validation; 4 tests passing; event dispatched with composed:true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path → org_id extraction | UUIDv7 regex is client-side UX; server enforces X-Org-Id middleware (FOUND-03) |
| org-picker localStorage → app state | Last-used org_id persisted to localStorage; no auth token; attacker can manually change it but server still enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Spoofing | org_id path tampering | mitigate | Client UUIDv7 regex is UX nicety (D6-13); server X-Org-Id middleware is authoritative enforcer |
| T-06-03-02 | Tampering | XSS via Lit template in shell | mitigate | Lit html`` template auto-escapes interpolated values; never use unsafeHTML per RESEARCH §Security |
| T-06-03-03 | Information Disclosure | org_id in localStorage | accept | localStorage is same-origin; no secret; attacker with local access can read it regardless |
</threat_model>

<verification>
After both tasks complete:
- pnpm --filter @open-routing/ui test exits 0 (catalog-shell + org-picker tests)
- grep "this.style.setProperty" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- grep -c "document.documentElement" web/packages/ui/src/components/shell/catalog-shell.ts returns 0
- grep "@lit-labs/router" web/packages/ui/src/components/shell/catalog-shell.ts exits 0
- pnpm --filter @open-routing/admin build exits 0
</verification>

<success_criteria>
1. OrCatalogShell with theme switching: setting theme='or-dark' sets --sl-color-primary-500 to #4faab2 on this.style
2. OrCatalogShell router has 8-entry sidebar per D6-11 with modules filter
3. OrOrgPicker validates on-submit with UUIDv7 regex
4. All 8 unit tests (4 shell + 4 org-picker) pass
5. Admin SPA still builds: pnpm --filter @open-routing/admin build exits 0
6. No document.documentElement usage in shell (Phase 7 compatibility — Pitfall 10)
</success_criteria>

<output>
Create `.planning/phases/06-shared-ui-library-standalone-admin/06-03-SUMMARY.md` when done
</output>
