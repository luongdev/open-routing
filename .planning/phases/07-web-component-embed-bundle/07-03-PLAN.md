---
phase: 07-web-component-embed-bundle
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - web/packages/ui/src/components/agents/agent-detail.ts
  - web/packages/ui/src/components/skills/skill-detail.ts
  - web/packages/ui/src/components/queues/queue-detail.ts
  - web/packages/ui/src/components/channels/channel-detail.ts
  - web/packages/ui/src/components/adapters/adapter-detail.ts
  - web/packages/ui/src/components/break-reasons/break-reason-detail.ts
  - web/packages/ui/src/components/status/status-panel.ts
  - web/packages/ui/src/components/shell/catalog-shell.ts
autonomous: true
requirements: [EMBED-04]
must_haves:
  truths:
    - "All 7 packages/ui files that previously used <sl-dialog> now render inline confirm panels (Lit dialogs with role=\"dialog\" + aria-modal + tabindex)"
    - "No <sl-dialog> markup remains in any packages/ui component (grep returns 0 hits across components/*/*.ts excluding tests)"
    - "Inline confirm panels follow the conflict-banner inline-alert template (aria-live host + inline div + action-row buttons)"
    - "Per-component confirm CustomEvents bubble + composed:true so the host shell can subscribe across nested Shadow DOMs (D7-04 prep for embed Shadow DOM hierarchy)"
    - "Existing Phase 6 detail-component unit tests still pass after replacement (no behavior regression — confirms still fire same dispatched events)"
    - "Phase 6 admin smoke test (Plan 06-06) still passes after replacement (admin uses these components; behavior contract preserved)"
    - "Plan 06-13 import-page.ts and Plan 06-12 status-panel.ts inline alerts (<sl-alert>) remain — they are inline (no portal), so D7-04 does not require their replacement; only <sl-dialog> is replaced"
    - "Zero <sl-tooltip hoist> usages exist in packages/ui/src/components — non-hoisted tooltips stay inside the parent Shadow DOM (BLOCKER #5: D7-04 + RESEARCH §Runtime State Inventory mandates audit of all THREE portal vectors — sl-dialog, sl-tooltip[hoist], sl-drawer)"
    - "catalog-shell.ts uses <nav class='sidebar'> + _sidebarOpen state for the mobile sidebar — NO Shoelace portal element is involved. Iteration-2 BLOCKER #2: the previously-imported `import '@shoelace-style/shoelace/dist/components/drawer/drawer.js'` (line ~37) was UNUSED dead code (Phase 6 placeholder); removing it freed ~3-5 KB of eager bundle toward the 70 KB budget. Plan 07-09 audits the `<nav.sidebar>` invariant on mobile viewports."
  artifacts:
    - path: "web/packages/ui/src/components/agents/agent-detail.ts"
      provides: "Agent detail page with inline confirm panel replacing <sl-dialog> (D7-04)"
      contains: "_renderConfirmPanel\\|role=\"dialog\""
    - path: "web/packages/ui/src/components/break-reasons/break-reason-detail.ts"
      provides: "Break reason detail with inline confirm panel"
      contains: "role=\"dialog\""
    - path: "web/packages/ui/src/components/status/status-panel.ts"
      provides: "Status panel break-reason picker with inline confirm/picker (no <sl-dialog>)"
      contains: "role=\"dialog\""
    - path: "web/packages/ui/src/components/shell/catalog-shell.ts"
      provides: "Dead-code cleanup — unused <sl-drawer> import removed (Iteration-2 BLOCKER #2 — shell uses <nav.sidebar> not <sl-drawer>; freeing ~3-5 KB toward EMBED-01 budget)"
      contains: ""
  key_links:
    - from: "web/packages/ui/src/components/agents/agent-detail.ts"
      to: "web/packages/ui/src/components/primitives/conflict-banner.ts"
      via: "shared inline-alert template (aria-live host + inline div + action-row)"
      pattern: "aria-live"
    - from: "web/packages/ui/src/components/*/(detail|status-panel).ts"
      to: "host Shadow DOM (embed)"
      via: "no portal escape from <sl-dialog>; everything renders inside the component shadowRoot"
      pattern: "role=\"dialog\""
---

<objective>
Audit the 7 packages/ui components that use <sl-dialog> (broken focus-trap
inside nested Shadow DOM per Shoelace #709 / #1382 — RESEARCH §Pitfall 1)
and replace each <sl-dialog> with an inline confirm panel following the
conflict-banner template. This is the D7-04 mandate: "Inline alerts only —
no Shoelace <sl-dialog> usage anywhere embed reaches."

Per RESEARCH §Runtime State Inventory + verified grep (executed during
planning): 7 files use <sl-dialog> — adapter-detail, agent-detail,
break-reason-detail, channel-detail, queue-detail, skill-detail (1 instance
each — Phase 6 cleanup left 7 files not 8), status-panel. <sl-alert>
inline usages (Phase 6 D6-04 lineage) DO NOT need replacement —
<sl-alert> renders inline in its parent (no portal), satisfying EMBED-04.

BLOCKER #5 — full portal-vector audit. D7-04 covers THREE Shoelace
escape vectors, not just `<sl-dialog>`:
1. `<sl-dialog>` (broken focus-trap in nested Shadow DOM — replaced
   inline; Task 1 + 2 handle the 7 files).
2. `<sl-tooltip hoist>` (Floating UI hoists to body — Task 2 verify
   adds a `! grep ... sl-tooltip[^>]*hoist ...` gate that fails if
   any hoisted tooltip exists; if Phase 6 codebase has zero hoisted
   tooltips, the gate just locks the invariant. If any are found, the
   audit replaces them with non-hoisted tooltip + `aria-describedby`).
3. `<sl-drawer>` import in catalog-shell — Iteration-2 BLOCKER #2:
   verified during iteration-2 review that the shell's mobile
   sidebar uses `<nav class="sidebar">` + `_sidebarOpen` state, NOT
   `<sl-drawer>`. The `import '@shoelace-style/shoelace/dist/components/drawer/drawer.js'`
   at line ~37 of catalog-shell.ts is UNUSED dead code from a Phase 6
   placeholder. Task 3 removes the import (frees ~3-5 KB toward the
   70 KB eager budget). Plan 07-09 was updated to audit `<nav.sidebar>`
   on mobile viewports — NOT `<sl-drawer>`.

Purpose: Without this audit + replacement, embed Shadow DOM mounting
breaks focus-trap accessibility (Pitfall 1) AND risks portal escape
when Shoelace re-implements dialog hoisting. Plan 07-04 will refactor
the shell to lazy-load these same components, so this work MUST land
before the shell refactor flips to lazy imports (Wave 0 = parallel).
Iteration-2 BLOCKER #2 dead-code cleanup is added to Task 3 to free
~3-5 KB and lock the `<nav.sidebar>` invariant.
Output: 7 modified entity/status files + 1 dead-code cleanup in
catalog-shell.ts; admin smoke unaffected; Phase 6 unit tests still
green for all 7 components.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-web-component-embed-bundle/07-CONTEXT.md
@.planning/phases/07-web-component-embed-bundle/07-RESEARCH.md
@.planning/phases/07-web-component-embed-bundle/07-PATTERNS.md
@.planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md
@web/packages/ui/src/components/primitives/conflict-banner.ts
@web/packages/ui/src/components/agents/agent-detail.ts
@web/packages/ui/src/components/break-reasons/break-reason-detail.ts
@web/packages/ui/src/components/status/status-panel.ts
@web/packages/ui/src/components/shell/catalog-shell.ts

<interfaces>
<!-- Conflict-banner inline-alert template — the reuse pattern. -->
From web/packages/ui/src/components/primitives/conflict-banner.ts (Phase 6 D6-04):
```typescript
override connectedCallback(): void {
  super.connectedCallback();
  this.setAttribute('aria-live', 'assertive');
  this.setAttribute('role', 'alert');
}

private _dispatch(action: 'review' | 'discard'): void {
  this.dispatchEvent(new CustomEvent('open-routing:conflict-acknowledged', {
    detail: { action },
    bubbles: true,
    composed: true,  // crosses Shadow DOM for Phase 7
  }));
}

// Render shape (lines 251-285):
html`
  <div class="conflict-banner">
    <div class="banner-header">
      <sl-icon class="banner-icon" name="exclamation-triangle"></sl-icon>
      <div>...</div>
    </div>
    <div class="action-row">
      <sl-button variant="primary" size="small" @click=${() => this._dispatch('review')}>...</sl-button>
      <sl-button variant="text" size="small" @click=${() => this._dispatch('discard')}>...</sl-button>
    </div>
  </div>
`
```

<!-- Existing <sl-dialog> usage shape in agent-detail.ts:666 — replacement target. -->
From web/packages/ui/src/components/agents/agent-detail.ts (line 666):
```typescript
<sl-dialog
  label="Disable agent?"
  ?open=${this._showDisableConfirm}
  @sl-after-hide=${() => this._showDisableConfirm = false}
>
  <p>...</p>
  <sl-button slot="footer" variant="default" @click=${...}>Cancel</sl-button>
  <sl-button slot="footer" variant="danger" @click=${...}>Disable</sl-button>
</sl-dialog>
```

<!-- catalog-shell.ts UNUSED import (Iteration-2 BLOCKER #2) — verified line ~37. -->
From web/packages/ui/src/components/shell/catalog-shell.ts:
```typescript
import '@shoelace-style/shoelace/dist/components/drawer/drawer.js';  // UNUSED — sidebar uses <nav>
```
The shell's `<nav class="sidebar">` block (line ~589) is the real
sidebar element; <sl-drawer> never appears in the template. This
import is Phase 6 placeholder dead code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace <sl-dialog> with inline confirm panel in 4 entity detail components (agents, skills, queues, channels)</name>
  <files>web/packages/ui/src/components/agents/agent-detail.ts, web/packages/ui/src/components/skills/skill-detail.ts, web/packages/ui/src/components/queues/queue-detail.ts, web/packages/ui/src/components/channels/channel-detail.ts</files>
  <read_first>
    - web/packages/ui/src/components/agents/agent-detail.ts (full file — locate `<sl-dialog` block at ~line 666)
    - web/packages/ui/src/components/skills/skill-detail.ts (locate <sl-dialog block)
    - web/packages/ui/src/components/queues/queue-detail.ts (locate <sl-dialog block)
    - web/packages/ui/src/components/channels/channel-detail.ts (locate <sl-dialog block)
    - web/packages/ui/src/components/agents/agent-detail.test.ts (existing tests — preserve event contract)
    - web/packages/ui/src/components/primitives/conflict-banner.ts (inline-alert template lines 196-285)
    - .planning/phases/07-web-component-embed-bundle/07-CONTEXT.md §D7-04 (inline alerts only mandate)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Pitfall 1 (Shoelace #709/#1382 focus-trap broken)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶<sl-dialog> Audit Replacement (lines 596-636)
  </read_first>
  <action>
For each of the 4 entity detail components — locate the single
`<sl-dialog>...</sl-dialog>` block (Phase 6 disable-confirm) and
replace with an inline confirm panel rendered via a private
`_renderConfirmPanel()` method. The replacement preserves the same
`@click` handlers, the same state flag (`_showDisableConfirm` or
equivalent), and the same dispatched CustomEvent contracts (no admin
behavior change).

Replacement pattern (apply to each of 4 files, adapt entity name):

1. Remove `import '@shoelace-style/shoelace/dist/components/dialog/dialog.js';`
import line at top.

2. Replace `<sl-dialog label="..." ?open=${this._showXxxConfirm}
@sl-after-hide=${...}>...<sl-button slot="footer"...>...</sl-button>
</sl-dialog>` block with the inline pattern. Use `when()` directive
from `lit/directives/when.js` to render only when `_showXxxConfirm` is
true:

```
${when(this._showDisableConfirm, () => html`
  <div class="confirm-overlay" role="presentation" @click=${this._onOverlayClick}>
    <div class="confirm-panel"
         role="dialog"
         aria-modal="true"
         aria-labelledby="confirm-title"
         @click=${(e: Event) => e.stopPropagation()}>
      <h3 id="confirm-title" class="confirm-title">Disable agent?</h3>
      <p class="confirm-body">Disabling will remove this agent from
        active routing immediately. You can re-enable later.</p>
      <div class="action-row">
        <sl-button variant="default" size="small"
          @click=${this._onCancelDisable}>Cancel</sl-button>
        <sl-button variant="danger" size="small"
          @click=${this._onConfirmDisable}>Disable</sl-button>
      </div>
    </div>
  </div>
`)}
```

3. Add CSS to the `static styles` block (or append to existing
styles). WHY note for the executor (per CLAUDE.md HARD RULE — add a
brief WHY comment in the CSS source above the `.confirm-overlay`
selector explaining the `position: fixed` + `contain: content`
interaction): `position: fixed` makes the overlay span the embed
Shadow DOM's viewport (anchored to the shell host); without `contain:
content` on `:host` (or an equivalent containment hint) the fixed
positioning would anchor to the **outer document's** viewport,
defeating Shadow DOM isolation. Phase 6 `<or-conflict-banner>` does
not need fixed positioning so this combo is Phase 7-specific. Do NOT
strip the `contain: content` declaration on `:host` (already declared
in embed-element.ts per Plan 07-06).

```
.confirm-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}
.confirm-panel {
  background: var(--or-color-card-bg, #ffffff);
  border: 1px solid var(--or-color-card-border, #d1d5db);
  border-radius: var(--sl-border-radius-medium, 6px);
  padding: 20px;
  max-width: 480px;
  width: 90%;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
}
.confirm-title { margin: 0 0 12px 0; font-size: 18px; font-weight: 600; }
.confirm-body { margin: 0 0 20px 0; }
.action-row { display: flex; gap: 8px; justify-content: flex-end; }
```

4. Add private methods _onOverlayClick (sets
`this._showDisableConfirm = false`), _onCancelDisable (same),
_onConfirmDisable (calls the existing PATCH+enabled=false handler,
which already exists in Phase 6 code — just rewire the existing
handler to be called from `_onConfirmDisable`).

5. Add `connectedCallback` (or extend existing one) to set
`this.setAttribute('aria-live', 'polite')` so screen readers announce
the panel when it appears (per conflict-banner template; "polite" not
"assertive" because this is a user-initiated confirm, not a system
alert).

6. Inline focus management: when `_showDisableConfirm` flips true,
`await this.updateComplete; this.shadowRoot?.querySelector('.confirm-panel')?.querySelector('sl-button[variant="danger"]')?.focus();`
Insert this via `updated(changed: PropertyValues)` lifecycle hook
when `changed.has('_showDisableConfirm') && this._showDisableConfirm === true`.

7. Escape-key handling: in connectedCallback add
`this._onKeydown = (e) => { if (e.key === 'Escape' && this._showDisableConfirm) { this._showDisableConfirm = false; } };`
+ `document.addEventListener('keydown', this._onKeydown);`. Remove
listener in disconnectedCallback. (This avoids relying on Shoelace's
broken focus-trap.)

8. DO NOT touch the `<sl-alert>` inline alerts elsewhere in these
files — `<sl-alert>` renders inline in its parent shadow root with no
portal (verified — RESEARCH §Runtime State Inventory). Only the
`<sl-dialog>` block is replaced.

Per CLAUDE.md HARD RULE: do NOT add WHAT/HOW comments. Add ONE WHY
comment above the confirm-overlay block in each file: "Inline confirm
panel — D7-04: <sl-dialog> has broken focus-trap inside nested
Shadow DOM (shoelace#709, #1382). Embed mounts inside Shadow DOM, so
this is replaced with an inline role=dialog + manual focus
management."

For skill-detail.ts specifically — RESEARCH §Runtime State Inventory
noted "2 instances" but verified grep shows 1 file. If the second
instance turns out to be a second `<sl-dialog>` for a separate
confirm flow (e.g., delete + status change), apply the same
replacement pattern to BOTH instances and use distinct state flags
(e.g., `_showDisableConfirm` AND `_showXxxConfirm`).
  </action>
  <verify>
    <automated>
      cd web && pnpm --filter @open-routing/ui test agents/agent-detail.test.ts skills/skill-detail.test.ts queues/queue-detail.test.ts channels/channel-detail.test.ts --run 2>&1 | tail -20
    </automated>
    <automated>
      cd web && pnpm --filter @open-routing/ui typecheck 2>&1 | tail -5
    </automated>
    <automated>
      grep -L "sl-dialog" web/packages/ui/src/components/agents/agent-detail.ts web/packages/ui/src/components/skills/skill-detail.ts web/packages/ui/src/components/queues/queue-detail.ts web/packages/ui/src/components/channels/channel-detail.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 files contain `role="dialog"` and `aria-modal="true"` exactly once each (inline confirm panel present)
    - All 4 files contain ZERO `<sl-dialog` strings (`grep -c "<sl-dialog" file` == 0 for each)
    - All 4 files DO NOT import `@shoelace-style/shoelace/dist/components/dialog/dialog.js` (`grep -c "dialog/dialog.js" file` == 0)
    - All 4 files preserve existing `_show<Xxx>Confirm` state flag(s) and the existing PATCH+enabled=false flow
    - `cd web && pnpm --filter @open-routing/ui test agents/agent-detail.test.ts --run` exits 0 (Phase 6 tests still green)
    - `cd web && pnpm --filter @open-routing/ui test skills/skill-detail.test.ts --run` exits 0
    - `cd web && pnpm --filter @open-routing/ui test queues/queue-detail.test.ts --run` exits 0
    - `cd web && pnpm --filter @open-routing/ui test channels/channel-detail.test.ts --run` exits 0
    - `cd web && pnpm --filter @open-routing/ui typecheck` exits 0 (no TS errors)
    - All 4 files contain Escape-key handler attached/removed in connectedCallback/disconnectedCallback (manual focus management replacing broken Shoelace focus-trap)
  </acceptance_criteria>
  <done><sl-dialog> removed from 4 entity detail components; replaced with inline confirm panel (role=dialog + aria-modal + manual focus/escape). Phase 6 unit tests green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace <sl-dialog> in adapter-detail + break-reason-detail + status-panel (3 remaining files)</name>
  <files>web/packages/ui/src/components/adapters/adapter-detail.ts, web/packages/ui/src/components/break-reasons/break-reason-detail.ts, web/packages/ui/src/components/status/status-panel.ts</files>
  <read_first>
    - web/packages/ui/src/components/adapters/adapter-detail.ts (full file)
    - web/packages/ui/src/components/break-reasons/break-reason-detail.ts (full file; <sl-dialog at ~444)
    - web/packages/ui/src/components/status/status-panel.ts (full file; may contain break-picker dialog or wrapup-confirm dialog)
    - web/packages/ui/src/components/adapters/adapter-detail.test.ts (existing tests)
    - web/packages/ui/src/components/break-reasons/break-reason-detail.test.ts (existing tests)
    - web/packages/ui/src/components/status/status-panel.test.ts (existing tests)
    - web/packages/ui/src/components/primitives/conflict-banner.ts (template, lines 196-285)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶<sl-dialog> Audit Replacement (lines 596-636)
    - .planning/phases/06-shared-ui-library-standalone-admin/06-CONTEXT.md §D6-26 (status-panel visibilitychange — preserved)
  </read_first>
  <action>
Apply the SAME inline confirm panel pattern from Task 1 to the 3
remaining files. Per file:

**adapter-detail.ts** — has a single `<sl-dialog>` for the disable
confirm. Same replacement pattern as Task 1. Adapter detail also has
a JSONB textarea (D6-04 carry-forward); leave that untouched.

**break-reason-detail.ts** — has a `<sl-dialog>` at line ~444. The
break-reason flow likely confirms a delete/disable; check the dialog
label and button copy. Apply the same pattern. Use `_showConfirm`
state. Confirm action calls existing PATCH+enabled=false handler.

**status-panel.ts** — status-panel uses `<sl-dialog>` for the
break-reason picker (D6-26 status state machine surfaces a picker
when transitioning Ready→Break). This is NOT a confirm dialog — it's
a picker. Replacement:

  - Remove `<sl-dialog>` wrapper around the break-reason picker.
  - Render an inline `<div class="picker-overlay" role="presentation"
    @click=${this._onPickerOverlayClick}>` containing a
    `<div class="picker-panel" role="dialog" aria-modal="true"
    aria-labelledby="picker-title" @click=${...stopPropagation}>`.
  - Inside the panel: `<h3 id="picker-title">Select break
    reason</h3>` + a list of `<sl-button>` per break_reason (existing
    rendering — preserved) + a Cancel button.
  - Same Escape-key + focus-on-open behavior as Task 1.
  - The `_brkPickerOpen` state flag (or equivalent) is preserved
    verbatim; only the rendering changes from `<sl-dialog>` to inline
    `role="dialog"`.

Status-panel ALSO has D6-26 `visibilitychange` listener (pause polling
when document hidden) — DO NOT TOUCH that; it's unrelated to the
dialog replacement and is required infrastructure for the status
machine.

Status-panel ALSO has D6-29 force flag UX (admin-only checkbox before
status PATCH calls). DO NOT TOUCH — this is policy not dialog.

Per CLAUDE.md HARD RULE: WHY comment above each replacement:
"Inline confirm/picker panel — D7-04 inline-alerts-only mandate.
<sl-dialog> portal-escape risk + broken focus-trap inside nested
Shadow DOM (shoelace#709). status-panel runs inside embed shell
which mounts in embed's outer Shadow DOM."

After the replacement, run Plan 06-06 admin smoke test (Plan 07-10
will retry this as part of Wave 1, but smoke against admin is the
canonical regression guard for status-panel + break-reason picker
behavior). If Wave 0 smoke isn't feasible due to environment, document
in summary that smoke is deferred to Wave 1 regression task.
  </action>
  <verify>
    <automated>
      cd web && pnpm --filter @open-routing/ui test adapters/adapter-detail.test.ts break-reasons/break-reason-detail.test.ts status/status-panel.test.ts --run 2>&1 | tail -20
    </automated>
    <automated>
      cd web && pnpm --filter @open-routing/ui typecheck 2>&1 | tail -5
    </automated>
    <automated>
      grep -c "<sl-dialog" web/packages/ui/src/components/adapters/adapter-detail.ts web/packages/ui/src/components/break-reasons/break-reason-detail.ts web/packages/ui/src/components/status/status-panel.ts 2>&1
    </automated>
    <automated>
      ! grep -rln "<sl-dialog" web/packages/ui/src/components --include="*.ts" --exclude="*.test.ts"
    </automated>
    <automated>
      ! grep -rln "sl-tooltip[^>]*hoist" web/packages/ui/src/components --include="*.ts" --exclude="*.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 3 files contain `role="dialog"` exactly once each (inline confirm/picker panel)
    - All 3 files contain ZERO `<sl-dialog` strings
    - All 3 files DO NOT import `@shoelace-style/shoelace/dist/components/dialog/dialog.js`
    - status-panel.ts retains D6-26 `visibilitychange` listener (`grep -q "visibilitychange" status-panel.ts`)
    - status-panel.ts retains D6-29 force-flag UX (`grep -q "force" status-panel.ts` matches Phase 6 force flag code)
    - REPO-WIDE: `! grep -rln "<sl-dialog" web/packages/ui/src/components --include="*.ts" --exclude="*.test.ts"` exits 0 (zero `<sl-dialog>` markup in non-test source — `!` inverts grep's exit code so success means no matches; grep with line-listing avoids `grep -c` counting comment lines and avoids `grep -v ":0"` matching errors)
    - REPO-WIDE: `! grep -rln "sl-tooltip[^>]*hoist" web/packages/ui/src/components --include="*.ts" --exclude="*.test.ts"` exits 0 (BLOCKER #5 — zero hoisted tooltips, EMBED-04 portal-escape guard for `<sl-tooltip hoist>` which uses Floating UI positioning that can escape Shadow DOM)
    - `cd web && pnpm --filter @open-routing/ui test adapters/adapter-detail.test.ts break-reasons/break-reason-detail.test.ts status/status-panel.test.ts --run` exits 0
    - `cd web && pnpm --filter @open-routing/ui typecheck` exits 0
  </acceptance_criteria>
  <done>All 7 packages/ui components no longer use <sl-dialog>; status-panel picker, break-reason confirm, and adapter confirm all render inline role=dialog. Phase 6 unit tests green for all 3 files.</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused <sl-drawer> import from catalog-shell.ts (Iteration-2 BLOCKER #2 — dead code cleanup)</name>
  <files>web/packages/ui/src/components/shell/catalog-shell.ts</files>
  <read_first>
    - web/packages/ui/src/components/shell/catalog-shell.ts (lines 30-40 — Shoelace imports; line ~589 — `<nav class="sidebar">` template confirming drawer is unused)
    - .planning/phases/07-web-component-embed-bundle/07-CONTEXT.md §D7-04 (portal-vector audit; <sl-drawer> is third vector)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Runtime State Inventory (D7-04 portal audit)
  </read_first>
  <action>
Iteration-2 BLOCKER #2 cleanup — the shell's mobile sidebar uses
`<nav class="sidebar">` with `_sidebarOpen` state; `<sl-drawer>` is
never rendered. The import at line ~37 of catalog-shell.ts is dead
code from a Phase 6 placeholder.

Step 1 — Delete this single line from `web/packages/ui/src/components/shell/catalog-shell.ts`:

```
import '@shoelace-style/shoelace/dist/components/drawer/drawer.js';
```

(The exact line is near other Shoelace per-component imports, typically
around line 37 between `tooltip/tooltip.js` and the org-picker import.)

Step 2 — Do NOT touch the other Shoelace imports (icon, icon-button,
button, button-group, select, option, tooltip) — these ARE used in the
shell render template (topbar chrome). Verify by grepping the render
methods after the edit: `sl-icon`, `sl-icon-button`, `sl-button`,
`sl-button-group`, `sl-select`, `sl-option`, `sl-tooltip` all still
appear in the shell template; `sl-drawer` does NOT.

Step 3 — Verify the `<nav class="sidebar">` template at line ~589 is
unchanged (the actual sidebar element; surfaced by `_sidebarOpen`
state + CSS media query for mobile breakpoint).

Step 4 — Add a single WHY note in the commit message (NOT the source
code per CLAUDE.md HARD RULE — no WHAT/HOW comments and no
"removed X" comments): "Iteration-2 BLOCKER #2 — unused
<sl-drawer> import removed; shell sidebar uses <nav class='sidebar'>.
Frees ~3-5 KB of eager bundle toward EMBED-01 70 KB budget."

This task is small, scoped, and zero-risk: it's a pure dead-code
delete validated by:
- `! grep -n "drawer/drawer.js" catalog-shell.ts` exits 0
- `! grep -n "<sl-drawer" catalog-shell.ts` exits 0 (the template
  never referenced sl-drawer; this just locks the invariant)
- `pnpm --filter @open-routing/ui test catalog-shell.test.ts --run`
  exits 0 (Phase 6 baseline still green)
- `pnpm --filter @open-routing/ui typecheck` exits 0
  </action>
  <verify>
    <automated>
      ! grep -n "drawer/drawer.js" web/packages/ui/src/components/shell/catalog-shell.ts
    </automated>
    <automated>
      ! grep -n "<sl-drawer" web/packages/ui/src/components/shell/catalog-shell.ts
    </automated>
    <automated>
      grep -c "<nav class=\"sidebar\"" web/packages/ui/src/components/shell/catalog-shell.ts
    </automated>
    <automated>
      cd web && pnpm --filter @open-routing/ui test catalog-shell.test.ts --run 2>&1 | tail -5
    </automated>
    <automated>
      cd web && pnpm --filter @open-routing/ui typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "drawer/drawer.js" web/packages/ui/src/components/shell/catalog-shell.ts` exits 0 (unused import removed)
    - `! grep -n "<sl-drawer" web/packages/ui/src/components/shell/catalog-shell.ts` exits 0 (Iteration-2 BLOCKER #2 invariant — shell never used sl-drawer; locked)
    - `grep -q '<nav class="sidebar"' web/packages/ui/src/components/shell/catalog-shell.ts` (real sidebar element retained)
    - `grep -q "_sidebarOpen" web/packages/ui/src/components/shell/catalog-shell.ts` (sidebar state retained)
    - Phase 6 shell test still green: `cd web && pnpm --filter @open-routing/ui test catalog-shell.test.ts --run` exits 0
    - Typecheck clean: `cd web && pnpm --filter @open-routing/ui typecheck` exits 0
  </acceptance_criteria>
  <done>Unused <sl-drawer> import removed from catalog-shell.ts; <nav class="sidebar"> + _sidebarOpen invariant locked (Iteration-2 BLOCKER #2); ~3-5 KB freed toward EMBED-01 budget; Phase 6 baseline preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| <sl-dialog> portal → host page | Shoelace dialog historically portals to document.body (Pitfall 1 — now verified not the case, but focus-trap still broken in nested Shadow DOM); replacement removes portal-escape risk entirely |
| Component shadowRoot → embed Shadow DOM | Inline role=dialog stays inside the component's own shadowRoot, fully contained inside embed Shadow DOM hierarchy |
| Unused <sl-drawer> import → eager bundle | Iteration-2 BLOCKER #2: import dragged Shoelace drawer (~3-5 KB) into the eager chunk despite no usage; removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Information Disclosure / Tampering | <sl-dialog> portal escape from Shadow DOM | mitigate | All 7 <sl-dialog> instances replaced with inline role=dialog rendered inside component's own shadowRoot. embed-shadow-dom-isolation.spec.ts "no portal escape" assertion (Plan 07-09) verifies; grep gate enforces zero <sl-dialog> markup |
| T-7-A11Y | DoS (a11y) | <sl-dialog> focus-trap broken inside nested Shadow DOM (shoelace#709, #1382) | mitigate | Manual Escape-key listener + focus-on-open in updated() lifecycle. Per-file aria-modal="true" + aria-labelledby="confirm-title" set so screen readers identify the modal correctly |
| T-7-DEAD-01 | DoS (perf) | Unused <sl-drawer> import bloats eager bundle | mitigate | Iteration-2 BLOCKER #2 — dead-code import removed; ~3-5 KB freed; Plan 07-12 size-limit gate confirms the saving holds |
</threat_model>

<verification>
- All 7 packages/ui detail/status files render inline confirm panels (not <sl-dialog>)
- Repo-wide grep: zero `<sl-dialog` matches in `web/packages/ui/src/components/`
- catalog-shell.ts no longer imports `drawer/drawer.js` (Iteration-2 BLOCKER #2)
- catalog-shell.ts retains `<nav class="sidebar">` + `_sidebarOpen` for mobile sidebar
- All Phase 6 unit tests for these 7 components still pass (`pnpm --filter @open-routing/ui test --run`)
- `pnpm --filter @open-routing/ui typecheck` clean
- Manual focus management + Escape-key handling preserves a11y per WCAG 2.1.1 (keyboard accessible)
</verification>

<success_criteria>
- 7 modified detail/status files; all <sl-dialog> usages replaced with inline role=dialog
- 1 modified shell file; unused <sl-drawer> import removed (Iteration-2 BLOCKER #2 — ~3-5 KB freed toward EMBED-01)
- D7-04 mandate satisfied: no <sl-dialog> remains in any packages/ui code path; <sl-drawer> invariant locked
- Phase 6 detail-component + status-panel + shell unit tests still green
- Typecheck clean; lint clean
- Plan 07-10 (Wave 1 admin regression) will confirm admin behavior unchanged via Playwright smoke
- Plan 07-09 (Wave 4 e2e) audits the `<nav.sidebar>` invariant on mobile viewport (replaces the iteration-1 <sl-drawer> assertion that asserted against dead code)
</success_criteria>

<output>
Create `.planning/phases/07-web-component-embed-bundle/07-03-SUMMARY.md` when done
</output>
