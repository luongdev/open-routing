---
phase: 07-web-component-embed-bundle
plan: w0-21
type: execute
wave: 0.0
depends_on: ["w0-03", "w0-17"]
files_modified:
  - web/packages/ui/src/components/primitives/or-dropdown.ts
  - web/packages/ui/src/components/primitives/or-dropdown.test.ts
  - web/packages/ui/src/components/primitives/index.ts
  - web/apps/admin/src/playground/slots/dropdown.ts
  - web/apps/admin/src/playground/playground-route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`or-dropdown` Lit primitive renders a trigger slot + uk-drop popup with menu items"
    - "Props: align ('left' | 'right' | 'center'), mode ('click' | 'hover'), offset (px), items (array `[{id, label, icon?, destructive?, disabled?, divider?}]`)"
    - "Emits `or-dropdown-select` CustomEvent with detail { id }"
    - "Keyboard: arrow up/down navigation, Enter to select, Esc to close"
    - "Playground entry shows: user-menu dropdown (avatar trigger, Profile/Settings/divider/Sign Out) + row-actions dropdown (kebab icon trigger, Edit/Duplicate/divider/Delete)"
---

<objective>
`<or-dropdown>` — menu popover. Wraps Frankenstyle uk-drop + uk-dropdown
with a type-safe items API and keyboard nav.
</objective>

<process>

## Step 1: or-dropdown.ts

```ts
import { LitElement, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';

export type DropdownItem =
  | { id: string; label: string; icon?: string; destructive?: boolean; disabled?: boolean; divider?: false }
  | { divider: true };

@customElement('or-dropdown')
export class OrDropdown extends LitElement {
  @property({ type: String }) align: 'left' | 'right' | 'center' = 'left';
  @property({ type: String }) mode: 'click' | 'hover' = 'click';
  @property({ type: Number }) offset = 8;
  @property({ type: Array }) items: DropdownItem[] = [];

  override createRenderRoot() { return this; }

  private onSelect(id: string, disabled?: boolean): void {
    if (disabled) return;
    this.dispatchEvent(new CustomEvent('or-dropdown-select', {
      detail: { id }, bubbles: true, composed: true,
    }));
  }

  override render() {
    return html`
      <div class="uk-inline">
        <slot name="trigger"></slot>
        <div uk-drop="mode: ${this.mode}; offset: ${this.offset}; pos: bottom-${this.align}">
          <ul class="uk-dropdown-nav uk-nav" role="menu">
            ${this.items.map(item => {
              if ('divider' in item && item.divider) {
                return html`<li class="uk-nav-divider" role="separator"></li>`;
              }
              const i = item as Exclude<DropdownItem, { divider: true }>;
              return html`
                <li class=${[i.disabled && 'uk-disabled', i.destructive && 'uk-text-danger'].filter(Boolean).join(' ')}>
                  <a role="menuitem" @click=${() => this.onSelect(i.id, i.disabled)}>
                    ${i.icon ? html`<or-icon name=${i.icon} size="14" style="margin-right:8px"></or-icon>` : nothing}
                    ${i.label}
                  </a>
                </li>
              `;
            })}
          </ul>
        </div>
      </div>
    `;
  }
}
```

## Step 2: Tests + playground

6 tests: items render, divider renders, select event fires, disabled doesn't fire, destructive class, align attribute maps to uk-drop pos.

Playground:
1. User menu: avatar trigger; items: Profile, Settings, divider, Sign Out (destructive)
2. Row actions: kebab `<or-icon name="more-vertical">` trigger; items: Edit (pencil), Duplicate (copy), divider, Delete (destructive)

## Step 3: Commit

```
feat(ui): or-dropdown primitive (W0.0-21)
```

</process>

<verification>

- [ ] 6 tests pass
- [ ] Keyboard arrow nav works (uk-drop built-in)
- [ ] Visual diff vs Ember user-menu within 5% delta

</verification>
