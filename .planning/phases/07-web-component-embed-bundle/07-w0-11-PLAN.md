---
phase: 07-web-component-embed-bundle
plan: w0-11
type: execute
wave: 0.0
depends_on: ["w0-03"]
files_modified:
  - web/packages/ui/src/components/primitives/or-input.ts
  - web/packages/ui/src/components/primitives/or-input.test.ts
  - web/packages/ui/src/components/primitives/code-input.ts
  - web/packages/ui/src/components/primitives/index.ts
  - web/apps/admin/src/playground/slots/input.ts
  - web/apps/admin/src/playground/playground-route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`or-input` Lit primitive at `packages/ui/src/components/primitives/or-input.ts`, light DOM, wraps `<input class='uk-input'>`"
    - "Props: type (text|email|url|password|number|search|tel), value, placeholder, disabled, required, readonly, name, label, helperText, errorText, prefix-icon (slot), suffix-icon (slot), size (sm|md|lg)"
    - "Emits `or-input` CustomEvent on every input with detail { value: string }; emits `or-change` on blur"
    - "When errorText is set, input has `uk-form-danger` class + errorText shown red below; when helperText only, shown muted"
    - "Existing `or-code-input` refactored to use or-input internally (preserve all D04_1-02 readonly behavior + setCustomValidity validation)"
    - "Playground slot shows: default/with label/with helper/with error/disabled/readonly/with prefix icon/with suffix icon; all 3 sizes"
    - "Visual diff vs Ember reference (Health Records search box) within 5% delta"
  artifacts:
    - path: "web/packages/ui/src/components/primitives/or-input.ts"
      provides: "or-input primitive"
---

<objective>
Build `<or-input>` — type-safe wrapper around `<input class="uk-input">`.
Refactor `or-code-input` to use or-input internally (preserve all its
D04_1 validation behavior, just swap the inner sl-input for or-input).
</objective>

<process>

## Step 1: `or-input.ts` (skeleton)

```ts
import { LitElement, html, css, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';

@customElement('or-input')
export class OrInput extends LitElement {
  @property({ type: String }) type = 'text';
  @property({ type: String }) value = '';
  @property({ type: String }) placeholder = '';
  @property({ type: Boolean }) disabled = false;
  @property({ type: Boolean }) required = false;
  @property({ type: Boolean }) readonly = false;
  @property({ type: String }) name = '';
  @property({ type: String }) label = '';
  @property({ type: String, attribute: 'helper-text' }) helperText = '';
  @property({ type: String, attribute: 'error-text' }) errorText = '';
  @property({ type: String }) size: 'sm' | 'md' | 'lg' = 'md';

  override createRenderRoot() { return this; }

  private get inputClasses(): string {
    const base = 'uk-input';
    const sizeClass = this.size === 'md' ? '' : `uk-form-${this.size}`;
    const errorClass = this.errorText ? 'uk-form-danger' : '';
    return [base, sizeClass, errorClass].filter(Boolean).join(' ');
  }

  private onInput(e: Event): void {
    this.value = (e.target as HTMLInputElement).value;
    this.dispatchEvent(new CustomEvent('or-input', {
      detail: { value: this.value }, bubbles: true, composed: true,
    }));
  }

  private onBlur(): void {
    this.dispatchEvent(new CustomEvent('or-change', {
      detail: { value: this.value }, bubbles: true, composed: true,
    }));
  }

  setCustomValidity(msg: string): void {
    const native = this.querySelector('input');
    native?.setCustomValidity(msg);
  }

  override render() {
    const inputId = `or-input-${this.name || crypto.randomUUID().slice(0,8)}`;
    return html`
      <div class="uk-form-controls">
        ${this.label ? html`<label class="uk-form-label" for=${inputId}>${this.label}</label>` : nothing}
        <div style="display:flex; align-items:center; gap:8px;">
          <slot name="prefix"></slot>
          <input
            id=${inputId}
            type=${this.type}
            class=${this.inputClasses}
            .value=${this.value}
            placeholder=${this.placeholder}
            ?disabled=${this.disabled}
            ?required=${this.required}
            ?readonly=${this.readonly}
            name=${this.name}
            aria-invalid=${this.errorText ? 'true' : 'false'}
            @input=${this.onInput}
            @blur=${this.onBlur}
          />
          <slot name="suffix"></slot>
        </div>
        ${this.errorText
          ? html`<div class="uk-form-help uk-text-danger">${this.errorText}</div>`
          : this.helperText
          ? html`<div class="uk-form-help">${this.helperText}</div>`
          : nothing}
      </div>
    `;
  }
}
```

## Step 2: Refactor `code-input.ts`

Replace internal `<sl-input>` with `<or-input>`. Keep ALL validation
logic (CODE_PATTERN test, setCustomValidity, _validationState). Just
swap the rendered tag + adjust event names: `sl-input` → `or-input`,
`sl-blur` → built into or-input's `or-change`.

Keep nameToCode() unchanged.

## Step 3: Tests

`or-input.test.ts` ~8 cases: renders input, value binding, label,
helper text, error text + uk-form-danger class, disabled, readonly,
or-input event fires on input. Re-run existing `code-input.test.ts` —
must still pass after refactor.

## Step 4: Playground slot `slots/input.ts`

8 entries: default, with label, with helper, with error, disabled,
readonly, with prefix icon (use a placeholder `<span>🔍</span>` in
prefix slot), with suffix icon. All sizes shown vertically stacked.

## Step 5: Verify + Commit

```
feat(ui): or-input primitive + refactor or-code-input (W0.0-11)
```

</process>

<verification>

- [ ] or-input uses light DOM
- [ ] 8 unit tests pass
- [ ] code-input tests still green
- [ ] Playground slot shows 8 variants
- [ ] Visual diff vs Ember reference search box <5% delta

</verification>
