---
phase: 07-web-component-embed-bundle
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - web/apps/embed/package.json
  - web/apps/embed/tsconfig.json
  - web/apps/embed/vite.config.ts
  - web/apps/embed/vitest.config.ts
  - web/apps/embed/test-setup.ts
  - web/apps/embed/index.html
  - web/apps/embed/.size-limit.json
  - web/apps/embed/src/embed-element.test.ts
  - web/apps/embed/src/hash-router-adapter.test.ts
  - web/apps/embed/e2e/specs/embed-shadow-dom-isolation.spec.ts
  - web/apps/embed/e2e/specs/embed-org-id-header.spec.ts
  - web/apps/embed/e2e/specs/embed-auth-expired.spec.ts
  - web/apps/embed/e2e/specs/embed-modules-filter.spec.ts
  - web/apps/embed/playwright.config.ts
  - web/apps/embed/e2e/hosts/react/index.html
  - web/apps/embed/e2e/hosts/vue/index.html
  - web/apps/embed/e2e/hosts/html/index.html
  - web/.gitignore
autonomous: true
requirements: [EMBED-01, EMBED-09, EMBED-10]
must_haves:
  truths:
    - "pnpm --filter @open-routing/embed install resolves without errors"
    - "pnpm --filter @open-routing/embed build emits dist/embed.js as a valid ES module"
    - "pnpm --filter @open-routing/embed test --run finds the vitest config and runs 0 specs OK"
    - "pnpm --filter @open-routing/embed test:e2e --list lists 12 prospective spec runs (4 specs x 3 hosts)"
    - "pnpm --filter @open-routing/embed size finds the .size-limit.json budget"
    - "Wave 0 stub test files contain skipped/no-op tests so vitest/playwright exit 0"
    - "@open-routing/ui is in devDependencies (NOT dependencies) so Vite library mode bundles workspace dep (D7-15, Pitfall 8)"
    - "Shadow DOM mode is 'open' (CONTEXT Discretion — Playwright assertions, host debug querySelector)"
    - "esm.sh pinning uses specific patch versions (react@18.3.1, vue@3.5.13) per CONTEXT Discretion"
  artifacts:
    - path: "web/apps/embed/package.json"
      provides: "Embed package scripts + deps + npm tarball metadata (D7-15)"
      contains: '"@open-routing/catalog-embed"'
    - path: "web/apps/embed/vite.config.ts"
      provides: "Library-mode Vite config (D7-01, RESEARCH §1)"
      contains: "build.lib"
    - path: "web/apps/embed/playwright.config.ts"
      provides: "3-project matrix (react/vue/html) with retries: 1 (D7-11, D7-12)"
      contains: "retries: 1"
    - path: "web/apps/embed/.size-limit.json"
      provides: "Declarative 70 KB gzipped budget (D7-09, D7-10)"
      contains: '"limit": "70 KB"'
    - path: "web/apps/embed/e2e/hosts/react/index.html"
      provides: "React 18.3.1 stub host with importmap + aggressive CSS reset (D7-11)"
      contains: "esm.sh/react@18.3.1"
    - path: "web/apps/embed/e2e/hosts/vue/index.html"
      provides: "Vue 3.5.13 stub host with importmap (D7-11)"
      contains: "esm.sh/vue@3.5.13"
    - path: "web/apps/embed/e2e/hosts/html/index.html"
      provides: "Plain HTML stub host with aggressive CSS reset (D7-11)"
      contains: "open-routing-catalog"
  key_links:
    - from: "web/apps/embed/package.json"
      to: "web/packages/ui"
      via: "workspace dep moved to devDependencies (Pitfall 8 — must bundle, not externalize)"
      pattern: "devDependencies.*@open-routing/ui.*workspace:\\*"
    - from: "web/apps/embed/vite.config.ts"
      to: "web/apps/embed/src/index.ts"
      via: "build.lib.entry"
      pattern: "src/index\\.ts"
    - from: "web/apps/embed/playwright.config.ts"
      to: "web/apps/embed/e2e/hosts/{react,vue,html}/"
      via: "projects matrix baseURLs"
      pattern: "hosts/(react|vue|html)/"
---

<objective>
Stand up Wave 0 build + test scaffold for `web/apps/embed/` so that all
subsequent Wave 1 implementation work has running test infrastructure to
verify against. This plan creates ALL test/config artifacts (vitest,
playwright, size-limit, stub hosts, spec stubs) BEFORE any embed code is
written, satisfying 07-VALIDATION.md's Wave 0 requirements (17 files).

Purpose: A clean-slate embed package with library-mode Vite, vitest + happy-dom,
Playwright 3-host matrix, and a size-limit budget — all wired and runnable
with no-op stubs that pass green.
Output: 18 new files; `pnpm --filter @open-routing/embed install && build && test && size` all green; `test:e2e --list` finds 12 runs.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-component-embed-bundle/07-CONTEXT.md
@.planning/phases/07-web-component-embed-bundle/07-RESEARCH.md
@.planning/phases/07-web-component-embed-bundle/07-PATTERNS.md
@.planning/phases/07-web-component-embed-bundle/07-VALIDATION.md
@web/apps/admin/package.json
@web/apps/admin/vite.config.ts
@web/apps/admin/playwright.config.ts
@web/apps/admin/index.html
@web/packages/ui/vitest.config.ts
@web/packages/ui/src/test-setup.ts
@web/tsconfig.base.json
@web/turbo.json

<interfaces>
<!-- Phase 6 admin package.json — exact shape Phase 7 forks from. -->
From web/apps/admin/package.json:
```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@lit-labs/router": "^0.1.4",
    "@lit/localize": "^0.12.2",
    "@open-routing/ui": "workspace:*",
    "@shoelace-style/shoelace": "^2.20.1",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@babel/plugin-proposal-decorators": "^7.29.0",
    "@playwright/test": "^1.60.0",
    "@rolldown/plugin-babel": "^0.2.3",
    "urlpattern-polyfill": "^10.1.0",
    "vite": "^8.0.13",
    "vitest": "^3.2.4"
  }
}
```

<!-- Phase 6 admin vite.config.ts — embed differs: library mode, no proxy, babel plugin. -->
From web/apps/admin/vite.config.ts (current):
```typescript
export default defineConfig({
  appType: 'spa',
  server: { proxy: { '/v1': 'http://localhost:8080', '/healthz': '...', '/readyz': '...' } },
  build: { target: 'esnext', rollupOptions: { output: { manualChunks: (id) => {...} } } },
});
```

<!-- Phase 6 packages/ui vitest.config.ts — shape embed reuses. -->
From web/packages/ui/vitest.config.ts (current):
```typescript
export default defineConfig({
  test: {
    environment: 'happy-dom',
    setupFiles: ['./src/test-setup.ts'],
    include: ['src/**/*.test.ts'],
    reporters: ['verbose'],
    globals: false,
  },
});
```

<!-- Phase 6 packages/ui test-setup.ts — verbatim reuse + Animation shim. -->
From web/packages/ui/src/test-setup.ts:
```typescript
import 'urlpattern-polyfill';
if (typeof Element !== 'undefined') {
  if (!Element.prototype.getAnimations) Element.prototype.getAnimations = function () { return []; };
  if (!Element.prototype.animate) { Element.prototype.animate = function () { return {/*...stub Animation...*/} as unknown as Animation; }; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json + tsconfig.json + .gitignore for embed package</name>
  <files>web/apps/embed/package.json, web/apps/embed/tsconfig.json, web/.gitignore</files>
  <read_first>
    - web/apps/embed/package.json (existing skeleton — 192 bytes, just typecheck+lint scripts)
    - web/apps/embed/tsconfig.json (existing skeleton — 137 bytes, extends tsconfig.base.json)
    - web/apps/admin/package.json (Phase 6 analog — full shape to fork)
    - web/packages/ui/package.json (workspace dep target)
    - web/.gitignore (existing — confirm dist/ entry pattern; create if missing)
    - .planning/phases/07-web-component-embed-bundle/07-CONTEXT.md §D7-15 (npm tarball contract)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md §`web/apps/embed/package.json` (modify)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Implementation Strategy 8 (Pitfall 8: noExternal)
  </read_first>
  <action>
Overwrite web/apps/embed/package.json with the full Phase 7 shape. Set
`"name": "@open-routing/catalog-embed"` (publishable name per D7-15) and
`"version": "0.1.0-pre.1"` (real version, not "0.0.0"). Set `"private":
true` (D7-15: do not publish in v0.1; pnpm pack --dry-run is the
validation path). Set `"type": "module"` and `"sideEffects":
["./dist/embed.js"]` (array form — Pitfall 8 + RESEARCH §8 explanation;
declares that only the built entry has side effects from the
customElements.define call).

Add npm-tarball metadata fields per D7-15: `"main":
"./dist/embed.js"`, `"module": "./dist/embed.js"`, `"types":
"./dist/embed.d.ts"`, `"exports": { ".": { "import": "./dist/embed.js",
"types": "./dist/embed.d.ts" }, "./hash-router-adapter": { "import":
"./dist/hash-router-adapter.js", "types":
"./dist/hash-router-adapter.d.ts" }, "./package.json":
"./package.json" }` (WARNING #7 — the `./hash-router-adapter` subpath
export lets `catalog-shell.ts` in `packages/ui` dynamically import the
adapter from embed),
`"files": ["dist/", "README.md", "LICENSE", "CHANGELOG.md"]`.

Scripts: `typecheck` (`tsc --noEmit`), `lint` (`eslint src
--max-warnings 0`), `dev` (`vite`), `build` (`vite build`), `preview`
(`vite preview`), `test` (`vitest run`), `test:e2e` (`playwright
test`), `size` (`size-limit`), `pack:dry` (`pnpm pack --dry-run`).

CRITICAL — devDependencies layout per Pitfall 8: `@open-routing/ui` is
`workspace:*` in **devDependencies** (NOT dependencies — Vite library
mode externalizes regular deps; embed must bundle the workspace UI
package into dist/embed.js). Also add `lit ^3.3.3` to devDependencies
(it gets bundled, not externalized). `@shoelace-style/shoelace`,
`@lit-labs/router`, `@lit/localize` come transitively via
`@open-routing/ui` — do NOT add as direct deps.

Full devDependencies set: `@babel/plugin-proposal-decorators ^7.29.0`,
`@open-routing/ui workspace:*`, `@playwright/test ^1.60.0`,
`@rolldown/plugin-babel ^0.2.3`, `@size-limit/preset-app ^12.1.0`,
`happy-dom ^20.9.0`, `lit ^3.3.3`, `size-limit ^12.1.0`,
`urlpattern-polyfill ^10.1.0`, `vite ^8.0.13`, `vitest ^3.2.4`.

No `dependencies` section (or empty object) — every runtime dep is
bundled, no `peerDependencies` (host expects a self-contained bundle).

Leave tsconfig.json unchanged (existing 137-byte skeleton extends
`tsconfig.base.json` which already has the Phase 6 decorator config
verified via Phase 6 admin in production: `experimentalDecorators:
false` + `useDefineForClassFields: true` for TC39 2023-11 standard
decorators with `accessor` keyword — see 07-PATTERNS.md ¶tsconfig).

Verify web/.gitignore contains `dist/` (or add it). Also add
`web/apps/embed/test-results/`, `web/apps/embed/playwright-report/`,
and `web/apps/embed/*.tgz` to .gitignore so Playwright artifacts and
pnpm-pack tarball smoke outputs don't pollute git. The `*.tgz` rule
is shared with Plan 07-12 Task 2 (WARNING #12 — single source of truth
for the gitignore touch lives here in Plan 07-01).
  </action>
  <verify>
    <automated>
      test "$(node -p "JSON.parse(require('fs').readFileSync('web/apps/embed/package.json','utf8')).name")" = "@open-routing/catalog-embed" \
        && test "$(node -p "JSON.parse(require('fs').readFileSync('web/apps/embed/package.json','utf8')).devDependencies['@open-routing/ui']")" = "workspace:*" \
        && test "$(node -p "JSON.parse(require('fs').readFileSync('web/apps/embed/package.json','utf8')).dependencies?.['@open-routing/ui'] ?? 'absent'")" = "absent" \
        && grep -q '"sideEffects": \[' web/apps/embed/package.json \
        && grep -q "^dist/" web/.gitignore \
        && grep -q "apps/embed/\*\.tgz\|^\*\.tgz" web/.gitignore
    </automated>
  </verify>
  <acceptance_criteria>
    - File `web/apps/embed/package.json` exists; `node -p "JSON.parse(...).name"` returns `@open-routing/catalog-embed`
    - `JSON.parse(pkg).devDependencies['@open-routing/ui']` === `workspace:*` (Pitfall 8)
    - `JSON.parse(pkg).dependencies?.['@open-routing/ui']` is undefined (Pitfall 8 — must NOT be in dependencies)
    - `JSON.parse(pkg).sideEffects` is an array equal to `["./dist/embed.js"]` (D7-15, RESEARCH §8)
    - `JSON.parse(pkg).files` array contains `dist/`, `README.md`, `LICENSE`, `CHANGELOG.md`
    - `JSON.parse(pkg).exports['.']` has both `import` and `types` keys (D7-15)
    - `JSON.parse(pkg).exports['./hash-router-adapter']` has `import: './dist/hash-router-adapter.js'` + `types: './dist/hash-router-adapter.d.ts'` (WARNING #7 — shell imports adapter via this subpath at runtime)
    - Scripts include: `typecheck`, `lint`, `dev`, `build`, `preview`, `test`, `test:e2e`, `size`, `pack:dry`
    - `pnpm --filter @open-routing/embed install` exits 0 (workspace resolve OK)
    - `web/.gitignore` contains `dist/` line, `web/apps/embed/test-results/` line, AND `web/apps/embed/*.tgz` line (WARNING #12 — Plan 07-12 pack smoke writes tarballs into apps/embed/; this gitignore rule keeps them out of git)
  </acceptance_criteria>
  <done>package.json defines tarball-ready npm metadata; workspace dep is in devDependencies (will be bundled); install resolves; .gitignore prevents committing build artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: vite.config.ts (library mode) + index.html (dev entry) + vitest.config.ts + test-setup.ts</name>
  <files>web/apps/embed/vite.config.ts, web/apps/embed/index.html, web/apps/embed/vitest.config.ts, web/apps/embed/test-setup.ts</files>
  <read_first>
    - web/apps/admin/vite.config.ts (Phase 6 admin analog — SPA mode, manualChunks func form)
    - web/apps/admin/index.html (Phase 6 dev HTML pattern)
    - web/packages/ui/vitest.config.ts (Phase 6 ui analog — happy-dom)
    - web/packages/ui/src/test-setup.ts (Phase 6 ui analog — urlpattern + animate shim)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Implementation Strategy 1 (lines 273-325)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Code Examples Vitest (lines 982-998)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶vite.config.ts (lines 120-160)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶vitest.config.ts (lines 202-222)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶test-setup.ts (lines 226-253)
  </read_first>
  <action>
Create web/apps/embed/vite.config.ts. Import `defineConfig` from `vite`,
`resolve` from `node:path`, and `babel` from `@rolldown/plugin-babel`.

Plugins: babel with preset `{ preset: () => ({ plugins:
[['@babel/plugin-proposal-decorators', { version: '2023-11' }]] }),
rolldown: { filter: { code: '@' } } }` — same shape as Phase 6
admin's intended pattern (RESEARCH §1 lines 282-294). The
`@` filter restricts the babel transform to source files containing the
`@` decorator sigil for build perf.

Build config: `target: 'esnext'`, `sourcemap: true` (external .map
files per CONTEXT Discretion), `cssCodeSplit: false` (library mode
default — single CSS file).

`build.lib`: `{ entry: resolve(import.meta.dirname, 'src/index.ts'),
formats: ['es'], fileName: () => 'embed.js' }` — produces
dist/embed.js exactly (Pitfall 8: noExternal is the default for the
workspace dep when listed in devDependencies; do NOT set `external`).

`rollupOptions.output.chunkFileNames: 'embed-[name]-[hash].js'` — per-entity
lazy chunks emit as embed-agents-{hash}.js, embed-skills-{hash}.js, etc.
Do NOT define `manualChunks` (Pitfall 2 + RESEARCH §1: natural
dynamic-import code splitting handles chunking for library mode).

Do NOT add `server.proxy` — embed has no dev server proxy; Playwright
stub hosts mock API via `page.route` (D7-11).

Set `publicDir: 'e2e/hosts'` so `vite preview` (which serves the
`dist/` output) also serves the Playwright stub hosts under
`/hosts/{react,vue,html}/` (mirrors Phase 6's pattern of co-locating
test assets with served roots). This is the canonical resolution for
the stub-host path being `e2e/hosts/` while Vite preview serves
`dist/`: Vite copies `publicDir` contents into the served root at
preview time. The Playwright config's baseURL points at
`http://localhost:4173/hosts/{react,vue,html}/` and this wiring serves
them straight from `e2e/hosts/`.

Create web/apps/embed/index.html (dev-only — NOT in production
bundle; library mode does not emit HTML). Pattern from
07-PATTERNS.md ¶index.html: doctype + html + head with title "Open
Routing Catalog Embed (dev)" + meta charset + viewport. Body mounts
`<open-routing-catalog org-id="01952a6b-1c00-7000-8000-000000000001"
api-base-url="http://localhost:8080" theme="or-light"
modules="agents,skills,queues,channels,adapters,break-reasons">
</open-routing-catalog>` + `<script type="module"
src="/src/index.ts"></script>`. This is for `pnpm dev` testing only.

Create web/apps/embed/vitest.config.ts (verbatim from 07-PATTERNS.md
¶vitest.config.ts with one deviation per RESEARCH §Code Examples line
992-993: `setupFiles: ['./test-setup.ts']` — at root, NOT under src/).
Environment: `happy-dom`. `include: ['src/**/*.test.ts']`. `reporters:
['verbose']`. `globals: false`.

Create web/apps/embed/test-setup.ts verbatim from
web/packages/ui/src/test-setup.ts (07-PATTERNS.md says "Reuse pattern
X verbatim"): `import 'urlpattern-polyfill';` + the `getAnimations` +
`animate` shim block (Shoelace dialog/overlay transitions inside
shell's transitive components need it).
  </action>
  <verify>
    <automated>
      test -f web/apps/embed/vite.config.ts \
        && test -f web/apps/embed/vitest.config.ts \
        && test -f web/apps/embed/test-setup.ts \
        && test -f web/apps/embed/index.html \
        && grep -q "build.lib\|lib: {" web/apps/embed/vite.config.ts \
        && grep -q "fileName.*embed.js" web/apps/embed/vite.config.ts \
        && grep -q "@rolldown/plugin-babel\|@babel/plugin-proposal-decorators" web/apps/embed/vite.config.ts \
        && grep -q "happy-dom" web/apps/embed/vitest.config.ts \
        && grep -q "urlpattern-polyfill" web/apps/embed/test-setup.ts \
        && grep -q "publicDir.*e2e/hosts" web/apps/embed/vite.config.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `web/apps/embed/vite.config.ts` exists; contains `build.lib` config block, `formats: ['es']`, `fileName: () => 'embed.js'`
    - vite.config.ts contains `@rolldown/plugin-babel` import + `@babel/plugin-proposal-decorators` with `version: '2023-11'`
    - vite.config.ts contains `chunkFileNames: 'embed-[name]-[hash].js'` (lazy chunks naming)
    - vite.config.ts does NOT contain `manualChunks` (Pitfall 2 — natural code splitting only)
    - vite.config.ts does NOT contain `server.proxy` (embed has no dev proxy)
    - vite.config.ts sets `publicDir: 'e2e/hosts'` so `vite preview` serves the Playwright stub hosts (BLOCKER #1 — keeps `e2e/hosts/` canonical while Vite preview serves dist/)
    - `web/apps/embed/index.html` contains `<open-routing-catalog org-id="..."`
    - `web/apps/embed/vitest.config.ts` sets `environment: 'happy-dom'` + `setupFiles: ['./test-setup.ts']`
    - `web/apps/embed/test-setup.ts` imports `urlpattern-polyfill` + shims `Element.prototype.getAnimations` + `animate`
  </acceptance_criteria>
  <done>Vite library-mode build pipeline ready; vitest + happy-dom configured with URLPattern polyfill; index.html present for `vite dev` smoke.</done>
</task>

<task type="auto">
  <name>Task 3: .size-limit.json + playwright.config.ts + 3 stub hosts + 4 spec stubs + 2 unit test stubs</name>
  <files>web/apps/embed/.size-limit.json, web/apps/embed/playwright.config.ts, web/apps/embed/e2e/hosts/react/index.html, web/apps/embed/e2e/hosts/vue/index.html, web/apps/embed/e2e/hosts/html/index.html, web/apps/embed/e2e/specs/embed-shadow-dom-isolation.spec.ts, web/apps/embed/e2e/specs/embed-org-id-header.spec.ts, web/apps/embed/e2e/specs/embed-auth-expired.spec.ts, web/apps/embed/e2e/specs/embed-modules-filter.spec.ts, web/apps/embed/src/embed-element.test.ts, web/apps/embed/src/hash-router-adapter.test.ts</files>
  <read_first>
    - web/apps/admin/playwright.config.ts (Phase 6 analog — single-project + reuseExistingServer)
    - web/apps/admin/e2e/smoke.spec.ts (Phase 6 spec shape — TEST_ORG_ID + test.describe + Shadow DOM piercing)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Implementation Strategy 9 (lines 668-742)
    - .planning/phases/07-web-component-embed-bundle/07-RESEARCH.md §Code Examples (.size-limit.json — lines 949-980)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶playwright.config.ts (lines 258-291)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶Playwright Stub Hosts (lines 644-684)
    - .planning/phases/07-web-component-embed-bundle/07-PATTERNS.md ¶Playwright Specs (lines 688-735)
    - .planning/phases/07-web-component-embed-bundle/07-VALIDATION.md (Wave 0 Requirements — 17 files listed)
  </read_first>
  <action>
Create web/apps/embed/.size-limit.json as a JSON array. Single eager
entry per D7-09 + D7-10: `{ "name": "embed.js (eager baseline)",
"path": "dist/embed.js", "limit": "70 KB", "gzip": true }`. Do NOT add
soft per-chunk entries in Wave 0 — Plan 07-10 wires them after lazy
chunks actually emit. The Wave 0 budget is the hard gate per CONTEXT
D7-09 (eager only).

Create web/apps/embed/playwright.config.ts per 07-PATTERNS.md
¶playwright.config.ts + RESEARCH §9 lines 672-698. Import
`defineConfig` + `devices` from `@playwright/test`. Config: `testDir:
'./e2e'`, `testMatch: 'specs/*.spec.ts'`, `timeout: 30_000`, `retries:
1` (D7-12), reporter `[['list'], ['html', { open: 'never' }]]`. `use`
section: `baseURL: 'http://localhost:4173'`, `trace:
'retain-on-failure'`, `screenshot: 'only-on-failure'`, `video:
'retain-on-failure'`. Three projects matrix: `[ { name: 'react', use:
{ ...devices['Desktop Chrome'], baseURL:
'http://localhost:4173/hosts/react/' } }, { name: 'vue', use: {
...devices['Desktop Chrome'], baseURL:
'http://localhost:4173/hosts/vue/' } }, { name: 'html', use: {
...devices['Desktop Chrome'], baseURL:
'http://localhost:4173/hosts/html/' } } ]`. `webServer: { command:
'pnpm --filter @open-routing/embed preview', url:
'http://localhost:4173', reuseExistingServer: true, timeout: 60_000
}` (60s because preview waits for build).

Create stub hosts (D7-11 + RESEARCH §9 lines 705-741) — verbatim
pattern from 07-PATTERNS.md ¶Playwright Stub Hosts.

`web/apps/embed/e2e/hosts/react/index.html`: doctype + head with
title "React host (embed test)", `<script type="importmap">` pinning
`react`: `https://esm.sh/react@18.3.1` and `react-dom/client`:
`https://esm.sh/react-dom@18.3.1/client`, style block with `* { all:
revert; box-sizing: border-box; } body { font-family: serif;
background: lime; color: red; }` (aggressive CSS reset — EMBED-04
isolation assertion is meaningful only when host CSS is poisoned).
Body: `<h1 style="font-size: 200%; color: purple;">Host CSS
poison</h1>` + `<div id="root"></div>` + `<script type="module"
src="/embed.js"></script>` + inline module script that imports React +
createRoot + mounts `React.createElement('open-routing-catalog', {
'org-id': '01952a6b-1c00-7000-8000-000000000001', 'api-base-url':
'http://localhost:4173', theme: 'or-light', modules: 'agents,skills'
})`.

`web/apps/embed/e2e/hosts/vue/index.html`: same CSS reset + Host
CSS poison heading, importmap pinning `vue`:
`https://esm.sh/vue@3.5.13`, inline module script that imports
`createApp` + `defineComponent` + `h` from `vue`, defines component
returning `h('open-routing-catalog', { 'org-id': '...', 'api-base-url':
'http://localhost:4173', theme: 'or-light', modules: 'agents,skills'
})`, mounts on `#root`. Per RESEARCH §Stub Host: Vue stubs use the
same aggressive reset + same org-id constant.

`web/apps/embed/e2e/hosts/html/index.html`: same CSS reset + Host
CSS poison heading. Body: `<open-routing-catalog org-id="..."
api-base-url="..." theme="or-light" modules="agents,skills">
</open-routing-catalog>` + `<script type="module"
src="/embed.js"></script>`. No framework script — direct HTML mount.

Create 4 spec stubs in web/apps/embed/e2e/specs/. Each spec uses
`import { test, expect } from '@playwright/test';` + `const
TEST_ORG_ID = '01952a6b-1c00-7000-8000-000000000001';` (mirrors admin
smoke.spec.ts:18) + `test.describe.skip(...)` block with stub `it`
bodies that match the EMBED-XX requirements:

- `embed-shadow-dom-isolation.spec.ts`: test.describe.skip('embed
Shadow DOM isolation (EMBED-04, D7-04)') with test cases "host CSS
does not leak in" and "embed CSS does not escape" and "no portal
escape from <sl-dialog> replacement".
- `embed-org-id-header.spec.ts`:
test.describe.skip('X-Org-Id propagation (EMBED-02)') with test case
"every API call carries X-Org-Id from attribute".
- `embed-auth-expired.spec.ts`: test.describe.skip('auth-expired
event (EMBED-07, D7-14)') with test cases "fires composed:true on
401" and "host receives event with request_id detail".
- `embed-modules-filter.spec.ts`: test.describe.skip('modules
filter (EMBED-06)') with test cases "sidebar shows only listed
modules" and "unlisted entity route redirects to first allowed".

Use `test.describe.skip(...)` so Wave 0 specs are runnable but pass
green (0 tests run). Plan 07-09 implements them. Inside each
describe, add a single placeholder `test('todo: Wave 2 implements',
() => {})` so `--list` enumerates them.

Create 2 unit test stubs:

- `web/apps/embed/src/embed-element.test.ts`:
`import { describe, it } from 'vitest';
describe.skip('OpenRoutingCatalog (Wave 1)', () => { it('todo: Wave 1 implements', () => {}); });`
- `web/apps/embed/src/hash-router-adapter.test.ts`:
`import { describe, it } from 'vitest';
describe.skip('HashRouterAdapter (Wave 1)', () => { it('todo: Wave 1 implements', () => {}); });`

These satisfy 07-VALIDATION.md's "Wave 0 Requirements" 17-file
contract.
  </action>
  <verify>
    <automated>
      test -f web/apps/embed/.size-limit.json \
        && test -f web/apps/embed/playwright.config.ts \
        && test -f web/apps/embed/e2e/hosts/react/index.html \
        && test -f web/apps/embed/e2e/hosts/vue/index.html \
        && test -f web/apps/embed/e2e/hosts/html/index.html \
        && test -f web/apps/embed/e2e/specs/embed-shadow-dom-isolation.spec.ts \
        && test -f web/apps/embed/e2e/specs/embed-org-id-header.spec.ts \
        && test -f web/apps/embed/e2e/specs/embed-auth-expired.spec.ts \
        && test -f web/apps/embed/e2e/specs/embed-modules-filter.spec.ts \
        && test -f web/apps/embed/src/embed-element.test.ts \
        && test -f web/apps/embed/src/hash-router-adapter.test.ts \
        && node -e "const c = require('./web/apps/embed/.size-limit.json'); if (c[0].limit !== '70 KB' || c[0].gzip !== true) process.exit(1)" \
        && grep -q "retries: 1" web/apps/embed/playwright.config.ts \
        && grep -q "esm.sh/react@18.3.1" web/apps/embed/e2e/hosts/react/index.html \
        && grep -q "esm.sh/vue@3.5.13" web/apps/embed/e2e/hosts/vue/index.html \
        && grep -q "all: revert" web/apps/embed/e2e/hosts/html/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `.size-limit.json` is a JSON array with 1 entry; entry has `limit: "70 KB"` and `gzip: true` (D7-09, D7-10)
    - `playwright.config.ts` has `retries: 1` (D7-12), `testMatch: 'specs/*.spec.ts'`, `baseURL: 'http://localhost:4173'`, and 3 projects (`react`, `vue`, `html`)
    - `playwright.config.ts.webServer.command` === `'pnpm --filter @open-routing/embed preview'` (RESEARCH §9)
    - All 3 stub hosts contain `* { all: revert` (aggressive CSS reset — EMBED-04 isolation test meaningfulness)
    - All 3 stub hosts mount `<open-routing-catalog org-id="01952a6b-1c00-7000-8000-000000000001"`
    - React stub pins `react@18.3.1` from esm.sh; Vue stub pins `vue@3.5.13` from esm.sh (CONTEXT Discretion — specific patch versions)
    - All 4 e2e specs are runnable but `test.describe.skip(...)` — they pass green with 0 actual tests
    - Both unit test stubs use `describe.skip(...)` with placeholder `it('todo: Wave 1 implements')`
  </acceptance_criteria>
  <done>Wave 0 test infrastructure complete (17 files per 07-VALIDATION.md); pnpm install resolves; specs runnable but skipped; size-limit budget declared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host page → embed | Untrusted HTML attributes (theme JSON, modules CSV) cross into LitElement |
| Host page CSS → embed Shadow DOM | EMBED-04 contract — must be isolated |
| esm.sh CDN → Playwright browser | First external CDN dep in CI — supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-CDN-01 | Tampering | esm.sh CDN pin drift in Playwright stub hosts | accept (info) | Specific patch pin (`react@18.3.1`, `vue@3.5.13`) per CONTEXT Discretion; `retries: 1` per D7-12 absorbs CDN flakes; impact scope is CI test reliability only, never production bundle |
| T-7-PKG-SC | Tampering | npm package installs (size-limit, @size-limit/preset-app, happy-dom, @rolldown/plugin-babel) | mitigate | All 5 packages already audited in 07-RESEARCH.md §Package Legitimacy Audit (`size-limit` 6+ yrs / 150k weekly / used by date-fns/redux/Preact; `@size-limit/preset-app` same publisher; `happy-dom` 4M weekly; `@rolldown/plugin-babel` official Rolldown team; `urlpattern-polyfill` already used by Phase 6). No [ASSUMED] or [SUS] packages — slopcheck disposition is `Approved` per audit. |
</threat_model>

<verification>
- `pnpm --filter @open-routing/embed install` exits 0 (workspace resolve OK, no peer-dep warnings about lit/Shoelace because they come transitively via @open-routing/ui)
- `pnpm --filter @open-routing/embed test --run` exits 0 (vitest finds config, runs 0 tests because both stubs are `describe.skip`)
- `pnpm --filter @open-routing/embed test:e2e --list` lists 12 prospective spec runs (4 stub specs x 3 projects); exits 0
- `pnpm --filter @open-routing/embed lint` passes (no source code touched in Wave 0)
- `pnpm --filter @open-routing/embed typecheck` passes (test stub files use only describe.skip)
- Wave 0 acceptance: 17 files from 07-VALIDATION.md §Wave 0 Requirements exist on disk
</verification>

<success_criteria>
- All Wave 0 test/config infrastructure exists on disk (17 files per 07-VALIDATION.md)
- `pnpm --filter @open-routing/embed install && test --run && test:e2e --list` all green
- `web/apps/embed/.size-limit.json` declares the 70 KB gzipped budget (no actual measurement yet — dist/embed.js doesn't exist; Plan 07-10 + 07-12 wire size-limit CI)
- Stub hosts ready to be served by Vite preview at localhost:4173 once `dist/embed.js` exists
- All Plan 07-XX Wave 1+ executors have running test infrastructure before they touch implementation code
</success_criteria>

<output>
Create `.planning/phases/07-web-component-embed-bundle/07-01-SUMMARY.md` when done
</output>
