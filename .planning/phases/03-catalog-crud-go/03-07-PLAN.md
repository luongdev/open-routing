---
phase: 03-catalog-crud-go
plan: 07
type: execute
wave: 4
depends_on: [03-06]
files_modified:
  - services/api/internal/catalog/skills.go
  - services/api/internal/catalog/skills_test.go
  - services/api/internal/catalog/break_reasons.go
  - services/api/internal/catalog/break_reasons_test.go
autonomous: true
requirements: [CAT-02, CAT-07, CAT-08, CAT-09, CAT-10, CAT-11]
requirements_addressed: [CAT-02, CAT-07, CAT-08, CAT-09, CAT-10, CAT-11]
user_setup: []

must_haves:
  truths:
    - "POST /v1/orgs/{org_id}/skills creates a skill with version=1 enabled=true (CAT-02); response 201 + Skill DTO."
    - "GET /v1/orgs/{org_id}/skills/{id} served from Redis under `or:{orgId}:skills:{id}` 60s TTL (CAT-11)."
    - "PATCH skills with wrong version → 409 + current; correct version → 200 + updated (CAT-08)."
    - "DELETE skills sets enabled=false; cache DELed; re-DELETE → 404 (CAT-09)."
    - "List skills supports cursor + ?include_disabled + ?name (CAT-09, CAT-10)."
    - "Cross-org GET on skills returns 404 (FOUND-08; verified by Plan 03-10 isolation suite)."
    - "POST /v1/orgs/{org_id}/break-reasons creates a break_reason with version=1 (CAT-07)."
    - "GET /v1/orgs/{org_id}/break-reasons/{id} served from cache 60s (CAT-11) under `or:{orgId}:break_reasons:{id}`."
    - "PATCH break-reasons wrong version → 409 (CAT-08)."
    - "DELETE break-reasons sets enabled=false (CAT-09)."
    - "break_reasons list supports cursor + filter + ?name (CAT-10); default sort is by `display_order` (CAT-07 product semantics) but cursor pagination uses (created_at, id) like other entities."
    - "skills_test.go covers happy CRUD + 409 + soft-delete + cursor + name + cache."
    - "break_reasons_test.go covers happy CRUD + 409 + soft-delete + cursor + display_order ordering + cache + routable flag round-trip."
  artifacts:
    - path: services/api/internal/catalog/skills.go
      provides: "Real CRUD bodies for ListSkills, CreateSkill, DeleteSkill, GetSkill, UpdateSkill (replaces Plan 03-05 placeholders)"
      contains: "cache.GetOrSet[api.Skill]"
      min_lines: 220
    - path: services/api/internal/catalog/skills_test.go
      provides: "End-to-end skills CRUD tests"
      contains: "TestSkills_CreateThenGet"
      min_lines: 250
    - path: services/api/internal/catalog/break_reasons.go
      provides: "Real CRUD bodies for ListBreakReasons + CreateBreakReason + DeleteBreakReason + GetBreakReason + UpdateBreakReason"
      contains: "cache.GetOrSet[api.BreakReason]"
      min_lines: 220
    - path: services/api/internal/catalog/break_reasons_test.go
      provides: "End-to-end break_reasons CRUD tests + display_order + routable flag tests"
      contains: "TestBreakReasons_DisplayOrder"
      min_lines: 250
  key_links:
    - from: services/api/internal/catalog/skills.go
      to: services/api/internal/db/generated/skills.sql.go (Plan 03-03 output)
      via: "generated.New(orgDB).<Query>"
      pattern: "q\\.(Insert|Get|Update|SoftDelete|List)Skill"
    - from: services/api/internal/catalog/break_reasons.go
      to: services/api/internal/db/generated/break_reasons.sql.go
      via: "generated.New(orgDB).<Query>"
      pattern: "q\\.(Insert|Get|Update|SoftDelete|List)BreakReason"
---

<objective>
Replace placeholder bodies in `skills.go` and `break_reasons.go` (Plan 03-05) with the real CAT-02 + CAT-07 + CAT-08 + CAT-09 + CAT-10 + CAT-11 implementations, plus integration tests for both entities. Both entities follow the agents.go template (Plan 03-06) — no cross-row FK validation needed (skills and break_reasons have no FK fields), no JSONB handling, no join table. Pure CRUD with version-locked UPDATE, soft-delete, cursor pagination, cache wrapping.

The two entities are bundled in one plan because (a) neither has FK probes (no D-76 cross-row check), (b) neither has join-table interaction (no agent_skills equivalent), (c) shape is identical to agents minus the skills replace logic. This keeps the plan single-concern (simple CRUD entities), well within the 2-3 task budget, and runs in parallel with Plan 03-08 (queues+channels) and Plan 03-09 (adapters+agent_skills).

Purpose: After this plan, 3 of the 6 catalog entities (agents from 03-06, skills + break_reasons from this plan) have real handlers + tests. Plan 03-08 finishes queues+channels (FK probe variant); Plan 03-09 finishes adapters (JSONB variant) + agent_skills (join semantics).

Output: 4 files (2 implementation + 2 tests). Each entity follows agents.go's structure verbatim with field swaps.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-catalog-crud-go/03-CONTEXT.md
@.planning/phases/03-catalog-crud-go/03-PATTERNS.md
@services/api/internal/catalog/agents.go
@services/api/internal/catalog/agents_test.go
@services/api/internal/catalog/cursor.go
@services/api/internal/catalog/mappers.go
@services/api/internal/catalog/errors.go
@services/api/internal/cache/cache.go
@services/api/internal/api/server.gen.go
@services/api/internal/api/types.gen.go

<interfaces>
<!-- Pattern: clone agents.go, swap field set. -->

**Skill DTO fields (verified `types.gen.go:854 Skill`):**
- `id UUIDv7`
- `org_id UUIDv7`
- `external_id string`
- `name string`
- `description *string` (nullable)
- `skill_type string`
- `enabled bool`
- `version int` (post Wave 0)
- `created_at *time.Time`
- `updated_at *time.Time`

Plus the request types:
- `CreateSkillRequest` — body shape for POST.
- `UpdateSkillRequest` — body shape for PATCH (includes required `version int` + optional `name`, `description`, `skill_type`, `enabled`).

**BreakReason DTO fields (verified `types.gen.go:465 BreakReason`):**
- `id UUIDv7`
- `org_id UUIDv7`
- `name string`
- `routable bool`
- `display_order int`
- `enabled bool`
- `version int` (post Wave 0 / always present)
- `created_at *time.Time`
- `updated_at *time.Time`

Note: **BreakReason has NO external_id** — per CAT-07 spec, uses `UNIQUE(org_id, name)` for the identity composite. Migration 002 (Plan 03-02) reflects this; sqlc-generated InsertBreakReaonParams has no `ExternalID` field. The `CreateBreakReasonRequest` body has `name`, `routable`, `display_order`, optional `enabled` — NO external_id field.

**`services/api/internal/catalog/skills.go` shape (~220 LOC):**

Clone the structure from `agents.go` but:
- Replace `Agent` → `Skill` everywhere.
- Replace `agents` → `skills` in cache keys (`cache.Key(orgID, "skills", id)`).
- Replace `q.InsertAgent / q.GetAgent / q.UpdateAgent / q.SoftDeleteAgent / q.ListAgents / q.ListAgentsIncludingDisabled / q.GetAgentByIdAnyVersion` → corresponding `Skill` query methods.
- Remove the `replaceAgentSkills` helper call from CreateSkill + UpdateSkill — skills has no embedded join.
- Adjust `mapSkill(row generated.Skill) api.Skill` mapper (similar to mapAgent but no skills array, includes the nullable `description` field via pgtype.Text → *string).
- For nullable `description` field in InsertSkillParams: convert from `*string` request body to `pgtype.Text` via `pgTextPtr` (mappers.go helper).
- 409 wrappers for skills are flat (NOT union) — `UpdateSkill409JSONResponse` has `Current Skill`, `Error UpdateSkill409JSONResponseBodyError`, `Reason string` (verify with `grep -A 10 "type UpdateSkill409" services/api/internal/api/types.gen.go`).
- `CreateSkill409JSONResponse` for unique-violation (23505 on UNIQUE(org_id, external_id)) — emit via `api.CreateSkill409JSONResponse{Error: api.ErrorCodeVersionConflict, Reason: "external_id_collision"}` (flat ErrorResponse, NOT a union — `CreateAgent409` is the ONLY union per Pitfall 2).

Key block — `mapSkill`:

```go
func mapSkill(row generated.Skill) api.Skill {
    var desc *string
    if row.Description.Valid {
        v := row.Description.String
        desc = &v
    }
    return api.Skill{
        Id:          api.UUIDv7(apiUUID(row.ID)),
        OrgId:       api.UUIDv7(apiUUID(row.OrgID)),
        ExternalId:  row.ExternalID,
        Name:        row.Name,
        Description: desc,
        SkillType:   row.SkillType,
        Enabled:     row.Enabled,
        Version:     int(row.Version),
        CreatedAt:   ptrTime(row.CreatedAt),
        UpdatedAt:   ptrTime(row.UpdatedAt),
    }
}
```

Key block — UpdateSkill UPDATE call:

```go
row, err := q.UpdateSkill(ctx, generated.UpdateSkillParams{
    ID:          pgUUID(skillID),
    Name:        derefOr(req.Body.Name, ""),
    Description: pgTextPtr(req.Body.Description),
    SkillType:   derefOr(req.Body.SkillType, ""),
    Enabled:     derefOr(req.Body.Enabled, true),
    OrgID:       pgUUID(orgID),
    Version:     int32(req.Body.Version),
})
```

(Verify the exact ListSkillsParams/UpdateSkillParams field names after `task gen` in Plan 03-03 — the `Description` field name may be `Column3` if sqlc emits positional names.)

**`services/api/internal/catalog/break_reasons.go` shape (~220 LOC):**

Same template, but:
- No `external_id` field — InsertBreakReasonParams has `name` and uses UNIQUE(org_id, name) for collision detection.
- `routable bool` field — straight pass-through from req.Body to params.
- `display_order int` field — straight pass-through.
- `cache.Key(orgID, "break_reasons", id)` (entity slug uses underscore form per D-58 verbatim).
- `mapBreakReason(row generated.BreakReason) api.BreakReason` mapper.
- 23505 unique violation maps to 409 with `reason: "name_collision"` (not `external_id_collision` — break_reasons has no external_id).

Key block — `mapBreakReason`:

```go
func mapBreakReason(row generated.BreakReason) api.BreakReason {
    return api.BreakReason{
        Id:           api.UUIDv7(apiUUID(row.ID)),
        OrgId:        api.UUIDv7(apiUUID(row.OrgID)),
        Name:         row.Name,
        Routable:     row.Routable,
        DisplayOrder: int(row.DisplayOrder),
        Enabled:      row.Enabled,
        Version:      int(row.Version),
        CreatedAt:    ptrTime(row.CreatedAt),
        UpdatedAt:    ptrTime(row.UpdatedAt),
    }
}
```

**`services/api/internal/catalog/skills_test.go` shape (~250 LOC):**

Mirror agents_test.go test list (12 tests) but with skill-specific fields and assertions. Drop the SkillsReplace_* tests (skills has no embedded join). Add a test for `description` nullable round-trip.

Required test cases:
1. TestSkills_CreateThenGet
2. TestSkills_GetMissing
3. TestSkills_VersionConflict
4. TestSkills_SoftDelete
5. TestSkills_SoftDelete_IncludeDisabled
6. TestSkills_Cursor
7. TestSkills_NameSearch
8. TestSkills_CacheInvalidationOnUpdate
9. TestSkills_CacheInvalidationOnDelete
10. TestSkills_DescriptionNullable — POST skill without description; GET returns description=null/omitted; PATCH adds description; GET returns it.
11. TestSkills_CrossOrgGet404 — POST in orgA; GET via orgB header → 404.
12. TestSkills_LimitOutOfRange (or BadCursor — pick one for coverage).

**`services/api/internal/catalog/break_reasons_test.go` shape (~250 LOC):**

Mirror agents_test.go (12 tests) with break_reason-specific assertions. Add:
- TestBreakReasons_DisplayOrder — POST 3 break_reasons with display_order=10, 20, 5; List default returns them sorted by created_at DESC (cursor pagination ordering — NOT display_order; that's the migration's index for the UI sort path, not the default list path). Document if the list endpoint should sort by display_order ASC instead — currently the spec says cursor pagination uses (created_at, id), and Plan 03-02 has both indexes. The integration test asserts the implemented behavior matches what's in `sqlc/queries/break_reasons.sql` from Plan 03-03.
- TestBreakReasons_RoutableFlag — POST routable=true and routable=false; verify round-trip.
- TestBreakReasons_NameUniqueCollision — POST two with same name in same org → second returns 409 with `reason: "name_collision"`.

Required test cases:
1. TestBreakReasons_CreateThenGet
2. TestBreakReasons_GetMissing
3. TestBreakReasons_VersionConflict
4. TestBreakReasons_SoftDelete
5. TestBreakReasons_SoftDelete_IncludeDisabled
6. TestBreakReasons_Cursor
7. TestBreakReasons_NameSearch
8. TestBreakReasons_CacheInvalidationOnUpdate
9. TestBreakReasons_CacheInvalidationOnDelete
10. TestBreakReasons_RoutableFlag
11. TestBreakReasons_DisplayOrder
12. TestBreakReasons_NameUniqueCollision
13. TestBreakReasons_CrossOrgGet404
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement skills.go end-to-end + write skills_test.go integration suite</name>
  <files>
    services/api/internal/catalog/skills.go,
    services/api/internal/catalog/skills_test.go
  </files>
  <action>Replace placeholder `skills.go` with the real implementation by cloning `agents.go` and adapting:

(a) Swap `Agent` → `Skill` types/wrappers/params.
(b) Swap `cache.Key(orgID, "agents", ...)` → `cache.Key(orgID, "skills", ...)`.
(c) Swap sqlc method names (`q.InsertAgent` → `q.InsertSkill`, etc.).
(d) DROP the `replaceAgentSkills` helper invocation from CreateSkill + UpdateSkill — skills has no embedded relation.
(e) ADD `description *string` handling — convert via `pgTextPtr(req.Body.Description)` for both Insert + Update params.
(f) Add `mapSkill(row generated.Skill) api.Skill` — read `row.Description.Valid` and emit `*string`.

For CreateSkill 23505 path: emit `api.CreateSkill409JSONResponse{Error: api.ErrorCodeVersionConflict, Reason: "external_id_collision"}` (flat ErrorResponse; CreateSkill409 is NOT a union — only CreateAgent409 is).

For UpdateSkill 409 path: emit `api.UpdateSkill409JSONResponse{Current: mapSkill(cur), Error: "version_conflict", Reason: "version_mismatch"}`. Verify the field shape via `grep -A 10 "type UpdateSkill409JSONResponse" services/api/internal/api/server.gen.go` — should match UpdateAgent409 shape.

For UpdateSkill 422 path (FK validation on skill_type or other): NONE. Skills has no FK reference fields. The 422 response wrapper may or may not exist on UpdateSkill — if `Update_Skill422JSONResponse` is NOT in server.gen.go (because Plan 03-01 Wave 0 only added 422 to CreateChannel + UpdateChannel + UpdateAgent), this is fine — skills handler simply never returns 422. Verify by `grep "UpdateSkill422\|CreateSkill422" services/api/internal/api/server.gen.go` — expect 0 hits.

Then write `skills_test.go` with the 12 test cases per `<interfaces>`. Mirror agents_test.go almost verbatim — use the same `newTestHandlers(t)` + `cleanCatalogTables(t, ctx)` + `httpPOST/Get/Patch/Delete` helpers; replace `/agents` with `/skills` and `api.Agent` with `api.Skill`.

Hazards:
- **Description nullable**: sqlc with `emit_pointers_for_null_types: true` emits `Description pgtype.Text` (Valid bool); the mapper reads `Valid` flag.
- **CreateSkill409 vs CreateAgent409**: CreateAgent409 is the only union (Pitfall 2); CreateSkill409 / CreateQueue409 / CreateChannel409 are flat ErrorResponse — verify shapes BEFORE writing handler.</action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/services/api && go build ./internal/catalog/... && go vet ./internal/catalog/... && go test -count=1 -race -timeout 120s -run TestSkills ./internal/catalog/... && grep -F "cache.GetOrSet[api.Skill]" internal/catalog/skills.go && grep -F "cache.Key(orgID, \"skills\"," internal/catalog/skills.go && grep -F "GetSkillByIdAnyVersion" internal/catalog/skills.go && grep -F "pgTextPtr" internal/catalog/skills.go && grep -cE "^func TestSkills_" internal/catalog/skills_test.go | awk '{if($1>=10) print "OK"; else print "FAIL"}' | grep OK && echo OK</automated>
  </verify>
  <done>skills.go has 5 handler bodies + mapSkill; skills_test.go has 10+ tests; full suite passes; cache keys use "skills" slug; description nullable handled.</done>
  <read_first>
    - services/api/internal/catalog/agents.go (the template; Plan 03-06 output — copy almost verbatim with entity swaps)
    - services/api/internal/api/types.gen.go (lines 854-? — Skill struct shape; lines around CreateSkillRequest + UpdateSkillRequest)
    - services/api/internal/api/server.gen.go (find UpdateSkill409JSONResponse + CreateSkill409JSONResponse shapes via grep)
    - services/api/internal/db/generated/skills.sql.go (Plan 03-03 output — confirm ListSkillsParams + InsertSkillParams + UpdateSkillParams field names)
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-49..D-66 — same patterns as agents)
    - decisions: CAT-02, CAT-08, CAT-09, CAT-10, CAT-11; Pitfall 2 (NOT applicable — CreateSkill409 is flat)
  </read_first>
  <acceptance_criteria>
    - `test -f services/api/internal/catalog/skills.go`
    - `test -f services/api/internal/catalog/skills_test.go`
    - `grep -F "cache.GetOrSet[api.Skill]" services/api/internal/catalog/skills.go` returns ≥ 1.
    - `grep -F "cache.Key(orgID, \"skills\"," services/api/internal/catalog/skills.go` returns ≥ 3.
    - `grep -F "h.deps.Cache.Del(" services/api/internal/catalog/skills.go` returns ≥ 3.
    - `grep -F "GetSkillByIdAnyVersion" services/api/internal/catalog/skills.go` returns ≥ 1 (D-66 disambiguation).
    - `grep -F "pgTextPtr(req.Body.Description)" services/api/internal/catalog/skills.go` returns ≥ 1 (nullable description handling).
    - `grep -F "func mapSkill(" services/api/internal/catalog/skills.go` returns 1.
    - `grep -cE "^func TestSkills_" services/api/internal/catalog/skills_test.go` returns ≥ 10.
    - `cd services/api && go build ./internal/catalog/...` exits 0.
    - `cd services/api && go test -count=1 -race -timeout 120s -run TestSkills ./internal/catalog/...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement break_reasons.go end-to-end + write break_reasons_test.go integration suite</name>
  <files>
    services/api/internal/catalog/break_reasons.go,
    services/api/internal/catalog/break_reasons_test.go
  </files>
  <action>Replace placeholder `break_reasons.go` with the real implementation. Clone skills.go (now complete from Task 1) and adapt:

(a) Swap `Skill` → `BreakReason` types/wrappers/params.
(b) DROP `external_id` field — break_reasons uses UNIQUE(org_id, name) instead. `CreateBreakReasonRequest` body has `name`, `routable`, `display_order`, `enabled?` only.
(c) Cache key uses `"break_reasons"` slug (D-58 verbatim — entity slug is the URL plural form; `/break-reasons` URL but the cache key uses the underscored DB form to match the table name).

Wait — D-58 says "entity slug is the URL plural form" — so should it be `"break-reasons"` (URL kebab-case) or `"break_reasons"` (snake_case)? CONTEXT.md D-58 says "URL plural form (`agents`, `skills`, `queues`, `channels`, `adapters`, `break_reasons`)". So `"break_reasons"` (with underscore) is the locked form. Use it verbatim.

(d) Adjust `mapBreakReason(row generated.BreakReason) api.BreakReason` — straight field mapping; no nullable fields except CreatedAt/UpdatedAt; routable + display_order are simple round-trips.
(e) 23505 collision message: `reason: "name_collision"` (NOT `external_id_collision`).

Then write `break_reasons_test.go` with 13 test cases per `<interfaces>`. Mirror skills_test.go with adaptations:
- DROP TestSkills_DescriptionNullable (no equivalent field).
- ADD TestBreakReasons_RoutableFlag — POST routable=true and routable=false; GET both; round-trip preserved.
- ADD TestBreakReasons_DisplayOrder — POST 3 break_reasons with display_order=10, 20, 5; assert the list endpoint orders them by (created_at, id) cursor pagination DESC, NOT by display_order. Document the expected behavior so the test pins it.
- ADD TestBreakReasons_NameUniqueCollision — POST {name:"Lunch"}; second POST {name:"Lunch"} → 409 (or possibly 422 — verify which the unique-violation handler maps to per Plan 03-06 errors.go). 23505 → 409 with `reason: "name_collision"`.

Hazards:
- The slug `"break_reasons"` in cache.Key is LOCKED — Plan 03-10's isolation test will inspect cache contents using this exact slug.
- BreakReason has no external_id, so the InsertBreakReasonParams struct does NOT have an ExternalID field. If your handler tries to pass one, compile fails.</action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/services/api && go build ./internal/catalog/... && go vet ./internal/catalog/... && go test -count=1 -race -timeout 120s -run TestBreakReasons ./internal/catalog/... && grep -F "cache.GetOrSet[api.BreakReason]" internal/catalog/break_reasons.go && grep -F "cache.Key(orgID, \"break_reasons\"," internal/catalog/break_reasons.go && grep -F "GetBreakReasonByIdAnyVersion" internal/catalog/break_reasons.go && ! grep -F "ExternalID:" internal/catalog/break_reasons.go && grep -cE "^func TestBreakReasons_" internal/catalog/break_reasons_test.go | awk '{if($1>=10) print "OK"; else print "FAIL"}' | grep OK && echo OK</automated>
  </verify>
  <done>break_reasons.go has 5 handler bodies + mapBreakReason; break_reasons_test.go has 10+ tests; full suite passes; cache slug "break_reasons" used verbatim; no external_id field referenced.</done>
  <read_first>
    - services/api/internal/catalog/skills.go (Task 1 output — clone & swap)
    - services/api/internal/api/types.gen.go (BreakReason struct at line 465; CreateBreakReasonRequest + UpdateBreakReasonRequest shapes)
    - services/api/internal/api/server.gen.go (UpdateBreakReason409 + CreateBreakReason 409 if present — verify flat ErrorResponse shape)
    - services/api/internal/db/generated/break_reasons.sql.go (Plan 03-03 output)
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-58 slug format; CAT-07 spec — no external_id)
    - migrations/000002_catalog_v0_1.up.sql (verify break_reasons table has UNIQUE(org_id, name) and display_order INTEGER)
    - decisions: CAT-07, CAT-08, CAT-09, CAT-10, CAT-11
  </read_first>
  <acceptance_criteria>
    - `test -f services/api/internal/catalog/break_reasons.go`
    - `test -f services/api/internal/catalog/break_reasons_test.go`
    - `grep -F "cache.GetOrSet[api.BreakReason]" services/api/internal/catalog/break_reasons.go` returns ≥ 1.
    - `grep -F "cache.Key(orgID, \"break_reasons\"," services/api/internal/catalog/break_reasons.go` returns ≥ 3.
    - `grep -F "h.deps.Cache.Del(" services/api/internal/catalog/break_reasons.go` returns ≥ 3.
    - `grep -F "GetBreakReasonByIdAnyVersion" services/api/internal/catalog/break_reasons.go` returns ≥ 1.
    - `grep -F "ExternalID" services/api/internal/catalog/break_reasons.go` returns 0 (break_reasons has NO external_id).
    - `grep -F "func mapBreakReason(" services/api/internal/catalog/break_reasons.go` returns 1.
    - `grep -F "name_collision" services/api/internal/catalog/break_reasons.go` returns ≥ 1.
    - `grep -cE "^func TestBreakReasons_" services/api/internal/catalog/break_reasons_test.go` returns ≥ 10.
    - `grep -F "TestBreakReasons_RoutableFlag" services/api/internal/catalog/break_reasons_test.go` returns 1.
    - `grep -F "TestBreakReasons_DisplayOrder" services/api/internal/catalog/break_reasons_test.go` returns 1.
    - `grep -F "TestBreakReasons_NameUniqueCollision" services/api/internal/catalog/break_reasons_test.go` returns 1.
    - `cd services/api && go build ./internal/catalog/...` exits 0.
    - `cd services/api && go test -count=1 -race -timeout 120s -run TestBreakReasons ./internal/catalog/...` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request body ↔ handler | Same as agents — Layer 1 oapi-codegen schema + Layer 2 (none required for skills/break_reasons, no FK fields). |
| Cache key construction | orgID from ctx, entity slug verbatim per D-58, id from URL path param (UUIDv7 validated by middleware). |
| Soft-delete row visibility | `enabled=false` row hidden from default list; `?include_disabled=true` surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-28 | Information Disclosure | Wrong cache slug → cross-entity bleed (e.g., agents cache returning skill data) | mitigate | Slug is hard-coded constant in each entity file; acceptance grep enforces verbatim slug. Plan 03-10 isolation test inspects cache contents per entity. |
| T-3-29 | Tampering | UNIQUE(org_id, name) violation on break_reasons returned as 500 instead of 409 | mitigate | errors.go::mapPgError checks pgconn.PgError{Code:"23505"} → 409; TestBreakReasons_NameUniqueCollision verifies wire shape. |
| T-3-30 | Information Disclosure | Cache hit returns stale row that another caller updated 60s ago | accept (v0.1) | TTL = 60s; D-55 + D-56 enforce DEL on writes including 409. Refresh-ahead (D-53) keeps hot keys warm. Acceptable staleness window. |
</threat_model>

<verification>
End-to-end Wave 4 (skills + break_reasons) acceptance:

```
cd services/api && go build ./internal/catalog/... && go vet ./internal/catalog/... && go test -count=1 -race -timeout 180s -run "TestSkills|TestBreakReasons" ./internal/catalog/...
```

All exit 0. 20+ tests cover CAT-02 + CAT-07 + CAT-08 + CAT-09 + CAT-10 + CAT-11 for these two simple entities.
</verification>

<success_criteria>
- [ ] `skills.go` ships 5 real handler bodies + mapSkill; nullable description round-trips.
- [ ] `break_reasons.go` ships 5 real handler bodies + mapBreakReason; routable flag round-trips; no external_id field.
- [ ] Cache keys for skills use `"skills"` slug; for break_reasons use `"break_reasons"` slug — verbatim per D-58.
- [ ] 10+ tests per entity (≥ 20 total) green under `-race`.
- [ ] `cd services/api && go test -count=1 -race -timeout 180s -run "TestSkills|TestBreakReasons" ./internal/catalog/...` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-catalog-crud-go/03-07-SUMMARY.md` when done. Include: list of all 20+ tests across both entities with their CAT-* coverage; the exact diff scope (skills.go & break_reasons.go changed from placeholders to full bodies); confirmation that no external_id field was added to break_reasons.
</output>
</content>
</invoke>