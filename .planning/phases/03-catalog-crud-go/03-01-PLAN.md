---
phase: 03-catalog-crud-go
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - openapi/openapi.yaml
  - services/api/internal/api/server.gen.go
  - services/api/internal/api/types.gen.go
  - services/api/internal/api/spec.gen.go
  - services/api/internal/server/server.go
  - services/api/internal/server/stubs.go
  - services/api/internal/server/request_id_exhaustiveness_test.go
  - services/api/internal/scaffold/handler.go
  - services/api/internal/scaffold/handler_test.go
  - services/api/internal/db/queries/scaffold.sql
  - services/api/internal/db/generated/scaffold.sql.go
  - services/api/internal/db/generated/models.go
  - services/api/go.mod
  - services/api/go.sum
  - web/packages/ui/src/api/generated.ts
autonomous: false
requirements: [CAT-03, CAT-08, CAT-10]
requirements_addressed: [CAT-03, CAT-08, CAT-10]
user_setup: []

must_haves:
  truths:
    - "openapi.yaml has `invalid_reference` AND `invalid_value` ErrorCode values (12 enum values total — was 10 in Phase 2, +invalid_reference +invalid_value in Phase 3)."
    - "Channel and Adapter schemas carry a `version` integer + `UpdateChannelRequest`/`UpdateAdapterRequest` require `version`."
    - "Every catalog CREATE/UPDATE that takes an FK reference OR a proficiency value declares a 422 response: ErrorCode=`invalid_reference` for FK misses (CreateChannel/UpdateChannel default_queue_id, CreateAgent skills[].skill_id, UpdateAgent skills[].skill_id), and ErrorCode=`invalid_value` for proficiency boundary violations (CreateAgent skills[].proficiency outside 1-10, UpdateAgent skills[].proficiency outside 1-10, per ROADMAP Phase 3 success criterion 4). **Codex C2 iter 3** — CreateAgent422 wrapper added because CreateAgent also accepts `skills[]` per the spec; without the wrapper the handler has no typed response shape for FK miss / out-of-range proficiency during create."
    - "LimitQuery default is 25 (max stays 100; param name stays `limit` per A5)."
    - "openapi.yaml `AgentSkillAssignment.proficiency` schema is `type: integer` WITHOUT `minimum:1, maximum:10` (removed per ROADMAP Phase 3 success criterion 4 so oapi-codegen Layer 1 does NOT short-circuit with 400; the handler in Plan 03-09 returns 422 invalid_value)."
    - "scaffold/, server/stubs.go, db/queries/scaffold.sql, and the /_scaffold paths are gone from the working tree."
    - "task gen produces zero diff against the committed *.gen.go + generated.ts after the spec edits."
    - "go vet ./... and go build ./... pass with the new error wrapper types present in injectRequestIDIntoErrorResponse + exhaustiveness test."
    - "miniredis v2 is a direct test-only dep in go.mod; golang.org/x/sync is a direct dep (was indirect)."
  artifacts:
    - path: openapi/openapi.yaml
      provides: "Spec amendment for invalid_reference + invalid_value + 422 (FK + proficiency) + Channel/Adapter version + limit default 25 + AgentSkillAssignment.proficiency min/max removal"
      contains: "invalid_value"
    - path: services/api/internal/api/server.gen.go
      provides: "Regenerated strict-server interface including new 422 wrappers (CreateChannel422, UpdateChannel422, CreateAgent422, UpdateAgent422 — covers both invalid_reference for FKs and invalid_value for proficiency; Codex C2 iter 3 adds CreateAgent422) and UpdateChannel409/UpdateAdapter409"
      contains: "CreateAgent422JSONResponse"
    - path: services/api/internal/server/server.go
      provides: "Extended injectRequestIDIntoErrorResponse switch covering new 422 wrappers"
      contains: "CreateChannel422JSONResponse"
    - path: services/api/internal/server/request_id_exhaustiveness_test.go
      provides: "New cases for every 422 wrapper + Channel/Adapter 409"
      contains: "UpdateChannel422JSONResponse"
    - path: services/api/go.mod
      provides: "Direct deps: golang.org/x/sync + github.com/alicebob/miniredis/v2"
      contains: "alicebob/miniredis/v2"
  key_links:
    - from: openapi/openapi.yaml
      to: services/api/internal/api/server.gen.go
      via: "task gen (oapi-codegen)"
      pattern: "CreateChannel422JSONResponse"
    - from: openapi/openapi.yaml
      to: web/packages/ui/src/api/generated.ts
      via: "task gen (openapi-typescript)"
      pattern: "invalid_reference"
    - from: services/api/internal/api/server.gen.go
      to: services/api/internal/server/server.go
      via: "type switch in injectRequestIDIntoErrorResponse"
      pattern: "case api\\.(Create|Update)\\w+422JSONResponse"
---

<objective>
Wave 0 foundations + spec amendment. This plan unblocks every downstream wave by (a) amending `openapi/openapi.yaml` with the new `invalid_reference` ErrorCode, new 422 responses on FK-bearing CREATE/UPDATE, new `version` fields on Channel + Adapter (universal CAT-08 parity per OQ-1/A4), and the page-size default flip 20 → 25 (OQ-2/A5); (b) deleting Phase 1/2 scaffold + stubs + scaffold sqlc query per D-77; (c) regenerating `services/api/internal/api/*.gen.go` + `web/packages/ui/src/api/generated.ts` via `task gen`; (d) extending the `injectRequestIDIntoErrorResponse` type switch in `server.go` + the matching `request_id_exhaustiveness_test.go` for every new error wrapper the regeneration produces (H1, H2 reference); (e) adding `golang.org/x/sync` as a direct dep and `github.com/alicebob/miniredis/v2` as a test-only direct dep (gated by a slopcheck-defensive human-verify checkpoint per RESEARCH A2).

Purpose: Without Wave 0, Wave 1 cannot regenerate codegen (drift CI fails) and Wave 2/3 cannot return `*422JSONResponse` typed responses for FK validation. Wave 0 also frees the working tree of the Phase 2 scaffold so the catalog package can claim ownership of every endpoint cleanly.

Output: Amended OpenAPI spec + clean working tree (no scaffold) + green `task gen` + extended server type switch + extended exhaustiveness test + miniredis + singleflight direct deps.
</objective>

<execution_context>
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luong/workspace/dev/open-solutions/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalog-crud-go/03-CONTEXT.md
@.planning/phases/03-catalog-crud-go/03-RESEARCH.md
@.planning/phases/03-catalog-crud-go/03-PATTERNS.md
@.planning/phases/03-catalog-crud-go/03-VALIDATION.md
@.planning/phases/02-openapi-contract-codegen/02-CONTEXT.md
@openapi/openapi.yaml
@services/api/internal/server/server.go
@services/api/internal/server/stubs.go
@services/api/internal/server/request_id_exhaustiveness_test.go
@services/api/internal/scaffold/handler.go

<interfaces>
<!-- Critical contracts the executor MUST use without further exploration. -->

Current LimitQuery (openapi/openapi.yaml lines 1341-1351):

```yaml
LimitQuery:
  name: limit
  in: query
  required: false
  description: >
    Maximum number of items to return per page. Defaults to 20. Maximum 100.
  schema:
    type: integer
    minimum: 1
    maximum: 100
    default: 20
```

Edit target (D-67, OQ-2/A5):

```yaml
LimitQuery:
  name: limit
  in: query
  required: false
  description: >
    Maximum number of items to return per page. Defaults to 25. Maximum 100.
  schema:
    type: integer
    minimum: 1
    maximum: 100
    default: 25
```

Current ErrorCode enum (openapi/openapi.yaml lines 98-113) — 10 values:

```yaml
ErrorCode:
  enum:
    - invalid_body
    - invalid_id
    - not_found
    - internal
    - version_conflict
    - cross_org
    - invalid_org_id
    - invalid_transition
    - import_failed
    - rate_limited
```

Add **TWO** new values (D-75 + ROADMAP Phase 3 success criterion 4): `invalid_reference` (FK target does not exist) AND `invalid_value` (semantically invalid value such as proficiency outside 1-10). Final enum — **12 values total**. Alphabetic insertion at appropriate spot is fine; keep the current ordering and append both after `rate_limited` if simpler — the order is informational, not semantic. Final enum (any order; example shown):

```yaml
ErrorCode:
  enum:
    - invalid_body
    - invalid_id
    - not_found
    - internal
    - version_conflict
    - cross_org
    - invalid_org_id
    - invalid_transition
    - import_failed
    - rate_limited
    - invalid_reference   # NEW (D-75) — FK target does not exist in caller's org
    - invalid_value       # NEW (ROADMAP CRIT 4) — value outside semantic range (e.g. proficiency 1-10)
```

Current Channel schema (openapi/openapi.yaml lines 661-709) lacks `version`. Add to required + properties:

```yaml
required:
  - id
  - org_id
  - external_id
  - name
  - channel_type
  - enabled
  - version           # NEW (OQ-1/A4)
  - created_at
  - updated_at
properties:
  ...
  version:
    type: integer
    minimum: 1
    description: >
      Monotonically increasing optimistic-lock version counter (CAT-08).
      Server-owned. Mismatch on update returns HTTP 409.
    example: 1
```

`UpdateChannelRequest` (lines 735-748) MUST add required `version`:

```yaml
UpdateChannelRequest:
  type: object
  required:
    - version           # NEW
  properties:
    version:
      type: integer
      minimum: 1
    name: ...
    ...
```

Same edits to Adapter (lines 751-803) and UpdateAdapterRequest (lines 825-?). Adapter and Channel additionally need a 409 response on the UPDATE operation following the same shape as `UpdateAgent409` (a `$ref: "#/components/responses/VersionConflict"` reusable response, OR an inline `VersionConflictErrorResponse` schema reference matching D-37). Lift the pattern verbatim from `UpdateAgent` (operationId at openapi.yaml line 1752). UpdateAdapter and UpdateChannel will after this amendment carry the EXACT same response set as UpdateAgent/UpdateSkill/UpdateQueue/UpdateBreakReason (200/400/404/409/422).

422 responses (D-75 + ROADMAP Phase 3 success criterion 4) — add to these operations (each operation already exists in the spec; we are extending its `responses` map):

| operationId | Why 422 | ErrorCode value |
|-------------|---------|-----------------|
| CreateChannel | `default_queue_id` FK must exist + be enabled in same org (D-76) | `invalid_reference` |
| UpdateChannel | same | `invalid_reference` |
| CreateAgent — FK path (Codex C2 iter 3) | `skills[].skill_id` FK must exist in same org (CAT-03 create-with-skills) | `invalid_reference` |
| CreateAgent — proficiency path (Codex C2 iter 3) | `skills[].proficiency` must be in inclusive range 1-10 (per ROADMAP CRIT 4) | `invalid_value` |
| UpdateAgent — FK path | `skills[].skill_id` FK must exist in same org (CAT-03 PUT semantics) | `invalid_reference` |
| UpdateAgent — proficiency path | `skills[].proficiency` must be in inclusive range 1-10 (per ROADMAP CRIT 4) | `invalid_value` |

Both CreateAgent error paths use the SAME `CreateAgent422JSONResponse` wrapper (Codex C2 iter 3). Both UpdateAgent error paths use the SAME `UpdateAgent422JSONResponse` wrapper. The handler picks the `ErrorCode` value (invalid_reference vs invalid_value) based on which validation failed. The wrapper TYPE is identical (single 422 response on each operation); the body's `error` field distinguishes the flavor.

The 422 response body shape:

```yaml
"422":
  description: >
    Semantically invalid request. Structurally valid JSON but business-rule
    violation. Two flavors share this status (D-75 + ROADMAP CRIT 4 + Codex
    C2 iter 3):
    (a) `invalid_reference` — a referenced foreign key does not exist in
        the caller's org (CreateChannel/UpdateChannel default_queue_id,
        CreateAgent/UpdateAgent skills[].skill_id).
    (b) `invalid_value` — a structurally-valid value falls outside its
        semantic range (CreateAgent/UpdateAgent skills[].proficiency outside 1-10).
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/ErrorResponse"
```

The `ErrorResponse` envelope already supports both: `error` is the `ErrorCode` enum which now includes both `invalid_reference` and `invalid_value`; `field` indicates the request body path (e.g. `"skills[0].proficiency"` for proficiency errors, `"default_queue_id"` for FK errors); `message` carries human-readable context. The reusable `#/components/responses/InvalidReference` is OK to factor out — at planner's option.

Current type switch case shape in `services/api/internal/server/server.go` (lines 205-228, used by `injectRequestIDIntoErrorResponse`):

```go
case api.BadRequestJSONResponse:
    v := api.ErrorResponse(r)
    v.RequestId = setIfNil(v.RequestId, id)
    return api.BadRequestJSONResponse(v)
```

For each new generated 422 wrapper, mirror this pattern:

```go
case api.CreateChannel422JSONResponse:
    v := api.ErrorResponse(r)
    v.RequestId = setIfNil(v.RequestId, id)
    return api.CreateChannel422JSONResponse(v)
```

For each new 409 wrapper (UpdateChannel409, UpdateAdapter409 — if the spec amendment adds them):

```go
case api.UpdateChannel409JSONResponse:
    // VersionConflictErrorResponse variant.
    r.RequestId = setIfNil(r.RequestId, id)
    return r
```

(Reference: lines 449-453 of server.go for the UpdateAgent409 pattern.)

Exhaustiveness test cases in `services/api/internal/server/request_id_exhaustiveness_test.go` follow this format (current pattern around line 77-103):

```go
case api.CreateChannel422JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
```

**AgentSkillAssignment.proficiency** — REMOVE the existing `minimum: 1, maximum: 10` validation from the schema. Per ROADMAP Phase 3 success criterion 4 + the new `invalid_value` ErrorCode added above, the handler (Plan 03-09) returns 422 invalid_value for out-of-range values; oapi-codegen Layer 1 must NOT short-circuit with 400. The schema keeps `type: integer` (so non-numeric input still produces 400 invalid_body at Layer 1) but DROPS min/max. Locate the `AgentSkillAssignment` component (search for `AgentSkillAssignment:` in openapi.yaml — likely in the components.schemas block around lines 200-300). Final schema:

```yaml
AgentSkillAssignment:
  type: object
  required:
    - skill_id
    - proficiency
  properties:
    skill_id:
      $ref: "#/components/schemas/UUIDv7"
    proficiency:
      type: integer
      description: >
        Skill proficiency rating in inclusive range 1-10. Out-of-range values
        return HTTP 422 with ErrorCode=invalid_value (validated handler-side
        per ROADMAP Phase 3 success criterion 4 — NOT Layer 1 schema, so the
        wire shape distinguishes "wrong value" (422) from "wrong type" (400).
      example: 5
    # No `minimum:` and no `maximum:` — handler enforces.
    name:
      type: string
      readOnly: true
      description: Embedded from the joined skills table for client convenience.
```

Existing scaffold paths to delete from openapi.yaml (around lines 1548-1638):
- `paths./v1/orgs/{org_id}/_scaffold:`
- `paths./v1/orgs/{org_id}/_scaffold/{id}:`
- `components.schemas.Scaffold`
- `components.schemas.CreateScaffoldRequest`
- All `Scaffold`-prefixed response wrappers / operationIds (CreateScaffold, ListScaffolds, GetScaffoldById)

After spec edits, run `task gen` from repo root. The drift CI gate (D-47/D-48) will fail the PR if any of these are not regenerated:
- `services/api/internal/api/server.gen.go`
- `services/api/internal/api/types.gen.go`
- `services/api/internal/api/spec.gen.go`
- `web/packages/ui/src/api/generated.ts`

Deps to add to `services/api/go.mod`:
- `golang.org/x/sync v0.20.0+` — promote from indirect to direct (used by Wave 1 cache pkg for singleflight.Group)
- `github.com/alicebob/miniredis/v2 v2.37.0+` — test-only direct dep (used by Wave 1 cache_test.go and Wave 2+ catalog testutil)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Slopcheck human-verify before adding miniredis</name>
  <what-built>Defensive gate per RESEARCH §Package Legitimacy Audit + threat T-3-SC. `slopcheck install --ecosystem go github.com/alicebob/miniredis/v2` flagged the package `[SLOP]` (false positive due to stale pkg.go.dev metadata). The package has been live since 2015 (continuous releases through v2.38.0 in May 2026), MIT license, source github.com/alicebob/miniredis, and is the canonical Go Redis test double already referenced by Phase 2 context. Even so, we do NOT auto-approve [SLOP] packages — Wave 0 pauses here so a human can confirm.</what-built>
  <how-to-verify>
    1. Open https://github.com/alicebob/miniredis — confirm: active repo, MIT license, current release ≥ v2.37.0.
    2. Open https://pkg.go.dev/github.com/alicebob/miniredis/v2 — confirm the module exists with a recent (≤ 6 months) version published.
    3. Verify the package is named `github.com/alicebob/miniredis/v2` (with /v2 suffix — v1 is unmaintained).
    4. Decide: APPROVE adding it as a test-only direct dep, OR reject and require an alternate (e.g. `testcontainers-go redis` — significantly slower).
  </how-to-verify>
  <resume-signal>Type `approved` to proceed with `go get` in Task 2, or describe rejection / alternative.</resume-signal>
  <read_first>
    - .planning/phases/03-catalog-crud-go/03-RESEARCH.md §Package Legitimacy Audit (lines ~183-194)
    - decisions: T-3-SC (threat in this plan's threat_model), D-73 (miniredis usage)
  </read_first>
  <acceptance_criteria>
    - User-typed "approved" response captured in execution log before Task 2 runs.
    - Without approval, Task 2 MUST be skipped.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Amend openapi/openapi.yaml — invalid_reference + 422 + Channel/Adapter version + limit default + delete scaffold paths</name>
  <files>openapi/openapi.yaml</files>
  <action>Edit `openapi/openapi.yaml` (single file, multiple coordinated edits):

(a) **Add TWO new values to the ErrorCode enum** (currently at lines 98-113, 10 values): `- invalid_reference` (per D-75 — FK target does not exist) AND `- invalid_value` (per ROADMAP Phase 3 success criterion 4 — value outside semantic range, e.g. proficiency 1-10). Final enum has **12 values total** (was 10 before Phase 3). Append to the existing enum list:

```yaml
ErrorCode:
  enum:
    - invalid_body
    - invalid_id
    - not_found
    - internal
    - version_conflict
    - cross_org
    - invalid_org_id
    - invalid_transition
    - import_failed
    - rate_limited
    - invalid_reference   # NEW (D-75)
    - invalid_value       # NEW (ROADMAP CRIT 4 — proficiency 1-10 boundary)
```

(a2) **Remove `minimum: 1, maximum: 10` from `AgentSkillAssignment.proficiency`** (search for `AgentSkillAssignment:` in openapi.yaml). The handler in Plan 03-09 returns 422 invalid_value for out-of-range values; oapi-codegen Layer 1 must NOT fire 400 first. Keep `type: integer` (type-level validation stays — non-numeric input still produces 400 invalid_body). Drop ONLY the min/max constraints. Update the field `description` to document the handler-level enforcement ("returns HTTP 422 with ErrorCode=invalid_value if outside 1-10").

(b) **Add `version: integer (minimum: 1)` field to the Channel schema** (lines 661-709) and to the Adapter schema (lines 751-803). Add `version` to the `required` array on both schemas. Use this exact subschema (copied from Agent schema at line 302):

```yaml
version:
  type: integer
  minimum: 1
  description: >
    Monotonically increasing optimistic-lock version counter (CAT-08, OQ-1/A4).
    Server-owned. Mismatch on update returns HTTP 409.
  example: 1
```

(c) **Add required `version` to `UpdateChannelRequest`** (lines 735-748) and `UpdateAdapterRequest` (lines 825+). Match the pattern in `UpdateAgentRequest` (line 406-?) verbatim — the field is required and uses the same subschema:

```yaml
required:
  - version
properties:
  version:
    type: integer
    minimum: 1
    description: Current optimistic-lock version. Mismatch → HTTP 409.
```

(d) **Add the `"409"` response to UpdateChannel + UpdateAdapter operations** (locate the operations by operationId `UpdateChannel` and `UpdateAdapter` — around lines ~? in the paths block). Use the EXACT same shape as `UpdateAgent`'s 409 (search for `operationId: UpdateAgent` then copy its 409 block). The 409 body schema is `VersionConflictErrorResponse` (already defined at the components level).

(e) **Add the `"422"` response to these operations**: `CreateChannel`, `UpdateChannel`, `CreateAgent` (Codex C2 iter 3), `UpdateAgent`. **CreateAgent + UpdateAgent each have ONE 422 response wrapper** that is a UNION of two error paths (invalid_reference for FK skill_id miss, invalid_value for proficiency boundary). The wrapper TYPE is the same for both error flavors; the handler chooses the ErrorCode value at runtime. Use this exact response shape:

```yaml
"422":
  description: >
    Semantically invalid request — structurally valid JSON that fails
    a business-rule check (D-75, D-76, ROADMAP Phase 3 CRIT 4). Two
    flavors share this status:
    (a) `invalid_reference` — a referenced foreign key does not exist
        in the caller's org. For UpdateAgent: a `skills[].skill_id`
        does not exist in the same org. For CreateChannel/UpdateChannel:
        `default_queue_id` does not exist or is disabled in the same org.
    (b) `invalid_value` — a structurally-valid value is outside its
        semantic range. For UpdateAgent: `skills[].proficiency` outside
        the inclusive range 1-10.
    The `field` slot in ErrorResponse identifies which request body path
    failed (e.g. "skills[0].proficiency" or "default_queue_id").
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/ErrorResponse"
```

Note: `CreateAgent` (Codex C2 iter 3) and `UpdateAgent` each have ONE 422 response wrapper in the generated server (`CreateAgent422JSONResponse` and `UpdateAgent422JSONResponse`). The Plan 03-06 handlers (CreateAgent + UpdateAgent) and Plan 03-09 helper (validateProficiencyRange + validateSkillsExistInOrg) pick the `ErrorCode` value at runtime: `invalid_value` when proficiency fails, `invalid_reference` when skill_id fails. The wrapper shape is identical across all four code paths (CreateAgent FK, CreateAgent proficiency, UpdateAgent FK, UpdateAgent proficiency). The response body's `error` field carries the flavor.

(f) **Edit `LimitQuery`** (line 1341-1351): change `default: 20` to `default: 25`. Update the description from "Defaults to 20" to "Defaults to 25". Keep the param name `limit` (NOT `page_size`, per OQ-2/A5 — pitfall H6 explicitly mandates minimal churn).

(g) **DELETE scaffold paths and schemas** (per D-77):
  - Remove `/v1/orgs/{org_id}/_scaffold:` and `/v1/orgs/{org_id}/_scaffold/{id}:` from `paths:` (around lines 1548-1638).
  - Remove `components.schemas.Scaffold` and `components.schemas.CreateScaffoldRequest`.
  - Verify no other `$ref` points to Scaffold-related schemas (grep `Scaffold` after edits).

Run `npx @redocly/cli@latest lint openapi/openapi.yaml` after edits — must report 0 errors. If lint warns about empty operations or unreferenced schemas, fix them.</action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions && npx -y @redocly/cli@latest lint openapi/openapi.yaml && grep -q "invalid_reference" openapi/openapi.yaml && grep -q "invalid_value" openapi/openapi.yaml && grep -q "default: 25" openapi/openapi.yaml && ! grep -q "Scaffold" openapi/openapi.yaml && grep -A2 "Channel:" openapi/openapi.yaml | head -40 | grep -q "version" && echo OK</automated>
  </verify>
  <done>Spec amended with all 8 edit groups (a, a2, b, c, d, e, f, g); redocly lint passes; no `Scaffold` references remain; BOTH `invalid_reference` AND `invalid_value` ErrorCode values present (12 total); AgentSkillAssignment.proficiency has NO min/max constraints; LimitQuery default is 25; Channel + Adapter carry `version` field; UpdateAgent/UpdateChannel each have 422 + 409 responses.</done>
  <read_first>
    - openapi/openapi.yaml (lines 98-113 ErrorCode, 661-709 Channel, 735-748 UpdateChannelRequest, 751-803 Adapter, 825+ UpdateAdapterRequest, 1341-1351 LimitQuery, 1548-1638 scaffold paths)
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-75 spec amendment, D-77 scaffold deletion)
    - .planning/ROADMAP.md §Phase 3 success criterion 4 (proficiency outside 1-10 returns HTTP 422 — drives the new `invalid_value` ErrorCode + AgentSkillAssignment.proficiency min/max removal)
    - .planning/phases/03-catalog-crud-go/03-RESEARCH.md (OQ-1/A4 add version, OQ-2/A5 keep `limit` name + default 25, Pitfall 6, Pitfall 8)
    - .planning/phases/02-openapi-contract-codegen/02-CONTEXT.md (D-35..D-37 envelope, D-44 strict-server, D-47 codegen drift CI)
    - decisions: D-67, D-75, D-77 (this plan), OQ-1/A4, OQ-2/A5
  </read_first>
  <acceptance_criteria>
    - `grep -c "invalid_reference" openapi/openapi.yaml` returns ≥ 2 (one in ErrorCode enum + at least one 422 response description).
    - `grep -c "invalid_value" openapi/openapi.yaml` returns ≥ 2 (one in ErrorCode enum + at least one 422 response description / AgentSkillAssignment description).
    - `grep -B 1 -A 6 "AgentSkillAssignment:" openapi/openapi.yaml | grep -F "proficiency" -A 4 | grep -cE "minimum: 1$|maximum: 10$"` returns **0** (the min/max constraints MUST be removed).
    - `grep -A 6 "ErrorCode:" openapi/openapi.yaml | head -16 | grep -cE "^\s+- (invalid_body|invalid_id|not_found|internal|version_conflict|cross_org|invalid_org_id|invalid_transition|import_failed|rate_limited|invalid_reference|invalid_value)$"` returns 12 (full enum after amendment).
    - `grep -A 10 "LimitQuery:" openapi/openapi.yaml | grep -F "default: 25"` returns exactly one hit.
    - `grep -c "^\s*Scaffold\|/_scaffold" openapi/openapi.yaml` returns 0.
    - `grep -A 20 "Channel:" openapi/openapi.yaml | grep -B 2 "^\s\+version:" | head -10` shows `version:` under Channel properties.
    - `grep -A 20 "Adapter:" openapi/openapi.yaml | grep -B 2 "^\s\+version:" | head -10` shows `version:` under Adapter properties.
    - `grep -B 1 "UpdateChannelRequest:\|UpdateAdapterRequest:" openapi/openapi.yaml | head -20` and the following block shows `required:` includes `version`.
    - `npx -y @redocly/cli@latest lint openapi/openapi.yaml` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete scaffold source + add direct deps (singleflight + miniredis) + regenerate</name>
  <files>
    services/api/internal/scaffold/handler.go,
    services/api/internal/scaffold/handler_test.go,
    services/api/internal/db/queries/scaffold.sql,
    services/api/go.mod,
    services/api/go.sum,
    services/api/internal/api/server.gen.go,
    services/api/internal/api/types.gen.go,
    services/api/internal/api/spec.gen.go,
    services/api/internal/db/generated/scaffold.sql.go,
    services/api/internal/db/generated/models.go,
    web/packages/ui/src/api/generated.ts
  </files>
  <action>Three coordinated deletions + dep adds + a full regen pass:

(a) **Delete scaffold source files** (D-77 commit 1):
  - `rm services/api/internal/scaffold/handler.go services/api/internal/scaffold/handler_test.go`
  - `rmdir services/api/internal/scaffold` (directory empty after rm)
  - `rm services/api/internal/db/queries/scaffold.sql`

  Note: `services/api/internal/server/stubs.go` will be DELETED in Task 4 of this plan (after server.go edits land). Keeping it for now so the in-tree build still compiles — Task 4 deletes it together with the corresponding NewMux/NewCompositeServer signature changes.

(b) **Add direct deps** (gated by Task 1 human-verify):

```bash
cd services/api && go get golang.org/x/sync@v0.20.0 && go get -t github.com/alicebob/miniredis/v2@latest && go mod tidy
```

This (i) promotes `golang.org/x/sync` from indirect to direct (Wave 1 cache pkg will import singleflight), and (ii) adds `github.com/alicebob/miniredis/v2` as a test-only direct dep.

(c) **Regenerate codegen** from repo root:

```bash
task gen
```

`task gen` runs (i) `cd services/api && sqlc generate` — which will DELETE `services/api/internal/db/generated/scaffold.sql.go` (since its source `scaffold.sql` is gone) and update `services/api/internal/db/generated/models.go` to drop the `Scaffold` model; (ii) `cd services/api && go generate ./internal/api/...` — which regenerates `services/api/internal/api/server.gen.go`, `types.gen.go`, `spec.gen.go` from the amended `openapi/openapi.yaml`; (iii) `cd web && pnpm -F @open-routing/ui gen:api` — which regenerates `web/packages/ui/src/api/generated.ts` from the same spec.

(d) **Verify zero diff after `task gen`** (the drift gate). Run:

```bash
task gen && cd /Users/luong/workspace/dev/open-solutions && git status --short services/api/internal/api/ services/api/internal/db/generated/ web/packages/ui/src/api/
```

Files in those directories MUST appear as M (modified) or D (deleted) — i.e. consistent with the spec edits and scaffold deletion. Then run `task gen` AGAIN — the second invocation MUST produce no further changes (zero diff = drift CI passes).

After regeneration, NEW generated wrapper types include (at minimum — exact names depend on the operations amended):
- `api.CreateChannel422JSONResponse`
- `api.UpdateChannel422JSONResponse`
- `api.UpdateAgent422JSONResponse` — used by Plan 03-09 for BOTH invalid_reference (skill_id FK) AND invalid_value (proficiency 1-10 boundary) error paths
- `api.UpdateChannel409JSONResponse` (if not already present — it was already present per existing grep)
- `api.UpdateAdapter409JSONResponse` (likely NEW)
- `api.ErrorCodeInvalidReference` — generated constant for `invalid_reference` enum value
- `api.ErrorCodeInvalidValue` — generated constant for `invalid_value` enum value (NEW per ROADMAP CRIT 4)

Capture the names emitted by `grep -E "type (Create|Update).*(422|409)JSONResponse" services/api/internal/api/server.gen.go` after regen — Task 4 needs the exact list.

Hazard H1: deleting `scaffold.sql` causes sqlc to remove `Scaffold` model and `*Scaffold*` query methods from `internal/db/generated/`. Any code that imports these will fail to compile — but `scaffold/handler.go` (which was the only consumer) is also deleted in (a), so this is consistent. If `go build ./...` fails after `task gen` with "undefined: generated.Scaffold" or similar, search for the lingering import and remove it.

The build is EXPECTED to fail compilation AFTER this task and BEFORE Task 4 because `services/api/internal/server/stubs.go` still references `scaffold.NewHandler` (or similar). That is intentional — Task 4 fixes it.</action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions && ls services/api/internal/scaffold/ 2>&1 | grep -q "No such file" && ! test -f services/api/internal/db/queries/scaffold.sql && grep -q "alicebob/miniredis/v2" services/api/go.mod && grep -q "golang.org/x/sync" services/api/go.mod && grep -q "CreateChannel422JSONResponse\|UpdateChannel422JSONResponse" services/api/internal/api/server.gen.go && grep -q "UpdateAgent422JSONResponse" services/api/internal/api/server.gen.go && grep -q "invalid_reference" web/packages/ui/src/api/generated.ts && grep -q "invalid_value" web/packages/ui/src/api/generated.ts && echo OK</automated>
  </verify>
  <done>scaffold directory + scaffold.sql gone; go.mod lists both miniredis and x/sync as direct deps; regenerated server.gen.go contains new 422 wrappers; generated.ts contains `invalid_reference`. (Note: `go build ./...` is expected to fail at this point — Task 4 fixes the lingering stubs.go references.)</done>
  <read_first>
    - services/api/internal/scaffold/handler.go (the file you are about to delete — verify no other in-tree imports remain by `grep -rn "scaffold\." services/api/internal/`)
    - services/api/internal/db/queries/scaffold.sql (verify it's the ONLY query file referencing _scaffold)
    - Taskfile.yml (lines 18-23) — confirm `task gen` runs sqlc + oapi-codegen + openapi-typescript
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-77 deletion order)
    - decisions: D-77, D-50 (cache pkg requires singleflight), D-73 (miniredis for tests)
  </read_first>
  <acceptance_criteria>
    - `test ! -d services/api/internal/scaffold` (directory does not exist).
    - `test ! -f services/api/internal/db/queries/scaffold.sql` (file does not exist).
    - `grep -F "github.com/alicebob/miniredis/v2" services/api/go.mod` returns ≥ 1 hit.
    - `grep -F "golang.org/x/sync" services/api/go.mod | grep -v indirect` returns ≥ 1 hit (i.e. promoted to direct dep).
    - `grep -cE "type (Create|Update)\\w+422JSONResponse" services/api/internal/api/server.gen.go` returns ≥ 4 (CreateChannel422, UpdateChannel422, CreateAgent422, UpdateAgent422 — Codex C2 iter 3 adds CreateAgent422).
    - `grep -F "CreateAgent422JSONResponse" services/api/internal/api/server.gen.go` returns ≥ 1 (Codex C2 iter 3 — used by Plan 03-06 CreateAgent handler for skills[] 422 paths).
    - `grep -F "UpdateAgent422JSONResponse" services/api/internal/api/server.gen.go` returns ≥ 1 (Plan 03-09 depends on this wrapper for the proficiency 422).
    - `grep -F "ErrorCodeInvalidValue" services/api/internal/api/server.gen.go` returns ≥ 1 (new generated constant for the `invalid_value` enum value, used by Plan 03-09 handler).
    - `grep -F "invalid_value" web/packages/ui/src/api/generated.ts` returns ≥ 1 hit.
    - `grep -F "invalid_reference" web/packages/ui/src/api/generated.ts` returns ≥ 1 hit.
    - Running `task gen` a second time produces 0 file changes: `task gen && git diff --exit-code services/api/internal/api/ services/api/internal/db/generated/ web/packages/ui/src/api/` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Delete server/stubs.go + extend injectRequestIDIntoErrorResponse + extend exhaustiveness test + simplify NewMux</name>
  <files>
    services/api/internal/server/stubs.go,
    services/api/internal/server/server.go,
    services/api/internal/server/request_id_exhaustiveness_test.go,
    services/api/cmd/api/main.go
  </files>
  <action>Four coordinated edits to make the working tree compile again after Task 3.

(a) **Delete `services/api/internal/server/stubs.go`** (D-77; removes `NewCompositeServer` + the 501-stub struct that was Phase 2's scaffold-coexistence vehicle).

(b) **Simplify `services/api/internal/server/server.go`'s `NewMux` signature** to no longer require `StrictHandlers api.StrictServerInterface` from `Deps`. Replace with a forward-compat `Catalog` field placeholder. Wave 2 will populate it with `*catalog.Handlers`; for now (Wave 0 build-fix only) `Catalog` accepts `api.StrictServerInterface` directly. The shape AFTER this edit:

```go
type Deps struct {
    Pool           *pgxpool.Pool
    Redis          *redis.Client
    OrgDB          *db.OrgDB
    Config         *config.Config
    StrictHandlers api.StrictServerInterface  // KEEP for Wave 0; Wave 2 renames to Catalog
    SpecBytes      []byte
}
```

**Decision: keep `StrictHandlers` field name through Wave 0.** Wave 2 (Plan 03-08) will rename to `Catalog *catalog.Handlers` once `catalog.Handlers` exists. This minimizes scope of Wave 0 (rename happens once, not twice).

(c) **Extend the `injectRequestIDIntoErrorResponse` type switch in `server.go`** with cases for every new error wrapper Task 3's regeneration emitted. Mandatory new cases (mirror the existing patterns at lines 205-610):

- `case api.CreateChannel422JSONResponse:` — flat ErrorResponse wrapper, follow lines 209-228 pattern.
- `case api.UpdateChannel422JSONResponse:` — same.
- `case api.CreateAgent422JSONResponse:` — **NEW Codex C2 iter 3.** ONE switch case handles BOTH invalid_reference (skill_id FK) and invalid_value (proficiency boundary) error paths for CreateAgent; they share the same JSON response wrapper.
- `case api.UpdateAgent422JSONResponse:` — same. ONE switch case handles BOTH `invalid_reference` (skill_id FK) and `invalid_value` (proficiency boundary) error paths because they share the same JSON response wrapper; the ErrorCode value is a field on the wrapped ErrorResponse, not part of the type identity.
- `case api.UpdateChannel409JSONResponse:` — flat ErrorResponse if Channel 409 uses ErrorResponse; OR VersionConflictErrorResponse pattern if Channel 409 uses VersionConflict (it should now, per Task 2 spec edit). Use the lines 449-453 pattern: `r.RequestId = setIfNil(r.RequestId, id); return r`.
- `case api.UpdateAdapter409JSONResponse:` — same as UpdateChannel409.

Important: run `grep -E "^type (Create|Update)\w+(409|422)JSONResponse" services/api/internal/api/server.gen.go` to enumerate the actual generated type names — copy them verbatim into the switch. The exhaustiveness test in step (d) will catch any omission.

For each new case, choose the right pattern:
- **Flat ErrorResponse wrapper** (e.g. `CreateChannel422JSONResponse = ErrorResponse`): use the lines 209-228 form.
- **VersionConflictErrorResponse-shaped 409** (e.g. `UpdateAgent409JSONResponse`): use the lines 449-453 form (`r.RequestId = setIfNil(r.RequestId, id); return r`).
- **Union wrapper** (only `CreateAgent409JSONResponse` — H2): currently passed through (line 508). Leave as-is for Wave 0; this is documented technical debt (H2) — production handlers in Wave 3 will stamp `RequestId` BEFORE calling `.FromErrorResponse(...)`. Add a comment to the existing pass-through case acknowledging this.

(d) **Extend `services/api/internal/server/request_id_exhaustiveness_test.go`** to assert every new wrapper sets `RequestId`. The test is 524 lines, 121 subtests, with one `case` per wrapper type. Add new cases mirroring lines 77-103 pattern:

```go
case api.CreateChannel422JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
case api.UpdateChannel422JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
case api.CreateAgent422JSONResponse:  // Codex C2 iter 3
    require.NotNil(t, r.RequestId, "%s", typeName)
case api.UpdateAgent422JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
case api.UpdateChannel409JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
case api.UpdateAdapter409JSONResponse:
    require.NotNil(t, r.RequestId, "%s", typeName)
```

The test also iterates over a slice of typed-zero values (around line ~?) — add the new types there too. Search the test file for `var allResponses` or similar to find the canonical list.

(e) **`services/api/cmd/api/main.go`** wiring — the existing `NewCompositeServer` call (around line 131 per RESEARCH PATTERNS Hazard) MUST be replaced because `stubs.go` (which defined `NewCompositeServer`) is now deleted. Replace with a temporary minimal `StrictServerInterface` implementation that returns 501 for ALL methods, so the binary builds and starts. Wave 2 (Plan 03-08) replaces this with `catalog.New(deps)`.

Minimal "all-501" implementation — place it inline in `main.go` or in a new tiny file `services/api/internal/server/wave0_temp_stubs.go` (delete after Wave 2):

```go
// wave0_temp_stubs.go — TEMPORARY (Wave 0 only). Deleted when catalog.Handlers
// implements StrictServerInterface in Wave 2 (Plan 03-08).
package server

// Wave0TempStubs returns api.ErrorCodeInternal/501-ish on every method.
// Allows the binary to build between Wave 0 spec amendment and Wave 2
// catalog skeleton.
type Wave0TempStubs struct{}

func (Wave0TempStubs) CreateAgent(ctx context.Context, req api.CreateAgentRequestObject) (api.CreateAgentResponseObject, error) {
    return api.CreateAgent500JSONResponse{InternalServerErrorJSONResponse: api.InternalServerErrorJSONResponse{
        Error: api.ErrorCodeInternal, Reason: "not_implemented_yet"}}, nil
}
// ... repeat for all 41 (or whatever the count is post-amendment) methods.
```

This is ugly but is the SHORTEST path to a green build at end of Wave 0. Wave 2 deletes the entire file. Alternative: declare `Wave0TempStubs` with an embedded `api.UnimplementedStrictServerInterface` if oapi-codegen v2.7 emits one — check `services/api/internal/api/server.gen.go` for `UnimplementedStrictServerInterface` and use it if present (massively shorter file). If not, generate the per-method body with a small one-off script.

Note: `cmd/api/main.go` line 131 currently calls `server.NewCompositeServer(orgDB, pool, rdb, specBytes)`. Replace with `server.Wave0TempStubs{}` (zero-value usable).

Hazards
- H1 — exhaustiveness test gates the build. Without the extension in (d), `go test ./internal/server -run TestRequestIDInjection_Exhaustiveness` fails the build for every new generated wrapper.
- H2 — CreateAgent409 union. We are NOT fixing the union pass-through in Wave 0; Wave 3 (plan 03-09 agents) addresses it. Leave the existing pass-through comment as-is.</action>
  <verify>
    <automated>cd /Users/luong/workspace/dev/open-solutions/services/api && go build ./... && go vet ./... && go test ./internal/server/... -run TestRequestIDInjection_Exhaustiveness -count=1 && ! test -f internal/server/stubs.go && echo OK</automated>
  </verify>
  <done>stubs.go deleted; server.go injectRequestIDIntoErrorResponse switch covers all new generated wrappers; exhaustiveness test extended; main.go wired to Wave0TempStubs so `go build ./...` succeeds and `go vet` is clean.</done>
  <read_first>
    - services/api/internal/server/server.go (lines 60-106 NewMux Deps, lines 205-610 injectRequestIDIntoErrorResponse, line 508 CreateAgent409 pass-through comment)
    - services/api/internal/server/stubs.go (the file being deleted — verify `NewCompositeServer` is its only public export and the only caller is `cmd/api/main.go`)
    - services/api/internal/server/request_id_exhaustiveness_test.go (lines 77-103 case pattern; locate the slice of typed-zero values that iterates them)
    - services/api/internal/api/server.gen.go (newly regenerated — locate every new 409/422 wrapper type)
    - services/api/cmd/api/main.go (line 131 NewCompositeServer call site to replace)
    - .planning/phases/02-openapi-contract-codegen/02-CONTEXT.md (D-44 strict-server contract)
    - .planning/phases/03-catalog-crud-go/03-CONTEXT.md (D-69 catalog.Handlers IS the StrictServerInterface impl, D-77 deletion order)
    - decisions: D-44 (Phase 2), D-69, D-77, H1, H2
  </read_first>
  <acceptance_criteria>
    - `cd services/api && go build ./...` exits 0.
    - `cd services/api && go vet ./...` exits 0.
    - `cd services/api && go test -count=1 ./internal/server -run TestRequestIDInjection_Exhaustiveness` exits 0.
    - `test ! -f services/api/internal/server/stubs.go` (file does not exist).
    - `grep -cE "case api\\.(Create|Update)\\w+422JSONResponse:" services/api/internal/server/server.go` returns ≥ 4 (Codex C2 iter 3 — CreateChannel422, UpdateChannel422, CreateAgent422, UpdateAgent422).
    - `grep -F "case api.CreateAgent422JSONResponse" services/api/internal/server/server.go` returns ≥ 1 (Codex C2 iter 3 — the new switch case).
    - `grep -cE "api\\.(Create|Update)\\w+422JSONResponse" services/api/internal/server/request_id_exhaustiveness_test.go` returns ≥ 4 (matches the switch — Codex C2 iter 3 adds CreateAgent422).
    - `grep -F "NewCompositeServer" services/api/` (recursively) returns 0 hits (the function is fully deleted).
    - Final `task gen && cd services/api && git diff --exit-code internal/api/ internal/db/generated/ ../../web/packages/ui/src/api/` exits 0 (drift gate green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAPI spec ↔ generated code | Spec edits flow through codegen; missing regeneration silently breaks the contract. |
| External package registry → go.mod | `go get` resolves from proxy.golang.org; supply-chain risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | openapi.yaml edits not regenerated → generated stubs drift from spec | mitigate | Final task in this plan asserts `task gen && git diff --exit-code ...` exits 0; same gate runs in CI (D-47). |
| T-3-02 | Spoofing | New ErrorCode value `invalid_reference` colliding with an existing client-side enum | accept | ErrorCode is documented as closed-but-extensible; clients branch on known values and fall through to `internal` for unknown. Phase 6 admin SPA does not exist yet so no live consumer to break. |
| T-3-03 | Information Disclosure | 422 response leaking row existence across orgs | mitigate | 422 fires AFTER `OrgContext` middleware sets ctx; cross-row check uses sqlc + orgDB SQLChecker, so probes are scoped to caller's org. FOUND-08 leakage policy still 404 on cross-org GET. |
| T-3-SC | Tampering | npm/pip/cargo installs — supply chain | mitigate | Task 1 BLOCKS execution until human verifies miniredis/v2 (slopcheck false-positive); golang.org/x/sync is golang.org stdlib mirror (auto-trusted per RESEARCH §Package Legitimacy Audit). |
| T-3-04 | Denial of Service | UpdateAgent skills[] PUT semantics — unbounded array DoS | accept (Phase 5 hardens) | Phase 5 introduces `http.MaxBytesReader`; Wave 0 / Phase 3 accepts the v0.1 risk because admin endpoints are gated behind trusted X-Org-Id stub auth. Documented in research §Security Domain. |
</threat_model>

<verification>
Phase-wide Wave 0 acceptance (overlay of every task's automated check):

```
cd /Users/luong/workspace/dev/open-solutions \
  && npx -y @redocly/cli@latest lint openapi/openapi.yaml \
  && task gen \
  && cd services/api \
  && go build ./... \
  && go vet ./... \
  && go test -count=1 ./internal/server -run TestRequestIDInjection_Exhaustiveness \
  && cd /Users/luong/workspace/dev/open-solutions \
  && git diff --exit-code services/api/internal/api/ services/api/internal/db/generated/ web/packages/ui/src/api/
```
All steps exit 0.
</verification>

<success_criteria>
- [ ] `openapi/openapi.yaml` carries: `invalid_reference` AND `invalid_value` ErrorCode values (**12 enum values total**), `AgentSkillAssignment.proficiency` WITHOUT min/max constraints (per ROADMAP CRIT 4), `version` field on Channel + Adapter, `version` required on UpdateChannelRequest + UpdateAdapterRequest, 422 responses on CreateChannel / UpdateChannel / **CreateAgent (Codex C2 iter 3)** / UpdateAgent (the CreateAgent + UpdateAgent 422 wrappers each cover BOTH invalid_reference and invalid_value error paths), 409 responses on UpdateChannel + UpdateAdapter, `LimitQuery` default 25, NO scaffold paths/schemas.
- [ ] `task gen` produces zero diff on second invocation (drift CI green).
- [ ] `services/api/internal/server/stubs.go` is deleted; `scaffold/` directory is deleted; `db/queries/scaffold.sql` is deleted.
- [ ] `services/api/internal/server/server.go` switch covers every new 409/422 wrapper; `request_id_exhaustiveness_test.go` asserts each of them.
- [ ] `services/api/go.mod` has `golang.org/x/sync` and `github.com/alicebob/miniredis/v2` as direct deps.
- [ ] `cd services/api && go build ./... && go vet ./...` exit 0.
- [ ] `cd services/api && go test -count=1 ./internal/server -run TestRequestIDInjection_Exhaustiveness` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-catalog-crud-go/03-01-SUMMARY.md` when done. Include: list of every generated wrapper type added to the server.go switch + exhaustiveness test; **the exact list of 12 ErrorCode enum values post-amendment** (including both `invalid_reference` AND `invalid_value`); confirmation that `AgentSkillAssignment.proficiency` had its min/max removed (per ROADMAP CRIT 4); confirmation that `UpdateAgent422JSONResponse` is present in server.gen.go for Plan 03-09 to consume; confirmation that the slopcheck checkpoint was approved with the user's response captured.
</output>
